# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($lum__vec3, $toSampleUV__vec2, $cellAvgColor3x3__vec2, $main__void);
    my $_retc;
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_resolution = exists $U->{'resolution'} ? $U->{'resolution'} : $rt->construct(2, 0.0);
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_squareSize = exists $U->{'squareSize'} ? $U->{'squareSize'} : $rt->f(0.0);
    my $_u_relief = exists $U->{'relief'} ? $U->{'relief'} : $rt->f(0.0);
    my $_u_lightAngle = exists $U->{'lightAngle'} ? $U->{'lightAngle'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $lum__vec3 = sub {
        my ($c) = @_;
        $c = $rt->copy($c, 'float');
        return $rt->dot($c, $rt->construct(3, $rt->f(0.21260000000000001), $rt->f(0.71519999999999995), $rt->f(0.0722)));
    };
    $toSampleUV__vec2 = sub {
        my ($globalPixelPos) = @_;
        $globalPixelPos = $rt->copy($globalPixelPos, 'float');
        return $rt->component_wise('clamp', $rt->binary('/', $rt->binary('-', $globalPixelPos, $_u_tileOffset, 2, 'float'), $_u_resolution, 2, 'float'), $rt->f(0), $rt->f(1));
    };
    $cellAvgColor3x3__vec2 = sub {
        my ($centerPx) = @_;
        $centerPx = $rt->copy($centerPx, 'float');
        my ($_for0_first, $_for1_first, $i, $j, $p, $sp, $sum);
        $sp = $rt->binary('*', $_u_squareSize, $rt->f(0.25), 1, 'float');
        $sum = $rt->construct(4, $rt->f(0));
        $j = $rt->unary('-', $rt->i(1));
        $_for0_first = 1;
        for my $_for0 (0 .. 1048575) {
            if (!$_for0_first) {
                $j = $rt->binary('+', $j, $rt->i(1), 1, 'int');
            }
            $_for0_first = 0;
            if (!($rt->binary('<=', $j, $rt->i(1)))) {
                last;
            }
            $i = $rt->unary('-', $rt->i(1));
            $_for1_first = 1;
            for my $_for1 (0 .. 1048575) {
                if (!$_for1_first) {
                    $i = $rt->binary('+', $i, $rt->i(1), 1, 'int');
                }
                $_for1_first = 0;
                if (!($rt->binary('<=', $i, $rt->i(1)))) {
                    last;
                }
                $p = $rt->binary('+', $centerPx, $rt->binary('*', $rt->construct(2, $rt->construct(1, $i), $rt->construct(1, $j)), $sp, 2, 'float'), 2, 'float');
                @{$sum} = map { $rt->f32($_) } @{($rt->binary('+', $sum, $rt->texture($_u_inputTex, $toSampleUV__vec2->($p)), 4, 'float'))};
            }
        }
        return $rt->binary('*', $sum, $rt->binary('/', $rt->f(1), $rt->f(9), 1, 'float'), 4, 'float');
    };
    $main__void = sub {
        my ($a, $bevelMul, $cellCenter, $cellColor, $cellIdxF, $dBottom, $dLeft, $dMin, $dRight, $dTop, $dh, $edgeNormal, $globalCoord, $h, $hNeighbor, $imgCenter, $lightDir, $localPx, $neighborCenter, $neighborIdx, $relPx, $result, $rimPx, $signTerm, $srcOwn, $topFaceShade, $uv);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $uv = $rt->binary('/', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_resolution, 2, 'float');
        $srcOwn = $rt->texture($_u_inputTex, $uv);
        $imgCenter = $rt->binary('*', $_u_fullResolution, $rt->f(0.5), 2, 'float');
        $relPx = $rt->binary('-', $globalCoord, $imgCenter, 2, 'float');
        $cellIdxF = $rt->component_wise('floor', $rt->binary('/', $relPx, $_u_squareSize, 2, 'float'));
        $localPx = $rt->binary('-', $relPx, $rt->binary('*', $cellIdxF, $_u_squareSize, 2, 'float'), 2, 'float');
        $cellCenter = $rt->binary('+', $imgCenter, $rt->binary('*', $rt->binary('+', $cellIdxF, $rt->f(0.5), 2, 'float'), $_u_squareSize, 2, 'float'), 2, 'float');
        $cellColor = $rt->swizzle($cellAvgColor3x3__vec2->($cellCenter), 'rgb');
        $h = $lum__vec3->($cellColor);
        $topFaceShade = $rt->binary('+', $rt->f(0.90000000000000002), $rt->binary('*', $rt->f(0.20000000000000001), $rt->binary('-', $h, $rt->f(0.5), 1, 'float'), 1, 'float'), 1, 'float');
        $rimPx = $rt->binary('*', $rt->f(0.14999999999999999), $_u_squareSize, 1, 'float');
        $dLeft = $rt->swizzle($localPx, 'x');
        $dRight = $rt->binary('-', $_u_squareSize, $rt->swizzle($localPx, 'x'), 1, 'float');
        $dBottom = $rt->swizzle($localPx, 'y');
        $dTop = $rt->binary('-', $_u_squareSize, $rt->swizzle($localPx, 'y'), 1, 'float');
        $dMin = $rt->component_wise('min', $rt->component_wise('min', $dLeft, $dRight), $rt->component_wise('min', $dBottom, $dTop));
        $bevelMul = $rt->f(1);
        $a = $rt->f(0.0);
        $dh = $rt->f(0.0);
        $edgeNormal = $rt->construct(2, 0.0);
        $hNeighbor = $rt->f(0.0);
        $lightDir = $rt->construct(2, 0.0);
        $neighborCenter = $rt->construct(2, 0.0);
        $neighborIdx = $rt->construct(2, 0.0);
        $signTerm = $rt->f(0.0);
        if ($rt->binary('<', $dMin, $rimPx)) {
            $neighborIdx = $cellIdxF;
            $edgeNormal = $rt->construct(2, 0.0);
            if ($rt->binary('==', $dMin, $dLeft)) {
                $neighborIdx = $rt->assign_swizzle($neighborIdx, 'x', $rt->binary('-', $rt->swizzle($neighborIdx, 'x'), $rt->f(1), 1, 'float'));
                @{$edgeNormal} = map { $rt->f32($_) } @{($rt->construct(2, $rt->unary('-', $rt->f(1)), $rt->f(0)))};
            } else {
                if ($rt->binary('==', $dMin, $dRight)) {
                    $neighborIdx = $rt->assign_swizzle($neighborIdx, 'x', $rt->binary('+', $rt->swizzle($neighborIdx, 'x'), $rt->f(1), 1, 'float'));
                    @{$edgeNormal} = map { $rt->f32($_) } @{($rt->construct(2, $rt->f(1), $rt->f(0)))};
                } else {
                    if ($rt->binary('==', $dMin, $dBottom)) {
                        $neighborIdx = $rt->assign_swizzle($neighborIdx, 'y', $rt->binary('-', $rt->swizzle($neighborIdx, 'y'), $rt->f(1), 1, 'float'));
                        @{$edgeNormal} = map { $rt->f32($_) } @{($rt->construct(2, $rt->f(0), $rt->unary('-', $rt->f(1))))};
                    } else {
                        $neighborIdx = $rt->assign_swizzle($neighborIdx, 'y', $rt->binary('+', $rt->swizzle($neighborIdx, 'y'), $rt->f(1), 1, 'float'));
                        @{$edgeNormal} = map { $rt->f32($_) } @{($rt->construct(2, $rt->f(0), $rt->f(1)))};
                    }
                }
            }
            $neighborCenter = $rt->binary('+', $imgCenter, $rt->binary('*', $rt->binary('+', $neighborIdx, $rt->f(0.5), 2, 'float'), $_u_squareSize, 2, 'float'), 2, 'float');
            $hNeighbor = $lum__vec3->($rt->swizzle($cellAvgColor3x3__vec2->($neighborCenter), 'rgb'));
            $dh = $rt->binary('-', $h, $hNeighbor, 1, 'float');
            $a = $rt->component_wise('radians', $_u_lightAngle);
            $lightDir = $rt->construct(2, $rt->component_wise('cos', $a), $rt->component_wise('sin', $a));
            $signTerm = $rt->dot($edgeNormal, $lightDir);
            $bevelMul = $rt->binary('+', $rt->f(1), $rt->binary('*', $rt->binary('*', $rt->binary('*', $rt->f(0.34999999999999998), $rt->binary('/', $_u_relief, $rt->f(100), 1, 'float'), 1, 'float'), $rt->component_wise('sign', $dh), 1, 'float'), $signTerm, 1, 'float'), 1, 'float');
        }
        $result = $rt->component_wise('clamp', $rt->binary('*', $rt->binary('*', $cellColor, $topFaceShade, 3, 'float'), $bevelMul, 3, 'float'), $rt->f(0), $rt->f(1));
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $result, $rt->swizzle($srcOwn, 'a')))};
    };
    $main__void->();
    my $_c = $g->{fragColor};
    $out->[0] = $rt->f32($_c->[0]); $out->[1] = $rt->f32($_c->[1]); $out->[2] = $rt->f32($_c->[2]); $out->[3] = $rt->f32($_c->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0 };
