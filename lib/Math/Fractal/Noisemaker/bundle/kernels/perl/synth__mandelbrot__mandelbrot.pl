# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($df64_quick_two_sum__float_float, $df64_two_sum__float_float, $df64_two_prod__float_float, $df64_add__vec2_vec2, $df64_sub__vec2_vec2, $df64_mul__vec2_vec2, $df64_mul_f__vec2_float, $df64_from__float, $df64_to_float__vec2, $getPoiMaxZoom__int, $getPOI__int_vec2_vec2, $transformCoords_df64__vec2_vec2_vec2_float_float_vec2_vec2, $inCardioid__float_float, $inPeriod2Bulb__float_float, $trapDistance__vec2_int, $mandelbrot_df64__vec2_vec2_int_float_float_vec2_vec2_float_float, $outputSmoothIteration__float_float_int, $outputDistance__vec2_vec2_float_int, $outputStripeAverage__float_float_float_int, $outputOrbitTrap__float_float_int, $computeValueAt_df64__vec2_vec2_vec2_float_float_int, $outputNormalMap__vec2_vec2_vec2_float_float_int_float, $getEffectiveZoom__int, $main__void);
    my $_retc;
    my $_u_resolution = exists $U->{'resolution'} ? $U->{'resolution'} : $rt->construct(2, 0.0);
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_time = exists $U->{'time'} ? $U->{'time'} : $rt->f(0.0);
    my $_u_poi = exists $U->{'poi'} ? $U->{'poi'} : 0;
    my $_u_outputMode = exists $U->{'outputMode'} ? $U->{'outputMode'} : 0;
    my $_u_iterations = exists $U->{'iterations'} ? $U->{'iterations'} : 0;
    my $_u_centerHiX = exists $U->{'centerHiX'} ? $U->{'centerHiX'} : $rt->f(0.0);
    my $_u_centerHiY = exists $U->{'centerHiY'} ? $U->{'centerHiY'} : $rt->f(0.0);
    my $_u_centerLoX = exists $U->{'centerLoX'} ? $U->{'centerLoX'} : $rt->f(0.0);
    my $_u_centerLoY = exists $U->{'centerLoY'} ? $U->{'centerLoY'} : $rt->f(0.0);
    my $_u_zoomSpeed = exists $U->{'zoomSpeed'} ? $U->{'zoomSpeed'} : $rt->f(0.0);
    my $_u_zoomDepth = exists $U->{'zoomDepth'} ? $U->{'zoomDepth'} : $rt->f(0.0);
    my $_u_invert = exists $U->{'invert'} ? $U->{'invert'} : $rt->f(0.0);
    my $_u_stripeFreq = exists $U->{'stripeFreq'} ? $U->{'stripeFreq'} : $rt->f(0.0);
    my $_u_trapShape = exists $U->{'trapShape'} ? $U->{'trapShape'} : 0;
    my $_u_lightAngle = exists $U->{'lightAngle'} ? $U->{'lightAngle'} : $rt->f(0.0);
    my $_u_rotation = exists $U->{'rotation'} ? $U->{'rotation'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $g->{PI} = $rt->f(3.1415926535900001);
    $g->{TAU} = $rt->f(6.2831853071800001);
    $g->{BAILOUT} = $rt->f(256);
    $g->{LOG2} = $rt->f(0.69314718055994529);
    $g->{MAX_ITER} = $rt->i(500);
    $df64_quick_two_sum__float_float = sub {
        my ($a, $b) = @_;
        my ($e, $s);
        $s = $rt->binary('+', $a, $b, 1, 'float');
        $e = $rt->binary('-', $b, $rt->binary('-', $s, $a, 1, 'float'), 1, 'float');
        return $rt->construct(2, $s, $e);
    };
    $df64_two_sum__float_float = sub {
        my ($a, $b) = @_;
        my ($e, $s, $v);
        $s = $rt->binary('+', $a, $b, 1, 'float');
        $v = $rt->binary('-', $s, $a, 1, 'float');
        $e = $rt->binary('+', $rt->binary('-', $a, $rt->binary('-', $s, $v, 1, 'float'), 1, 'float'), $rt->binary('-', $b, $v, 1, 'float'), 1, 'float');
        return $rt->construct(2, $s, $e);
    };
    $df64_two_prod__float_float = sub {
        my ($a, $b) = @_;
        my ($ah, $al, $bh, $bl, $ca, $cb, $e, $p);
        $p = $rt->binary('*', $a, $b, 1, 'float');
        $ca = $rt->binary('*', $rt->f(4097), $a, 1, 'float');
        $ah = $rt->binary('-', $ca, $rt->binary('-', $ca, $a, 1, 'float'), 1, 'float');
        $al = $rt->binary('-', $a, $ah, 1, 'float');
        $cb = $rt->binary('*', $rt->f(4097), $b, 1, 'float');
        $bh = $rt->binary('-', $cb, $rt->binary('-', $cb, $b, 1, 'float'), 1, 'float');
        $bl = $rt->binary('-', $b, $bh, 1, 'float');
        $e = $rt->binary('+', $rt->binary('+', $rt->binary('+', $rt->binary('-', $rt->binary('*', $ah, $bh, 1, 'float'), $p, 1, 'float'), $rt->binary('*', $ah, $bl, 1, 'float'), 1, 'float'), $rt->binary('*', $al, $bh, 1, 'float'), 1, 'float'), $rt->binary('*', $al, $bl, 1, 'float'), 1, 'float');
        return $rt->construct(2, $p, $e);
    };
    $df64_add__vec2_vec2 = sub {
        my ($a, $b) = @_;
        $a = $rt->copy($a, 'float');
        $b = $rt->copy($b, 'float');
        my ($s);
        $s = $df64_two_sum__float_float->($rt->swizzle($a, 'x'), $rt->swizzle($b, 'x'));
        $s = $rt->assign_swizzle($s, 'y', $rt->binary('+', $rt->swizzle($s, 'y'), $rt->binary('+', $rt->swizzle($a, 'y'), $rt->swizzle($b, 'y'), 1, 'float'), 1, 'float'));
        return $df64_quick_two_sum__float_float->($rt->swizzle($s, 'x'), $rt->swizzle($s, 'y'));
    };
    $df64_sub__vec2_vec2 = sub {
        my ($a, $b) = @_;
        $a = $rt->copy($a, 'float');
        $b = $rt->copy($b, 'float');
        return $df64_add__vec2_vec2->($a, $rt->construct(2, $rt->unary('-', $rt->swizzle($b, 'x')), $rt->unary('-', $rt->swizzle($b, 'y'))));
    };
    $df64_mul__vec2_vec2 = sub {
        my ($a, $b) = @_;
        $a = $rt->copy($a, 'float');
        $b = $rt->copy($b, 'float');
        my ($p);
        $p = $df64_two_prod__float_float->($rt->swizzle($a, 'x'), $rt->swizzle($b, 'x'));
        $p = $rt->assign_swizzle($p, 'y', $rt->binary('+', $rt->swizzle($p, 'y'), $rt->binary('+', $rt->binary('*', $rt->swizzle($a, 'x'), $rt->swizzle($b, 'y'), 1, 'float'), $rt->binary('*', $rt->swizzle($a, 'y'), $rt->swizzle($b, 'x'), 1, 'float'), 1, 'float'), 1, 'float'));
        return $df64_quick_two_sum__float_float->($rt->swizzle($p, 'x'), $rt->swizzle($p, 'y'));
    };
    $df64_mul_f__vec2_float = sub {
        my ($a, $b) = @_;
        $a = $rt->copy($a, 'float');
        my ($p);
        $p = $df64_two_prod__float_float->($rt->swizzle($a, 'x'), $b);
        $p = $rt->assign_swizzle($p, 'y', $rt->binary('+', $rt->swizzle($p, 'y'), $rt->binary('*', $rt->swizzle($a, 'y'), $b, 1, 'float'), 1, 'float'));
        return $df64_quick_two_sum__float_float->($rt->swizzle($p, 'x'), $rt->swizzle($p, 'y'));
    };
    $df64_from__float = sub {
        my ($a) = @_;
        return $rt->construct(2, $a, $rt->f(0));
    };
    $df64_to_float__vec2 = sub {
        my ($a) = @_;
        $a = $rt->copy($a, 'float');
        return $rt->binary('+', $rt->swizzle($a, 'x'), $rt->swizzle($a, 'y'), 1, 'float');
    };
    $getPoiMaxZoom__int = sub {
        my ($index) = @_;
        if ((($rt->binary('==', $index, $rt->i(2))) || ($rt->binary('==', $index, $rt->i(7))) ? 1 : 0)) {
            return $rt->f(7);
        }
        if ($rt->binary('==', $index, $rt->i(8))) {
            return $rt->f(10);
        }
        return $rt->f(14);
    };
    $getPOI__int_vec2_vec2 = sub {
        my ($index, $cX_df, $cY_df) = @_;
        $cX_df = $rt->copy($cX_df, 'float');
        $cY_df = $rt->copy($cY_df, 'float');
        if ($rt->binary('==', $index, $rt->i(1))) {
            @{$cX_df} = map { $rt->f32($_) } @{($rt->construct(2, $rt->unary('-', $rt->f(0.74453985691070557)), $rt->unary('-', $rt->f(3.4452027897000001e-09))))};
            @{$cY_df} = map { $rt->f32($_) } @{($rt->construct(2, $rt->f(0.12172377109527588), $rt->f(2.7991489404000002e-09)))};
        } else {
            if ($rt->binary('==', $index, $rt->i(2))) {
                @{$cX_df} = map { $rt->f32($_) } @{($rt->construct(2, $rt->f(0.29833000898361206), $rt->unary('-', $rt->f(8.9836120765000006e-09))))};
                @{$cY_df} = map { $rt->f32($_) } @{($rt->construct(2, $rt->f(0.0011099999537691474), $rt->f(4.6230852696000003e-11)))};
            } else {
                if ($rt->binary('==', $index, $rt->i(3))) {
                    @{$cX_df} = map { $rt->f32($_) } @{($rt->construct(2, $rt->unary('-', $rt->f(1.7548776865005493)), $rt->f(2.0253856592e-08)))};
                    @{$cY_df} = map { $rt->f32($_) } @{($rt->construct(2, $rt->f(0), $rt->f(0)))};
                } else {
                    if ($rt->binary('==', $index, $rt->i(4))) {
                        @{$cX_df} = map { $rt->f32($_) } @{($rt->construct(2, $rt->unary('-', $rt->f(1.7400623559951782)), $rt->unary('-', $rt->f(2.6584161760999999e-08))))};
                        @{$cY_df} = map { $rt->f32($_) } @{($rt->construct(2, $rt->f(0.028175339102745056), $rt->f(6.7646594229000005e-10)))};
                    } else {
                        if ($rt->binary('==', $index, $rt->i(5))) {
                            @{$cX_df} = map { $rt->f32($_) } @{($rt->construct(2, $rt->unary('-', $rt->f(1.4011552333831787)), $rt->f(4.4291128098000002e-08)))};
                            @{$cY_df} = map { $rt->f32($_) } @{($rt->construct(2, $rt->f(0), $rt->f(0)))};
                        } else {
                            if ($rt->binary('==', $index, $rt->i(6))) {
                                @{$cX_df} = map { $rt->f32($_) } @{($rt->construct(2, $rt->f(0.37500011920928955), $rt->f(8.5257595428000001e-10)))};
                                @{$cY_df} = map { $rt->f32($_) } @{($rt->construct(2, $rt->unary('-', $rt->f(0.21663938462734222)), $rt->unary('-', $rt->f(3.8103704636000004e-09))))};
                            } else {
                                if ($rt->binary('==', $index, $rt->i(7))) {
                                    @{$cX_df} = map { $rt->f32($_) } @{($rt->construct(2, $rt->unary('-', $rt->f(0.74453890323638916)), $rt->unary('-', $rt->f(1.6763610832999999e-08))))};
                                    @{$cY_df} = map { $rt->f32($_) } @{($rt->construct(2, $rt->f(0.12172418087720871), $rt->unary('-', $rt->f(8.7720870844999997e-10))))};
                                } else {
                                    if ($rt->binary('==', $index, $rt->i(8))) {
                                        @{$cX_df} = map { $rt->f32($_) } @{($rt->construct(2, $rt->unary('-', $rt->f(1.2553445100784302)), $rt->unary('-', $rt->f(1.4721569741000001e-08))))};
                                        @{$cY_df} = map { $rt->f32($_) } @{($rt->construct(2, $rt->unary('-', $rt->f(0.3822004497051239)), $rt->unary('-', $rt->f(1.3294876088999999e-08))))};
                                    } else {
                                        @{$cX_df} = map { $rt->f32($_) } @{($rt->construct(2, $_u_centerHiX, $_u_centerLoX))};
                                        @{$cY_df} = map { $rt->f32($_) } @{($rt->construct(2, $_u_centerHiY, $_u_centerLoY))};
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
        return (undef, $cX_df, $cY_df);
    };
    $transformCoords_df64__vec2_vec2_vec2_float_float_vec2_vec2 = sub {
        my ($fragCoord, $cX_df, $cY_df, $z, $rot, $re_df, $im_df) = @_;
        $fragCoord = $rt->copy($fragCoord, 'float');
        $cX_df = $rt->copy($cX_df, 'float');
        $cY_df = $rt->copy($cY_df, 'float');
        $re_df = $rt->copy($re_df, 'float');
        $im_df = $rt->copy($im_df, 'float');
        my ($angle, $c, $s, $scale, $uv);
        $uv = $rt->binary('/', $rt->binary('-', $fragCoord, $rt->binary('*', $rt->f(0.5), $_u_fullResolution, 2, 'float'), 2, 'float'), $rt->component_wise('min', $rt->swizzle($_u_fullResolution, 'x'), $rt->swizzle($_u_fullResolution, 'y')), 2, 'float');
        $angle = $rt->binary('/', $rt->binary('*', $rt->unary('-', $rot), $g->{TAU}, 1, 'float'), $rt->f(360), 1, 'float');
        $c = $rt->component_wise('cos', $angle);
        $s = $rt->component_wise('sin', $angle);
        @{$uv} = map { $rt->f32($_) } @{($rt->matrix_mult($rt->construct(4, $c, $rt->unary('-', $s), $s, $c), $uv, 2))};
        $scale = $rt->binary('/', $rt->f(2.5), $z, 1, 'float');
        @{$re_df} = map { $rt->f32($_) } @{($df64_add__vec2_vec2->($df64_from__float->($rt->binary('*', $rt->swizzle($uv, 'x'), $scale, 1, 'float')), $cX_df))};
        @{$im_df} = map { $rt->f32($_) } @{($df64_add__vec2_vec2->($df64_from__float->($rt->binary('*', $rt->swizzle($uv, 'y'), $scale, 1, 'float')), $cY_df))};
        return (undef, $re_df, $im_df);
    };
    $inCardioid__float_float = sub {
        my ($x, $y) = @_;
        my ($q, $y2);
        $y2 = $rt->binary('*', $y, $y, 1, 'float');
        $q = $rt->binary('+', $rt->binary('*', $rt->binary('-', $x, $rt->f(0.25), 1, 'float'), $rt->binary('-', $x, $rt->f(0.25), 1, 'float'), 1, 'float'), $y2, 1, 'float');
        return $rt->binary('<=', $rt->binary('*', $q, $rt->binary('+', $q, $rt->binary('-', $x, $rt->f(0.25), 1, 'float'), 1, 'float'), 1, 'float'), $rt->binary('*', $rt->f(0.25), $y2, 1, 'float'));
    };
    $inPeriod2Bulb__float_float = sub {
        my ($x, $y) = @_;
        my ($xp1);
        $xp1 = $rt->binary('+', $x, $rt->f(1), 1, 'float');
        return $rt->binary('<=', $rt->binary('+', $rt->binary('*', $xp1, $xp1, 1, 'float'), $rt->binary('*', $y, $y, 1, 'float'), 1, 'float'), $rt->f(0.0625));
    };
    $trapDistance__vec2_int = sub {
        my ($z, $shape) = @_;
        $z = $rt->copy($z, 'float');
        if ($rt->binary('==', $shape, $rt->i(0))) {
            return $rt->length($z);
        } else {
            if ($rt->binary('==', $shape, $rt->i(1))) {
                return $rt->component_wise('min', $rt->component_wise('abs', $rt->swizzle($z, 'x')), $rt->component_wise('abs', $rt->swizzle($z, 'y')));
            } else {
                return $rt->component_wise('abs', $rt->binary('-', $rt->length($z), $rt->f(1), 1, 'float'));
            }
        }
    };
    $mandelbrot_df64__vec2_vec2_int_float_float_vec2_vec2_float_float = sub {
        my ($c_re, $c_im, $maxIter, $smoothIter, $rawIter, $z_final, $dz_final, $stripeAcc, $trapMin) = @_;
        $c_re = $rt->copy($c_re, 'float');
        $c_im = $rt->copy($c_im, 'float');
        $z_final = $rt->copy($z_final, 'float');
        $dz_final = $rt->copy($dz_final, 'float');
        my ($_for0_first, $cx, $cy, $dz, $fx, $fy, $i, $log_zn, $mag2, $n, $new_zi, $new_zr, $nu, $post_mag2, $post_zx, $post_zy, $stripe, $trap, $zi, $zi2, $zr, $zr2, $zri, $zx, $zy);
        $cx = $df64_to_float__vec2->($c_re);
        $cy = $df64_to_float__vec2->($c_im);
        if ((($inCardioid__float_float->($cx, $cy)) || ($inPeriod2Bulb__float_float->($cx, $cy)) ? 1 : 0)) {
            $smoothIter = $rt->construct(1, $maxIter);
            $rawIter = $rt->construct(1, $maxIter);
            @{$z_final} = map { $rt->f32($_) } @{($rt->construct(2, $rt->f(0)))};
            @{$dz_final} = map { $rt->f32($_) } @{($rt->construct(2, $rt->f(0)))};
            $stripeAcc = $rt->f(0);
            $trapMin = $rt->f(1e+20);
            return (undef, $smoothIter, $rawIter, $z_final, $dz_final, $stripeAcc, $trapMin);
        }
        $zr = $rt->construct(2, $rt->f(0));
        $zi = $rt->construct(2, $rt->f(0));
        $dz = $rt->construct(2, $rt->f(1), $rt->f(0));
        $stripe = $rt->f(0);
        $trap = $rt->f(1e+20);
        $i = $rt->f(0);
        $n = $rt->i(0);
        $_for0_first = 1;
        for my $_for0 (0 .. 1048575) {
            if (!$_for0_first) {
                $n = $rt->binary('+', $n, $rt->i(1), 1, 'int');
            }
            $_for0_first = 0;
            if (!($rt->binary('<', $n, $g->{MAX_ITER}))) {
                last;
            }
            if ($rt->binary('>=', $n, $maxIter)) {
                last;
            }
            $zx = $df64_to_float__vec2->($zr);
            $zy = $df64_to_float__vec2->($zi);
            @{$dz} = map { $rt->f32($_) } @{($rt->construct(2, $rt->binary('+', $rt->binary('*', $rt->f(2), $rt->binary('-', $rt->binary('*', $zx, $rt->swizzle($dz, 'x'), 1, 'float'), $rt->binary('*', $zy, $rt->swizzle($dz, 'y'), 1, 'float'), 1, 'float'), 1, 'float'), $rt->f(1), 1, 'float'), $rt->binary('*', $rt->f(2), $rt->binary('+', $rt->binary('*', $zx, $rt->swizzle($dz, 'y'), 1, 'float'), $rt->binary('*', $zy, $rt->swizzle($dz, 'x'), 1, 'float'), 1, 'float'), 1, 'float')))};
            $zr2 = $df64_mul__vec2_vec2->($zr, $zr);
            $zi2 = $df64_mul__vec2_vec2->($zi, $zi);
            $zri = $df64_mul__vec2_vec2->($zr, $zi);
            $new_zr = $df64_add__vec2_vec2->($df64_sub__vec2_vec2->($zr2, $zi2), $c_re);
            $new_zi = $df64_add__vec2_vec2->($df64_mul_f__vec2_float->($zri, $rt->f(2)), $c_im);
            @{$zr} = map { $rt->f32($_) } @{($new_zr)};
            @{$zi} = map { $rt->f32($_) } @{($new_zi)};
            $post_zx = $df64_to_float__vec2->($zr);
            $post_zy = $df64_to_float__vec2->($zi);
            $post_mag2 = $rt->binary('+', $rt->binary('*', $post_zx, $post_zx, 1, 'float'), $rt->binary('*', $post_zy, $post_zy, 1, 'float'), 1, 'float');
            if ($rt->binary('>', $_u_stripeFreq, $rt->f(0))) {
                $stripe = $rt->binary('+', $stripe, $rt->component_wise('sin', $rt->binary('*', $_u_stripeFreq, $rt->component_wise('atan', $post_zy, $post_zx), 1, 'float')), 1, 'float');
            }
            $trap = $rt->component_wise('min', $trap, $trapDistance__vec2_int->($rt->construct(2, $post_zx, $post_zy), $_u_trapShape));
            if ($rt->binary('>', $post_mag2, $rt->binary('*', $g->{BAILOUT}, $g->{BAILOUT}, 1, 'float'))) {
                last;
            }
            $i = $rt->binary('+', $i, $rt->f(1), 1, 'float');
        }
        $rawIter = $i;
        $fx = $df64_to_float__vec2->($zr);
        $fy = $df64_to_float__vec2->($zi);
        @{$z_final} = map { $rt->f32($_) } @{($rt->construct(2, $fx, $fy))};
        @{$dz_final} = map { $rt->f32($_) } @{($dz)};
        $stripeAcc = $stripe;
        $trapMin = $trap;
        $mag2 = $rt->dot($z_final, $z_final);
        $log_zn = $rt->f(0.0);
        $nu = $rt->f(0.0);
        if ((($rt->binary('<', $i, $rt->construct(1, $maxIter))) && ($rt->binary('>', $mag2, $rt->f(1))) ? 1 : 0)) {
            $log_zn = $rt->binary('*', $rt->component_wise('log', $mag2), $rt->f(0.5), 1, 'float');
            $nu = $rt->binary('/', $rt->component_wise('log', $rt->binary('/', $log_zn, $g->{LOG2}, 1, 'float')), $g->{LOG2}, 1, 'float');
            $smoothIter = $rt->binary('-', $rt->binary('+', $i, $rt->f(1), 1, 'float'), $nu, 1, 'float');
        } else {
            $smoothIter = $i;
        }
        return (undef, $smoothIter, $rawIter, $z_final, $dz_final, $stripeAcc, $trapMin);
    };
    $outputSmoothIteration__float_float_int = sub {
        my ($smoothIter, $rawIter, $maxIter) = @_;
        if ($rt->binary('>=', $rawIter, $rt->construct(1, $maxIter))) {
            return $rt->f(0);
        }
        return $rt->binary('/', $smoothIter, $rt->construct(1, $maxIter), 1, 'float');
    };
    $outputDistance__vec2_vec2_float_int = sub {
        my ($z, $dz, $rawIter, $maxIter) = @_;
        $z = $rt->copy($z, 'float');
        $dz = $rt->copy($dz, 'float');
        my ($dist, $dmag, $mag);
        if ($rt->binary('>=', $rawIter, $rt->construct(1, $maxIter))) {
            return $rt->f(0);
        }
        $mag = $rt->length($z);
        $dmag = $rt->length($dz);
        if ($rt->binary('==', $dmag, $rt->f(0))) {
            return $rt->f(0);
        }
        $dist = $rt->binary('/', $rt->binary('*', $rt->binary('*', $rt->f(2), $mag, 1, 'float'), $rt->component_wise('log', $mag), 1, 'float'), $dmag, 1, 'float');
        return $rt->component_wise('clamp', $rt->binary('*', $rt->component_wise('sqrt', $rt->binary('*', $dist, $rt->construct(1, $maxIter), 1, 'float')), $rt->f(0.5), 1, 'float'), $rt->f(0), $rt->f(1));
    };
    $outputStripeAverage__float_float_float_int = sub {
        my ($smoothIter, $rawIter, $stripeAcc, $maxIter) = @_;
        my ($avg, $count, $frac);
        if ($rt->binary('>=', $rawIter, $rt->construct(1, $maxIter))) {
            return $rt->f(0);
        }
        $count = $rt->component_wise('max', $rawIter, $rt->f(1));
        $avg = $rt->binary('/', $stripeAcc, $count, 1, 'float');
        $frac = $rt->binary('-', $smoothIter, $rt->component_wise('floor', $smoothIter), 1, 'float');
        return $rt->component_wise('clamp', $rt->binary('+', $rt->f(0.5), $rt->binary('*', $rt->binary('*', $rt->f(0.5), $avg, 1, 'float'), $rt->binary('-', $rt->f(1), $frac, 1, 'float'), 1, 'float'), 1, 'float'), $rt->f(0), $rt->f(1));
    };
    $outputOrbitTrap__float_float_int = sub {
        my ($trapMin, $rawIter, $maxIter) = @_;
        if ($rt->binary('>=', $rawIter, $rt->construct(1, $maxIter))) {
            return $rt->f(0);
        }
        return $rt->component_wise('clamp', $rt->binary('-', $rt->f(1), $rt->binary('*', $trapMin, $rt->f(0.5), 1, 'float'), 1, 'float'), $rt->f(0), $rt->f(1));
    };
    $computeValueAt_df64__vec2_vec2_vec2_float_float_int = sub {
        my ($fragCoord, $cX_df, $cY_df, $z_zoom, $rot, $maxIter) = @_;
        $fragCoord = $rt->copy($fragCoord, 'float');
        $cX_df = $rt->copy($cX_df, 'float');
        $cY_df = $rt->copy($cY_df, 'float');
        my ($dzf, $im_df, $rI, $re_df, $sI, $sa, $tm, $zf);
        $re_df = $rt->construct(2, 0.0);
        $im_df = $rt->construct(2, 0.0);
        ($_retc, $re_df, $im_df) = $transformCoords_df64__vec2_vec2_vec2_float_float_vec2_vec2->($fragCoord, $cX_df, $cY_df, $z_zoom, $rot, $re_df, $im_df);
        $sI = $rt->f(0.0);
        $rI = $rt->f(0.0);
        $zf = $rt->construct(2, 0.0);
        $dzf = $rt->construct(2, 0.0);
        $sa = $rt->f(0.0);
        $tm = $rt->f(0.0);
        ($_retc, $sI, $rI, $zf, $dzf, $sa, $tm) = $mandelbrot_df64__vec2_vec2_int_float_float_vec2_vec2_float_float->($re_df, $im_df, $maxIter, $sI, $rI, $zf, $dzf, $sa, $tm);
        return $outputDistance__vec2_vec2_float_int->($zf, $dzf, $rI, $maxIter);
    };
    $outputNormalMap__vec2_vec2_vec2_float_float_int_float = sub {
        my ($fragCoord, $cX_df, $cY_df, $z_zoom, $rot, $maxIter, $angle) = @_;
        $fragCoord = $rt->copy($fragCoord, 'float');
        $cX_df = $rt->copy($cX_df, 'float');
        $cY_df = $rt->copy($cY_df, 'float');
        my ($diffuse, $eps, $h0, $hx, $hy, $lightDir, $normal, $rad);
        $eps = $rt->binary('/', $rt->f(1), $rt->component_wise('min', $rt->swizzle($_u_fullResolution, 'x'), $rt->swizzle($_u_fullResolution, 'y')), 1, 'float');
        $h0 = $computeValueAt_df64__vec2_vec2_vec2_float_float_int->($fragCoord, $cX_df, $cY_df, $z_zoom, $rot, $maxIter);
        $hx = $computeValueAt_df64__vec2_vec2_vec2_float_float_int->($rt->binary('+', $fragCoord, $rt->construct(2, $rt->f(1), $rt->f(0)), 2, 'float'), $cX_df, $cY_df, $z_zoom, $rot, $maxIter);
        $hy = $computeValueAt_df64__vec2_vec2_vec2_float_float_int->($rt->binary('+', $fragCoord, $rt->construct(2, $rt->f(0), $rt->f(1)), 2, 'float'), $cX_df, $cY_df, $z_zoom, $rot, $maxIter);
        $normal = $rt->normalize($rt->construct(3, $rt->binary('-', $h0, $hx, 1, 'float'), $rt->binary('-', $h0, $hy, 1, 'float'), $eps));
        $rad = $rt->binary('/', $rt->binary('*', $angle, $g->{TAU}, 1, 'float'), $rt->f(360), 1, 'float');
        $lightDir = $rt->normalize($rt->construct(3, $rt->component_wise('cos', $rad), $rt->component_wise('sin', $rad), $rt->f(0.69999999999999996)));
        $diffuse = $rt->component_wise('max', $rt->dot($normal, $lightDir), $rt->f(0));
        return $rt->component_wise('clamp', $diffuse, $rt->f(0), $rt->f(1));
    };
    $getEffectiveZoom__int = sub {
        my ($poiIndex) = @_;
        my ($effDepth, $maxDepth, $zoomPhase);
        $maxDepth = (($rt->binary('>', $poiIndex, $rt->i(0))) ? ($getPoiMaxZoom__int->($poiIndex)) : ($rt->f(14)));
        $effDepth = $rt->component_wise('min', $_u_zoomDepth, $maxDepth);
        $zoomPhase = $rt->f(0.0);
        if ($rt->binary('>', $_u_zoomSpeed, $rt->f(0))) {
            $zoomPhase = $rt->binary('*', $rt->f(0.5), $rt->binary('-', $rt->f(1), $rt->component_wise('cos', $rt->binary('*', $rt->binary('*', $_u_time, $_u_zoomSpeed, 1, 'float'), $g->{TAU}, 1, 'float')), 1, 'float'), 1, 'float');
            return $rt->component_wise('pow', $rt->f(10), $rt->binary('*', $effDepth, $zoomPhase, 1, 'float'));
        }
        return $rt->component_wise('pow', $rt->f(10), $effDepth);
    };
    $main__void = sub {
        my ($cX_df, $cY_df, $dz_final, $effZoom, $globalCoord, $im_df, $maxIter, $rawI, $re_df, $rot, $smoothI, $stripeAcc, $trapMin, $value, $z_final);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $maxIter = $rt->component_wise('min', $_u_iterations, $g->{MAX_ITER});
        $effZoom = $getEffectiveZoom__int->($_u_poi);
        $rot = (($rt->binary('>', $_u_poi, $rt->i(0))) ? ($rt->f(0)) : ($_u_rotation));
        $cX_df = $rt->construct(2, 0.0);
        $cY_df = $rt->construct(2, 0.0);
        ($_retc, $cX_df, $cY_df) = $getPOI__int_vec2_vec2->($_u_poi, $cX_df, $cY_df);
        $value = $rt->f(0.0);
        $dz_final = $rt->construct(2, 0.0);
        $im_df = $rt->construct(2, 0.0);
        $rawI = $rt->f(0.0);
        $re_df = $rt->construct(2, 0.0);
        $smoothI = $rt->f(0.0);
        $stripeAcc = $rt->f(0.0);
        $trapMin = $rt->f(0.0);
        $z_final = $rt->construct(2, 0.0);
        if ($rt->binary('==', $_u_outputMode, $rt->i(4))) {
            $value = $outputNormalMap__vec2_vec2_vec2_float_float_int_float->($globalCoord, $cX_df, $cY_df, $effZoom, $rot, $maxIter, $_u_lightAngle);
        } else {
            $smoothI = $rt->f(0.0);
            $rawI = $rt->f(0.0);
            $z_final = $rt->construct(2, 0.0);
            $dz_final = $rt->construct(2, 0.0);
            $stripeAcc = $rt->f(0.0);
            $trapMin = $rt->f(0.0);
            $re_df = $rt->construct(2, 0.0);
            $im_df = $rt->construct(2, 0.0);
            ($_retc, $re_df, $im_df) = $transformCoords_df64__vec2_vec2_vec2_float_float_vec2_vec2->($globalCoord, $cX_df, $cY_df, $effZoom, $rot, $re_df, $im_df);
            ($_retc, $smoothI, $rawI, $z_final, $dz_final, $stripeAcc, $trapMin) = $mandelbrot_df64__vec2_vec2_int_float_float_vec2_vec2_float_float->($re_df, $im_df, $maxIter, $smoothI, $rawI, $z_final, $dz_final, $stripeAcc, $trapMin);
            if ($rt->binary('==', $_u_outputMode, $rt->i(0))) {
                $value = $outputSmoothIteration__float_float_int->($smoothI, $rawI, $maxIter);
            } else {
                if ($rt->binary('==', $_u_outputMode, $rt->i(1))) {
                    $value = $outputDistance__vec2_vec2_float_int->($z_final, $dz_final, $rawI, $maxIter);
                } else {
                    if ($rt->binary('==', $_u_outputMode, $rt->i(2))) {
                        $value = $outputStripeAverage__float_float_float_int->($smoothI, $rawI, $stripeAcc, $maxIter);
                    } else {
                        if ($rt->binary('==', $_u_outputMode, $rt->i(3))) {
                            $value = $outputOrbitTrap__float_float_int->($trapMin, $rawI, $maxIter);
                        } else {
                            $value = $outputSmoothIteration__float_float_int->($smoothI, $rawI, $maxIter);
                        }
                    }
                }
            }
        }
        if ($rt->binary('>', $_u_invert, $rt->f(0.5))) {
            $value = $rt->binary('-', $rt->f(1), $value, 1, 'float');
        }
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $rt->construct(3, $value), $rt->f(1)))};
    };
    $main__void->();
    my $_c = $g->{fragColor};
    $out->[0] = $rt->f32($_c->[0]); $out->[1] = $rt->f32($_c->[1]); $out->[2] = $rt->f32($_c->[2]); $out->[3] = $rt->f32($_c->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0 };
