# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($computeVignetteMask__vec2_vec2, $main__void);
    my $_retc;
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_vignetteBrightness = exists $U->{'vignetteBrightness'} ? $U->{'vignetteBrightness'} : $rt->f(0.0);
    my $_u_alpha = exists $U->{'alpha'} ? $U->{'alpha'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $computeVignetteMask__vec2_vec2 = sub {
        my ($uv, $dims) = @_;
        $uv = $rt->copy($uv, 'float');
        $dims = $rt->copy($dims, 'float');
        my ($aspect, $delta, $maxRadius, $normalizedDist, $scaled);
        if ((($rt->binary('<=', $rt->swizzle($dims, 'x'), $rt->f(0))) || ($rt->binary('<=', $rt->swizzle($dims, 'y'), $rt->f(0))) ? 1 : 0)) {
            return $rt->f(0);
        }
        $delta = $rt->component_wise('abs', $rt->binary('-', $uv, $rt->construct(2, $rt->f(0.5)), 2, 'float'));
        $aspect = $rt->binary('/', $rt->swizzle($dims, 'x'), $rt->component_wise('max', $rt->swizzle($dims, 'y'), $rt->f(1)), 1, 'float');
        $scaled = $rt->construct(2, $rt->binary('*', $rt->swizzle($delta, 'x'), $aspect, 1, 'float'), $rt->swizzle($delta, 'y'));
        $maxRadius = $rt->length($rt->construct(2, $rt->binary('*', $aspect, $rt->f(0.5), 1, 'float'), $rt->f(0.5)));
        if ($rt->binary('<=', $maxRadius, $rt->f(0))) {
            return $rt->f(0);
        }
        $normalizedDist = $rt->component_wise('clamp', $rt->binary('/', $rt->length($scaled), $maxRadius, 1, 'float'), $rt->f(0), $rt->f(1));
        return $rt->binary('*', $normalizedDist, $normalizedDist, 1, 'float');
    };
    $main__void = sub {
        my ($brightnessRgb, $dims, $edgeBlend, $finalRgb, $globalCoord, $globalUV, $mask, $texSize, $texel, $tileDims, $uv);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $texSize = $rt->texture_size($_u_inputTex);
        $tileDims = $rt->construct(2, $texSize);
        $dims = (($rt->binary('>', $rt->swizzle($_u_fullResolution, 'x'), $rt->f(0))) ? ($_u_fullResolution) : ($tileDims));
        $uv = $rt->binary('/', $rt->swizzle($ctx->{frag_coord}, 'xy'), $tileDims, 2, 'float');
        $globalUV = $rt->binary('/', $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float'), $dims, 2, 'float');
        $texel = $rt->texture($_u_inputTex, $uv);
        $mask = $computeVignetteMask__vec2_vec2->($globalUV, $dims);
        $brightnessRgb = $rt->construct(3, $_u_vignetteBrightness);
        $edgeBlend = $rt->component_wise('mix', $rt->swizzle($texel, 'rgb'), $brightnessRgb, $mask);
        $finalRgb = $rt->component_wise('mix', $rt->swizzle($texel, 'rgb'), $edgeBlend, $_u_alpha);
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $finalRgb, $rt->swizzle($texel, 'a')))};
    };
    $main__void->();
    my $_c0 = $g->{fragColor};
    $out->[0] = $rt->f32($_c0->[0]); $out->[1] = $rt->f32($_c0->[1]); $out->[2] = $rt->f32($_c0->[2]); $out->[3] = $rt->f32($_c0->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0, output_names => ['fragColor'] };
