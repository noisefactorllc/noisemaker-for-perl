# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($main__void);
    my $_retc;
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_sharpenRadius = exists $U->{'sharpenRadius'} ? $U->{'sharpenRadius'} : 0;
    my $_u_sharpenAmount = exists $U->{'sharpenAmount'} ? $U->{'sharpenAmount'} : $rt->f(0.0);
    my $_u_renderScale = exists $U->{'renderScale'} ? $U->{'renderScale'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $main__void = sub {
        my ($_for0_first, $_for1_first, $blurSum, $blurred, $center, $coord, $dist2, $kx, $ky, $samplePos, $scaledRadius, $sharpened, $sigma, $sigma2, $texSample, $texSize, $weight, $weightSum);
        $texSize = $rt->texture_size($_u_inputTex);
        $coord = $rt->construct(2, $rt->swizzle($ctx->{frag_coord}, 'xy'), 'int');
        $center = $rt->texel_fetch($_u_inputTex, $coord, $rt->i(0));
        $scaledRadius = $rt->construct(1, $rt->binary('*', $rt->construct(1, $_u_sharpenRadius), $_u_renderScale, 1, 'float'), 'int');
        if ((($rt->binary('<=', $scaledRadius, $rt->i(0))) || ($rt->binary('<=', $_u_sharpenAmount, $rt->f(0))) ? 1 : 0)) {
            @{$g->{fragColor}} = map { $rt->f32($_) } @{($center)};
            return;
        }
        $sigma = $rt->binary('/', $rt->construct(1, $scaledRadius), $rt->f(2), 1, 'float');
        $sigma2 = $rt->binary('*', $sigma, $sigma, 1, 'float');
        $blurSum = $rt->construct(3, $rt->f(0));
        $weightSum = $rt->f(0);
        $ky = $rt->unary('-', $scaledRadius);
        $_for0_first = 1;
        for my $_for0 (0 .. 1048575) {
            if (!$_for0_first) {
                $ky = $rt->binary('+', $ky, $rt->i(1), 1, 'int');
            }
            $_for0_first = 0;
            if (!($rt->binary('<=', $ky, $scaledRadius))) {
                last;
            }
            $kx = $rt->unary('-', $scaledRadius);
            $_for1_first = 1;
            for my $_for1 (0 .. 1048575) {
                if (!$_for1_first) {
                    $kx = $rt->binary('+', $kx, $rt->i(1), 1, 'int');
                }
                $_for1_first = 0;
                if (!($rt->binary('<=', $kx, $scaledRadius))) {
                    last;
                }
                $samplePos = $rt->binary('+', $coord, $rt->construct(2, $kx, $ky, 'int'), 2, 'int');
                @{$samplePos} = @{($rt->component_wise('clamp', $samplePos, $rt->construct(2, $rt->i(0), 'int'), $rt->binary('-', $texSize, $rt->i(1), 2, 'int')))};
                $dist2 = $rt->construct(1, $rt->binary('+', $rt->binary('*', $kx, $kx, 1, 'int'), $rt->binary('*', $ky, $ky, 1, 'int'), 1, 'int'));
                $weight = $rt->component_wise('exp', $rt->binary('/', $rt->unary('-', $dist2), $rt->binary('*', $rt->f(2), $sigma2, 1, 'float'), 1, 'float'));
                $texSample = $rt->texel_fetch($_u_inputTex, $samplePos, $rt->i(0));
                @{$blurSum} = map { $rt->f32($_) } @{($rt->binary('+', $blurSum, $rt->binary('*', $rt->swizzle($texSample, 'rgb'), $weight, 3, 'float'), 3, 'float'))};
                $weightSum = $rt->binary('+', $weightSum, $weight, 1, 'float');
            }
        }
        $blurred = $rt->binary('/', $blurSum, $weightSum, 3, 'float');
        $sharpened = $rt->binary('+', $rt->swizzle($center, 'rgb'), $rt->binary('*', $_u_sharpenAmount, $rt->binary('-', $rt->swizzle($center, 'rgb'), $blurred, 3, 'float'), 3, 'float'), 3, 'float');
        @{$sharpened} = map { $rt->f32($_) } @{($rt->component_wise('clamp', $sharpened, $rt->f(0), $rt->f(1)))};
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $sharpened, $rt->swizzle($center, 'a')))};
    };
    $main__void->();
    my $_c0 = $g->{fragColor};
    $out->[0] = $rt->f32($_c0->[0]); $out->[1] = $rt->f32($_c0->[1]); $out->[2] = $rt->f32($_c0->[2]); $out->[3] = $rt->f32($_c0->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0, output_names => ['fragColor'] };
