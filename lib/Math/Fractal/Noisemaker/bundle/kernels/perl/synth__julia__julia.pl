# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($getPOI__int, $getAnimatedC__int_float_float, $cmul__vec2_vec2, $df64_from__float, $df64_add__vec2_vec2, $df64_sub__vec2_vec2, $df64_split__float_float_float, $df64_mul__vec2_vec2, $df64_mul_f__vec2_float, $resolveC__void, $transformCoords__vec2_float_vec2_vec2, $juliaIterate__vec2_vec2_vec2_int_float_int, $outputSmoothIteration__struct1_float, $outputDistanceEstimation__struct1_float, $outputStripeAverage__struct1_float, $outputOrbitTrap__struct1_float, $iterateSmooth__vec2_vec2_int_float, $outputNormalMap__vec2_vec2_int_float_float, $main__void);
    my $_retc;
    my $_u_resolution = exists $U->{'resolution'} ? $U->{'resolution'} : $rt->construct(2, 0.0);
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_time = exists $U->{'time'} ? $U->{'time'} : $rt->f(0.0);
    my $_u_cReal = exists $U->{'cReal'} ? $U->{'cReal'} : $rt->f(0.0);
    my $_u_cImag = exists $U->{'cImag'} ? $U->{'cImag'} : $rt->f(0.0);
    my $_u_poi = exists $U->{'poi'} ? $U->{'poi'} : 0;
    my $_u_outputMode = exists $U->{'outputMode'} ? $U->{'outputMode'} : 0;
    my $_u_centerX = exists $U->{'centerX'} ? $U->{'centerX'} : $rt->f(0.0);
    my $_u_centerY = exists $U->{'centerY'} ? $U->{'centerY'} : $rt->f(0.0);
    my $_u_rotation = exists $U->{'rotation'} ? $U->{'rotation'} : $rt->f(0.0);
    my $_u_iterations = exists $U->{'iterations'} ? $U->{'iterations'} : 0;
    my $_u_stripeFreq = exists $U->{'stripeFreq'} ? $U->{'stripeFreq'} : $rt->f(0.0);
    my $_u_trapShape = exists $U->{'trapShape'} ? $U->{'trapShape'} : 0;
    my $_u_lightAngle = exists $U->{'lightAngle'} ? $U->{'lightAngle'} : $rt->f(0.0);
    my $_u_cPath = exists $U->{'cPath'} ? $U->{'cPath'} : 0;
    my $_u_cSpeed = exists $U->{'cSpeed'} ? $U->{'cSpeed'} : $rt->f(0.0);
    my $_u_cRadius = exists $U->{'cRadius'} ? $U->{'cRadius'} : $rt->f(0.0);
    my $_u_invert = exists $U->{'invert'} ? $U->{'invert'} : 0;
    my $_u_zoomSpeed = exists $U->{'zoomSpeed'} ? $U->{'zoomSpeed'} : $rt->f(0.0);
    my $_u_zoomDepth = exists $U->{'zoomDepth'} ? $U->{'zoomDepth'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $g->{PI} = $rt->f(3.1415926535900001);
    $g->{TAU} = $rt->f(6.2831853071800001);
    $g->{BAILOUT} = $rt->f(256);
    $g->{LOG2} = $rt->f(0.69314718055994529);
    $g->{df64_split_const} = $rt->f(4097);
    $getPOI__int = sub {
        my ($idx) = @_;
        if ($rt->binary('==', $idx, $rt->i(1))) {
            return $rt->construct(2, $rt->unary('-', $rt->f(0.123)), $rt->f(0.745));
        }
        if ($rt->binary('==', $idx, $rt->i(2))) {
            return $rt->construct(2, $rt->unary('-', $rt->f(0.39050000000000001)), $rt->f(0.58679999999999999));
        }
        if ($rt->binary('==', $idx, $rt->i(3))) {
            return $rt->construct(2, $rt->f(0), $rt->f(1));
        }
        if ($rt->binary('==', $idx, $rt->i(4))) {
            return $rt->construct(2, $rt->unary('-', $rt->f(1)), $rt->f(0));
        }
        if ($rt->binary('==', $idx, $rt->i(5))) {
            return $rt->construct(2, $rt->unary('-', $rt->f(0.74550000000000005)), $rt->f(0.113));
        }
        if ($rt->binary('==', $idx, $rt->i(6))) {
            return $rt->construct(2, $rt->unary('-', $rt->f(0.098599999999999993)), $rt->f(0.65339999999999998));
        }
        if ($rt->binary('==', $idx, $rt->i(7))) {
            return $rt->construct(2, $rt->unary('-', $rt->f(0.80000000000000004)), $rt->f(0.156));
        }
        if ($rt->binary('==', $idx, $rt->i(8))) {
            return $rt->construct(2, $rt->unary('-', $rt->f(0.75)), $rt->f(0));
        }
        if ($rt->binary('==', $idx, $rt->i(9))) {
            return $rt->construct(2, $rt->unary('-', $rt->f(0.57920000000000005)), $rt->f(0.53849999999999998));
        }
        if ($rt->binary('==', $idx, $rt->i(10))) {
            return $rt->construct(2, $rt->f(0.28000000000000003), $rt->f(0.0080000000000000002));
        }
        return $rt->construct(2, $rt->unary('-', $rt->f(0.123)), $rt->f(0.745));
    };
    $getAnimatedC__int_float_float = sub {
        my ($pathType, $t, $radius) = @_;
        my ($theta);
        $theta = $rt->binary('*', $t, $g->{TAU}, 1, 'float');
        if ($rt->binary('==', $pathType, $rt->i(1))) {
            return $rt->construct(2, $rt->binary('-', $rt->binary('*', $rt->component_wise('cos', $theta), $rt->f(0.5), 1, 'float'), $rt->binary('*', $rt->component_wise('cos', $rt->binary('*', $rt->f(2), $theta, 1, 'float')), $rt->f(0.25), 1, 'float'), 1, 'float'), $rt->binary('-', $rt->binary('*', $rt->component_wise('sin', $theta), $rt->f(0.5), 1, 'float'), $rt->binary('*', $rt->component_wise('sin', $rt->binary('*', $rt->f(2), $theta, 1, 'float')), $rt->f(0.25), 1, 'float'), 1, 'float'));
        }
        if ($rt->binary('==', $pathType, $rt->i(2))) {
            return $rt->binary('*', $rt->construct(2, $rt->component_wise('cos', $theta), $rt->component_wise('sin', $theta)), $radius, 2, 'float');
        }
        if ($rt->binary('==', $pathType, $rt->i(3))) {
            return $rt->construct(2, $rt->binary('+', $rt->unary('-', $rt->f(1)), $rt->binary('*', $rt->component_wise('cos', $theta), $rt->f(0.25), 1, 'float'), 1, 'float'), $rt->binary('*', $rt->component_wise('sin', $theta), $rt->f(0.25), 1, 'float'));
        }
        return $rt->construct(2, $rt->f(0));
    };
    $cmul__vec2_vec2 = sub {
        my ($a, $b) = @_;
        $a = $rt->copy($a, 'float');
        $b = $rt->copy($b, 'float');
        return $rt->construct(2, $rt->binary('-', $rt->binary('*', $rt->swizzle($a, 'x'), $rt->swizzle($b, 'x'), 1, 'float'), $rt->binary('*', $rt->swizzle($a, 'y'), $rt->swizzle($b, 'y'), 1, 'float'), 1, 'float'), $rt->binary('+', $rt->binary('*', $rt->swizzle($a, 'x'), $rt->swizzle($b, 'y'), 1, 'float'), $rt->binary('*', $rt->swizzle($a, 'y'), $rt->swizzle($b, 'x'), 1, 'float'), 1, 'float'));
    };
    $df64_from__float = sub {
        my ($a) = @_;
        return $rt->construct(2, $a, $rt->f(0));
    };
    $df64_add__vec2_vec2 = sub {
        my ($a, $b) = @_;
        $a = $rt->copy($a, 'float');
        $b = $rt->copy($b, 'float');
        my ($e, $s, $v);
        $s = $rt->binary('+', $rt->swizzle($a, 'x'), $rt->swizzle($b, 'x'), 1, 'float');
        $v = $rt->binary('-', $s, $rt->swizzle($a, 'x'), 1, 'float');
        $e = $rt->binary('+', $rt->binary('-', $rt->swizzle($a, 'x'), $rt->binary('-', $s, $v, 1, 'float'), 1, 'float'), $rt->binary('-', $rt->swizzle($b, 'x'), $v, 1, 'float'), 1, 'float');
        return $rt->construct(2, $s, $rt->binary('+', $rt->binary('+', $e, $rt->swizzle($a, 'y'), 1, 'float'), $rt->swizzle($b, 'y'), 1, 'float'));
    };
    $df64_sub__vec2_vec2 = sub {
        my ($a, $b) = @_;
        $a = $rt->copy($a, 'float');
        $b = $rt->copy($b, 'float');
        return $df64_add__vec2_vec2->($a, $rt->construct(2, $rt->unary('-', $rt->swizzle($b, 'x')), $rt->unary('-', $rt->swizzle($b, 'y'))));
    };
    $df64_split__float_float_float = sub {
        my ($a, $hi, $lo) = @_;
        my ($t);
        $t = $rt->binary('*', $g->{df64_split_const}, $a, 1, 'float');
        $hi = $rt->binary('-', $t, $rt->binary('-', $t, $a, 1, 'float'), 1, 'float');
        $lo = $rt->binary('-', $a, $hi, 1, 'float');
        return (undef, $hi, $lo);
    };
    $df64_mul__vec2_vec2 = sub {
        my ($a, $b) = @_;
        $a = $rt->copy($a, 'float');
        $b = $rt->copy($b, 'float');
        my ($ahi, $alo, $bhi, $blo, $e, $p);
        $p = $rt->binary('*', $rt->swizzle($a, 'x'), $rt->swizzle($b, 'x'), 1, 'float');
        $ahi = $rt->f(0.0);
        $alo = $rt->f(0.0);
        $bhi = $rt->f(0.0);
        $blo = $rt->f(0.0);
        do { ($_retc, $ahi, $alo) = $df64_split__float_float_float->($rt->swizzle($a, 'x'), $ahi, $alo); };
        do { ($_retc, $bhi, $blo) = $df64_split__float_float_float->($rt->swizzle($b, 'x'), $bhi, $blo); };
        $e = $rt->binary('+', $rt->binary('+', $rt->binary('+', $rt->binary('-', $rt->binary('*', $ahi, $bhi, 1, 'float'), $p, 1, 'float'), $rt->binary('*', $ahi, $blo, 1, 'float'), 1, 'float'), $rt->binary('*', $alo, $bhi, 1, 'float'), 1, 'float'), $rt->binary('*', $alo, $blo, 1, 'float'), 1, 'float');
        $e = $rt->binary('+', $e, $rt->binary('+', $rt->binary('*', $rt->swizzle($a, 'x'), $rt->swizzle($b, 'y'), 1, 'float'), $rt->binary('*', $rt->swizzle($a, 'y'), $rt->swizzle($b, 'x'), 1, 'float'), 1, 'float'), 1, 'float');
        return $rt->construct(2, $p, $e);
    };
    $df64_mul_f__vec2_float = sub {
        my ($a, $b) = @_;
        $a = $rt->copy($a, 'float');
        my ($ahi, $alo, $bhi, $blo, $e, $p);
        $p = $rt->binary('*', $rt->swizzle($a, 'x'), $b, 1, 'float');
        $ahi = $rt->f(0.0);
        $alo = $rt->f(0.0);
        $bhi = $rt->f(0.0);
        $blo = $rt->f(0.0);
        do { ($_retc, $ahi, $alo) = $df64_split__float_float_float->($rt->swizzle($a, 'x'), $ahi, $alo); };
        do { ($_retc, $bhi, $blo) = $df64_split__float_float_float->($b, $bhi, $blo); };
        $e = $rt->binary('+', $rt->binary('+', $rt->binary('+', $rt->binary('-', $rt->binary('*', $ahi, $bhi, 1, 'float'), $p, 1, 'float'), $rt->binary('*', $ahi, $blo, 1, 'float'), 1, 'float'), $rt->binary('*', $alo, $bhi, 1, 'float'), 1, 'float'), $rt->binary('*', $alo, $blo, 1, 'float'), 1, 'float');
        $e = $rt->binary('+', $e, $rt->binary('*', $rt->swizzle($a, 'y'), $b, 1, 'float'), 1, 'float');
        return $rt->construct(2, $p, $e);
    };
    $resolveC__void = sub {
        if ($rt->binary('>', $_u_poi, $rt->i(0))) {
            return $getPOI__int->($_u_poi);
        }
        if ($rt->binary('>', $_u_cPath, $rt->i(0))) {
            return $getAnimatedC__int_float_float->($_u_cPath, $rt->binary('*', $_u_time, $_u_cSpeed, 1, 'float'), $_u_cRadius);
        }
        return $rt->construct(2, $_u_cReal, $_u_cImag);
    };
    $transformCoords__vec2_float_vec2_vec2 = sub {
        my ($fragCoord, $zm, $reDF, $imDF) = @_;
        $fragCoord = $rt->copy($fragCoord, 'float');
        $reDF = $rt->copy($reDF, 'float');
        $imDF = $rt->copy($imDF, 'float');
        my ($angle, $cs, $scale, $sn, $uv);
        $uv = $rt->binary('/', $rt->binary('-', $fragCoord, $rt->binary('*', $rt->f(0.5), $_u_fullResolution, 2, 'float'), 2, 'float'), $rt->component_wise('min', $rt->swizzle($_u_fullResolution, 'x'), $rt->swizzle($_u_fullResolution, 'y')), 2, 'float');
        $angle = $rt->binary('/', $rt->binary('*', $rt->unary('-', $_u_rotation), $g->{TAU}, 1, 'float'), $rt->f(360), 1, 'float');
        $cs = $rt->component_wise('cos', $angle);
        $sn = $rt->component_wise('sin', $angle);
        @{$uv} = map { $rt->f32($_) } @{($rt->matrix_mult($rt->construct(4, $cs, $rt->unary('-', $sn), $sn, $cs), $uv, 2))};
        $scale = $rt->binary('/', $rt->f(2.5), $zm, 1, 'float');
        @{$reDF} = map { $rt->f32($_) } @{($df64_add__vec2_vec2->($df64_mul_f__vec2_float->($df64_from__float->($rt->swizzle($uv, 'x')), $scale), $df64_from__float->($_u_centerX)))};
        @{$imDF} = map { $rt->f32($_) } @{($df64_add__vec2_vec2->($df64_mul_f__vec2_float->($df64_from__float->($rt->swizzle($uv, 'y')), $scale), $df64_from__float->($_u_centerY)))};
        return (undef, $reDF, $imDF);
    };
    $juliaIterate__vec2_vec2_vec2_int_float_int = sub {
        my ($z0Re, $z0Im, $c, $maxIter, $freq, $trap) = @_;
        $z0Re = $rt->copy($z0Re, 'float');
        $z0Im = $rt->copy($z0Im, 'float');
        $c = $rt->copy($c, 'float');
        my ($_for0_first, $bail2, $dz, $i, $n, $period, $r, $stripeCount, $stripeLast, $stripeSum, $td, $trapMin, $zF, $zHi, $zIm, $zIm2, $zMag2, $zRe, $zRe2, $zReIm, $zSlow);
        $r = [$rt->f(0.0), $rt->f(0.0), $rt->f(0.0), $rt->f(0.0), $rt->f(0.0), $rt->f(0.0), $rt->f(0.0)];
        $zRe = $z0Re;
        $zIm = $z0Im;
        $dz = $rt->construct(2, $rt->f(1), $rt->f(0));
        $i = $rt->f(0);
        $stripeSum = $rt->f(0);
        $stripeLast = $rt->f(0);
        $stripeCount = $rt->f(0);
        $trapMin = $rt->f(10000000000);
        $bail2 = $rt->binary('*', $g->{BAILOUT}, $g->{BAILOUT}, 1, 'float');
        $zSlow = $rt->construct(2, $rt->swizzle($z0Re, 'x'), $rt->swizzle($z0Im, 'x'));
        $period = $rt->i(0);
        $n = $rt->i(0);
        $_for0_first = 1;
        for my $_for0 (0 .. 1048575) {
            if (!$_for0_first) {
                $n = $rt->binary('+', $n, $rt->i(1), 1, 'int');
            }
            $_for0_first = 0;
            if (!($rt->binary('<', $n, $rt->i(1000)))) {
                last;
            }
            if ($rt->binary('>=', $n, $maxIter)) {
                last;
            }
            $zF = $rt->construct(2, $rt->swizzle($zRe, 'x'), $rt->swizzle($zIm, 'x'));
            @{$dz} = map { $rt->f32($_) } @{($rt->binary('*', $rt->f(2), $cmul__vec2_vec2->($zF, $dz), 2, 'float'))};
            $zRe2 = $df64_mul__vec2_vec2->($zRe, $zRe);
            $zIm2 = $df64_mul__vec2_vec2->($zIm, $zIm);
            $zReIm = $df64_mul__vec2_vec2->($zRe, $zIm);
            @{$zRe} = map { $rt->f32($_) } @{($df64_add__vec2_vec2->($df64_sub__vec2_vec2->($zRe2, $zIm2), $df64_from__float->($rt->swizzle($c, 'x'))))};
            @{$zIm} = map { $rt->f32($_) } @{($df64_add__vec2_vec2->($df64_mul_f__vec2_float->($zReIm, $rt->f(2)), $df64_from__float->($rt->swizzle($c, 'y'))))};
            $zMag2 = $rt->binary('+', $rt->binary('*', $rt->swizzle($zRe, 'x'), $rt->swizzle($zRe, 'x'), 1, 'float'), $rt->binary('*', $rt->swizzle($zIm, 'x'), $rt->swizzle($zIm, 'x'), 1, 'float'), 1, 'float');
            if ($rt->binary('>', $zMag2, $bail2)) {
                last;
            }
            $i = $rt->binary('+', $i, $rt->f(1), 1, 'float');
            $zHi = $rt->construct(2, $rt->swizzle($zRe, 'x'), $rt->swizzle($zIm, 'x'));
            if ($rt->binary('>', $freq, $rt->f(0))) {
                $stripeLast = $rt->binary('+', $rt->binary('*', $rt->f(0.5), $rt->component_wise('sin', $rt->binary('*', $freq, $rt->component_wise('atan', $rt->swizzle($zHi, 'y'), $rt->swizzle($zHi, 'x')), 1, 'float')), 1, 'float'), $rt->f(0.5), 1, 'float');
                $stripeSum = $rt->binary('+', $stripeSum, $stripeLast, 1, 'float');
                $stripeCount = $rt->binary('+', $stripeCount, $rt->f(1), 1, 'float');
            }
            $td = $rt->f(0.0);
            if ($rt->binary('==', $trap, $rt->i(0))) {
                $td = $rt->length($zHi);
            } else {
                if ($rt->binary('==', $trap, $rt->i(1))) {
                    $td = $rt->component_wise('min', $rt->component_wise('abs', $rt->swizzle($zHi, 'x')), $rt->component_wise('abs', $rt->swizzle($zHi, 'y')));
                } else {
                    $td = $rt->component_wise('abs', $rt->binary('-', $rt->length($zHi), $rt->f(1), 1, 'float'));
                }
            }
            $trapMin = $rt->component_wise('min', $trapMin, $td);
            $period = $rt->binary('+', $period, $rt->i(1), 1, 'int');
            if ($rt->binary('==', $period, $rt->i(20))) {
                $period = $rt->i(0);
                @{$zSlow} = map { $rt->f32($_) } @{($zHi)};
            } else {
                if ($rt->binary('<', $rt->distance($zHi, $zSlow), $rt->f(1e-10))) {
                    $i = $rt->construct(1, $maxIter);
                    last;
                }
            }
        }
        $r->[0] = $i;
        $r->[1] = $rt->binary('+', $rt->binary('*', $rt->swizzle($zRe, 'x'), $rt->swizzle($zRe, 'x'), 1, 'float'), $rt->binary('*', $rt->swizzle($zIm, 'x'), $rt->swizzle($zIm, 'x'), 1, 'float'), 1, 'float');
        $r->[2] = $rt->dot($dz, $dz);
        $r->[3] = $stripeSum;
        $r->[4] = $stripeCount;
        $r->[5] = $stripeLast;
        $r->[6] = $trapMin;
        return $r;
    };
    $outputSmoothIteration__struct1_float = sub {
        my ($r, $maxIter) = @_;
        my ($log_zn, $nu);
        if ($rt->binary('>=', $r->[0], $maxIter)) {
            return $rt->f(0);
        }
        $log_zn = $rt->binary('*', $rt->component_wise('log', $r->[1]), $rt->f(0.5), 1, 'float');
        $nu = $rt->binary('/', $rt->component_wise('log', $rt->binary('/', $log_zn, $g->{LOG2}, 1, 'float')), $g->{LOG2}, 1, 'float');
        return $rt->component_wise('clamp', $rt->binary('/', $rt->binary('-', $rt->binary('+', $r->[0], $rt->f(1), 1, 'float'), $nu, 1, 'float'), $maxIter, 1, 'float'), $rt->f(0), $rt->f(1));
    };
    $outputDistanceEstimation__struct1_float = sub {
        my ($r, $maxIter) = @_;
        my ($dist, $dzMag, $zMag);
        if ($rt->binary('>=', $r->[0], $maxIter)) {
            return $rt->f(0);
        }
        $zMag = $rt->component_wise('sqrt', $r->[1]);
        $dzMag = $rt->component_wise('sqrt', $r->[2]);
        if ($rt->binary('<', $dzMag, $rt->f(1e-10))) {
            return $rt->f(0);
        }
        $dist = $rt->binary('/', $rt->binary('*', $rt->binary('*', $rt->f(2), $zMag, 1, 'float'), $rt->component_wise('log', $zMag), 1, 'float'), $dzMag, 1, 'float');
        return $rt->component_wise('clamp', $rt->binary('*', $rt->component_wise('log', $rt->binary('+', $dist, $rt->f(1), 1, 'float')), $rt->f(2), 1, 'float'), $rt->f(0), $rt->f(1));
    };
    $outputStripeAverage__struct1_float = sub {
        my ($r, $maxIter) = @_;
        my ($avg, $frac, $log_zn, $nu, $prevAvg);
        if ($rt->binary('>=', $r->[0], $maxIter)) {
            return $rt->f(0);
        }
        if ($rt->binary('<', $r->[4], $rt->f(1))) {
            return $rt->f(0);
        }
        $avg = $rt->binary('/', $r->[3], $r->[4], 1, 'float');
        $prevAvg = (($rt->binary('>', $r->[4], $rt->f(1))) ? ($rt->binary('/', $rt->binary('-', $r->[3], $r->[5], 1, 'float'), $rt->binary('-', $r->[4], $rt->f(1), 1, 'float'), 1, 'float')) : ($avg));
        $log_zn = $rt->binary('*', $rt->component_wise('log', $r->[1]), $rt->f(0.5), 1, 'float');
        $nu = $rt->binary('/', $rt->component_wise('log', $rt->binary('/', $log_zn, $g->{LOG2}, 1, 'float')), $g->{LOG2}, 1, 'float');
        $frac = $rt->component_wise('clamp', $rt->binary('+', $rt->binary('-', $rt->f(1), $nu, 1, 'float'), $rt->component_wise('floor', $nu), 1, 'float'), $rt->f(0), $rt->f(1));
        return $rt->component_wise('clamp', $rt->component_wise('mix', $prevAvg, $avg, $frac), $rt->f(0), $rt->f(1));
    };
    $outputOrbitTrap__struct1_float = sub {
        my ($r, $maxIter) = @_;
        if ($rt->binary('>=', $r->[0], $maxIter)) {
            return $rt->f(0);
        }
        return $rt->component_wise('clamp', $rt->binary('-', $rt->f(1), $r->[6], 1, 'float'), $rt->f(0), $rt->f(1));
    };
    $iterateSmooth__vec2_vec2_int_float = sub {
        my ($fragCoord, $c, $maxIter, $zm) = @_;
        $fragCoord = $rt->copy($fragCoord, 'float');
        $c = $rt->copy($c, 'float');
        my ($_for1_first, $bail2, $i, $imDF, $log_zn, $n, $nu, $reDF, $zIm, $zIm2, $zMag2, $zRe, $zRe2, $zReIm);
        $reDF = $rt->construct(2, 0.0);
        $imDF = $rt->construct(2, 0.0);
        do { ($_retc, $reDF, $imDF) = $transformCoords__vec2_float_vec2_vec2->($fragCoord, $zm, $reDF, $imDF); };
        $zRe = $reDF;
        $zIm = $imDF;
        $i = $rt->f(0);
        $bail2 = $rt->binary('*', $g->{BAILOUT}, $g->{BAILOUT}, 1, 'float');
        $n = $rt->i(0);
        $_for1_first = 1;
        for my $_for1 (0 .. 1048575) {
            if (!$_for1_first) {
                $n = $rt->binary('+', $n, $rt->i(1), 1, 'int');
            }
            $_for1_first = 0;
            if (!($rt->binary('<', $n, $rt->i(1000)))) {
                last;
            }
            if ($rt->binary('>=', $n, $maxIter)) {
                last;
            }
            $zRe2 = $df64_mul__vec2_vec2->($zRe, $zRe);
            $zIm2 = $df64_mul__vec2_vec2->($zIm, $zIm);
            $zReIm = $df64_mul__vec2_vec2->($zRe, $zIm);
            @{$zRe} = map { $rt->f32($_) } @{($df64_add__vec2_vec2->($df64_sub__vec2_vec2->($zRe2, $zIm2), $df64_from__float->($rt->swizzle($c, 'x'))))};
            @{$zIm} = map { $rt->f32($_) } @{($df64_add__vec2_vec2->($df64_mul_f__vec2_float->($zReIm, $rt->f(2)), $df64_from__float->($rt->swizzle($c, 'y'))))};
            $zMag2 = $rt->binary('+', $rt->binary('*', $rt->swizzle($zRe, 'x'), $rt->swizzle($zRe, 'x'), 1, 'float'), $rt->binary('*', $rt->swizzle($zIm, 'x'), $rt->swizzle($zIm, 'x'), 1, 'float'), 1, 'float');
            if ($rt->binary('>', $zMag2, $bail2)) {
                last;
            }
            $i = $rt->binary('+', $i, $rt->f(1), 1, 'float');
        }
        if ($rt->binary('>=', $i, $rt->construct(1, $maxIter))) {
            return $rt->f(0);
        }
        $zMag2 = $rt->binary('+', $rt->binary('*', $rt->swizzle($zRe, 'x'), $rt->swizzle($zRe, 'x'), 1, 'float'), $rt->binary('*', $rt->swizzle($zIm, 'x'), $rt->swizzle($zIm, 'x'), 1, 'float'), 1, 'float');
        $log_zn = $rt->binary('*', $rt->component_wise('log', $zMag2), $rt->f(0.5), 1, 'float');
        $nu = $rt->binary('/', $rt->component_wise('log', $rt->binary('/', $log_zn, $g->{LOG2}, 1, 'float')), $g->{LOG2}, 1, 'float');
        return $rt->component_wise('clamp', $rt->binary('/', $rt->binary('-', $rt->binary('+', $i, $rt->f(1), 1, 'float'), $nu, 1, 'float'), $rt->construct(1, $maxIter), 1, 'float'), $rt->f(0), $rt->f(1));
    };
    $outputNormalMap__vec2_vec2_int_float_float = sub {
        my ($fragCoord, $c, $maxIter, $angle, $zm) = @_;
        $fragCoord = $rt->copy($fragCoord, 'float');
        $c = $rt->copy($c, 'float');
        my ($d0, $d1, $d2, $lightDir, $normal, $rad);
        $d0 = $iterateSmooth__vec2_vec2_int_float->($fragCoord, $c, $maxIter, $zm);
        $d1 = $iterateSmooth__vec2_vec2_int_float->($rt->binary('+', $fragCoord, $rt->construct(2, $rt->f(1), $rt->f(0)), 2, 'float'), $c, $maxIter, $zm);
        $d2 = $iterateSmooth__vec2_vec2_int_float->($rt->binary('+', $fragCoord, $rt->construct(2, $rt->f(0), $rt->f(1)), 2, 'float'), $c, $maxIter, $zm);
        $normal = $rt->normalize($rt->construct(3, $rt->binary('-', $d1, $d0, 1, 'float'), $rt->binary('-', $d2, $d0, 1, 'float'), $rt->f(0.050000000000000003)));
        $rad = $rt->binary('/', $rt->binary('*', $angle, $g->{TAU}, 1, 'float'), $rt->f(360), 1, 'float');
        $lightDir = $rt->normalize($rt->construct(3, $rt->component_wise('cos', $rad), $rt->component_wise('sin', $rad), $rt->f(0.69999999999999996)));
        return $rt->component_wise('clamp', $rt->component_wise('max', $rt->dot($normal, $lightDir), $rt->f(0)), $rt->f(0), $rt->f(1));
    };
    $main__void = sub {
        my ($c, $effectiveZoom, $globalCoord, $imDF, $phase, $r, $reDF, $value);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $c = $resolveC__void->();
        $effectiveZoom = $rt->f(0.0);
        $phase = $rt->f(0.0);
        if ($rt->binary('>', $_u_zoomSpeed, $rt->f(0))) {
            $phase = $rt->binary('*', $rt->f(0.5), $rt->binary('-', $rt->f(1), $rt->component_wise('cos', $rt->binary('*', $rt->binary('*', $_u_time, $_u_zoomSpeed, 1, 'float'), $g->{TAU}, 1, 'float')), 1, 'float'), 1, 'float');
            $effectiveZoom = $rt->component_wise('pow', $rt->f(10), $rt->binary('*', $_u_zoomDepth, $phase, 1, 'float'));
        } else {
            $effectiveZoom = $rt->component_wise('pow', $rt->f(10), $_u_zoomDepth);
        }
        $value = $rt->f(0.0);
        $imDF = $rt->construct(2, 0.0);
        $r = [$rt->f(0.0), $rt->f(0.0), $rt->f(0.0), $rt->f(0.0), $rt->f(0.0), $rt->f(0.0), $rt->f(0.0)];
        $reDF = $rt->construct(2, 0.0);
        if ($rt->binary('==', $_u_outputMode, $rt->i(4))) {
            $value = $outputNormalMap__vec2_vec2_int_float_float->($globalCoord, $c, $_u_iterations, $_u_lightAngle, $effectiveZoom);
        } else {
            $reDF = $rt->construct(2, 0.0);
            $imDF = $rt->construct(2, 0.0);
            do { ($_retc, $reDF, $imDF) = $transformCoords__vec2_float_vec2_vec2->($globalCoord, $effectiveZoom, $reDF, $imDF); };
            $r = $juliaIterate__vec2_vec2_vec2_int_float_int->($reDF, $imDF, $c, $_u_iterations, $_u_stripeFreq, $_u_trapShape);
            if ($rt->binary('==', $_u_outputMode, $rt->i(0))) {
                $value = $outputSmoothIteration__struct1_float->($r, $rt->construct(1, $_u_iterations));
            } else {
                if ($rt->binary('==', $_u_outputMode, $rt->i(1))) {
                    $value = $outputDistanceEstimation__struct1_float->($r, $rt->construct(1, $_u_iterations));
                } else {
                    if ($rt->binary('==', $_u_outputMode, $rt->i(2))) {
                        $value = $outputStripeAverage__struct1_float->($r, $rt->construct(1, $_u_iterations));
                    } else {
                        if ($rt->binary('==', $_u_outputMode, $rt->i(3))) {
                            $value = $outputOrbitTrap__struct1_float->($r, $rt->construct(1, $_u_iterations));
                        } else {
                            $value = $outputSmoothIteration__struct1_float->($r, $rt->construct(1, $_u_iterations));
                        }
                    }
                }
            }
        }
        if ($_u_invert) {
            $value = $rt->binary('-', $rt->f(1), $value, 1, 'float');
        }
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $rt->construct(3, $value), $rt->f(1)))};
    };
    $main__void->();
    my $_c0 = $g->{fragColor};
    $out->[0] = $rt->f32($_c0->[0]); $out->[1] = $rt->f32($_c0->[1]); $out->[2] = $rt->f32($_c0->[2]); $out->[3] = $rt->f32($_c0->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0, output_names => ['fragColor'] };
