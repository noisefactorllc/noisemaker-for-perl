# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($main__void);
    my $_retc;
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_accumTex = $ctx->texture_binding('accumTex');
    my $_u_resolution = exists $U->{'resolution'} ? $U->{'resolution'} : $rt->construct(2, 0.0);
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_alpha = exists $U->{'alpha'} ? $U->{'alpha'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $main__void = sub {
        my ($_for0_first, $_for1_first, $accum, $count, $globalCoord, $gx, $gy, $mean, $normalized, $s, $sampleUV, $sqrtVal, $src, $sum, $uv, $v);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $uv = $rt->binary('/', $globalCoord, $_u_fullResolution, 2, 'float');
        $src = $rt->texture($_u_inputTex, $rt->binary('/', $rt->swizzle($ctx->{frag_coord}, 'xy'), $rt->construct(2, $rt->texture_size($_u_inputTex)), 2, 'float'));
        $accum = $rt->texture($_u_accumTex, $rt->binary('/', $rt->swizzle($ctx->{frag_coord}, 'xy'), $rt->construct(2, $rt->texture_size($_u_accumTex)), 2, 'float'));
        $sum = $rt->f(0);
        $count = $rt->f(0);
        $gy = $rt->i(0);
        $_for0_first = 1;
        for my $_for0 (0 .. 1048575) {
            if (!$_for0_first) {
                $gy = $rt->binary('+', $gy, $rt->i(1), 1, 'int');
            }
            $_for0_first = 0;
            if (!($rt->binary('<', $gy, $rt->i(32)))) {
                last;
            }
            $gx = $rt->i(0);
            $_for1_first = 1;
            for my $_for1 (0 .. 1048575) {
                if (!$_for1_first) {
                    $gx = $rt->binary('+', $gx, $rt->i(1), 1, 'int');
                }
                $_for1_first = 0;
                if (!($rt->binary('<', $gx, $rt->i(32)))) {
                    last;
                }
                $sampleUV = $rt->binary('/', $rt->binary('+', $rt->construct(2, $rt->construct(1, $gx), $rt->construct(1, $gy)), $rt->f(0.5), 2, 'float'), $rt->f(32), 2, 'float');
                $s = $rt->texture($_u_accumTex, $sampleUV);
                $v = $rt->binary('/', $rt->binary('+', $rt->binary('+', $rt->swizzle($s, 'r'), $rt->swizzle($s, 'g'), 1, 'float'), $rt->swizzle($s, 'b'), 1, 'float'), $rt->f(3), 1, 'float');
                $sum = $rt->binary('+', $sum, $v, 1, 'float');
                $count = $rt->binary('+', $count, $rt->f(1), 1, 'float');
            }
        }
        $mean = $rt->binary('/', $sum, $count, 1, 'float');
        $normalized = $rt->construct(3, 0.0);
        if ($rt->binary('>', $mean, $rt->f(0))) {
            @{$normalized} = map { $rt->f32($_) } @{($rt->component_wise('clamp', $rt->binary('/', $rt->swizzle($accum, 'rgb'), $rt->binary('*', $mean, $rt->f(4), 1, 'float'), 3, 'float'), $rt->f(0), $rt->f(1)))};
        } else {
            @{$normalized} = map { $rt->f32($_) } @{($rt->swizzle($accum, 'rgb'))};
        }
        $sqrtVal = $rt->component_wise('sqrt', $normalized);
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $rt->component_wise('mix', $rt->swizzle($src, 'rgb'), $sqrtVal, $_u_alpha), $rt->swizzle($src, 'a')))};
    };
    $main__void->();
    my $_c = $g->{fragColor};
    $out->[0] = $rt->f32($_c->[0]); $out->[1] = $rt->f32($_c->[1]); $out->[2] = $rt->f32($_c->[2]); $out->[3] = $rt->f32($_c->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0 };
