# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($luminance__vec4, $main__void);
    my $_retc;
    my $_u_rTex = $ctx->texture_binding('rTex');
    my $_u_gTex = $ctx->texture_binding('gTex');
    my $_u_bTex = $ctx->texture_binding('bTex');
    my $_u_resolution = exists $U->{'resolution'} ? $U->{'resolution'} : $rt->construct(2, 0.0);
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_rLevel = exists $U->{'rLevel'} ? $U->{'rLevel'} : $rt->f(0.0);
    my $_u_gLevel = exists $U->{'gLevel'} ? $U->{'gLevel'} : $rt->f(0.0);
    my $_u_bLevel = exists $U->{'bLevel'} ? $U->{'bLevel'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $luminance__vec4 = sub {
        my ($c) = @_;
        $c = $rt->copy($c, 'float');
        return $rt->dot($rt->swizzle($c, 'rgb'), $rt->construct(3, $rt->f(0.21260000000000001), $rt->f(0.71519999999999995), $rt->f(0.0722)));
    };
    $main__void = sub {
        my ($_g, $b, $globalCoord, $r, $st);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $st = $rt->binary('/', $globalCoord, $_u_fullResolution, 2, 'float');
        $r = $rt->binary('/', $rt->binary('*', $luminance__vec4->($rt->texture($_u_rTex, $rt->binary('/', $rt->swizzle($ctx->{frag_coord}, 'xy'), $rt->construct(2, $rt->texture_size($_u_rTex)), 2, 'float'))), $_u_rLevel, 1, 'float'), $rt->f(100), 1, 'float');
        $_g = $rt->binary('/', $rt->binary('*', $luminance__vec4->($rt->texture($_u_gTex, $rt->binary('/', $rt->swizzle($ctx->{frag_coord}, 'xy'), $rt->construct(2, $rt->texture_size($_u_gTex)), 2, 'float'))), $_u_gLevel, 1, 'float'), $rt->f(100), 1, 'float');
        $b = $rt->binary('/', $rt->binary('*', $luminance__vec4->($rt->texture($_u_bTex, $rt->binary('/', $rt->swizzle($ctx->{frag_coord}, 'xy'), $rt->construct(2, $rt->texture_size($_u_bTex)), 2, 'float'))), $_u_bLevel, 1, 'float'), $rt->f(100), 1, 'float');
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $r, $_g, $b, $rt->f(1)))};
    };
    $main__void->();
    my $_c0 = $g->{fragColor};
    $out->[0] = $rt->f32($_c0->[0]); $out->[1] = $rt->f32($_c0->[1]); $out->[2] = $rt->f32($_c0->[2]); $out->[3] = $rt->f32($_c0->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0, output_names => ['fragColor'] };
