# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($main__void);
    my $_retc;
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    $g->{fragColor} = $rt->construct(4, 0.0);
    $main__void = sub {
        my ($_for0_first, $_for1_first, $color, $inSize, $maxVal, $minVal, $x, $y);
        $inSize = $rt->texture_size($_u_inputTex);
        $minVal = $rt->f(100000);
        $maxVal = $rt->unary('-', $rt->f(100000));
        $y = $rt->i(0);
        $_for0_first = 1;
        for my $_for0 (0 .. 1048575) {
            if (!$_for0_first) {
                $y = $rt->binary('+', $y, $rt->i(1), 1, 'int');
            }
            $_for0_first = 0;
            if (!($rt->binary('<', $y, $rt->swizzle($inSize, 'y')))) {
                last;
            }
            $x = $rt->i(0);
            $_for1_first = 1;
            for my $_for1 (0 .. 1048575) {
                if (!$_for1_first) {
                    $x = $rt->binary('+', $x, $rt->i(1), 1, 'int');
                }
                $_for1_first = 0;
                if (!($rt->binary('<', $x, $rt->swizzle($inSize, 'x')))) {
                    last;
                }
                $color = $rt->texel_fetch($_u_inputTex, $rt->construct(2, $x, $y, 'int'), $rt->i(0));
                $minVal = $rt->component_wise('min', $minVal, $rt->swizzle($color, 'r'));
                $maxVal = $rt->component_wise('max', $maxVal, $rt->swizzle($color, 'g'));
            }
        }
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $minVal, $maxVal, $rt->f(0), $rt->f(1)))};
    };
    $main__void->();
    my $_c = $g->{fragColor};
    $out->[0] = $rt->f32($_c->[0]); $out->[1] = $rt->f32($_c->[1]); $out->[2] = $rt->f32($_c->[2]); $out->[3] = $rt->f32($_c->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0 };
