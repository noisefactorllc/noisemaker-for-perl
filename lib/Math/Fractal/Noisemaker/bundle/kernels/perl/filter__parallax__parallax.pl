# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($getLuminosity__vec3, $getHeight__vec2, $getInput__vec2, $main__void);
    my $_retc;
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_heightMap = $ctx->texture_binding('heightMap');
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_direction = exists $U->{'direction'} ? $U->{'direction'} : $rt->construct(3, 0.0);
    my $_u_pivot = exists $U->{'pivot'} ? $U->{'pivot'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $g->{MARCH_STEPS} = $rt->i(32);
    $g->{SHIFT_SCALE} = $rt->f(0.14999999999999999);
    $getLuminosity__vec3 = sub {
        my ($color) = @_;
        $color = $rt->copy($color, 'float');
        return $rt->dot($color, $rt->construct(3, $rt->f(0.29899999999999999), $rt->f(0.58699999999999997), $rt->f(0.114)));
    };
    $getHeight__vec2 = sub {
        my ($uv) = @_;
        $uv = $rt->copy($uv, 'float');
        my ($localUV, $mapSize);
        $mapSize = $rt->construct(2, $rt->texture_size($_u_heightMap));
        $localUV = $rt->binary('/', $rt->binary('-', $rt->binary('*', $uv, $_u_fullResolution, 2, 'float'), $_u_tileOffset, 2, 'float'), $mapSize, 2, 'float');
        return $getLuminosity__vec3->($rt->swizzle($rt->texture($_u_heightMap, $localUV), 'rgb'));
    };
    $getInput__vec2 = sub {
        my ($uv) = @_;
        $uv = $rt->copy($uv, 'float');
        my ($localUV, $texSize);
        $texSize = $rt->construct(2, $rt->texture_size($_u_inputTex));
        $localUV = $rt->binary('/', $rt->binary('-', $rt->binary('*', $uv, $_u_fullResolution, 2, 'float'), $_u_tileOffset, 2, 'float'), $texSize, 2, 'float');
        return $rt->texture($_u_inputTex, $localUV);
    };
    $main__void = sub {
        my ($_for0_first, $dispPixels, $f, $globalCoord, $i, $isTileRendering, $maxDispPixels, $prevF, $prevUV, $rayUV, $shift, $stepSize, $t, $uv, $v, $w);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $uv = $rt->binary('/', $globalCoord, $_u_fullResolution, 2, 'float');
        $v = (($rt->binary('>', $rt->length($_u_direction), $rt->f(0))) ? ($rt->normalize($_u_direction)) : ($rt->construct(3, $rt->f(0), $rt->f(0), $rt->f(1))));
        $shift = $rt->binary('*', $rt->swizzle($v, 'xy'), $g->{SHIFT_SCALE}, 2, 'float');
        $isTileRendering = $rt->binary('>', $rt->length($_u_tileOffset), $rt->f(0));
        $dispPixels = $rt->f(0.0);
        $maxDispPixels = $rt->f(0.0);
        if ($isTileRendering) {
            $maxDispPixels = $rt->f(256);
            $dispPixels = $rt->length($rt->binary('*', $shift, $_u_fullResolution, 2, 'float'));
            if ($rt->binary('>', $dispPixels, $maxDispPixels)) {
                @{$shift} = map { $rt->f32($_) } @{($rt->binary('*', $shift, $rt->binary('/', $maxDispPixels, $dispPixels, 1, 'float'), 2, 'float'))};
            }
        }
        $t = $rt->f(1);
        $rayUV = $rt->binary('+', $uv, $rt->binary('*', $shift, $rt->binary('-', $rt->f(1), $_u_pivot, 1, 'float'), 2, 'float'), 2, 'float');
        $f = $rt->binary('-', $t, $getHeight__vec2->($rayUV), 1, 'float');
        $stepSize = $rt->f(0.0);
        if ($rt->binary('>', $f, $rt->f(0))) {
            $stepSize = $rt->binary('/', $rt->f(1), $rt->construct(1, $g->{MARCH_STEPS}), 1, 'float');
            $i = $rt->i(1);
            $_for0_first = 1;
            for my $_for0 (0 .. 1048575) {
                if (!$_for0_first) {
                    $i = $rt->binary('+', $i, $rt->i(1), 1, 'int');
                }
                $_for0_first = 0;
                if (!($rt->binary('<=', $i, $g->{MARCH_STEPS}))) {
                    last;
                }
                $prevF = $f;
                $prevUV = $rayUV;
                $t = $rt->binary('-', $rt->f(1), $rt->binary('*', $rt->construct(1, $i), $stepSize, 1, 'float'), 1, 'float');
                @{$rayUV} = map { $rt->f32($_) } @{($rt->binary('+', $uv, $rt->binary('*', $shift, $rt->binary('-', $t, $_u_pivot, 1, 'float'), 2, 'float'), 2, 'float'))};
                $f = $rt->binary('-', $t, $getHeight__vec2->($rayUV), 1, 'float');
                $w = $rt->f(0.0);
                if ($rt->binary('<=', $f, $rt->f(0))) {
                    $w = $rt->binary('/', $f, $rt->binary('-', $f, $prevF, 1, 'float'), 1, 'float');
                    @{$rayUV} = map { $rt->f32($_) } @{($rt->component_wise('mix', $rayUV, $prevUV, $w))};
                    last;
                }
            }
        }
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($getInput__vec2->($rayUV))};
    };
    $main__void->();
    my $_c = $g->{fragColor};
    $out->[0] = $rt->f32($_c->[0]); $out->[1] = $rt->f32($_c->[1]); $out->[2] = $rt->f32($_c->[2]); $out->[3] = $rt->f32($_c->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0 };
