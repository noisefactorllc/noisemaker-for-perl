# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($rotate2D__vec2_float, $sdfCircle__vec2_float, $sdfPolygon__vec2_float_float, $sdfTriangle__vec2_float, $sdfFlower__vec2_float, $sdfStar5__vec2_float, $sdfRing__vec2_float, $main__void);
    my $_retc;
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_tex = $ctx->texture_binding('tex');
    my $_u_resolution = exists $U->{'resolution'} ? $U->{'resolution'} : $rt->construct(2, 0.0);
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_shape = exists $U->{'shape'} ? $U->{'shape'} : 0;
    my $_u_radius = exists $U->{'radius'} ? $U->{'radius'} : $rt->f(0.0);
    my $_u_edgeSmooth = exists $U->{'edgeSmooth'} ? $U->{'edgeSmooth'} : $rt->f(0.0);
    my $_u_rotation = exists $U->{'rotation'} ? $U->{'rotation'} : $rt->f(0.0);
    my $_u_posX = exists $U->{'posX'} ? $U->{'posX'} : $rt->f(0.0);
    my $_u_posY = exists $U->{'posY'} ? $U->{'posY'} : $rt->f(0.0);
    my $_u_invert = exists $U->{'invert'} ? $U->{'invert'} : 0;
    my $_u_speed = exists $U->{'speed'} ? $U->{'speed'} : 0;
    my $_u_time = exists $U->{'time'} ? $U->{'time'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $rotate2D__vec2_float = sub {
        my ($p, $angle) = @_;
        $p = $rt->copy($p, 'float');
        my ($c, $s);
        $c = $rt->component_wise('cos', $angle);
        $s = $rt->component_wise('sin', $angle);
        return $rt->construct(2, $rt->binary('-', $rt->binary('*', $rt->swizzle($p, 'x'), $c, 1, 'float'), $rt->binary('*', $rt->swizzle($p, 'y'), $s, 1, 'float'), 1, 'float'), $rt->binary('+', $rt->binary('*', $rt->swizzle($p, 'x'), $s, 1, 'float'), $rt->binary('*', $rt->swizzle($p, 'y'), $c, 1, 'float'), 1, 'float'));
    };
    $sdfCircle__vec2_float = sub {
        my ($p, $r) = @_;
        $p = $rt->copy($p, 'float');
        return $rt->binary('-', $rt->length($p), $r, 1, 'float');
    };
    $sdfPolygon__vec2_float_float = sub {
        my ($p, $r, $sides) = @_;
        $p = $rt->copy($p, 'float');
        my ($a, $seg);
        $a = $rt->binary('+', $rt->component_wise('atan', $rt->swizzle($p, 'x'), $rt->swizzle($p, 'y')), $rt->f(3.1415926535900001), 1, 'float');
        $seg = $rt->binary('/', $rt->f(6.2831853071800001), $sides, 1, 'float');
        return $rt->binary('-', $rt->binary('*', $rt->component_wise('cos', $rt->binary('-', $rt->binary('*', $rt->component_wise('floor', $rt->binary('+', $rt->f(0.5), $rt->binary('/', $a, $seg, 1, 'float'), 1, 'float')), $seg, 1, 'float'), $a, 1, 'float')), $rt->length($p), 1, 'float'), $r, 1, 'float');
    };
    $sdfTriangle__vec2_float = sub {
        my ($p, $r) = @_;
        $p = $rt->copy($p, 'float');
        my ($k);
        $k = $rt->f(1.7320508080000001);
        $p = $rt->assign_swizzle($p, 'x', $rt->binary('-', $rt->component_wise('abs', $rt->swizzle($p, 'x')), $r, 1, 'float'));
        $p = $rt->assign_swizzle($p, 'y', $rt->binary('+', $rt->swizzle($p, 'y'), $rt->binary('/', $r, $k, 1, 'float'), 1, 'float'));
        if ($rt->binary('>', $rt->binary('+', $rt->swizzle($p, 'x'), $rt->binary('*', $k, $rt->swizzle($p, 'y'), 1, 'float'), 1, 'float'), $rt->f(0))) {
            @{$p} = map { $rt->f32($_) } @{($rt->binary('/', $rt->construct(2, $rt->binary('-', $rt->swizzle($p, 'x'), $rt->binary('*', $k, $rt->swizzle($p, 'y'), 1, 'float'), 1, 'float'), $rt->binary('-', $rt->binary('*', $rt->unary('-', $k), $rt->swizzle($p, 'x'), 1, 'float'), $rt->swizzle($p, 'y'), 1, 'float')), $rt->f(2), 2, 'float'))};
        }
        $p = $rt->assign_swizzle($p, 'x', $rt->binary('-', $rt->swizzle($p, 'x'), $rt->component_wise('clamp', $rt->swizzle($p, 'x'), $rt->binary('*', $rt->unary('-', $rt->f(2)), $r, 1, 'float'), $rt->f(0)), 1, 'float'));
        return $rt->binary('*', $rt->unary('-', $rt->length($p)), $rt->component_wise('sign', $rt->swizzle($p, 'y')), 1, 'float');
    };
    $sdfFlower__vec2_float = sub {
        my ($p, $r) = @_;
        $p = $rt->copy($p, 'float');
        my ($a, $halfSeg, $innerR, $outerR, $seg, $segAngle, $starR, $t);
        $outerR = $r;
        $innerR = $rt->binary('*', $r, $rt->f(0.45000000000000001), 1, 'float');
        $a = $rt->binary('+', $rt->component_wise('atan', $rt->swizzle($p, 'x'), $rt->swizzle($p, 'y')), $rt->f(3.1415926535900001), 1, 'float');
        $seg = $rt->binary('/', $rt->f(6.2831853071800001), $rt->f(5), 1, 'float');
        $halfSeg = $rt->binary('*', $seg, $rt->f(0.5), 1, 'float');
        $segAngle = $rt->component_wise('mod', $a, $seg);
        $t = $rt->binary('/', $rt->component_wise('abs', $rt->binary('-', $segAngle, $halfSeg, 1, 'float')), $halfSeg, 1, 'float');
        $starR = $rt->component_wise('mix', $innerR, $outerR, $t);
        return $rt->binary('-', $rt->length($p), $starR, 1, 'float');
    };
    $sdfStar5__vec2_float = sub {
        my ($p, $r) = @_;
        $p = $rt->copy($p, 'float');
        my ($ba, $h, $k1, $k2, $rf);
        $rf = $rt->f(0.40000000000000002);
        $k1 = $rt->construct(2, $rt->f(0.80901699437499996), $rt->unary('-', $rt->f(0.58778525229199996)));
        $k2 = $rt->construct(2, $rt->unary('-', $rt->swizzle($k1, 'x')), $rt->swizzle($k1, 'y'));
        $p = $rt->assign_swizzle($p, 'x', $rt->component_wise('abs', $rt->swizzle($p, 'x')));
        @{$p} = map { $rt->f32($_) } @{($rt->binary('-', $p, $rt->binary('*', $rt->binary('*', $rt->f(2), $rt->component_wise('max', $rt->dot($k1, $p), $rt->f(0)), 1, 'float'), $k1, 2, 'float'), 2, 'float'))};
        @{$p} = map { $rt->f32($_) } @{($rt->binary('-', $p, $rt->binary('*', $rt->binary('*', $rt->f(2), $rt->component_wise('max', $rt->dot($k2, $p), $rt->f(0)), 1, 'float'), $k2, 2, 'float'), 2, 'float'))};
        $p = $rt->assign_swizzle($p, 'x', $rt->component_wise('abs', $rt->swizzle($p, 'x')));
        $p = $rt->assign_swizzle($p, 'y', $rt->binary('-', $rt->swizzle($p, 'y'), $r, 1, 'float'));
        $ba = $rt->binary('-', $rt->binary('*', $rf, $rt->construct(2, $rt->unary('-', $rt->swizzle($k1, 'y')), $rt->swizzle($k1, 'x')), 2, 'float'), $rt->construct(2, $rt->f(0), $rt->f(1)), 2, 'float');
        $h = $rt->component_wise('clamp', $rt->binary('/', $rt->dot($p, $ba), $rt->dot($ba, $ba), 1, 'float'), $rt->f(0), $r);
        return $rt->binary('*', $rt->length($rt->binary('-', $p, $rt->binary('*', $ba, $h, 2, 'float'), 2, 'float')), $rt->component_wise('sign', $rt->binary('-', $rt->binary('*', $rt->swizzle($p, 'y'), $rt->swizzle($ba, 'x'), 1, 'float'), $rt->binary('*', $rt->swizzle($p, 'x'), $rt->swizzle($ba, 'y'), 1, 'float'), 1, 'float')), 1, 'float');
    };
    $sdfRing__vec2_float = sub {
        my ($p, $r) = @_;
        $p = $rt->copy($p, 'float');
        my ($ringWidth);
        $ringWidth = $rt->binary('*', $r, $rt->f(0.14999999999999999), 1, 'float');
        return $rt->binary('-', $rt->component_wise('abs', $rt->binary('-', $rt->length($p), $r, 1, 'float')), $ringWidth, 1, 'float');
    };
    $main__void = sub {
        my ($aspect, $color, $colorA, $colorB, $d, $fullRes, $globalCoord, $globalUV, $mask, $p, $r, $rad, $st);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $st = $rt->binary('/', $globalCoord, $_u_fullResolution, 2, 'float');
        $colorA = $rt->texture($_u_inputTex, $rt->binary('/', $rt->swizzle($ctx->{frag_coord}, 'xy'), $rt->construct(2, $rt->texture_size($_u_inputTex)), 2, 'float'));
        $colorB = $rt->texture($_u_tex, $rt->binary('/', $rt->swizzle($ctx->{frag_coord}, 'xy'), $rt->construct(2, $rt->texture_size($_u_tex)), 2, 'float'));
        $fullRes = (($rt->binary('>', $rt->swizzle($_u_fullResolution, 'x'), $rt->f(0))) ? ($_u_fullResolution) : ($_u_resolution));
        $aspect = $rt->binary('/', $rt->swizzle($fullRes, 'x'), $rt->swizzle($fullRes, 'y'), 1, 'float');
        $globalUV = $rt->binary('/', $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float'), $fullRes, 2, 'float');
        $p = $rt->binary('*', $rt->binary('-', $globalUV, $rt->f(0.5), 2, 'float'), $rt->f(2), 2, 'float');
        $p = $rt->assign_swizzle($p, 'x', $rt->binary('*', $rt->swizzle($p, 'x'), $aspect, 1, 'float'));
        @{$p} = map { $rt->f32($_) } @{($rt->binary('-', $p, $rt->construct(2, $rt->binary('*', $_u_posX, $aspect, 1, 'float'), $rt->unary('-', $_u_posY)), 2, 'float'))};
        $rad = $rt->binary('/', $rt->binary('*', $_u_rotation, $rt->f(3.1415926535900001), 1, 'float'), $rt->f(180), 1, 'float');
        @{$p} = map { $rt->f32($_) } @{($rotate2D__vec2_float->($p, $rad))};
        $r = $_u_radius;
        if ($rt->binary('>', $_u_speed, $rt->i(0))) {
            $r = $rt->binary('+', $rt->binary('*', $_u_radius, $rt->f(0.5), 1, 'float'), $rt->binary('*', $rt->binary('*', $rt->component_wise('sin', $rt->binary('*', $rt->binary('*', $_u_time, $rt->f(6.2831853071800001), 1, 'float'), $rt->construct(1, $_u_speed), 1, 'float')), $_u_radius, 1, 'float'), $rt->f(0.5), 1, 'float'), 1, 'float');
        }
        $d = $rt->f(0);
        if ($rt->binary('==', $_u_shape, $rt->i(0))) {
            $d = $sdfCircle__vec2_float->($p, $r);
        } else {
            if ($rt->binary('==', $_u_shape, $rt->i(1))) {
                $d = $sdfTriangle__vec2_float->($p, $r);
            } else {
                if ($rt->binary('==', $_u_shape, $rt->i(2))) {
                    $d = $sdfPolygon__vec2_float_float->($p, $r, $rt->f(4));
                } else {
                    if ($rt->binary('==', $_u_shape, $rt->i(3))) {
                        $d = $sdfPolygon__vec2_float_float->($p, $r, $rt->f(5));
                    } else {
                        if ($rt->binary('==', $_u_shape, $rt->i(4))) {
                            $d = $sdfPolygon__vec2_float_float->($p, $r, $rt->f(6));
                        } else {
                            if ($rt->binary('==', $_u_shape, $rt->i(5))) {
                                $d = $sdfFlower__vec2_float->($p, $r);
                            } else {
                                if ($rt->binary('==', $_u_shape, $rt->i(6))) {
                                    $d = $sdfRing__vec2_float->($p, $r);
                                } else {
                                    if ($rt->binary('==', $_u_shape, $rt->i(7))) {
                                        $d = $sdfStar5__vec2_float->($p, $r);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
        $mask = $rt->component_wise('smoothstep', $rt->unary('-', $_u_edgeSmooth), $_u_edgeSmooth, $d);
        if ($rt->binary('==', $_u_invert, $rt->i(1))) {
            $mask = $rt->binary('-', $rt->f(1), $mask, 1, 'float');
        }
        $color = $rt->component_wise('mix', $colorA, $colorB, $mask);
        $color = $rt->assign_swizzle($color, 'a', $rt->component_wise('max', $rt->swizzle($colorA, 'a'), $rt->swizzle($colorB, 'a')));
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($color)};
    };
    $main__void->();
    my $_c = $g->{fragColor};
    $out->[0] = $rt->f32($_c->[0]); $out->[1] = $rt->f32($_c->[1]); $out->[2] = $rt->f32($_c->[2]); $out->[3] = $rt->f32($_c->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0 };
