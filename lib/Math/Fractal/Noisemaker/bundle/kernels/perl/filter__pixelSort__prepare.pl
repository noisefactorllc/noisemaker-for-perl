# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($applyWrap__vec2_vec2, $main__void);
    my $_retc;
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_resolution = exists $U->{'resolution'} ? $U->{'resolution'} : $rt->construct(2, 0.0);
    my $_u_angled = exists $U->{'angled'} ? $U->{'angled'} : $rt->f(0.0);
    my $_u_time = exists $U->{'time'} ? $U->{'time'} : $rt->f(0.0);
    my $_u_darkest = exists $U->{'darkest'} ? $U->{'darkest'} : 0;
    my $_u_wrap = exists $U->{'wrap'} ? $U->{'wrap'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $g->{PI} = $rt->f(3.1415926535897931);
    $applyWrap__vec2_vec2 = sub {
        my ($coord, $size) = @_;
        $coord = $rt->copy($coord, 'float');
        $size = $rt->copy($size, 'float');
        my ($mode, $uv);
        $uv = $rt->binary('/', $coord, $size, 2, 'float');
        $mode = $rt->construct(1, $_u_wrap, 'int');
        if ($rt->binary('==', $mode, $rt->i(0))) {
            @{$uv} = map { $rt->f32($_) } @{($rt->component_wise('abs', $rt->binary('-', $rt->component_wise('mod', $rt->binary('+', $uv, $rt->f(1), 2, 'float'), $rt->f(2)), $rt->f(1), 2, 'float')))};
        } else {
            if ($rt->binary('==', $mode, $rt->i(1))) {
                @{$uv} = map { $rt->f32($_) } @{($rt->component_wise('fract', $uv))};
            } else {
                @{$uv} = map { $rt->f32($_) } @{($rt->component_wise('clamp', $uv, $rt->f(0), $rt->f(1)))};
            }
        }
        return $uv;
    };
    $main__void = sub {
        my ($angle, $c, $center, $color, $pixelCoord, $rad, $s, $srcCoord, $texSize, $wrappedUV);
        $texSize = $rt->construct(2, $rt->texture_size($_u_inputTex));
        $center = $rt->binary('*', $texSize, $rt->f(0.5), 2, 'float');
        $pixelCoord = $rt->binary('-', $rt->swizzle($ctx->{frag_coord}, 'xy'), $center, 2, 'float');
        $angle = $_u_angled;
        $rad = $rt->binary('/', $rt->binary('*', $angle, $g->{PI}, 1, 'float'), $rt->f(180), 1, 'float');
        $c = $rt->component_wise('cos', $rad);
        $s = $rt->component_wise('sin', $rad);
        $srcCoord = $rt->construct(2, 0.0);
        $srcCoord = $rt->assign_swizzle($srcCoord, 'x', $rt->binary('+', $rt->binary('*', $c, $rt->swizzle($pixelCoord, 'x'), 1, 'float'), $rt->binary('*', $s, $rt->swizzle($pixelCoord, 'y'), 1, 'float'), 1, 'float'));
        $srcCoord = $rt->assign_swizzle($srcCoord, 'y', $rt->binary('+', $rt->binary('*', $rt->unary('-', $s), $rt->swizzle($pixelCoord, 'x'), 1, 'float'), $rt->binary('*', $c, $rt->swizzle($pixelCoord, 'y'), 1, 'float'), 1, 'float'));
        @{$srcCoord} = map { $rt->f32($_) } @{($rt->binary('+', $srcCoord, $center, 2, 'float'))};
        $wrappedUV = $applyWrap__vec2_vec2->($srcCoord, $texSize);
        $color = $rt->texture($_u_inputTex, $wrappedUV);
        if ($_u_darkest) {
            @{$color} = map { $rt->f32($_) } @{($rt->construct(4, $rt->binary('-', $rt->construct(3, $rt->f(1)), $rt->swizzle($color, 'rgb'), 3, 'float'), $rt->swizzle($color, 'a')))};
        }
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($color)};
    };
    $main__void->();
    my $_c0 = $g->{fragColor};
    $out->[0] = $rt->f32($_c0->[0]); $out->[1] = $rt->f32($_c0->[1]); $out->[2] = $rt->f32($_c0->[2]); $out->[3] = $rt->f32($_c0->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0, output_names => ['fragColor'] };
