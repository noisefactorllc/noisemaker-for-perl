# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($smod__float_float, $smod2__vec2_float, $polarCoords__vec2_float, $vortexCoords__vec2_float, $main__void);
    my $_retc;
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_time = exists $U->{'time'} ? $U->{'time'} : $rt->f(0.0);
    my $_u_polarMode = exists $U->{'polarMode'} ? $U->{'polarMode'} : 0;
    my $_u_speed = exists $U->{'speed'} ? $U->{'speed'} : $rt->f(0.0);
    my $_u_rotation = exists $U->{'rotation'} ? $U->{'rotation'} : $rt->f(0.0);
    my $_u_scale = exists $U->{'scale'} ? $U->{'scale'} : $rt->f(0.0);
    my $_u_aspectLens = exists $U->{'aspectLens'} ? $U->{'aspectLens'} : 0;
    my $_u_antialias = exists $U->{'antialias'} ? $U->{'antialias'} : 0;
    $g->{fragColor} = $rt->construct(4, 0.0);
    $g->{TAU} = $rt->f(6.2831853071800001);
    $smod__float_float = sub {
        my ($v, $m) = @_;
        return $rt->binary('*', $m, $rt->binary('-', $rt->binary('-', $rt->f(0.75), $rt->component_wise('abs', $rt->binary('-', $rt->component_wise('fract', $v), $rt->f(0.5), 1, 'float')), 1, 'float'), $rt->f(0.25), 1, 'float'), 1, 'float');
    };
    $smod2__vec2_float = sub {
        my ($v, $m) = @_;
        $v = $rt->copy($v, 'float');
        return $rt->binary('*', $m, $rt->binary('-', $rt->binary('-', $rt->f(0.75), $rt->component_wise('abs', $rt->binary('-', $rt->component_wise('fract', $v), $rt->f(0.5), 2, 'float')), 2, 'float'), $rt->f(0.25), 2, 'float'), 2, 'float');
    };
    $polarCoords__vec2_float = sub {
        my ($uv, $aspect) = @_;
        $uv = $rt->copy($uv, 'float');
        my ($coord);
        @{$uv} = map { $rt->f32($_) } @{($rt->binary('-', $uv, $rt->f(0.5), 2, 'float'))};
        if ($_u_aspectLens) {
            $uv = $rt->assign_swizzle($uv, 'x', $rt->binary('*', $rt->swizzle($uv, 'x'), $aspect, 1, 'float'));
        }
        $coord = $rt->construct(2, $rt->binary('+', $rt->binary('/', $rt->component_wise('atan', $rt->swizzle($uv, 'y'), $rt->swizzle($uv, 'x')), $g->{TAU}, 1, 'float'), $rt->f(0.5), 1, 'float'), $rt->binary('-', $rt->length($uv), $rt->binary('*', $_u_scale, $rt->f(0.074999999999999997), 1, 'float'), 1, 'float'));
        $coord = $rt->assign_swizzle($coord, 'x', $smod__float_float->($rt->binary('+', $rt->swizzle($coord, 'x'), $rt->binary('*', $_u_time, $rt->unary('-', $_u_rotation), 1, 'float'), 1, 'float'), $rt->f(1)));
        $coord = $rt->assign_swizzle($coord, 'y', $smod__float_float->($rt->binary('+', $rt->swizzle($coord, 'y'), $rt->binary('*', $_u_time, $_u_speed, 1, 'float'), 1, 'float'), $rt->f(1)));
        return $coord;
    };
    $vortexCoords__vec2_float = sub {
        my ($uv, $aspect) = @_;
        $uv = $rt->copy($uv, 'float');
        my ($r2);
        @{$uv} = map { $rt->f32($_) } @{($rt->binary('-', $uv, $rt->f(0.5), 2, 'float'))};
        if ($_u_aspectLens) {
            $uv = $rt->assign_swizzle($uv, 'x', $rt->binary('*', $rt->swizzle($uv, 'x'), $aspect, 1, 'float'));
        }
        $r2 = $rt->binary('-', $rt->dot($uv, $uv), $rt->binary('*', $_u_scale, $rt->f(0.01), 1, 'float'), 1, 'float');
        @{$uv} = map { $rt->f32($_) } @{($rt->binary('/', $uv, $r2, 2, 'float'))};
        $uv = $rt->assign_swizzle($uv, 'x', $smod__float_float->($rt->binary('+', $rt->swizzle($uv, 'x'), $rt->binary('*', $_u_time, $rt->unary('-', $_u_rotation), 1, 'float'), 1, 'float'), $rt->f(1)));
        $uv = $rt->assign_swizzle($uv, 'y', $smod__float_float->($rt->binary('+', $rt->swizzle($uv, 'y'), $rt->binary('*', $_u_time, $_u_speed, 1, 'float'), 1, 'float'), $rt->f(1)));
        return $uv;
    };
    $main__void = sub {
        my ($aspect, $col, $coord, $dx, $dy, $fullRes, $texSize, $tileDims, $uv);
        $texSize = $rt->texture_size($_u_inputTex);
        $tileDims = $rt->construct(2, $texSize);
        $fullRes = (($rt->binary('>', $rt->swizzle($_u_fullResolution, 'x'), $rt->f(0))) ? ($_u_fullResolution) : ($tileDims));
        $uv = $rt->binary('/', $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float'), $fullRes, 2, 'float');
        $aspect = $rt->binary('/', $rt->swizzle($fullRes, 'x'), $rt->swizzle($fullRes, 'y'), 1, 'float');
        $coord = $rt->construct(2, 0.0);
        if ($rt->binary('==', $_u_polarMode, $rt->i(0))) {
            @{$coord} = map { $rt->f32($_) } @{($polarCoords__vec2_float->($uv, $aspect))};
        } else {
            @{$coord} = map { $rt->f32($_) } @{($vortexCoords__vec2_float->($uv, $aspect))};
        }
        $col = $rt->construct(4, 0.0);
        $dx = $rt->construct(2, 0.0);
        $dy = $rt->construct(2, 0.0);
        if ($_u_antialias) {
            $dx = $rt->dFdx($coord);
            $dy = $rt->dFdy($coord);
            $col = $rt->construct(4, $rt->f(0));
            @{$col} = map { $rt->f32($_) } @{($rt->binary('+', $col, $rt->texture($_u_inputTex, $rt->binary('+', $rt->binary('+', $coord, $rt->binary('*', $dx, $rt->unary('-', $rt->f(0.375)), 2, 'float'), 2, 'float'), $rt->binary('*', $dy, $rt->unary('-', $rt->f(0.125)), 2, 'float'), 2, 'float')), 4, 'float'))};
            @{$col} = map { $rt->f32($_) } @{($rt->binary('+', $col, $rt->texture($_u_inputTex, $rt->binary('+', $rt->binary('+', $coord, $rt->binary('*', $dx, $rt->f(0.125), 2, 'float'), 2, 'float'), $rt->binary('*', $dy, $rt->unary('-', $rt->f(0.375)), 2, 'float'), 2, 'float')), 4, 'float'))};
            @{$col} = map { $rt->f32($_) } @{($rt->binary('+', $col, $rt->texture($_u_inputTex, $rt->binary('+', $rt->binary('+', $coord, $rt->binary('*', $dx, $rt->f(0.375), 2, 'float'), 2, 'float'), $rt->binary('*', $dy, $rt->f(0.125), 2, 'float'), 2, 'float')), 4, 'float'))};
            @{$col} = map { $rt->f32($_) } @{($rt->binary('+', $col, $rt->texture($_u_inputTex, $rt->binary('+', $rt->binary('+', $coord, $rt->binary('*', $dx, $rt->unary('-', $rt->f(0.125)), 2, 'float'), 2, 'float'), $rt->binary('*', $dy, $rt->f(0.375), 2, 'float'), 2, 'float')), 4, 'float'))};
            @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->binary('*', $col, $rt->f(0.25), 4, 'float'))};
        } else {
            @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->texture($_u_inputTex, $coord))};
        }
    };
    $main__void->();
    my $_c = $g->{fragColor};
    $out->[0] = $rt->f32($_c->[0]); $out->[1] = $rt->f32($_c->[1]); $out->[2] = $rt->f32($_c->[2]); $out->[3] = $rt->f32($_c->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 1 };
