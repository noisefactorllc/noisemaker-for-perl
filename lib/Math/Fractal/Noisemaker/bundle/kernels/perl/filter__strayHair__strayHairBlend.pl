# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($main__void);
    my $_retc;
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_overlayTex = $ctx->texture_binding('overlayTex');
    my $_u_alpha = exists $U->{'alpha'} ? $U->{'alpha'} : $rt->f(0.0);
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0, 'int');
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0, 'int');
    my $_u_renderScale = exists $U->{'renderScale'} ? $U->{'renderScale'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $main__void = sub {
        my ($a, $base, $baseSize, $coord, $overlay, $overlaySize, $result);
        $coord = $rt->construct(2, $rt->swizzle($ctx->{frag_coord}, 'xy'), 'int');
        $baseSize = $rt->texture_size($_u_inputTex);
        $overlaySize = $rt->texture_size($_u_overlayTex);
        $base = $rt->texel_fetch($_u_inputTex, $rt->component_wise('clamp', $coord, $rt->construct(2, $rt->i(0), 'int'), $rt->binary('-', $baseSize, $rt->i(1), 2, 'int')), $rt->i(0));
        $overlay = $rt->texel_fetch($_u_overlayTex, $rt->component_wise('clamp', $coord, $rt->construct(2, $rt->i(0), 'int'), $rt->binary('-', $overlaySize, $rt->i(1), 2, 'int')), $rt->i(0));
        $a = $rt->binary('*', $rt->swizzle($overlay, 'a'), $_u_alpha, 1, 'float');
        $result = $rt->binary('+', $rt->binary('*', $rt->swizzle($base, 'rgb'), $rt->binary('-', $rt->f(1), $a, 1, 'float'), 3, 'float'), $rt->binary('*', $rt->swizzle($overlay, 'rgb'), $a, 3, 'float'), 3, 'float');
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $result, $rt->swizzle($base, 'a')))};
    };
    $main__void->();
    my $_c0 = $g->{fragColor};
    $out->[0] = $rt->f32($_c0->[0]); $out->[1] = $rt->f32($_c0->[1]); $out->[2] = $rt->f32($_c0->[2]); $out->[3] = $rt->f32($_c0->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0, output_names => ['fragColor'] };
