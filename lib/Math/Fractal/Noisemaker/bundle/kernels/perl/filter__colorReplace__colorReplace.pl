# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($main__void);
    my $_retc;
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_targetColor = exists $U->{'targetColor'} ? $U->{'targetColor'} : $rt->construct(3, 0.0);
    my $_u_replaceColor = exists $U->{'replaceColor'} ? $U->{'replaceColor'} : $rt->construct(3, 0.0);
    my $_u_sensitivity = exists $U->{'sensitivity'} ? $U->{'sensitivity'} : $rt->f(0.0);
    my $_u_smoothing = exists $U->{'smoothing'} ? $U->{'smoothing'} : $rt->f(0.0);
    my $_u_colorMix = exists $U->{'colorMix'} ? $U->{'colorMix'} : $rt->f(0.0);
    my $_u_replaceAlpha = exists $U->{'replaceAlpha'} ? $U->{'replaceAlpha'} : $rt->f(0.0);
    my $_u_keepAlpha = exists $U->{'keepAlpha'} ? $U->{'keepAlpha'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $main__void = sub {
        my ($dist, $edge0, $edge1, $globalCoord, $halfBand, $match, $outA, $outRgb, $src, $st);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $st = $rt->binary('/', $rt->swizzle($ctx->{frag_coord}, 'xy'), $rt->construct(2, $rt->component_wise('max', $rt->texture_size($_u_inputTex), $rt->construct(2, $rt->i(1), 'int'))), 2, 'float');
        $src = $rt->texture($_u_inputTex, $st);
        $dist = $rt->binary('/', $rt->length($rt->binary('-', $rt->swizzle($src, 'rgb'), $_u_targetColor, 3, 'float')), $rt->f(1.7320507999999999), 1, 'float');
        $halfBand = $rt->binary('*', $_u_smoothing, $rt->f(0.5), 1, 'float');
        $edge0 = $rt->component_wise('max', $rt->binary('-', $_u_sensitivity, $halfBand, 1, 'float'), $rt->f(0));
        $edge1 = $rt->binary('+', $_u_sensitivity, $halfBand, 1, 'float');
        $match = $rt->binary('-', $rt->f(1), $rt->component_wise('smoothstep', $edge0, $edge1, $dist), 1, 'float');
        $outRgb = $rt->component_wise('mix', $rt->swizzle($src, 'rgb'), $_u_replaceColor, $rt->binary('*', $match, $_u_colorMix, 1, 'float'));
        $outA = $rt->binary('*', $rt->swizzle($src, 'a'), $rt->component_wise('mix', $_u_keepAlpha, $_u_replaceAlpha, $match), 1, 'float');
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $outRgb, $outA))};
    };
    $main__void->();
    my $_c0 = $g->{fragColor};
    $out->[0] = $rt->f32($_c0->[0]); $out->[1] = $rt->f32($_c0->[1]); $out->[2] = $rt->f32($_c0->[2]); $out->[3] = $rt->f32($_c0->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0, output_names => ['fragColor'] };
