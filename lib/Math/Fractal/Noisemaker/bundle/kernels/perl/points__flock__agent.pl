# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($hash_uint__uint, $hash__uint, $hash2__uint, $hashFloat__float, $noise2D__vec2, $wrapPosition__vec2_vec2, $limitVec__vec2_float, $setMag__vec2_float, $getGridCell__vec2_vec2, $main__void);
    my $_retc;
    my $_u_resolution = exists $U->{'resolution'} ? $U->{'resolution'} : $rt->construct(2, 0.0);
    my $_u_time = exists $U->{'time'} ? $U->{'time'} : $rt->f(0.0);
    my $_u_separation = exists $U->{'separation'} ? $U->{'separation'} : $rt->f(0.0);
    my $_u_alignment = exists $U->{'alignment'} ? $U->{'alignment'} : $rt->f(0.0);
    my $_u_cohesion = exists $U->{'cohesion'} ? $U->{'cohesion'} : $rt->f(0.0);
    my $_u_perceptionRadius = exists $U->{'perceptionRadius'} ? $U->{'perceptionRadius'} : $rt->f(0.0);
    my $_u_separationRadius = exists $U->{'separationRadius'} ? $U->{'separationRadius'} : $rt->f(0.0);
    my $_u_maxSpeed = exists $U->{'maxSpeed'} ? $U->{'maxSpeed'} : $rt->f(0.0);
    my $_u_maxForce = exists $U->{'maxForce'} ? $U->{'maxForce'} : $rt->f(0.0);
    my $_u_boundaryMode = exists $U->{'boundaryMode'} ? $U->{'boundaryMode'} : 0;
    my $_u_wallMargin = exists $U->{'wallMargin'} ? $U->{'wallMargin'} : $rt->f(0.0);
    my $_u_noiseWeight = exists $U->{'noiseWeight'} ? $U->{'noiseWeight'} : $rt->f(0.0);
    my $_u_xyzTex = $ctx->texture_binding('xyzTex');
    my $_u_velTex = $ctx->texture_binding('velTex');
    my $_u_rgbaTex = $ctx->texture_binding('rgbaTex');
    $g->{outXYZ} = $rt->construct(4, 0.0);
    $g->{outVel} = $rt->construct(4, 0.0);
    $g->{outRGBA} = $rt->construct(4, 0.0);
    $g->{GRID_SIZE} = $rt->i(16);
    $hash_uint__uint = sub {
        my ($seed) = @_;
        my ($state, $word);
        $state = $rt->binary('+', $rt->binary('*', $seed, $rt->i(747796405), 1, 'uint'), $rt->i(2891336453), 1, 'uint');
        $word = $rt->binary('*', $rt->binary('^', $rt->binary('>>', $state, $rt->binary('+', $rt->binary('>>', $state, $rt->i(28), 1, 'uint'), $rt->i(4), 1, 'uint'), 1, 'uint'), $state, 1, 'uint'), $rt->i(277803737), 1, 'uint');
        return $rt->binary('^', $rt->binary('>>', $word, $rt->i(22), 1, 'uint'), $word, 1, 'uint');
    };
    $hash__uint = sub {
        my ($seed) = @_;
        return $rt->binary('/', $rt->construct(1, $rt->hash_uint($seed)), $rt->f(4294967295), 1, 'float');
    };
    $hash2__uint = sub {
        my ($seed) = @_;
        return $rt->construct(2, $hash__uint->($seed), $hash__uint->($rt->binary('+', $seed, $rt->i(1), 1, 'uint')));
    };
    $hashFloat__float = sub {
        my ($n) = @_;
        return $rt->binary('/', $rt->construct(1, $rt->hash_uint($rt->float_bits_to_uint($n))), $rt->f(4294967295), 1, 'float');
    };
    $noise2D__vec2 = sub {
        my ($p) = @_;
        $p = $rt->copy($p, 'float');
        my ($f, $i, $n);
        $i = $rt->component_wise('floor', $p);
        $f = $rt->component_wise('fract', $p);
        @{$f} = map { $rt->f32($_) } @{($rt->binary('*', $rt->binary('*', $f, $f, 2, 'float'), $rt->binary('-', $rt->f(3), $rt->binary('*', $rt->f(2), $f, 2, 'float'), 2, 'float'), 2, 'float'))};
        $n = $rt->binary('+', $rt->swizzle($i, 'x'), $rt->binary('*', $rt->swizzle($i, 'y'), $rt->f(57), 1, 'float'), 1, 'float');
        return $rt->binary('-', $rt->binary('*', $rt->component_wise('mix', $rt->component_wise('mix', $hashFloat__float->($n), $hashFloat__float->($rt->binary('+', $n, $rt->f(1), 1, 'float')), $rt->swizzle($f, 'x')), $rt->component_wise('mix', $hashFloat__float->($rt->binary('+', $n, $rt->f(57), 1, 'float')), $hashFloat__float->($rt->binary('+', $n, $rt->f(58), 1, 'float')), $rt->swizzle($f, 'x')), $rt->swizzle($f, 'y')), $rt->f(2), 1, 'float'), $rt->f(1), 1, 'float');
    };
    $wrapPosition__vec2_vec2 = sub {
        my ($position, $bounds) = @_;
        $position = $rt->copy($position, 'float');
        $bounds = $rt->copy($bounds, 'float');
        return $rt->component_wise('mod', $rt->binary('+', $position, $bounds, 2, 'float'), $bounds);
    };
    $limitVec__vec2_float = sub {
        my ($v, $maxLen) = @_;
        $v = $rt->copy($v, 'float');
        my ($len);
        $len = $rt->length($v);
        if ((($rt->binary('>', $len, $maxLen)) && ($rt->binary('>', $len, $rt->f(0))) ? 1 : 0)) {
            return $rt->binary('*', $v, $rt->binary('/', $maxLen, $len, 1, 'float'), 2, 'float');
        }
        return $v;
    };
    $setMag__vec2_float = sub {
        my ($v, $mag) = @_;
        $v = $rt->copy($v, 'float');
        my ($len);
        $len = $rt->length($v);
        if ($rt->binary('>', $len, $rt->f(0))) {
            return $rt->binary('*', $v, $rt->binary('/', $mag, $len, 1, 'float'), 2, 'float');
        }
        return $v;
    };
    $getGridCell__vec2_vec2 = sub {
        my ($pos, $res) = @_;
        $pos = $rt->copy($pos, 'float');
        $res = $rt->copy($res, 'float');
        my ($cellSize);
        $cellSize = $rt->binary('/', $res, $rt->construct(1, $g->{GRID_SIZE}), 2, 'float');
        return $rt->construct(2, $rt->component_wise('clamp', $rt->binary('/', $pos, $cellSize, 2, 'float'), $rt->construct(2, $rt->f(0)), $rt->construct(2, $rt->construct(1, $rt->binary('-', $g->{GRID_SIZE}, $rt->i(1), 1, 'int')))), 'int');
    };
    $main__void = sub {
        my ($_for0_first, $_for1_first, $_for2_first, $age, $alignSteer, $alignmentCount, $alignmentSum, $alive, $angle, $avgPos, $avgVel, $away, $boidId, $cellSeed, $checkCell, $cohesionCount, $cohesionSteer, $cohesionSum, $coord, $desired, $diff, $dist, $distSq, $dx, $dy, $myCell, $newPx, $newPy, $noiseForce, $noiseScale, $nx, $ny, $otherPos, $otherVel, $otherVelocity, $otherXyz, $perceptionSq, $pos, $px, $py, $rgba, $s, $sampleIdx, $sampleSeed, $seed, $separationCount, $separationForce, $separationSq, $speed, $stateSize, $steer, $sx, $sy, $totalBoids, $turnStrength, $vel, $velocity, $vx, $vy, $wallForce, $xyz);
        $coord = $rt->construct(2, $rt->swizzle($ctx->{frag_coord}, 'xy'), 'int');
        $stateSize = $rt->texture_size($_u_xyzTex);
        $xyz = $rt->texel_fetch($_u_xyzTex, $coord, $rt->i(0));
        $vel = $rt->texel_fetch($_u_velTex, $coord, $rt->i(0));
        $rgba = $rt->texel_fetch($_u_rgbaTex, $coord, $rt->i(0));
        $px = $rt->swizzle($xyz, 'x');
        $py = $rt->swizzle($xyz, 'y');
        $alive = $rt->swizzle($xyz, 'w');
        $vx = $rt->swizzle($vel, 'x');
        $vy = $rt->swizzle($vel, 'y');
        $age = $rt->swizzle($vel, 'z');
        $seed = $rt->swizzle($vel, 'w');
        $boidId = $rt->construct(1, $rt->binary('+', $rt->swizzle($coord, 'x'), $rt->binary('*', $rt->swizzle($coord, 'y'), $rt->swizzle($stateSize, 'x'), 1, 'int'), 1, 'int'), 'uint');
        $pos = $rt->binary('*', $rt->construct(2, $px, $py), $_u_resolution, 2, 'float');
        $velocity = $rt->construct(2, $vx, $vy);
        if ($rt->binary('<', $alive, $rt->f(0.5))) {
            @{$g->{outXYZ}} = map { $rt->f32($_) } @{($xyz)};
            @{$g->{outVel}} = map { $rt->f32($_) } @{($vel)};
            @{$g->{outRGBA}} = map { $rt->f32($_) } @{($rgba)};
            return;
        }
        $angle = $rt->f(0.0);
        $speed = $rt->f(0.0);
        if ((($rt->binary('==', $rt->length($velocity), $rt->f(0))) && ($rt->binary('==', $seed, $rt->f(0))) ? 1 : 0)) {
            $seed = $hash__uint->($rt->binary('+', $boidId, $rt->i(99999), 1, 'uint'));
            $angle = $rt->binary('*', $hash__uint->($rt->binary('+', $boidId, $rt->i(12345), 1, 'uint')), $rt->f(6.2831853071800001), 1, 'float');
            $speed = $rt->binary('+', $rt->binary('*', $rt->binary('*', $hash__uint->($rt->binary('+', $boidId, $rt->i(23456), 1, 'uint')), $_u_maxSpeed, 1, 'float'), $rt->f(0.5), 1, 'float'), $rt->binary('*', $_u_maxSpeed, $rt->f(0.25), 1, 'float'), 1, 'float');
            @{$velocity} = map { $rt->f32($_) } @{($rt->binary('*', $rt->construct(2, $rt->component_wise('cos', $angle), $rt->component_wise('sin', $angle)), $speed, 2, 'float'))};
        }
        $separationForce = $rt->construct(2, $rt->f(0));
        $alignmentSum = $rt->construct(2, $rt->f(0));
        $cohesionSum = $rt->construct(2, $rt->f(0));
        $separationCount = $rt->i(0);
        $alignmentCount = $rt->i(0);
        $cohesionCount = $rt->i(0);
        $myCell = $getGridCell__vec2_vec2->($pos, $_u_resolution);
        $perceptionSq = $rt->binary('*', $_u_perceptionRadius, $_u_perceptionRadius, 1, 'float');
        $separationSq = $rt->binary('*', $_u_separationRadius, $_u_separationRadius, 1, 'float');
        $totalBoids = $rt->binary('*', $rt->swizzle($stateSize, 'x'), $rt->swizzle($stateSize, 'y'), 1, 'int');
        $dy = $rt->unary('-', $rt->i(1));
        $_for0_first = 1;
        for my $_for0 (0 .. 1048575) {
            if (!$_for0_first) {
                $dy = $rt->binary('+', $dy, $rt->i(1), 1, 'int');
            }
            $_for0_first = 0;
            if (!($rt->binary('<=', $dy, $rt->i(1)))) {
                last;
            }
            $dx = $rt->unary('-', $rt->i(1));
            $_for1_first = 1;
            for my $_for1 (0 .. 1048575) {
                if (!$_for1_first) {
                    $dx = $rt->binary('+', $dx, $rt->i(1), 1, 'int');
                }
                $_for1_first = 0;
                if (!($rt->binary('<=', $dx, $rt->i(1)))) {
                    last;
                }
                $checkCell = $rt->binary('+', $myCell, $rt->construct(2, $dx, $dy, 'int'), 2, 'int');
                if ($rt->binary('==', $_u_boundaryMode, $rt->i(0))) {
                    @{$checkCell} = @{($rt->binary('%', $rt->binary('+', $checkCell, $g->{GRID_SIZE}, 2, 'int'), $g->{GRID_SIZE}, 2, 'int'))};
                } else {
                    @{$checkCell} = @{($rt->component_wise('clamp', $checkCell, $rt->construct(2, $rt->i(0), 'int'), $rt->construct(2, $rt->binary('-', $g->{GRID_SIZE}, $rt->i(1), 1, 'int'), 'int')))};
                }
                $cellSeed = $rt->construct(1, $rt->binary('+', $rt->binary('*', $rt->swizzle($checkCell, 'y'), $g->{GRID_SIZE}, 1, 'int'), $rt->swizzle($checkCell, 'x'), 1, 'int'), 'uint');
                $s = $rt->i(0);
                $_for2_first = 1;
                for my $_for2 (0 .. 1048575) {
                    if (!$_for2_first) {
                        $s = $rt->binary('+', $s, $rt->i(1), 1, 'int');
                    }
                    $_for2_first = 0;
                    if (!($rt->binary('<', $s, $rt->i(8)))) {
                        last;
                    }
                    $sampleSeed = $rt->binary('+', $rt->binary('+', $rt->binary('*', $cellSeed, $rt->i(31), 1, 'uint'), $rt->construct(1, $s, 'uint'), 1, 'uint'), $rt->construct(1, $rt->binary('*', $_u_time, $rt->f(10), 1, 'float'), 'uint'), 1, 'uint');
                    $sampleIdx = $rt->construct(1, $rt->binary('%', $rt->hash_uint($sampleSeed), $rt->construct(1, $totalBoids, 'uint'), 1, 'uint'), 'int');
                    $sx = $rt->binary('%', $sampleIdx, $rt->swizzle($stateSize, 'x'), 1, 'int');
                    $sy = $rt->binary('/', $sampleIdx, $rt->swizzle($stateSize, 'x'), 1, 'int');
                    if ((($rt->binary('==', $sx, $rt->swizzle($coord, 'x'))) && ($rt->binary('==', $sy, $rt->swizzle($coord, 'y'))) ? 1 : 0)) {
                        next;
                    }
                    $otherXyz = $rt->texel_fetch($_u_xyzTex, $rt->construct(2, $sx, $sy, 'int'), $rt->i(0));
                    $otherVel = $rt->texel_fetch($_u_velTex, $rt->construct(2, $sx, $sy, 'int'), $rt->i(0));
                    if ($rt->binary('<', $rt->swizzle($otherXyz, 'w'), $rt->f(0.5))) {
                        next;
                    }
                    $otherPos = $rt->binary('*', $rt->swizzle($otherXyz, 'xy'), $_u_resolution, 2, 'float');
                    $otherVelocity = $rt->swizzle($otherVel, 'xy');
                    $diff = $rt->binary('-', $otherPos, $pos, 2, 'float');
                    if ($rt->binary('==', $_u_boundaryMode, $rt->i(0))) {
                        if ($rt->binary('>', $rt->swizzle($diff, 'x'), $rt->binary('*', $rt->swizzle($_u_resolution, 'x'), $rt->f(0.5), 1, 'float'))) {
                            $diff = $rt->assign_swizzle($diff, 'x', $rt->binary('-', $rt->swizzle($diff, 'x'), $rt->swizzle($_u_resolution, 'x'), 1, 'float'));
                        }
                        if ($rt->binary('<', $rt->swizzle($diff, 'x'), $rt->binary('*', $rt->unary('-', $rt->swizzle($_u_resolution, 'x')), $rt->f(0.5), 1, 'float'))) {
                            $diff = $rt->assign_swizzle($diff, 'x', $rt->binary('+', $rt->swizzle($diff, 'x'), $rt->swizzle($_u_resolution, 'x'), 1, 'float'));
                        }
                        if ($rt->binary('>', $rt->swizzle($diff, 'y'), $rt->binary('*', $rt->swizzle($_u_resolution, 'y'), $rt->f(0.5), 1, 'float'))) {
                            $diff = $rt->assign_swizzle($diff, 'y', $rt->binary('-', $rt->swizzle($diff, 'y'), $rt->swizzle($_u_resolution, 'y'), 1, 'float'));
                        }
                        if ($rt->binary('<', $rt->swizzle($diff, 'y'), $rt->binary('*', $rt->unary('-', $rt->swizzle($_u_resolution, 'y')), $rt->f(0.5), 1, 'float'))) {
                            $diff = $rt->assign_swizzle($diff, 'y', $rt->binary('+', $rt->swizzle($diff, 'y'), $rt->swizzle($_u_resolution, 'y'), 1, 'float'));
                        }
                    }
                    $distSq = $rt->dot($diff, $diff);
                    $away = $rt->construct(2, 0.0);
                    $dist = $rt->f(0.0);
                    if ((($rt->binary('<', $distSq, $separationSq)) && ($rt->binary('>', $distSq, $rt->f(0))) ? 1 : 0)) {
                        $away = $rt->unary('-', $diff);
                        $dist = $rt->component_wise('sqrt', $distSq);
                        @{$separationForce} = map { $rt->f32($_) } @{($rt->binary('+', $separationForce, $rt->binary('/', $away, $dist, 2, 'float'), 2, 'float'))};
                        $separationCount = $rt->binary('+', $separationCount, $rt->i(1), 1, 'int');
                    }
                    if ((($rt->binary('<', $distSq, $perceptionSq)) && ($rt->binary('>', $distSq, $rt->f(0))) ? 1 : 0)) {
                        @{$alignmentSum} = map { $rt->f32($_) } @{($rt->binary('+', $alignmentSum, $otherVelocity, 2, 'float'))};
                        $alignmentCount = $rt->binary('+', $alignmentCount, $rt->i(1), 1, 'int');
                        @{$cohesionSum} = map { $rt->f32($_) } @{($rt->binary('+', $cohesionSum, $otherPos, 2, 'float'))};
                        $cohesionCount = $rt->binary('+', $cohesionCount, $rt->i(1), 1, 'int');
                    }
                }
            }
        }
        $steer = $rt->construct(2, $rt->f(0));
        if ($rt->binary('>', $separationCount, $rt->i(0))) {
            @{$separationForce} = map { $rt->f32($_) } @{($rt->binary('/', $separationForce, $rt->construct(1, $separationCount), 2, 'float'))};
            if ($rt->binary('>', $rt->length($separationForce), $rt->f(0))) {
                @{$separationForce} = map { $rt->f32($_) } @{($setMag__vec2_float->($separationForce, $_u_maxSpeed))};
                @{$separationForce} = map { $rt->f32($_) } @{($rt->binary('-', $separationForce, $velocity, 2, 'float'))};
                @{$separationForce} = map { $rt->f32($_) } @{($limitVec__vec2_float->($separationForce, $_u_maxForce))};
                @{$steer} = map { $rt->f32($_) } @{($rt->binary('+', $steer, $rt->binary('*', $separationForce, $_u_separation, 2, 'float'), 2, 'float'))};
            }
        }
        $avgVel = $rt->construct(2, 0.0);
        if ($rt->binary('>', $alignmentCount, $rt->i(0))) {
            $avgVel = $rt->binary('/', $alignmentSum, $rt->construct(1, $alignmentCount), 2, 'float');
            $alignSteer = $rt->construct(2, 0.0);
            if ($rt->binary('>', $rt->length($avgVel), $rt->f(0))) {
                @{$avgVel} = map { $rt->f32($_) } @{($setMag__vec2_float->($avgVel, $_u_maxSpeed))};
                $alignSteer = $rt->binary('-', $avgVel, $velocity, 2, 'float');
                @{$alignSteer} = map { $rt->f32($_) } @{($limitVec__vec2_float->($alignSteer, $_u_maxForce))};
                @{$steer} = map { $rt->f32($_) } @{($rt->binary('+', $steer, $rt->binary('*', $alignSteer, $_u_alignment, 2, 'float'), 2, 'float'))};
            }
        }
        $avgPos = $rt->construct(2, 0.0);
        $desired = $rt->construct(2, 0.0);
        if ($rt->binary('>', $cohesionCount, $rt->i(0))) {
            $avgPos = $rt->binary('/', $cohesionSum, $rt->construct(1, $cohesionCount), 2, 'float');
            $desired = $rt->binary('-', $avgPos, $pos, 2, 'float');
            $cohesionSteer = $rt->construct(2, 0.0);
            if ($rt->binary('>', $rt->length($desired), $rt->f(0))) {
                @{$desired} = map { $rt->f32($_) } @{($setMag__vec2_float->($desired, $_u_maxSpeed))};
                $cohesionSteer = $rt->binary('-', $desired, $velocity, 2, 'float');
                @{$cohesionSteer} = map { $rt->f32($_) } @{($limitVec__vec2_float->($cohesionSteer, $_u_maxForce))};
                @{$steer} = map { $rt->f32($_) } @{($rt->binary('+', $steer, $rt->binary('*', $cohesionSteer, $_u_cohesion, 2, 'float'), 2, 'float'))};
            }
        }
        $noiseForce = $rt->construct(2, 0.0);
        $noiseScale = $rt->f(0.0);
        $nx = $rt->f(0.0);
        $ny = $rt->f(0.0);
        if ($rt->binary('>', $_u_noiseWeight, $rt->f(0))) {
            $noiseScale = $rt->f(0.01);
            $nx = $noise2D__vec2->($rt->binary('+', $rt->binary('*', $pos, $noiseScale, 2, 'float'), $rt->binary('*', $_u_time, $rt->f(0.5), 1, 'float'), 2, 'float'));
            $ny = $noise2D__vec2->($rt->binary('+', $rt->binary('+', $rt->binary('*', $pos, $noiseScale, 2, 'float'), $rt->construct(2, $rt->f(100), $rt->f(100)), 2, 'float'), $rt->binary('*', $_u_time, $rt->f(0.5), 1, 'float'), 2, 'float'));
            $noiseForce = $rt->binary('*', $rt->binary('*', $rt->construct(2, $nx, $ny), $_u_maxForce, 2, 'float'), $_u_noiseWeight, 2, 'float');
            @{$steer} = map { $rt->f32($_) } @{($rt->binary('+', $steer, $noiseForce, 2, 'float'))};
        }
        $turnStrength = $rt->f(0.0);
        $wallForce = $rt->construct(2, 0.0);
        if ($rt->binary('==', $_u_boundaryMode, $rt->i(1))) {
            $wallForce = $rt->construct(2, $rt->f(0));
            $turnStrength = $rt->binary('*', $_u_maxForce, $rt->f(2), 1, 'float');
            if ($rt->binary('<', $rt->swizzle($pos, 'x'), $_u_wallMargin)) {
                $wallForce = $rt->assign_swizzle($wallForce, 'x', $rt->binary('*', $turnStrength, $rt->binary('-', $rt->f(1), $rt->binary('/', $rt->swizzle($pos, 'x'), $_u_wallMargin, 1, 'float'), 1, 'float'), 1, 'float'));
            } else {
                if ($rt->binary('>', $rt->swizzle($pos, 'x'), $rt->binary('-', $rt->swizzle($_u_resolution, 'x'), $_u_wallMargin, 1, 'float'))) {
                    $wallForce = $rt->assign_swizzle($wallForce, 'x', $rt->binary('*', $rt->unary('-', $turnStrength), $rt->binary('-', $rt->f(1), $rt->binary('/', $rt->binary('-', $rt->swizzle($_u_resolution, 'x'), $rt->swizzle($pos, 'x'), 1, 'float'), $_u_wallMargin, 1, 'float'), 1, 'float'), 1, 'float'));
                }
            }
            if ($rt->binary('<', $rt->swizzle($pos, 'y'), $_u_wallMargin)) {
                $wallForce = $rt->assign_swizzle($wallForce, 'y', $rt->binary('*', $turnStrength, $rt->binary('-', $rt->f(1), $rt->binary('/', $rt->swizzle($pos, 'y'), $_u_wallMargin, 1, 'float'), 1, 'float'), 1, 'float'));
            } else {
                if ($rt->binary('>', $rt->swizzle($pos, 'y'), $rt->binary('-', $rt->swizzle($_u_resolution, 'y'), $_u_wallMargin, 1, 'float'))) {
                    $wallForce = $rt->assign_swizzle($wallForce, 'y', $rt->binary('*', $rt->unary('-', $turnStrength), $rt->binary('-', $rt->f(1), $rt->binary('/', $rt->binary('-', $rt->swizzle($_u_resolution, 'y'), $rt->swizzle($pos, 'y'), 1, 'float'), $_u_wallMargin, 1, 'float'), 1, 'float'), 1, 'float'));
                }
            }
            @{$steer} = map { $rt->f32($_) } @{($rt->binary('+', $steer, $wallForce, 2, 'float'))};
        }
        @{$velocity} = map { $rt->f32($_) } @{($rt->binary('+', $velocity, $steer, 2, 'float'))};
        @{$velocity} = map { $rt->f32($_) } @{($limitVec__vec2_float->($velocity, $_u_maxSpeed))};
        @{$pos} = map { $rt->f32($_) } @{($rt->binary('+', $pos, $velocity, 2, 'float'))};
        if ($rt->binary('==', $_u_boundaryMode, $rt->i(0))) {
            @{$pos} = map { $rt->f32($_) } @{($wrapPosition__vec2_vec2->($pos, $_u_resolution))};
        } else {
            @{$pos} = map { $rt->f32($_) } @{($rt->component_wise('clamp', $pos, $rt->construct(2, $rt->f(1)), $rt->binary('-', $_u_resolution, $rt->construct(2, $rt->f(1)), 2, 'float')))};
        }
        $age = $rt->binary('+', $age, $rt->f(0.016), 1, 'float');
        $newPx = $rt->binary('/', $rt->swizzle($pos, 'x'), $rt->swizzle($_u_resolution, 'x'), 1, 'float');
        $newPy = $rt->binary('/', $rt->swizzle($pos, 'y'), $rt->swizzle($_u_resolution, 'y'), 1, 'float');
        @{$g->{outXYZ}} = map { $rt->f32($_) } @{($rt->construct(4, $newPx, $newPy, $rt->swizzle($xyz, 'z'), $rt->f(1)))};
        @{$g->{outVel}} = map { $rt->f32($_) } @{($rt->construct(4, $velocity, $age, $seed))};
        @{$g->{outRGBA}} = map { $rt->f32($_) } @{($rgba)};
    };
    $main__void->();
    my $_c0 = $g->{outXYZ};
    $out->[0] = $rt->f32($_c0->[0]); $out->[1] = $rt->f32($_c0->[1]); $out->[2] = $rt->f32($_c0->[2]); $out->[3] = $rt->f32($_c0->[3]);
    my $_c1 = $g->{outVel};
    $out->[4] = $rt->f32($_c1->[0]); $out->[5] = $rt->f32($_c1->[1]); $out->[6] = $rt->f32($_c1->[2]); $out->[7] = $rt->f32($_c1->[3]);
    my $_c2 = $g->{outRGBA};
    $out->[8] = $rt->f32($_c2->[0]); $out->[9] = $rt->f32($_c2->[1]); $out->[10] = $rt->f32($_c2->[2]); $out->[11] = $rt->f32($_c2->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0, output_names => ['outXYZ', 'outVel', 'outRGBA'] };
