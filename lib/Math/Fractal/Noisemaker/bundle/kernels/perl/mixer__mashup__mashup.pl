# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($getLuminosity__vec3, $sampleLayer__int_vec2, $layerActive__int, $bandWeight__float_float, $main__void);
    my $_retc;
    my $_u_resolution = exists $U->{'resolution'} ? $U->{'resolution'} : $rt->construct(2, 0.0);
    my $_u_source = $ctx->texture_binding('source');
    my $_u_layer0_tex = $ctx->texture_binding('layer0_tex');
    my $_u_layer1_tex = $ctx->texture_binding('layer1_tex');
    my $_u_layer2_tex = $ctx->texture_binding('layer2_tex');
    my $_u_layer3_tex = $ctx->texture_binding('layer3_tex');
    my $_u_layer4_tex = $ctx->texture_binding('layer4_tex');
    my $_u_layer5_tex = $ctx->texture_binding('layer5_tex');
    my $_u_layer6_tex = $ctx->texture_binding('layer6_tex');
    my $_u_layer7_tex = $ctx->texture_binding('layer7_tex');
    my $_u_layers = exists $U->{'layers'} ? $U->{'layers'} : 0;
    my $_u_smoothness = exists $U->{'smoothness'} ? $U->{'smoothness'} : $rt->f(0.0);
    my $_u_layer0_active = exists $U->{'layer0_active'} ? $U->{'layer0_active'} : 0;
    my $_u_layer1_active = exists $U->{'layer1_active'} ? $U->{'layer1_active'} : 0;
    my $_u_layer2_active = exists $U->{'layer2_active'} ? $U->{'layer2_active'} : 0;
    my $_u_layer3_active = exists $U->{'layer3_active'} ? $U->{'layer3_active'} : 0;
    my $_u_layer4_active = exists $U->{'layer4_active'} ? $U->{'layer4_active'} : 0;
    my $_u_layer5_active = exists $U->{'layer5_active'} ? $U->{'layer5_active'} : 0;
    my $_u_layer6_active = exists $U->{'layer6_active'} ? $U->{'layer6_active'} : 0;
    my $_u_layer7_active = exists $U->{'layer7_active'} ? $U->{'layer7_active'} : 0;
    $g->{fragColor} = $rt->construct(4, 0.0);
    $getLuminosity__vec3 = sub {
        my ($color) = @_;
        $color = $rt->copy($color, 'float');
        return $rt->dot($color, $rt->construct(3, $rt->f(0.29899999999999999), $rt->f(0.58699999999999997), $rt->f(0.114)));
    };
    $sampleLayer__int_vec2 = sub {
        my ($i, $uv) = @_;
        $uv = $rt->copy($uv, 'float');
        if ($rt->binary('==', $i, $rt->i(0))) {
            return $rt->texture($_u_layer0_tex, $uv);
        }
        if ($rt->binary('==', $i, $rt->i(1))) {
            return $rt->texture($_u_layer1_tex, $uv);
        }
        if ($rt->binary('==', $i, $rt->i(2))) {
            return $rt->texture($_u_layer2_tex, $uv);
        }
        if ($rt->binary('==', $i, $rt->i(3))) {
            return $rt->texture($_u_layer3_tex, $uv);
        }
        if ($rt->binary('==', $i, $rt->i(4))) {
            return $rt->texture($_u_layer4_tex, $uv);
        }
        if ($rt->binary('==', $i, $rt->i(5))) {
            return $rt->texture($_u_layer5_tex, $uv);
        }
        if ($rt->binary('==', $i, $rt->i(6))) {
            return $rt->texture($_u_layer6_tex, $uv);
        }
        return $rt->texture($_u_layer7_tex, $uv);
    };
    $layerActive__int = sub {
        my ($i) = @_;
        if ($rt->binary('==', $i, $rt->i(0))) {
            return $_u_layer0_active;
        }
        if ($rt->binary('==', $i, $rt->i(1))) {
            return $_u_layer1_active;
        }
        if ($rt->binary('==', $i, $rt->i(2))) {
            return $_u_layer2_active;
        }
        if ($rt->binary('==', $i, $rt->i(3))) {
            return $_u_layer3_active;
        }
        if ($rt->binary('==', $i, $rt->i(4))) {
            return $_u_layer4_active;
        }
        if ($rt->binary('==', $i, $rt->i(5))) {
            return $_u_layer5_active;
        }
        if ($rt->binary('==', $i, $rt->i(6))) {
            return $_u_layer6_active;
        }
        return $_u_layer7_active;
    };
    $bandWeight__float_float = sub {
        my ($lum, $boundary) = @_;
        if ($rt->binary('<=', $_u_smoothness, $rt->f(0))) {
            return $rt->component_wise('step', $boundary, $lum);
        }
        return $rt->component_wise('smoothstep', $rt->binary('-', $boundary, $_u_smoothness, 1, 'float'), $rt->binary('+', $boundary, $_u_smoothness, 1, 'float'), $lum);
    };
    $main__void = sub {
        my ($_for0_first, $boundary, $controlColor, $k, $lum, $n, $result, $src, $uv, $w);
        $uv = $rt->binary('/', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_resolution, 2, 'float');
        $controlColor = $rt->texture($_u_source, $uv);
        $lum = $getLuminosity__vec3->($rt->swizzle($controlColor, 'rgb'));
        $n = $rt->component_wise('clamp', $_u_layers, $rt->i(2), $rt->i(8));
        $result = (($rt->binary('==', $layerActive__int->($rt->i(0)), $rt->i(1))) ? ($sampleLayer__int_vec2->($rt->i(0), $uv)) : ($controlColor));
        $k = $rt->i(1);
        $_for0_first = 1;
        for my $_for0 (0 .. 1048575) {
            if (!$_for0_first) {
                $k = $rt->binary('+', $k, $rt->i(1), 1, 'int');
            }
            $_for0_first = 0;
            if (!($rt->binary('<', $k, $rt->i(8)))) {
                last;
            }
            if ($rt->binary('>=', $k, $n)) {
                last;
            }
            $src = (($rt->binary('==', $layerActive__int->($k), $rt->i(1))) ? ($sampleLayer__int_vec2->($k, $uv)) : ($controlColor));
            $boundary = $rt->binary('/', $rt->construct(1, $k), $rt->construct(1, $n), 1, 'float');
            $w = $bandWeight__float_float->($lum, $boundary);
            @{$result} = map { $rt->f32($_) } @{($rt->component_wise('mix', $result, $src, $w))};
        }
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($result)};
    };
    $main__void->();
    my $_c = $g->{fragColor};
    $out->[0] = $rt->f32($_c->[0]); $out->[1] = $rt->f32($_c->[1]); $out->[2] = $rt->f32($_c->[2]); $out->[3] = $rt->f32($_c->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0 };
