# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($getLuminosity__vec3, $quantizeValue__float_int, $calculateBlendFactor__float_float_float, $main__void);
    my $_retc;
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_tex = $ctx->texture_binding('tex');
    my $_u_resolution = exists $U->{'resolution'} ? $U->{'resolution'} : $rt->construct(2, 0.0);
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_mode = exists $U->{'mode'} ? $U->{'mode'} : 0;
    my $_u_quantize = exists $U->{'quantize'} ? $U->{'quantize'} : 0;
    my $_u_mapSource = exists $U->{'mapSource'} ? $U->{'mapSource'} : 0;
    my $_u_threshold = exists $U->{'threshold'} ? $U->{'threshold'} : $rt->f(0.0);
    my $_u_range = exists $U->{'range'} ? $U->{'range'} : $rt->f(0.0);
    my $_u_thresholdR = exists $U->{'thresholdR'} ? $U->{'thresholdR'} : $rt->f(0.0);
    my $_u_rangeR = exists $U->{'rangeR'} ? $U->{'rangeR'} : $rt->f(0.0);
    my $_u_thresholdG = exists $U->{'thresholdG'} ? $U->{'thresholdG'} : $rt->f(0.0);
    my $_u_rangeG = exists $U->{'rangeG'} ? $U->{'rangeG'} : $rt->f(0.0);
    my $_u_thresholdB = exists $U->{'thresholdB'} ? $U->{'thresholdB'} : $rt->f(0.0);
    my $_u_rangeB = exists $U->{'rangeB'} ? $U->{'rangeB'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $getLuminosity__vec3 = sub {
        my ($color) = @_;
        $color = $rt->copy($color, 'float');
        return $rt->dot($color, $rt->construct(3, $rt->f(0.29899999999999999), $rt->f(0.58699999999999997), $rt->f(0.114)));
    };
    $quantizeValue__float_int = sub {
        my ($value, $bands) = @_;
        my ($numBands);
        if ($rt->binary('<=', $bands, $rt->i(0))) {
            return $value;
        }
        $numBands = $rt->construct(1, $bands);
        return $rt->binary('/', $rt->component_wise('floor', $rt->binary('*', $value, $numBands, 1, 'float')), $numBands, 1, 'float');
    };
    $calculateBlendFactor__float_float_float = sub {
        my ($mapValue, $thresh, $rng) = @_;
        my ($lower, $upper);
        $lower = $rt->f(0.0);
        $upper = $rt->f(0.0);
        if ($rt->binary('<=', $rng, $rt->f(0))) {
            return $rt->component_wise('step', $thresh, $mapValue);
        } else {
            $lower = $thresh;
            $upper = $rt->binary('+', $thresh, $rng, 1, 'float');
            return $rt->component_wise('smoothstep', $lower, $upper, $mapValue);
        }
    };
    $main__void = sub {
        my ($blendB, $blendFactor, $blendG, $blendR, $colorA, $colorB, $globalCoord, $lum, $mapColor, $result, $uv);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $uv = $rt->binary('/', $globalCoord, $_u_fullResolution, 2, 'float');
        $colorA = $rt->texture($_u_inputTex, $rt->binary('/', $rt->swizzle($ctx->{frag_coord}, 'xy'), $rt->construct(2, $rt->texture_size($_u_inputTex)), 2, 'float'));
        $colorB = $rt->texture($_u_tex, $rt->binary('/', $rt->swizzle($ctx->{frag_coord}, 'xy'), $rt->construct(2, $rt->texture_size($_u_tex)), 2, 'float'));
        $mapColor = $rt->construct(3, 0.0);
        if ($rt->binary('==', $_u_mapSource, $rt->i(0))) {
            @{$mapColor} = map { $rt->f32($_) } @{($rt->swizzle($colorA, 'rgb'))};
        } else {
            @{$mapColor} = map { $rt->f32($_) } @{($rt->swizzle($colorB, 'rgb'))};
        }
        if ($rt->binary('>', $_u_quantize, $rt->i(0))) {
            $mapColor = $rt->assign_swizzle($mapColor, 'r', $quantizeValue__float_int->($rt->swizzle($mapColor, 'r'), $_u_quantize));
            $mapColor = $rt->assign_swizzle($mapColor, 'g', $quantizeValue__float_int->($rt->swizzle($mapColor, 'g'), $_u_quantize));
            $mapColor = $rt->assign_swizzle($mapColor, 'b', $quantizeValue__float_int->($rt->swizzle($mapColor, 'b'), $_u_quantize));
        }
        $result = $rt->construct(4, 0.0);
        $blendB = $rt->f(0.0);
        $blendFactor = $rt->f(0.0);
        $blendG = $rt->f(0.0);
        $blendR = $rt->f(0.0);
        $lum = $rt->f(0.0);
        if ($rt->binary('==', $_u_mode, $rt->i(0))) {
            $lum = $getLuminosity__vec3->($mapColor);
            $blendFactor = $calculateBlendFactor__float_float_float->($lum, $_u_threshold, $_u_range);
            @{$result} = map { $rt->f32($_) } @{($rt->component_wise('mix', $colorA, $colorB, $blendFactor))};
        } else {
            $blendR = $calculateBlendFactor__float_float_float->($rt->swizzle($mapColor, 'r'), $_u_thresholdR, $_u_rangeR);
            $blendG = $calculateBlendFactor__float_float_float->($rt->swizzle($mapColor, 'g'), $_u_thresholdG, $_u_rangeG);
            $blendB = $calculateBlendFactor__float_float_float->($rt->swizzle($mapColor, 'b'), $_u_thresholdB, $_u_rangeB);
            $result = $rt->assign_swizzle($result, 'r', $rt->component_wise('mix', $rt->swizzle($colorA, 'r'), $rt->swizzle($colorB, 'r'), $blendR));
            $result = $rt->assign_swizzle($result, 'g', $rt->component_wise('mix', $rt->swizzle($colorA, 'g'), $rt->swizzle($colorB, 'g'), $blendG));
            $result = $rt->assign_swizzle($result, 'b', $rt->component_wise('mix', $rt->swizzle($colorA, 'b'), $rt->swizzle($colorB, 'b'), $blendB));
            $result = $rt->assign_swizzle($result, 'a', $rt->component_wise('mix', $rt->swizzle($colorA, 'a'), $rt->swizzle($colorB, 'a'), $rt->binary('/', $rt->binary('+', $rt->binary('+', $blendR, $blendG, 1, 'float'), $blendB, 1, 'float'), $rt->f(3), 1, 'float')));
        }
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($result)};
    };
    $main__void->();
    my $_c = $g->{fragColor};
    $out->[0] = $rt->f32($_c->[0]); $out->[1] = $rt->f32($_c->[1]); $out->[2] = $rt->f32($_c->[2]); $out->[3] = $rt->f32($_c->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0 };
