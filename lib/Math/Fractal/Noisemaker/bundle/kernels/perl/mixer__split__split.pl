# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($main__void);
    my $_retc;
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_tex = $ctx->texture_binding('tex');
    my $_u_resolution = exists $U->{'resolution'} ? $U->{'resolution'} : $rt->construct(2, 0.0);
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_position = exists $U->{'position'} ? $U->{'position'} : $rt->f(0.0);
    my $_u_rotation = exists $U->{'rotation'} ? $U->{'rotation'} : $rt->f(0.0);
    my $_u_softness = exists $U->{'softness'} ? $U->{'softness'} : $rt->f(0.0);
    my $_u_invert = exists $U->{'invert'} ? $U->{'invert'} : 0;
    my $_u_speed = exists $U->{'speed'} ? $U->{'speed'} : $rt->f(0.0);
    my $_u_time = exists $U->{'time'} ? $U->{'time'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $main__void = sub {
        my ($animPos, $aspect, $c, $centered, $color, $colorA, $colorB, $cycle, $d, $extent, $flipCycle, $fullRes, $globalCoord, $globalUV, $halfSoft, $mask, $rad, $rotated, $s, $st, $t);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $st = $rt->binary('/', $globalCoord, $_u_fullResolution, 2, 'float');
        $colorA = $rt->texture($_u_inputTex, $rt->binary('/', $rt->swizzle($ctx->{frag_coord}, 'xy'), $rt->construct(2, $rt->texture_size($_u_inputTex)), 2, 'float'));
        $colorB = $rt->texture($_u_tex, $rt->binary('/', $rt->swizzle($ctx->{frag_coord}, 'xy'), $rt->construct(2, $rt->texture_size($_u_tex)), 2, 'float'));
        $fullRes = (($rt->binary('>', $rt->swizzle($_u_fullResolution, 'x'), $rt->f(0))) ? ($_u_fullResolution) : ($_u_resolution));
        $aspect = $rt->binary('/', $rt->swizzle($fullRes, 'x'), $rt->swizzle($fullRes, 'y'), 1, 'float');
        $globalUV = $rt->binary('/', $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float'), $fullRes, 2, 'float');
        $centered = $rt->binary('*', $rt->binary('-', $globalUV, $rt->f(0.5), 2, 'float'), $rt->f(2), 2, 'float');
        $centered = $rt->assign_swizzle($centered, 'x', $rt->binary('*', $rt->swizzle($centered, 'x'), $aspect, 1, 'float'));
        $rad = $rt->binary('/', $rt->binary('*', $_u_rotation, $rt->f(3.1415926535900001), 1, 'float'), $rt->f(180), 1, 'float');
        $c = $rt->component_wise('cos', $rad);
        $s = $rt->component_wise('sin', $rad);
        $rotated = $rt->construct(2, $rt->binary('-', $rt->binary('*', $rt->swizzle($centered, 'x'), $c, 1, 'float'), $rt->binary('*', $rt->swizzle($centered, 'y'), $s, 1, 'float'), 1, 'float'), $rt->binary('+', $rt->binary('*', $rt->swizzle($centered, 'x'), $s, 1, 'float'), $rt->binary('*', $rt->swizzle($centered, 'y'), $c, 1, 'float'), 1, 'float'));
        $extent = $rt->binary('+', $rt->binary('+', $rt->binary('*', $aspect, $rt->component_wise('abs', $s), 1, 'float'), $rt->component_wise('abs', $c), 1, 'float'), $_u_softness, 1, 'float');
        $animPos = $_u_position;
        $flipCycle = 0;
        $cycle = $rt->f(0.0);
        $t = $rt->f(0.0);
        if ($rt->binary('>', $_u_speed, $rt->f(0))) {
            $cycle = $rt->binary('*', $rt->binary('*', $_u_time, $_u_speed, 1, 'float'), $rt->f(2), 1, 'float');
            $t = $rt->component_wise('fract', $cycle);
            $flipCycle = $rt->binary('==', $rt->component_wise('mod', $rt->component_wise('floor', $cycle), $rt->f(2)), $rt->f(1));
            $animPos = $rt->binary('-', $rt->binary('*', $rt->binary('*', $t, $extent, 1, 'float'), $rt->f(2), 1, 'float'), $extent, 1, 'float');
        }
        $d = $rt->binary('-', $rt->swizzle($rotated, 'y'), $animPos, 1, 'float');
        $halfSoft = $rt->component_wise('max', $rt->binary('*', $_u_softness, $rt->f(0.5), 1, 'float'), $rt->f(0.001));
        $mask = $rt->component_wise('smoothstep', $rt->unary('-', $halfSoft), $halfSoft, $d);
        if ($rt->binary('!=', $rt->binary('==', $_u_invert, $rt->i(1)), $flipCycle)) {
            $mask = $rt->binary('-', $rt->f(1), $mask, 1, 'float');
        }
        $color = $rt->component_wise('mix', $colorA, $colorB, $mask);
        $color = $rt->assign_swizzle($color, 'a', $rt->component_wise('max', $rt->swizzle($colorA, 'a'), $rt->swizzle($colorB, 'a')));
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($color)};
    };
    $main__void->();
    my $_c = $g->{fragColor};
    $out->[0] = $rt->f32($_c->[0]); $out->[1] = $rt->f32($_c->[1]); $out->[2] = $rt->f32($_c->[2]); $out->[3] = $rt->f32($_c->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0 };
