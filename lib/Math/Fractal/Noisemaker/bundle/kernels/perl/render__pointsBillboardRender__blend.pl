# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($main__void);
    my $_retc;
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_trailTex = $ctx->texture_binding('trailTex');
    my $_u_resolution = exists $U->{'resolution'} ? $U->{'resolution'} : $rt->construct(2, 0.0);
    my $_u_inputIntensity = exists $U->{'inputIntensity'} ? $U->{'inputIntensity'} : $rt->f(0.0);
    my $_u_blendMode = exists $U->{'blendMode'} ? $U->{'blendMode'} : 0;
    $g->{fragColor} = $rt->construct(4, 0.0);
    $main__void = sub {
        my ($inputColor, $outAlpha, $outRGB, $outRGB_pre, $scaledInput, $t, $trail, $trailColor, $trailPresence, $uv);
        $uv = $rt->binary('/', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_resolution, 2, 'float');
        $inputColor = $rt->texture($_u_inputTex, $uv);
        $trailColor = $rt->texture($_u_trailTex, $uv);
        $t = $rt->binary('/', $_u_inputIntensity, $rt->f(100), 1, 'float');
        $scaledInput = $rt->binary('*', $inputColor, $t, 4, 'float');
        $outRGB = $rt->construct(3, 0.0);
        $outAlpha = $rt->f(0.0);
        $outRGB_pre = $rt->construct(3, 0.0);
        $trail = $rt->construct(3, 0.0);
        $trailPresence = $rt->f(0.0);
        if ($rt->binary('==', $_u_blendMode, $rt->i(1))) {
            $outAlpha = $rt->binary('+', $rt->swizzle($trailColor, 'a'), $rt->binary('*', $rt->swizzle($scaledInput, 'a'), $rt->binary('-', $rt->f(1), $rt->swizzle($trailColor, 'a'), 1, 'float'), 1, 'float'), 1, 'float');
            $outRGB_pre = $rt->binary('+', $rt->swizzle($trailColor, 'rgb'), $rt->binary('*', $rt->binary('*', $rt->swizzle($scaledInput, 'rgb'), $rt->swizzle($scaledInput, 'a'), 3, 'float'), $rt->binary('-', $rt->f(1), $rt->swizzle($trailColor, 'a'), 1, 'float'), 3, 'float'), 3, 'float');
            @{$outRGB} = map { $rt->f32($_) } @{((($rt->binary('>', $outAlpha, $rt->f(0))) ? ($rt->binary('/', $outRGB_pre, $outAlpha, 3, 'float')) : ($rt->construct(3, $rt->f(0)))))};
        } else {
            $trail = $rt->component_wise('clamp', $rt->swizzle($trailColor, 'rgb'), $rt->f(0), $rt->f(1));
            $trailPresence = $rt->component_wise('max', $rt->component_wise('max', $rt->swizzle($trail, 'r'), $rt->swizzle($trail, 'g')), $rt->swizzle($trail, 'b'));
            @{$outRGB} = map { $rt->f32($_) } @{($rt->binary('+', $trail, $rt->binary('*', $rt->swizzle($scaledInput, 'rgb'), $rt->binary('-', $rt->f(1), $trail, 3, 'float'), 3, 'float'), 3, 'float'))};
            $outAlpha = $rt->component_wise('max', $trailPresence, $rt->swizzle($scaledInput, 'a'));
        }
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->component_wise('clamp', $rt->construct(4, $outRGB, $outAlpha), $rt->f(0), $rt->f(1)))};
    };
    $main__void->();
    my $_c0 = $g->{fragColor};
    $out->[0] = $rt->f32($_c0->[0]); $out->[1] = $rt->f32($_c0->[1]); $out->[2] = $rt->f32($_c0->[2]); $out->[3] = $rt->f32($_c0->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0, output_names => ['fragColor'] };
