# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($polygon__vec2_float, $main__void);
    my $_retc;
    my $_u_resolution = exists $U->{'resolution'} ? $U->{'resolution'} : $rt->construct(2, 0.0);
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_aspect = exists $U->{'aspect'} ? $U->{'aspect'} : $rt->f(0.0);
    my $_u_sides = exists $U->{'sides'} ? $U->{'sides'} : 0;
    my $_u_radius = exists $U->{'radius'} ? $U->{'radius'} : $rt->f(0.0);
    my $_u_smoothing = exists $U->{'smoothing'} ? $U->{'smoothing'} : $rt->f(0.0);
    my $_u_rotation = exists $U->{'rotation'} ? $U->{'rotation'} : $rt->f(0.0);
    my $_u_fgColor = exists $U->{'fgColor'} ? $U->{'fgColor'} : $rt->construct(3, 0.0);
    my $_u_fgAlpha = exists $U->{'fgAlpha'} ? $U->{'fgAlpha'} : $rt->f(0.0);
    my $_u_bgColor = exists $U->{'bgColor'} ? $U->{'bgColor'} : $rt->construct(3, 0.0);
    my $_u_bgAlpha = exists $U->{'bgAlpha'} ? $U->{'bgAlpha'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $polygon__vec2_float = sub {
        my ($st, $sides) = @_;
        $st = $rt->copy($st, 'float');
        my ($a, $r);
        $a = $rt->binary('+', $rt->component_wise('atan', $rt->swizzle($st, 'y'), $rt->swizzle($st, 'x')), $rt->f(3.1415926500000002), 1, 'float');
        $r = $rt->binary('/', $rt->f(6.2831853000000004), $sides, 1, 'float');
        return $rt->binary('*', $rt->component_wise('cos', $rt->binary('-', $rt->binary('*', $rt->component_wise('floor', $rt->binary('+', $rt->f(0.5), $rt->binary('/', $a, $r, 1, 'float'), 1, 'float')), $r, 1, 'float'), $a, 1, 'float')), $rt->length($st), 1, 'float');
    };
    $main__void = sub {
        my ($bgMask, $c, $d, $fgMask, $globalCoord, $m, $outColor, $s, $sidesF, $st, $totalAlpha);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $st = $rt->binary('/', $globalCoord, $_u_fullResolution, 2, 'float');
        @{$st} = map { $rt->f32($_) } @{($rt->binary('*', $rt->binary('-', $st, $rt->f(0.5), 2, 'float'), $rt->f(2), 2, 'float'))};
        $st = $rt->assign_swizzle($st, 'x', $rt->binary('*', $rt->swizzle($st, 'x'), $_u_aspect, 1, 'float'));
        $c = $rt->component_wise('cos', $rt->binary('/', $rt->binary('*', $_u_rotation, $rt->f(3.1415926535900001), 1, 'float'), $rt->f(180), 1, 'float'));
        $s = $rt->component_wise('sin', $rt->binary('/', $rt->binary('*', $_u_rotation, $rt->f(3.1415926535900001), 1, 'float'), $rt->f(180), 1, 'float'));
        @{$st} = map { $rt->f32($_) } @{($rt->construct(2, $rt->binary('-', $rt->binary('*', $rt->swizzle($st, 'x'), $c, 1, 'float'), $rt->binary('*', $rt->swizzle($st, 'y'), $s, 1, 'float'), 1, 'float'), $rt->binary('+', $rt->binary('*', $rt->swizzle($st, 'x'), $s, 1, 'float'), $rt->binary('*', $rt->swizzle($st, 'y'), $c, 1, 'float'), 1, 'float')))};
        $sidesF = $rt->construct(1, $rt->component_wise('max', $_u_sides, $rt->i(3)));
        if ($rt->binary('==', $_u_sides, $rt->i(3))) {
            @{$st} = map { $rt->f32($_) } @{($rt->construct(2, $rt->swizzle($st, 'y'), $rt->unary('-', $rt->swizzle($st, 'x'))))};
        }
        $d = $rt->binary('/', $polygon__vec2_float->($st, $sidesF), $rt->component_wise('cos', $rt->binary('/', $rt->f(3.1415926535900001), $sidesF, 1, 'float')), 1, 'float');
        $m = $rt->component_wise('smoothstep', $_u_radius, $rt->binary('-', $_u_radius, $_u_smoothing, 1, 'float'), $d);
        $fgMask = $rt->binary('*', $m, $_u_fgAlpha, 1, 'float');
        $bgMask = $rt->binary('*', $rt->binary('-', $rt->f(1), $m, 1, 'float'), $_u_bgAlpha, 1, 'float');
        $totalAlpha = $rt->binary('+', $fgMask, $bgMask, 1, 'float');
        $outColor = (($rt->binary('>', $totalAlpha, $rt->f(0))) ? ($rt->binary('/', $rt->binary('+', $rt->binary('*', $_u_fgColor, $fgMask, 3, 'float'), $rt->binary('*', $_u_bgColor, $bgMask, 3, 'float'), 3, 'float'), $totalAlpha, 3, 'float')) : ($rt->construct(3, $rt->f(0))));
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $rt->binary('*', $outColor, $totalAlpha, 3, 'float'), $totalAlpha))};
    };
    $main__void->();
    my $_c0 = $g->{fragColor};
    $out->[0] = $rt->f32($_c0->[0]); $out->[1] = $rt->f32($_c0->[1]); $out->[2] = $rt->f32($_c0->[2]); $out->[3] = $rt->f32($_c0->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0, output_names => ['fragColor'] };
