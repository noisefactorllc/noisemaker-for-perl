# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($hash__float, $trefoilKnot__float_float, $tiltedOrbit__float_float, $lissajousOrbit__float_float, $getOrbitPosition__float, $getOrbitTangent__float, $getWobbleOffset__float_vec3, $getCameraState__float_vec3_vec3_vec3, $mandelbulb__vec3_float_int_float, $boxFold__vec3_float, $mandelbox__vec3_float_int_float, $computeFractal__vec3, $computeGradient__vec3_float, $applyCollisionAvoidance__vec3, $main__void);
    my $_retc;
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_time = exists $U->{'time'} ? $U->{'time'} : $rt->f(0.0);
    my $_u_volumeSize = exists $U->{'volumeSize'} ? $U->{'volumeSize'} : 0;
    my $_u_noiseType = exists $U->{'noiseType'} ? $U->{'noiseType'} : 0;
    my $_u_power = exists $U->{'power'} ? $U->{'power'} : $rt->f(0.0);
    my $_u_iterations = exists $U->{'iterations'} ? $U->{'iterations'} : 0;
    my $_u_bailout = exists $U->{'bailout'} ? $U->{'bailout'} : $rt->f(0.0);
    my $_u_speed = exists $U->{'speed'} ? $U->{'speed'} : $rt->f(0.0);
    my $_u_voiSize = exists $U->{'voiSize'} ? $U->{'voiSize'} : $rt->f(0.0);
    my $_u_seed = exists $U->{'seed'} ? $U->{'seed'} : $rt->f(0.0);
    $g->{SAFETY_RADIUS} = $rt->f(0.080000000000000002);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $g->{geoOut} = $rt->construct(4, 0.0);
    $g->{PI} = $rt->f(3.1415926535897931);
    $g->{TAU} = $rt->f(6.2831853071795862);
    $hash__float = sub {
        my ($n) = @_;
        return $rt->component_wise('fract', $rt->binary('*', $rt->component_wise('sin', $rt->binary('+', $n, $_u_seed, 1, 'float')), $rt->f(43758.545312299997), 1, 'float'));
    };
    $trefoilKnot__float_float = sub {
        my ($t, $scale) = @_;
        my ($p, $q, $r);
        $p = $rt->f(2);
        $q = $rt->f(3);
        $r = $rt->binary('+', $rt->f(0.5), $rt->binary('*', $rt->f(0.20000000000000001), $rt->component_wise('cos', $rt->binary('*', $q, $t, 1, 'float')), 1, 'float'), 1, 'float');
        return $rt->binary('*', $scale, $rt->construct(3, $rt->binary('*', $r, $rt->component_wise('cos', $rt->binary('*', $p, $t, 1, 'float')), 1, 'float'), $rt->binary('*', $r, $rt->component_wise('sin', $rt->binary('*', $p, $t, 1, 'float')), 1, 'float'), $rt->binary('*', $rt->f(0.29999999999999999), $rt->component_wise('sin', $rt->binary('*', $q, $t, 1, 'float')), 1, 'float')), 3, 'float');
    };
    $tiltedOrbit__float_float = sub {
        my ($t, $scale) = @_;
        my ($a, $b, $c, $pos, $s, $tilt);
        $tilt = $rt->f(0.40000000000000002);
        $a = $rt->f(1);
        $b = $rt->f(0.69999999999999996);
        $pos = $rt->construct(3, $rt->binary('*', $a, $rt->component_wise('cos', $t), 1, 'float'), $rt->binary('*', $b, $rt->component_wise('sin', $t), 1, 'float'), $rt->f(0));
        $c = $rt->component_wise('cos', $tilt);
        $s = $rt->component_wise('sin', $tilt);
        @{$pos} = map { $rt->f32($_) } @{($rt->construct(3, $rt->swizzle($pos, 'x'), $rt->binary('-', $rt->binary('*', $rt->swizzle($pos, 'y'), $c, 1, 'float'), $rt->binary('*', $rt->swizzle($pos, 'z'), $s, 1, 'float'), 1, 'float'), $rt->binary('+', $rt->binary('*', $rt->swizzle($pos, 'y'), $s, 1, 'float'), $rt->binary('*', $rt->swizzle($pos, 'z'), $c, 1, 'float'), 1, 'float')))};
        return $rt->binary('*', $scale, $pos, 3, 'float');
    };
    $lissajousOrbit__float_float = sub {
        my ($t, $scale) = @_;
        my ($fx, $fy, $fz, $px, $py, $pz);
        $fx = $rt->f(1);
        $fy = $rt->f(1.6180000000000001);
        $fz = $rt->f(2);
        $px = $rt->f(0);
        $py = $rt->binary('*', $g->{PI}, $rt->f(0.5), 1, 'float');
        $pz = $rt->binary('*', $g->{PI}, $rt->f(0.25), 1, 'float');
        return $rt->binary('*', $scale, $rt->construct(3, $rt->component_wise('sin', $rt->binary('+', $rt->binary('*', $fx, $t, 1, 'float'), $px, 1, 'float')), $rt->binary('*', $rt->component_wise('sin', $rt->binary('+', $rt->binary('*', $fy, $t, 1, 'float'), $py, 1, 'float')), $rt->f(0.59999999999999998), 1, 'float'), $rt->binary('*', $rt->component_wise('sin', $rt->binary('+', $rt->binary('*', $fz, $t, 1, 'float'), $pz, 1, 'float')), $rt->f(0.40000000000000002), 1, 'float')), 3, 'float');
    };
    $getOrbitPosition__float = sub {
        my ($t) = @_;
        my ($orbitScale, $orbitType);
        $orbitScale = $rt->f(0.69999999999999996);
        $orbitType = $rt->construct(1, $rt->component_wise('mod', $rt->binary('*', $_u_seed, $rt->f(3), 1, 'float'), $rt->f(3)), 'int');
        if ($rt->binary('==', $orbitType, $rt->i(0))) {
            return $trefoilKnot__float_float->($t, $orbitScale);
        } else {
            if ($rt->binary('==', $orbitType, $rt->i(1))) {
                return $tiltedOrbit__float_float->($t, $orbitScale);
            } else {
                return $lissajousOrbit__float_float->($t, $orbitScale);
            }
        }
    };
    $getOrbitTangent__float = sub {
        my ($t) = @_;
        my ($dt, $p0, $p1);
        $dt = $rt->f(0.01);
        $p0 = $getOrbitPosition__float->($t);
        $p1 = $getOrbitPosition__float->($rt->binary('+', $t, $dt, 1, 'float'));
        return $rt->normalize($rt->binary('-', $p1, $p0, 3, 'float'));
    };
    $getWobbleOffset__float_vec3 = sub {
        my ($t, $tangent) = @_;
        $tangent = $rt->copy($tangent, 'float');
        my ($realUp, $right, $up, $wobbleAmp, $wx, $wy);
        $up = $rt->construct(3, $rt->f(0), $rt->f(1), $rt->f(0));
        if ($rt->binary('>', $rt->component_wise('abs', $rt->dot($tangent, $up)), $rt->f(0.98999999999999999))) {
            @{$up} = map { $rt->f32($_) } @{($rt->construct(3, $rt->f(1), $rt->f(0), $rt->f(0)))};
        }
        $right = $rt->normalize($rt->cross($tangent, $up));
        $realUp = $rt->normalize($rt->cross($right, $tangent));
        $wobbleAmp = $rt->f(0.14999999999999999);
        $wx = $rt->binary('*', $rt->component_wise('sin', $rt->binary('+', $rt->binary('*', $t, $rt->f(2.7000000000000002), 1, 'float'), $rt->binary('*', $_u_seed, $g->{PI}, 1, 'float'), 1, 'float')), $wobbleAmp, 1, 'float');
        $wy = $rt->binary('*', $rt->binary('*', $rt->component_wise('sin', $rt->binary('+', $rt->binary('*', $t, $rt->f(1.8999999999999999), 1, 'float'), $rt->binary('*', $_u_seed, $g->{TAU}, 1, 'float'), 1, 'float')), $wobbleAmp, 1, 'float'), $rt->f(0.69999999999999996), 1, 'float');
        return $rt->binary('+', $rt->binary('*', $right, $wx, 3, 'float'), $rt->binary('*', $realUp, $wy, 3, 'float'), 3, 'float');
    };
    $getCameraState__float_vec3_vec3_vec3 = sub {
        my ($t, $pos, $dir, $up) = @_;
        $pos = $rt->copy($pos, 'float');
        $dir = $rt->copy($dir, 'float');
        $up = $rt->copy($up, 'float');
        my ($orbitPos, $orbitTime, $right, $roll, $rollRight, $tangent, $wobble, $worldUp);
        $orbitTime = $rt->binary('*', $rt->binary('*', $t, $_u_speed, 1, 'float'), $rt->f(0.29999999999999999), 1, 'float');
        $orbitPos = $getOrbitPosition__float->($orbitTime);
        $tangent = $getOrbitTangent__float->($orbitTime);
        $wobble = $getWobbleOffset__float_vec3->($orbitTime, $tangent);
        @{$pos} = map { $rt->f32($_) } @{($rt->binary('+', $orbitPos, $wobble, 3, 'float'))};
        @{$dir} = map { $rt->f32($_) } @{($tangent)};
        $worldUp = $rt->construct(3, $rt->f(0), $rt->f(1), $rt->f(0));
        $right = $rt->normalize($rt->cross($worldUp, $dir));
        @{$up} = map { $rt->f32($_) } @{($rt->normalize($rt->cross($dir, $right)))};
        $roll = $rt->binary('*', $rt->component_wise('sin', $rt->binary('*', $orbitTime, $rt->f(0.5), 1, 'float')), $rt->f(0.10000000000000001), 1, 'float');
        $rollRight = $rt->binary('+', $rt->binary('*', $right, $rt->component_wise('cos', $roll), 3, 'float'), $rt->binary('*', $up, $rt->component_wise('sin', $roll), 3, 'float'), 3, 'float');
        @{$up} = map { $rt->f32($_) } @{($rt->normalize($rt->cross($rollRight, $dir)))};
        return (undef, $pos, $dir, $up);
    };
    $mandelbulb__vec3_float_int_float = sub {
        my ($pos, $n, $maxIter, $bail) = @_;
        $pos = $rt->copy($pos, 'float');
        my ($_for0_first, $dr, $i, $iter, $newPhi, $newTheta, $phi, $r, $result, $theta, $trap, $z, $zr);
        $result = [$rt->f(0.0), $rt->f(0.0), $rt->f(0.0)];
        $z = $pos;
        $dr = $rt->f(1);
        $r = $rt->f(0);
        $trap = $rt->f(10000000000);
        $iter = $rt->f(0);
        $i = $rt->i(0);
        $_for0_first = 1;
        for my $_for0 (0 .. 1048575) {
            if (!$_for0_first) {
                $i = $rt->binary('+', $i, $rt->i(1), 1, 'int');
            }
            $_for0_first = 0;
            if (!($rt->binary('<', $i, $maxIter))) {
                last;
            }
            $r = $rt->length($z);
            if ($rt->binary('>', $r, $bail)) {
                last;
            }
            $trap = $rt->component_wise('min', $trap, $r);
            $theta = $rt->component_wise('acos', $rt->binary('/', $rt->swizzle($z, 'z'), $r, 1, 'float'));
            $phi = $rt->component_wise('atan', $rt->swizzle($z, 'y'), $rt->swizzle($z, 'x'));
            $dr = $rt->binary('+', $rt->binary('*', $rt->binary('*', $rt->component_wise('pow', $r, $rt->binary('-', $n, $rt->f(1), 1, 'float')), $n, 1, 'float'), $dr, 1, 'float'), $rt->f(1), 1, 'float');
            $zr = $rt->component_wise('pow', $r, $n);
            $newTheta = $rt->binary('*', $theta, $n, 1, 'float');
            $newPhi = $rt->binary('*', $phi, $n, 1, 'float');
            @{$z} = map { $rt->f32($_) } @{($rt->binary('*', $zr, $rt->construct(3, $rt->binary('*', $rt->component_wise('sin', $newTheta), $rt->component_wise('cos', $newPhi), 1, 'float'), $rt->binary('*', $rt->component_wise('sin', $newTheta), $rt->component_wise('sin', $newPhi), 1, 'float'), $rt->component_wise('cos', $newTheta)), 3, 'float'))};
            @{$z} = map { $rt->f32($_) } @{($rt->binary('+', $z, $pos, 3, 'float'))};
            $iter = $rt->binary('+', $iter, $rt->f(1), 1, 'float');
        }
        $result->[0] = $rt->binary('/', $rt->binary('*', $rt->binary('*', $rt->f(0.5), $rt->component_wise('log', $r), 1, 'float'), $r, 1, 'float'), $dr, 1, 'float');
        $result->[1] = $trap;
        $result->[2] = $rt->binary('/', $iter, $rt->construct(1, $maxIter), 1, 'float');
        return $result;
    };
    $boxFold__vec3_float = sub {
        my ($z, $foldLimit) = @_;
        $z = $rt->copy($z, 'float');
        return $rt->binary('-', $rt->binary('*', $rt->component_wise('clamp', $z, $rt->unary('-', $foldLimit), $foldLimit), $rt->f(2), 3, 'float'), $z, 3, 'float');
    };
    $mandelbox__vec3_float_int_float = sub {
        my ($pos, $scale, $maxIter, $bail) = @_;
        $pos = $rt->copy($pos, 'float');
        my ($_for1_first, $dr, $factor, $fixedRadius2, $foldLimit, $i, $iter, $minRadius2, $planeTrap, $r, $r2, $result, $trap, $z);
        $result = [$rt->f(0.0), $rt->f(0.0), $rt->f(0.0)];
        $z = $pos;
        $dr = $rt->f(1);
        $trap = $rt->f(10000000000);
        $iter = $rt->f(0);
        $foldLimit = $rt->f(1);
        $minRadius2 = $rt->f(0.25);
        $fixedRadius2 = $rt->f(1);
        $i = $rt->i(0);
        $_for1_first = 1;
        for my $_for1 (0 .. 1048575) {
            if (!$_for1_first) {
                $i = $rt->binary('+', $i, $rt->i(1), 1, 'int');
            }
            $_for1_first = 0;
            if (!($rt->binary('<', $i, $maxIter))) {
                last;
            }
            @{$z} = map { $rt->f32($_) } @{($boxFold__vec3_float->($z, $foldLimit))};
            $r2 = $rt->dot($z, $z);
            $factor = $rt->f(0.0);
            if ($rt->binary('<', $r2, $minRadius2)) {
                $factor = $rt->binary('/', $fixedRadius2, $minRadius2, 1, 'float');
                @{$z} = map { $rt->f32($_) } @{($rt->binary('*', $z, $factor, 3, 'float'))};
                $dr = $rt->binary('*', $dr, $factor, 1, 'float');
            } else {
                if ($rt->binary('<', $r2, $fixedRadius2)) {
                    $factor = $rt->binary('/', $fixedRadius2, $r2, 1, 'float');
                    @{$z} = map { $rt->f32($_) } @{($rt->binary('*', $z, $factor, 3, 'float'))};
                    $dr = $rt->binary('*', $dr, $factor, 1, 'float');
                }
            }
            @{$z} = map { $rt->f32($_) } @{($rt->binary('+', $rt->binary('*', $z, $scale, 3, 'float'), $pos, 3, 'float'))};
            $dr = $rt->binary('+', $rt->binary('*', $dr, $rt->component_wise('abs', $scale), 1, 'float'), $rt->f(1), 1, 'float');
            $planeTrap = $rt->component_wise('min', $rt->component_wise('min', $rt->component_wise('abs', $rt->swizzle($z, 'x')), $rt->component_wise('abs', $rt->swizzle($z, 'y'))), $rt->component_wise('abs', $rt->swizzle($z, 'z')));
            $trap = $rt->component_wise('min', $trap, $planeTrap);
            $iter = $rt->binary('+', $iter, $rt->f(1), 1, 'float');
            if ($rt->binary('>', $rt->length($z), $bail)) {
                last;
            }
        }
        $r = $rt->length($z);
        $result->[0] = $rt->binary('/', $r, $rt->component_wise('abs', $dr), 1, 'float');
        $result->[1] = $trap;
        $result->[2] = $rt->binary('/', $iter, $rt->construct(1, $maxIter), 1, 'float');
        return $result;
    };
    $computeFractal__vec3 = sub {
        my ($p) = @_;
        $p = $rt->copy($p, 'float');
        if ($rt->binary('==', $_u_noiseType, $rt->i(0))) {
            return $mandelbulb__vec3_float_int_float->($p, $_u_power, $_u_iterations, $_u_bailout);
        } else {
            return $mandelbox__vec3_float_int_float->($p, $_u_power, $_u_iterations, $_u_bailout);
        }
    };
    $computeGradient__vec3_float = sub {
        my ($p, $eps) = @_;
        $p = $rt->copy($p, 'float');
        my ($d0, $dx, $dy, $dz);
        $d0 = $computeFractal__vec3->($p)->[0];
        $dx = $computeFractal__vec3->($rt->binary('+', $p, $rt->construct(3, $eps, $rt->f(0), $rt->f(0)), 3, 'float'))->[0];
        $dy = $computeFractal__vec3->($rt->binary('+', $p, $rt->construct(3, $rt->f(0), $eps, $rt->f(0)), 3, 'float'))->[0];
        $dz = $computeFractal__vec3->($rt->binary('+', $p, $rt->construct(3, $rt->f(0), $rt->f(0), $eps), 3, 'float'))->[0];
        return $rt->binary('/', $rt->construct(3, $rt->binary('-', $dx, $d0, 1, 'float'), $rt->binary('-', $dy, $d0, 1, 'float'), $rt->binary('-', $dz, $d0, 1, 'float')), $eps, 3, 'float');
    };
    $applyCollisionAvoidance__vec3 = sub {
        my ($pos) = @_;
        $pos = $rt->copy($pos, 'float');
        my ($fr, $grad, $pushDir, $pushDist);
        $fr = $computeFractal__vec3->($pos);
        $grad = $rt->construct(3, 0.0);
        $pushDir = $rt->construct(3, 0.0);
        $pushDist = $rt->f(0.0);
        if ($rt->binary('<', $fr->[0], $g->{SAFETY_RADIUS})) {
            $grad = $computeGradient__vec3_float->($pos, $rt->f(0.01));
            $pushDir = $rt->normalize($rt->binary('+', $grad, $rt->construct(3, $rt->f(9.9999999999999995e-07)), 3, 'float'));
            $pushDist = $rt->binary('-', $g->{SAFETY_RADIUS}, $fr->[0], 1, 'float');
            @{$pos} = map { $rt->f32($_) } @{($rt->binary('+', $pos, $rt->binary('*', $rt->binary('*', $pushDir, $pushDist, 3, 'float'), $rt->f(1.5), 3, 'float'), 3, 'float'))};
        }
        return $pos;
    };
    $main__void = sub {
        my ($camDir, $camPos, $camRight, $camUp, $dist, $eps, $fr, $globalCoord, $gradient, $halfExtent, $iterRatio, $normal, $normalizedCoord, $normalizedDist, $pixelCoord, $trap, $voiOffset, $volSize, $volSizeF, $vx, $vy, $vz, $worldPos);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $volSize = $_u_volumeSize;
        $volSizeF = $rt->construct(1, $volSize);
        $pixelCoord = $rt->construct(2, $rt->swizzle($ctx->{frag_coord}, 'xy'), 'int');
        $vx = $rt->swizzle($pixelCoord, 'x');
        $vy = $rt->binary('%', $rt->swizzle($pixelCoord, 'y'), $volSize, 1, 'int');
        $vz = $rt->binary('/', $rt->swizzle($pixelCoord, 'y'), $volSize, 1, 'int');
        if ((((($rt->binary('>=', $vx, $volSize)) || ($rt->binary('>=', $vy, $volSize)) ? 1 : 0)) || ($rt->binary('>=', $vz, $volSize)) ? 1 : 0)) {
            @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $rt->f(0)))};
            @{$g->{geoOut}} = map { $rt->f32($_) } @{($rt->construct(4, $rt->f(0.5), $rt->f(0.5), $rt->f(0.5), $rt->f(0)))};
            return;
        }
        $camPos = $rt->construct(3, 0.0);
        $camDir = $rt->construct(3, 0.0);
        $camUp = $rt->construct(3, 0.0);
        do { ($_retc, $camPos, $camDir, $camUp) = $getCameraState__float_vec3_vec3_vec3->($_u_time, $camPos, $camDir, $camUp); };
        @{$camPos} = map { $rt->f32($_) } @{($applyCollisionAvoidance__vec3->($camPos))};
        $camRight = $rt->normalize($rt->cross($camDir, $camUp));
        @{$camUp} = map { $rt->f32($_) } @{($rt->normalize($rt->cross($camRight, $camDir)))};
        $normalizedCoord = $rt->binary('-', $rt->binary('*', $rt->binary('/', $rt->construct(3, $rt->construct(1, $vx), $rt->construct(1, $vy), $rt->construct(1, $vz)), $rt->binary('-', $volSizeF, $rt->f(1), 1, 'float'), 3, 'float'), $rt->f(2), 3, 'float'), $rt->f(1), 3, 'float');
        $halfExtent = $rt->binary('*', $_u_voiSize, $rt->f(0.5), 1, 'float');
        $voiOffset = $rt->binary('*', $camDir, $halfExtent, 3, 'float');
        $worldPos = $rt->binary('+', $rt->binary('+', $rt->binary('+', $rt->binary('+', $camPos, $voiOffset, 3, 'float'), $rt->binary('*', $rt->binary('*', $camRight, $rt->swizzle($normalizedCoord, 'x'), 3, 'float'), $halfExtent, 3, 'float'), 3, 'float'), $rt->binary('*', $rt->binary('*', $camUp, $rt->swizzle($normalizedCoord, 'y'), 3, 'float'), $halfExtent, 3, 'float'), 3, 'float'), $rt->binary('*', $rt->binary('*', $camDir, $rt->swizzle($normalizedCoord, 'z'), 3, 'float'), $halfExtent, 3, 'float'), 3, 'float');
        $fr = $computeFractal__vec3->($worldPos);
        $dist = $fr->[0];
        $normalizedDist = $rt->binary('-', $rt->f(1), $rt->component_wise('clamp', $rt->binary('+', $rt->binary('*', $dist, $rt->f(2), 1, 'float'), $rt->f(0.5), 1, 'float'), $rt->f(0), $rt->f(1)), 1, 'float');
        $trap = $rt->component_wise('clamp', $rt->binary('*', $fr->[1], $rt->f(0.5), 1, 'float'), $rt->f(0), $rt->f(1));
        $iterRatio = $fr->[2];
        $eps = $rt->f(0.02);
        $gradient = $computeGradient__vec3_float->($worldPos, $eps);
        $normal = $rt->normalize($rt->binary('+', $gradient, $rt->construct(3, $rt->f(9.9999999999999995e-07)), 3, 'float'));
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $normalizedDist, $trap, $iterRatio, $rt->f(1)))};
        @{$g->{geoOut}} = map { $rt->f32($_) } @{($rt->construct(4, $rt->binary('+', $rt->binary('*', $normal, $rt->f(0.5), 3, 'float'), $rt->f(0.5), 3, 'float'), $normalizedDist))};
    };
    $main__void->();
    my $_c0 = $g->{fragColor};
    $out->[0] = $rt->f32($_c0->[0]); $out->[1] = $rt->f32($_c0->[1]); $out->[2] = $rt->f32($_c0->[2]); $out->[3] = $rt->f32($_c0->[3]);
    my $_c1 = $g->{geoOut};
    $out->[4] = $rt->f32($_c1->[0]); $out->[5] = $rt->f32($_c1->[1]); $out->[6] = $rt->f32($_c1->[2]); $out->[7] = $rt->f32($_c1->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0, output_names => ['fragColor', 'geoOut'] };
