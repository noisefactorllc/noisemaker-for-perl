# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($srgbToLinear__vec3, $linearToSrgb__vec3, $applyVibrance__vec3_float, $applyFadedFilm__vec3_float, $applySplitTone__vec3_vec3_vec3_float, $main__void);
    my $_retc;
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_vibrance = exists $U->{'vibrance'} ? $U->{'vibrance'} : $rt->f(0.0);
    my $_u_fadedFilm = exists $U->{'fadedFilm'} ? $U->{'fadedFilm'} : $rt->f(0.0);
    my $_u_shadowTint = exists $U->{'shadowTint'} ? $U->{'shadowTint'} : $rt->construct(3, 0.0);
    my $_u_highlightTint = exists $U->{'highlightTint'} ? $U->{'highlightTint'} : $rt->construct(3, 0.0);
    my $_u_splitToneBalance = exists $U->{'splitToneBalance'} ? $U->{'splitToneBalance'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $g->{LUMA_WEIGHTS} = $rt->construct(3, $rt->f(0.21260000000000001), $rt->f(0.71519999999999995), $rt->f(0.0722));
    $srgbToLinear__vec3 = sub {
        my ($srgb) = @_;
        $srgb = $rt->copy($srgb, 'float');
        my ($_for0_first, $i, $linear);
        $linear = $rt->construct(3, 0.0);
        $i = $rt->i(0);
        $_for0_first = 1;
        for my $_for0 (0 .. 1048575) {
            if (!$_for0_first) {
                $i = $rt->binary('+', $i, $rt->i(1), 1, 'int');
            }
            $_for0_first = 0;
            if (!($rt->binary('<', $i, $rt->i(3)))) {
                last;
            }
            if ($rt->binary('<=', $srgb->[int($i)], $rt->f(0.04045))) {
                $linear->[int($i)] = $rt->binary('/', $srgb->[int($i)], $rt->f(12.92), 1, 'float');
            } else {
                $linear->[int($i)] = $rt->component_wise('pow', $rt->binary('/', $rt->binary('+', $srgb->[int($i)], $rt->f(0.055), 1, 'float'), $rt->f(1.0549999999999999), 1, 'float'), $rt->f(2.3999999999999999));
            }
        }
        return $linear;
    };
    $linearToSrgb__vec3 = sub {
        my ($linear) = @_;
        $linear = $rt->copy($linear, 'float');
        my ($_for1_first, $i, $srgb);
        $srgb = $rt->construct(3, 0.0);
        $i = $rt->i(0);
        $_for1_first = 1;
        for my $_for1 (0 .. 1048575) {
            if (!$_for1_first) {
                $i = $rt->binary('+', $i, $rt->i(1), 1, 'int');
            }
            $_for1_first = 0;
            if (!($rt->binary('<', $i, $rt->i(3)))) {
                last;
            }
            if ($rt->binary('<=', $linear->[int($i)], $rt->f(0.0031308))) {
                $srgb->[int($i)] = $rt->binary('*', $linear->[int($i)], $rt->f(12.92), 1, 'float');
            } else {
                $srgb->[int($i)] = $rt->binary('-', $rt->binary('*', $rt->f(1.0549999999999999), $rt->component_wise('pow', $linear->[int($i)], $rt->binary('/', $rt->f(1), $rt->f(2.3999999999999999), 1, 'float')), 1, 'float'), $rt->f(0.055), 1, 'float');
            }
        }
        return $srgb;
    };
    $applyVibrance__vec3_float = sub {
        my ($rgb, $vibrance) = @_;
        $rgb = $rt->copy($rgb, 'float');
        my ($chroma, $finalGain, $hueScore, $luma, $maxC, $minC, $sat, $skinFactor, $vibranceGain);
        if ($rt->binary('<', $rt->component_wise('abs', $vibrance), $rt->f(0.001))) {
            return $rgb;
        }
        $luma = $rt->dot($rgb, $g->{LUMA_WEIGHTS});
        $chroma = $rt->binary('-', $rgb, $luma, 3, 'float');
        $maxC = $rt->component_wise('max', $rt->component_wise('max', $rt->swizzle($rgb, 'r'), $rt->swizzle($rgb, 'g')), $rt->swizzle($rgb, 'b'));
        $minC = $rt->component_wise('min', $rt->component_wise('min', $rt->swizzle($rgb, 'r'), $rt->swizzle($rgb, 'g')), $rt->swizzle($rgb, 'b'));
        $sat = (($rt->binary('>', $maxC, $rt->f(0.001))) ? ($rt->binary('/', $rt->binary('-', $maxC, $minC, 1, 'float'), $maxC, 1, 'float')) : ($rt->f(0)));
        $vibranceGain = $rt->binary('+', $rt->f(1), $rt->binary('*', $vibrance, $rt->binary('-', $rt->f(1), $sat, 1, 'float'), 1, 'float'), 1, 'float');
        $skinFactor = $rt->f(1);
        $hueScore = $rt->f(0.0);
        if ((($rt->binary('>', $rt->swizzle($rgb, 'r'), $rt->swizzle($rgb, 'g'))) && ($rt->binary('>', $rt->swizzle($rgb, 'g'), $rt->swizzle($rgb, 'b'))) ? 1 : 0)) {
            $hueScore = $rt->binary('/', $rt->binary('-', $rt->swizzle($rgb, 'r'), $rt->swizzle($rgb, 'b'), 1, 'float'), $rt->binary('+', $rt->binary('-', $maxC, $minC, 1, 'float'), $rt->f(0.001), 1, 'float'), 1, 'float');
            $skinFactor = $rt->binary('+', $rt->binary('*', $rt->component_wise('smoothstep', $rt->f(0.29999999999999999), $rt->f(0.69999999999999996), $sat), $rt->f(0.5), 1, 'float'), $rt->f(0.5), 1, 'float');
        }
        $finalGain = $rt->component_wise('mix', $rt->f(1), $vibranceGain, $skinFactor);
        return $rt->binary('+', $luma, $rt->binary('*', $chroma, $finalGain, 3, 'float'), 3, 'float');
    };
    $applyFadedFilm__vec3_float = sub {
        my ($rgb, $amount) = @_;
        $rgb = $rt->copy($rgb, 'float');
        my ($chroma, $contrastFactor, $lifted, $luma, $newLuma, $pivot);
        if ($rt->binary('<', $amount, $rt->f(0.001))) {
            return $rgb;
        }
        $lifted = $rt->component_wise('mix', $rgb, $rt->construct(3, $rt->f(0.20000000000000001)), $rt->binary('*', $amount, $rt->f(0.5), 1, 'float'));
        $luma = $rt->dot($lifted, $g->{LUMA_WEIGHTS});
        $chroma = $rt->binary('-', $lifted, $luma, 3, 'float');
        $pivot = $rt->f(0.5);
        $contrastFactor = $rt->binary('-', $rt->f(1), $rt->binary('*', $amount, $rt->f(0.29999999999999999), 1, 'float'), 1, 'float');
        $newLuma = $rt->binary('+', $rt->binary('*', $rt->binary('-', $luma, $pivot, 1, 'float'), $contrastFactor, 1, 'float'), $pivot, 1, 'float');
        return $rt->binary('+', $newLuma, $rt->binary('*', $chroma, $rt->binary('-', $rt->f(1), $rt->binary('*', $amount, $rt->f(0.20000000000000001), 1, 'float'), 1, 'float'), 3, 'float'), 3, 'float');
    };
    $applySplitTone__vec3_vec3_vec3_float = sub {
        my ($rgb, $shadowTint, $highlightTint, $balance) = @_;
        $rgb = $rt->copy($rgb, 'float');
        $shadowTint = $rt->copy($shadowTint, 'float');
        $highlightTint = $rt->copy($highlightTint, 'float');
        my ($balancePoint, $highlightShift, $highlightWeight, $luma, $shadowShift, $shadowWeight, $tintedRgb);
        $shadowShift = $rt->binary('*', $rt->binary('-', $shadowTint, $rt->f(0.5), 3, 'float'), $rt->f(2), 3, 'float');
        $highlightShift = $rt->binary('*', $rt->binary('-', $highlightTint, $rt->f(0.5), 3, 'float'), $rt->f(2), 3, 'float');
        if ((($rt->binary('<', $rt->length($shadowShift), $rt->f(0.01))) && ($rt->binary('<', $rt->length($highlightShift), $rt->f(0.01))) ? 1 : 0)) {
            return $rgb;
        }
        $luma = $rt->dot($rgb, $g->{LUMA_WEIGHTS});
        $balancePoint = $rt->binary('+', $rt->f(0.5), $rt->binary('*', $balance, $rt->f(0.29999999999999999), 1, 'float'), 1, 'float');
        $shadowWeight = $rt->binary('-', $rt->f(1), $rt->component_wise('smoothstep', $rt->f(0), $balancePoint, $luma), 1, 'float');
        $highlightWeight = $rt->component_wise('smoothstep', $balancePoint, $rt->f(1), $luma);
        $tintedRgb = $rgb;
        @{$tintedRgb} = map { $rt->f32($_) } @{($rt->binary('+', $tintedRgb, $rt->binary('*', $rt->binary('*', $shadowShift, $shadowWeight, 3, 'float'), $rt->f(0.29999999999999999), 3, 'float'), 3, 'float'))};
        @{$tintedRgb} = map { $rt->f32($_) } @{($rt->binary('+', $tintedRgb, $rt->binary('*', $rt->binary('*', $highlightShift, $highlightWeight, 3, 'float'), $rt->f(0.29999999999999999), 3, 'float'), 3, 'float'))};
        return $tintedRgb;
    };
    $main__void = sub {
        my ($color, $coord, $globalCoord, $rgb);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $coord = $rt->construct(2, $rt->swizzle($ctx->{frag_coord}, 'xy'), 'int');
        $color = $rt->texel_fetch($_u_inputTex, $coord, $rt->i(0));
        $rgb = $srgbToLinear__vec3->($rt->swizzle($color, 'rgb'));
        @{$rgb} = map { $rt->f32($_) } @{($applyVibrance__vec3_float->($rgb, $_u_vibrance))};
        @{$rgb} = map { $rt->f32($_) } @{($applyFadedFilm__vec3_float->($rgb, $_u_fadedFilm))};
        @{$rgb} = map { $rt->f32($_) } @{($applySplitTone__vec3_vec3_vec3_float->($rgb, $_u_shadowTint, $_u_highlightTint, $_u_splitToneBalance))};
        @{$rgb} = map { $rt->f32($_) } @{($linearToSrgb__vec3->($rt->component_wise('max', $rgb, $rt->construct(3, $rt->f(0)))))};
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $rgb, $rt->swizzle($color, 'a')))};
    };
    $main__void->();
    my $_c0 = $g->{fragColor};
    $out->[0] = $rt->f32($_c0->[0]); $out->[1] = $rt->f32($_c0->[1]); $out->[2] = $rt->f32($_c0->[2]); $out->[3] = $rt->f32($_c0->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0, output_names => ['fragColor'] };
