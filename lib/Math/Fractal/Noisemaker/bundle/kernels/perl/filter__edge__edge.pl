# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($getWeight__int_int_int, $applyBlend__vec4_vec4_int, $contourConv__vec2_vec2_vec3_float_bool_bool, $main__void);
    my $_retc;
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u__kernel = exists $U->{'kernel'} ? $U->{'kernel'} : $rt->f(0.0);
    my $_u_size = exists $U->{'size'} ? $U->{'size'} : $rt->f(0.0);
    my $_u_renderScale = exists $U->{'renderScale'} ? $U->{'renderScale'} : $rt->f(0.0);
    my $_u_blend = exists $U->{'blend'} ? $U->{'blend'} : $rt->f(0.0);
    my $_u_invert = exists $U->{'invert'} ? $U->{'invert'} : $rt->f(0.0);
    my $_u_channel = exists $U->{'channel'} ? $U->{'channel'} : $rt->f(0.0);
    my $_u_threshold = exists $U->{'threshold'} ? $U->{'threshold'} : $rt->f(0.0);
    my $_u_amount = exists $U->{'amount'} ? $U->{'amount'} : $rt->f(0.0);
    my $_u_mixAmt = exists $U->{'mixAmt'} ? $U->{'mixAmt'} : $rt->f(0.0);
    my $_u_level = exists $U->{'level'} ? $U->{'level'} : $rt->f(0.0);
    my $_u_contourSide = exists $U->{'contourSide'} ? $U->{'contourSide'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $g->{LUMA} = $rt->construct(3, $rt->f(0.21260000000000001), $rt->f(0.71519999999999995), $rt->f(0.0722));
    $getWeight__int_int_int = sub {
        my ($dx, $dy, $kernelType) = @_;
        if ((($rt->binary('==', $dx, $rt->i(0))) && ($rt->binary('==', $dy, $rt->i(0))) ? 1 : 0)) {
            return $rt->f(0);
        }
        if ($rt->binary('==', $kernelType, $rt->i(0))) {
            if ((($rt->binary('==', $dx, $rt->i(0))) || ($rt->binary('==', $dy, $rt->i(0))) ? 1 : 0)) {
                return $rt->unary('-', $rt->f(1));
            }
            return $rt->f(0);
        } else {
            return $rt->unary('-', $rt->f(1));
        }
    };
    $applyBlend__vec4_vec4_int = sub {
        my ($edge, $orig, $mode) = @_;
        $edge = $rt->copy($edge, 'float');
        $orig = $rt->copy($orig, 'float');
        my ($result);
        if ($rt->binary('==', $mode, $rt->i(0))) {
            return $rt->component_wise('min', $rt->binary('+', $orig, $edge, 4, 'float'), $rt->construct(4, $rt->f(1)));
        }
        if ($rt->binary('==', $mode, $rt->i(1))) {
            return $rt->component_wise('min', $orig, $edge);
        }
        if ($rt->binary('==', $mode, $rt->i(2))) {
            return $rt->component_wise('abs', $rt->binary('-', $orig, $edge, 4, 'float'));
        }
        if ($rt->binary('==', $mode, $rt->i(3))) {
            return $rt->component_wise('min', $rt->binary('/', $orig, $rt->component_wise('max', $rt->binary('-', $rt->f(1), $edge, 4, 'float'), $rt->construct(4, $rt->f(0.001))), 4, 'float'), $rt->construct(4, $rt->f(1)));
        }
        if ($rt->binary('==', $mode, $rt->i(4))) {
            return $rt->component_wise('max', $orig, $edge);
        }
        if ($rt->binary('==', $mode, $rt->i(5))) {
            return $rt->binary('*', $orig, $edge, 4, 'float');
        }
        $result = $rt->construct(4, 0.0);
        if ($rt->binary('==', $mode, $rt->i(7))) {
            $result = $rt->construct(4, 0.0);
            $result = $rt->assign_swizzle($result, 'r', (($rt->binary('<', $rt->swizzle($orig, 'r'), $rt->f(0.5))) ? ($rt->binary('*', $rt->binary('*', $rt->f(2), $rt->swizzle($orig, 'r'), 1, 'float'), $rt->swizzle($edge, 'r'), 1, 'float')) : ($rt->binary('-', $rt->f(1), $rt->binary('*', $rt->binary('*', $rt->f(2), $rt->binary('-', $rt->f(1), $rt->swizzle($orig, 'r'), 1, 'float'), 1, 'float'), $rt->binary('-', $rt->f(1), $rt->swizzle($edge, 'r'), 1, 'float'), 1, 'float'), 1, 'float'))));
            $result = $rt->assign_swizzle($result, 'g', (($rt->binary('<', $rt->swizzle($orig, 'g'), $rt->f(0.5))) ? ($rt->binary('*', $rt->binary('*', $rt->f(2), $rt->swizzle($orig, 'g'), 1, 'float'), $rt->swizzle($edge, 'g'), 1, 'float')) : ($rt->binary('-', $rt->f(1), $rt->binary('*', $rt->binary('*', $rt->f(2), $rt->binary('-', $rt->f(1), $rt->swizzle($orig, 'g'), 1, 'float'), 1, 'float'), $rt->binary('-', $rt->f(1), $rt->swizzle($edge, 'g'), 1, 'float'), 1, 'float'), 1, 'float'))));
            $result = $rt->assign_swizzle($result, 'b', (($rt->binary('<', $rt->swizzle($orig, 'b'), $rt->f(0.5))) ? ($rt->binary('*', $rt->binary('*', $rt->f(2), $rt->swizzle($orig, 'b'), 1, 'float'), $rt->swizzle($edge, 'b'), 1, 'float')) : ($rt->binary('-', $rt->f(1), $rt->binary('*', $rt->binary('*', $rt->f(2), $rt->binary('-', $rt->f(1), $rt->swizzle($orig, 'b'), 1, 'float'), 1, 'float'), $rt->binary('-', $rt->f(1), $rt->swizzle($edge, 'b'), 1, 'float'), 1, 'float'), 1, 'float'))));
            $result = $rt->assign_swizzle($result, 'a', $rt->swizzle($orig, 'a'));
            return $result;
        }
        if ($rt->binary('==', $mode, $rt->i(8))) {
            return $rt->binary('-', $rt->f(1), $rt->binary('*', $rt->binary('-', $rt->f(1), $orig, 4, 'float'), $rt->binary('-', $rt->f(1), $edge, 4, 'float'), 4, 'float'), 4, 'float');
        }
        return $edge;
    };
    $contourConv__vec2_vec2_vec3_float_bool_bool = sub {
        my ($fragCoord, $texelSize, $centerRGB, $lvl, $useLuma, $upperSide) = @_;
        $fragCoord = $rt->copy($fragCoord, 'float');
        $texelSize = $rt->copy($texelSize, 'float');
        $centerRGB = $rt->copy($centerRGB, 'float');
        my ($centerL, $centerOnSide, $crossing, $eastRGB, $northRGB, $southRGB, $westRGB);
        $northRGB = $rt->swizzle($rt->texture($_u_inputTex, $rt->binary('*', $rt->binary('+', $fragCoord, $rt->construct(2, $rt->f(0), $rt->f(1)), 2, 'float'), $texelSize, 2, 'float')), 'rgb');
        $southRGB = $rt->swizzle($rt->texture($_u_inputTex, $rt->binary('*', $rt->binary('+', $fragCoord, $rt->construct(2, $rt->f(0), $rt->unary('-', $rt->f(1))), 2, 'float'), $texelSize, 2, 'float')), 'rgb');
        $eastRGB = $rt->swizzle($rt->texture($_u_inputTex, $rt->binary('*', $rt->binary('+', $fragCoord, $rt->construct(2, $rt->f(1), $rt->f(0)), 2, 'float'), $texelSize, 2, 'float')), 'rgb');
        $westRGB = $rt->swizzle($rt->texture($_u_inputTex, $rt->binary('*', $rt->binary('+', $fragCoord, $rt->construct(2, $rt->unary('-', $rt->f(1)), $rt->f(0)), 2, 'float'), $texelSize, 2, 'float')), 'rgb');
        $centerL = $rt->f(0.0);
        $centerOnSide = 0;
        $crossing = 0;
        if ($useLuma) {
            $centerL = $rt->dot($centerRGB, $g->{LUMA});
            $centerOnSide = (($upperSide) ? ($rt->binary('>=', $centerL, $lvl)) : ($rt->binary('<', $centerL, $lvl)));
            $crossing = (($centerOnSide) && ((($upperSide) ? ((((((($rt->binary('<', $rt->dot($northRGB, $g->{LUMA}), $lvl)) || ($rt->binary('<', $rt->dot($southRGB, $g->{LUMA}), $lvl)) ? 1 : 0)) || ($rt->binary('<', $rt->dot($eastRGB, $g->{LUMA}), $lvl)) ? 1 : 0)) || ($rt->binary('<', $rt->dot($westRGB, $g->{LUMA}), $lvl)) ? 1 : 0)) : ((((((($rt->binary('>=', $rt->dot($northRGB, $g->{LUMA}), $lvl)) || ($rt->binary('>=', $rt->dot($southRGB, $g->{LUMA}), $lvl)) ? 1 : 0)) || ($rt->binary('>=', $rt->dot($eastRGB, $g->{LUMA}), $lvl)) ? 1 : 0)) || ($rt->binary('>=', $rt->dot($westRGB, $g->{LUMA}), $lvl)) ? 1 : 0)))) ? 1 : 0);
            return $rt->construct(3, (($crossing) ? ($rt->f(0)) : ($rt->f(1))));
        }
        $centerOnSide = (($upperSide) ? ($rt->component_wise('greaterThanEqual', $centerRGB, $rt->construct(3, $lvl))) : ($rt->component_wise('lessThan', $centerRGB, $rt->construct(3, $lvl))));
        $crossing = $rt->construct(3, (($rt->swizzle($centerOnSide, 'r')) && ((($upperSide) ? ((((((($rt->binary('<', $rt->swizzle($northRGB, 'r'), $lvl)) || ($rt->binary('<', $rt->swizzle($southRGB, 'r'), $lvl)) ? 1 : 0)) || ($rt->binary('<', $rt->swizzle($eastRGB, 'r'), $lvl)) ? 1 : 0)) || ($rt->binary('<', $rt->swizzle($westRGB, 'r'), $lvl)) ? 1 : 0)) : ((((((($rt->binary('>=', $rt->swizzle($northRGB, 'r'), $lvl)) || ($rt->binary('>=', $rt->swizzle($southRGB, 'r'), $lvl)) ? 1 : 0)) || ($rt->binary('>=', $rt->swizzle($eastRGB, 'r'), $lvl)) ? 1 : 0)) || ($rt->binary('>=', $rt->swizzle($westRGB, 'r'), $lvl)) ? 1 : 0)))) ? 1 : 0), (($rt->swizzle($centerOnSide, 'g')) && ((($upperSide) ? ((((((($rt->binary('<', $rt->swizzle($northRGB, 'g'), $lvl)) || ($rt->binary('<', $rt->swizzle($southRGB, 'g'), $lvl)) ? 1 : 0)) || ($rt->binary('<', $rt->swizzle($eastRGB, 'g'), $lvl)) ? 1 : 0)) || ($rt->binary('<', $rt->swizzle($westRGB, 'g'), $lvl)) ? 1 : 0)) : ((((((($rt->binary('>=', $rt->swizzle($northRGB, 'g'), $lvl)) || ($rt->binary('>=', $rt->swizzle($southRGB, 'g'), $lvl)) ? 1 : 0)) || ($rt->binary('>=', $rt->swizzle($eastRGB, 'g'), $lvl)) ? 1 : 0)) || ($rt->binary('>=', $rt->swizzle($westRGB, 'g'), $lvl)) ? 1 : 0)))) ? 1 : 0), (($rt->swizzle($centerOnSide, 'b')) && ((($upperSide) ? ((((((($rt->binary('<', $rt->swizzle($northRGB, 'b'), $lvl)) || ($rt->binary('<', $rt->swizzle($southRGB, 'b'), $lvl)) ? 1 : 0)) || ($rt->binary('<', $rt->swizzle($eastRGB, 'b'), $lvl)) ? 1 : 0)) || ($rt->binary('<', $rt->swizzle($westRGB, 'b'), $lvl)) ? 1 : 0)) : ((((((($rt->binary('>=', $rt->swizzle($northRGB, 'b'), $lvl)) || ($rt->binary('>=', $rt->swizzle($southRGB, 'b'), $lvl)) ? 1 : 0)) || ($rt->binary('>=', $rt->swizzle($eastRGB, 'b'), $lvl)) ? 1 : 0)) || ($rt->binary('>=', $rt->swizzle($westRGB, 'b'), $lvl)) ? 1 : 0)))) ? 1 : 0));
        return $rt->construct(3, (($rt->swizzle($crossing, 'r')) ? ($rt->f(0)) : ($rt->f(1))), (($rt->swizzle($crossing, 'g')) ? ($rt->f(0)) : ($rt->f(1))), (($rt->swizzle($crossing, 'b')) ? ($rt->f(0)) : ($rt->f(1))));
    };
    $main__void = sub {
        my ($_for0_first, $_for1_first, $blendMode, $blended, $centerSample, $centerWeight, $conv, $doInvert, $dx, $dy, $edge, $edgeColor, $kernelType, $localUV, $m, $mask, $origColor, $radius, $resolution, $s, $sampleCoord, $texSize, $texelSize, $thresh, $useLuma, $w);
        $texSize = $rt->texture_size($_u_inputTex);
        $resolution = $rt->construct(2, $texSize);
        $texelSize = $rt->binary('/', $rt->f(1), $resolution, 2, 'float');
        $origColor = $rt->texture($_u_inputTex, $rt->binary('*', $rt->swizzle($ctx->{frag_coord}, 'xy'), $texelSize, 2, 'float'));
        $kernelType = $rt->construct(1, $_u__kernel, 'int');
        $radius = $rt->component_wise('min', $rt->construct(1, $rt->binary('*', $rt->binary('+', $_u_size, $rt->f(1), 1, 'float'), $_u_renderScale, 1, 'float'), 'int'), $rt->i(256));
        $blendMode = $rt->construct(1, $_u_blend, 'int');
        $doInvert = $rt->binary('>', $_u_invert, $rt->f(0.5));
        $useLuma = $rt->binary('>', $_u_channel, $rt->f(0.5));
        $conv = $rt->construct(3, $rt->f(0));
        $centerWeight = $rt->f(0);
        $centerSample = $rt->construct(3, 0.0);
        if ($rt->binary('==', $kernelType, $rt->i(2))) {
            @{$conv} = map { $rt->f32($_) } @{($contourConv__vec2_vec2_vec3_float_bool_bool->($rt->swizzle($ctx->{frag_coord}, 'xy'), $texelSize, $rt->swizzle($origColor, 'rgb'), $rt->binary('/', $_u_level, $rt->f(100), 1, 'float'), $useLuma, $rt->binary('>', $_u_contourSide, $rt->f(0.5))))};
        } else {
            $dy = $rt->unary('-', $rt->i(3));
            $_for0_first = 1;
            for my $_for0 (0 .. 1048575) {
                if (!$_for0_first) {
                    $dy = $rt->binary('+', $dy, $rt->i(1), 1, 'int');
                }
                $_for0_first = 0;
                if (!($rt->binary('<=', $dy, $rt->i(3)))) {
                    last;
                }
                $dx = $rt->unary('-', $rt->i(3));
                $_for1_first = 1;
                for my $_for1 (0 .. 1048575) {
                    if (!$_for1_first) {
                        $dx = $rt->binary('+', $dx, $rt->i(1), 1, 'int');
                    }
                    $_for1_first = 0;
                    if (!($rt->binary('<=', $dx, $rt->i(3)))) {
                        last;
                    }
                    if ((($rt->binary('>', $rt->component_wise('abs', $dx), $radius)) || ($rt->binary('>', $rt->component_wise('abs', $dy), $radius)) ? 1 : 0)) {
                        next;
                    }
                    if ((($rt->binary('==', $dx, $rt->i(0))) && ($rt->binary('==', $dy, $rt->i(0))) ? 1 : 0)) {
                        next;
                    }
                    $w = $getWeight__int_int_int->($dx, $dy, $kernelType);
                    if ($rt->binary('==', $w, $rt->f(0))) {
                        next;
                    }
                    $sampleCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $rt->construct(2, $rt->construct(1, $dx), $rt->construct(1, $dy)), 2, 'float');
                    $localUV = $rt->binary('*', $sampleCoord, $texelSize, 2, 'float');
                    $s = $rt->swizzle($rt->texture($_u_inputTex, $localUV), 'rgb');
                    if ($useLuma) {
                        @{$conv} = map { $rt->f32($_) } @{($rt->binary('+', $conv, $rt->binary('*', $rt->construct(3, $rt->dot($s, $g->{LUMA})), $w, 3, 'float'), 3, 'float'))};
                    } else {
                        @{$conv} = map { $rt->f32($_) } @{($rt->binary('+', $conv, $rt->binary('*', $s, $w, 3, 'float'), 3, 'float'))};
                    }
                    $centerWeight = $rt->binary('-', $centerWeight, $w, 1, 'float');
                }
            }
            $centerSample = $rt->swizzle($origColor, 'rgb');
            if ($useLuma) {
                @{$centerSample} = map { $rt->f32($_) } @{($rt->construct(3, $rt->dot($centerSample, $g->{LUMA})))};
            }
            @{$conv} = map { $rt->f32($_) } @{($rt->binary('+', $conv, $rt->binary('*', $centerSample, $centerWeight, 3, 'float'), 3, 'float'))};
        }
        @{$conv} = map { $rt->f32($_) } @{($rt->binary('*', $conv, $rt->binary('/', $_u_amount, $rt->f(50), 1, 'float'), 3, 'float'))};
        @{$conv} = map { $rt->f32($_) } @{($rt->component_wise('clamp', $conv, $rt->f(0), $rt->f(1)))};
        $edge = $rt->f(0.0);
        $mask = $rt->f(0.0);
        $thresh = $rt->f(0.0);
        if ($rt->binary('>', $_u_threshold, $rt->f(0))) {
            $thresh = $rt->binary('/', $_u_threshold, $rt->f(100), 1, 'float');
            $edge = $rt->f(0.0);
            if ($useLuma) {
                $edge = $rt->swizzle($conv, 'r');
            } else {
                $edge = $rt->dot($conv, $g->{LUMA});
            }
            $mask = $rt->component_wise('smoothstep', $rt->binary('-', $thresh, $rt->f(0.01), 1, 'float'), $rt->binary('+', $thresh, $rt->f(0.01), 1, 'float'), $edge);
            @{$conv} = map { $rt->f32($_) } @{($rt->binary('*', $conv, $mask, 3, 'float'))};
        }
        if ($doInvert) {
            @{$conv} = map { $rt->f32($_) } @{($rt->binary('-', $rt->f(1), $conv, 3, 'float'))};
        }
        $edgeColor = $rt->construct(4, $conv, $rt->swizzle($origColor, 'a'));
        $blended = $applyBlend__vec4_vec4_int->($edgeColor, $origColor, $blendMode);
        $m = $rt->binary('/', $_u_mixAmt, $rt->f(100), 1, 'float');
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $rt->component_wise('mix', $rt->swizzle($origColor, 'rgb'), $rt->swizzle($blended, 'rgb'), $m), $rt->swizzle($origColor, 'a')))};
    };
    $main__void->();
    my $_c0 = $g->{fragColor};
    $out->[0] = $rt->f32($_c0->[0]); $out->[1] = $rt->f32($_c0->[1]); $out->[2] = $rt->f32($_c0->[2]); $out->[3] = $rt->f32($_c0->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0, output_names => ['fragColor'] };
