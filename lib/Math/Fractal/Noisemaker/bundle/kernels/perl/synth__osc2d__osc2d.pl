# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($hash11__float_float, $tilingNoise1D__float_float_float, $periodicValue__float_float, $rotate2D__vec2_float, $oscSine__float, $oscLinear__float, $oscSawtooth__float, $oscSawtoothInv__float, $oscSquare__float, $main__void);
    my $_retc;
    my $_u_resolution = exists $U->{'resolution'} ? $U->{'resolution'} : $rt->construct(2, 0.0);
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_aspect = exists $U->{'aspect'} ? $U->{'aspect'} : $rt->f(0.0);
    my $_u_time = exists $U->{'time'} ? $U->{'time'} : $rt->f(0.0);
    my $_u_oscType = exists $U->{'oscType'} ? $U->{'oscType'} : 0;
    my $_u_frequency = exists $U->{'frequency'} ? $U->{'frequency'} : 0;
    my $_u_speed = exists $U->{'speed'} ? $U->{'speed'} : $rt->f(0.0);
    my $_u_rotation = exists $U->{'rotation'} ? $U->{'rotation'} : $rt->f(0.0);
    my $_u_seed = exists $U->{'seed'} ? $U->{'seed'} : 0;
    $g->{fragColor} = $rt->construct(4, 0.0);
    $g->{PI} = $rt->f(3.1415926535897931);
    $g->{TAU} = $rt->f(6.2831853071795862);
    $hash11__float_float = sub {
        my ($p, $s) = @_;
        $p = $rt->component_wise('fract', $rt->binary('+', $rt->binary('*', $p, $rt->f(234.34), 1, 'float'), $rt->binary('*', $s, $rt->f(0.71830000000000005), 1, 'float'), 1, 'float'));
        $p = $rt->binary('+', $p, $rt->binary('*', $p, $rt->binary('+', $p, $rt->f(34.229999999999997), 1, 'float'), 1, 'float'), 1, 'float');
        return $rt->component_wise('fract', $rt->binary('*', $p, $p, 1, 'float'));
    };
    $tilingNoise1D__float_float_float = sub {
        my ($x, $freq, $s) = @_;
        my ($a, $b, $f, $i, $i0, $i1, $p);
        $p = $rt->binary('*', $x, $freq, 1, 'float');
        $i = $rt->component_wise('floor', $p);
        $f = $rt->component_wise('fract', $p);
        $f = $rt->binary('*', $rt->binary('*', $f, $f, 1, 'float'), $rt->binary('-', $rt->f(3), $rt->binary('*', $rt->f(2), $f, 1, 'float'), 1, 'float'), 1, 'float');
        $i0 = $rt->component_wise('mod', $i, $freq);
        $i1 = $rt->component_wise('mod', $rt->binary('+', $i, $rt->f(1), 1, 'float'), $freq);
        $a = $hash11__float_float->($i0, $s);
        $b = $hash11__float_float->($i1, $s);
        return $rt->component_wise('mix', $a, $b, $f);
    };
    $periodicValue__float_float = sub {
        my ($t, $v) = @_;
        return $rt->binary('*', $rt->binary('+', $rt->component_wise('sin', $rt->binary('*', $rt->binary('-', $t, $v, 1, 'float'), $g->{TAU}, 1, 'float')), $rt->f(1), 1, 'float'), $rt->f(0.5), 1, 'float');
    };
    $rotate2D__vec2_float = sub {
        my ($p, $angle) = @_;
        $p = $rt->copy($p, 'float');
        my ($c, $s);
        $s = $rt->component_wise('sin', $angle);
        $c = $rt->component_wise('cos', $angle);
        return $rt->construct(2, $rt->binary('-', $rt->binary('*', $rt->swizzle($p, 'x'), $c, 1, 'float'), $rt->binary('*', $rt->swizzle($p, 'y'), $s, 1, 'float'), 1, 'float'), $rt->binary('+', $rt->binary('*', $rt->swizzle($p, 'x'), $s, 1, 'float'), $rt->binary('*', $rt->swizzle($p, 'y'), $c, 1, 'float'), 1, 'float'));
    };
    $oscSine__float = sub {
        my ($t) = @_;
        return $rt->component_wise('sin', $rt->binary('*', $rt->component_wise('fract', $t), $g->{PI}, 1, 'float'));
    };
    $oscLinear__float = sub {
        my ($t) = @_;
        $t = $rt->component_wise('fract', $t);
        return $rt->binary('-', $rt->f(1), $rt->component_wise('abs', $rt->binary('-', $rt->binary('*', $t, $rt->f(2), 1, 'float'), $rt->f(1), 1, 'float')), 1, 'float');
    };
    $oscSawtooth__float = sub {
        my ($t) = @_;
        return $rt->component_wise('fract', $t);
    };
    $oscSawtoothInv__float = sub {
        my ($t) = @_;
        return $rt->binary('-', $rt->f(1), $rt->component_wise('fract', $t), 1, 'float');
    };
    $oscSquare__float = sub {
        my ($t) = @_;
        return $rt->component_wise('step', $rt->f(0.5), $rt->component_wise('fract', $t));
    };
    $main__void = sub {
        my ($freq, $globalCoord, $res, $rotRad, $scaledTime, $scrollOffset, $scrolledPos, $spatialPhase, $spatialPos, $st, $t, $timeNoise, $timePhase, $val, $valueNoise);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $res = $_u_fullResolution;
        if ($rt->binary('<', $rt->swizzle($res, 'x'), $rt->f(1))) {
            @{$res} = map { $rt->f32($_) } @{($rt->construct(2, $rt->f(1024), $rt->f(1024)))};
        }
        $st = $rt->binary('/', $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float'), $res, 2, 'float');
        @{$st} = map { $rt->f32($_) } @{($rt->binary('-', $st, $rt->f(0.5), 2, 'float'))};
        $st = $rt->assign_swizzle($st, 'x', $rt->binary('*', $rt->swizzle($st, 'x'), $_u_aspect, 1, 'float'));
        $rotRad = $rt->binary('/', $rt->binary('*', $_u_rotation, $g->{PI}, 1, 'float'), $rt->f(180), 1, 'float');
        @{$st} = map { $rt->f32($_) } @{($rotate2D__vec2_float->($st, $rotRad))};
        $spatialPos = $rt->binary('+', $rt->swizzle($st, 'y'), $rt->f(0.5), 1, 'float');
        $freq = $rt->construct(1, $_u_frequency);
        $spatialPhase = $rt->binary('*', $rt->swizzle($st, 'y'), $freq, 1, 'float');
        $timePhase = $rt->binary('*', $_u_time, $_u_speed, 1, 'float');
        $t = $rt->binary('+', $spatialPhase, $timePhase, 1, 'float');
        $val = $rt->f(0.0);
        $scaledTime = $rt->f(0.0);
        $scrollOffset = $rt->f(0.0);
        $scrolledPos = $rt->f(0.0);
        $timeNoise = $rt->f(0.0);
        $valueNoise = $rt->f(0.0);
        if ($rt->binary('==', $_u_oscType, $rt->i(0))) {
            $val = $oscSine__float->($t);
        } else {
            if ($rt->binary('==', $_u_oscType, $rt->i(1))) {
                $val = $oscLinear__float->($t);
            } else {
                if ($rt->binary('==', $_u_oscType, $rt->i(2))) {
                    $val = $oscSawtooth__float->($t);
                } else {
                    if ($rt->binary('==', $_u_oscType, $rt->i(3))) {
                        $val = $oscSawtoothInv__float->($t);
                    } else {
                        if ($rt->binary('==', $_u_oscType, $rt->i(4))) {
                            $val = $oscSquare__float->($t);
                        } else {
                            if ($rt->binary('==', $_u_oscType, $rt->i(5))) {
                                $scrollOffset = $rt->component_wise('fract', $rt->binary('*', $_u_time, $_u_speed, 1, 'float'));
                                $scrolledPos = $rt->component_wise('fract', $rt->binary('+', $spatialPos, $scrollOffset, 1, 'float'));
                                $timeNoise = $tilingNoise1D__float_float_float->($scrolledPos, $freq, $rt->binary('+', $rt->construct(1, $_u_seed), $rt->f(12345), 1, 'float'));
                                $valueNoise = $tilingNoise1D__float_float_float->($scrolledPos, $freq, $rt->construct(1, $_u_seed));
                                $scaledTime = $rt->binary('*', $periodicValue__float_float->($rt->f(0), $timeNoise), $_u_speed, 1, 'float');
                                $val = $periodicValue__float_float->($scaledTime, $valueNoise);
                            } else {
                                $timeNoise = $tilingNoise1D__float_float_float->($spatialPos, $freq, $rt->binary('+', $rt->construct(1, $_u_seed), $rt->f(12345), 1, 'float'));
                                $valueNoise = $tilingNoise1D__float_float_float->($spatialPos, $freq, $rt->construct(1, $_u_seed));
                                $scaledTime = $rt->binary('*', $periodicValue__float_float->($_u_time, $timeNoise), $_u_speed, 1, 'float');
                                $val = $periodicValue__float_float->($scaledTime, $valueNoise);
                            }
                        }
                    }
                }
            }
        }
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $rt->construct(3, $val), $rt->f(1)))};
    };
    $main__void->();
    my $_c = $g->{fragColor};
    $out->[0] = $rt->f32($_c->[0]); $out->[1] = $rt->f32($_c->[1]); $out->[2] = $rt->f32($_c->[2]); $out->[3] = $rt->f32($_c->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0 };
