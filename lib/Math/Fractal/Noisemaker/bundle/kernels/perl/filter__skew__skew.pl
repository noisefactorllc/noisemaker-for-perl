# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($main__void);
    my $_retc;
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_skewAmt = exists $U->{'skewAmt'} ? $U->{'skewAmt'} : $rt->f(0.0);
    my $_u_rotation = exists $U->{'rotation'} ? $U->{'rotation'} : $rt->f(0.0);
    my $_u_wrap = exists $U->{'wrap'} ? $U->{'wrap'} : $rt->f(0.0);
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_renderScale = exists $U->{'renderScale'} ? $U->{'renderScale'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $g->{PI} = $rt->f(3.1415926535900001);
    $main__void = sub {
        my ($angle, $aspect, $c, $effectiveSkewAmt, $globalPixel, $globalUV, $localUV, $maxSkew, $resolution, $s, $st, $texSize, $wrapMode);
        $texSize = $rt->texture_size($_u_inputTex);
        $resolution = $rt->construct(2, $texSize);
        $globalPixel = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $globalUV = $rt->binary('/', $globalPixel, $_u_fullResolution, 2, 'float');
        $aspect = $rt->binary('/', $rt->swizzle($_u_fullResolution, 'x'), $rt->swizzle($_u_fullResolution, 'y'), 1, 'float');
        $st = $globalUV;
        @{$st} = map { $rt->f32($_) } @{($rt->binary('-', $st, $rt->f(0.5), 2, 'float'))};
        $st = $rt->assign_swizzle($st, 'x', $rt->binary('*', $rt->swizzle($st, 'x'), $aspect, 1, 'float'));
        $angle = $rt->binary('/', $rt->binary('*', $_u_rotation, $g->{PI}, 1, 'float'), $rt->f(180), 1, 'float');
        $c = $rt->component_wise('cos', $angle);
        $s = $rt->component_wise('sin', $angle);
        @{$st} = map { $rt->f32($_) } @{($rt->matrix_mult($rt->construct(4, $c, $rt->unary('-', $s), $s, $c), $st, 2))};
        $maxSkew = $rt->binary('/', $rt->f(512), $rt->swizzle($_u_fullResolution, 'y'), 1, 'float');
        $effectiveSkewAmt = $rt->component_wise('clamp', $_u_skewAmt, $rt->unary('-', $maxSkew), $maxSkew);
        $st = $rt->assign_swizzle($st, 'x', $rt->binary('+', $rt->swizzle($st, 'x'), $rt->binary('*', $rt->swizzle($st, 'y'), $rt->unary('-', $effectiveSkewAmt), 1, 'float'), 1, 'float'));
        $st = $rt->assign_swizzle($st, 'x', $rt->binary('/', $rt->swizzle($st, 'x'), $aspect, 1, 'float'));
        @{$st} = map { $rt->f32($_) } @{($rt->binary('+', $st, $rt->f(0.5), 2, 'float'))};
        $localUV = $rt->binary('/', $rt->binary('-', $rt->binary('*', $st, $_u_fullResolution, 2, 'float'), $_u_tileOffset, 2, 'float'), $resolution, 2, 'float');
        $wrapMode = $rt->construct(1, $_u_wrap, 'int');
        if ($rt->binary('==', $wrapMode, $rt->i(0))) {
            @{$localUV} = map { $rt->f32($_) } @{($rt->component_wise('clamp', $localUV, $rt->f(0), $rt->f(1)))};
        } else {
            if ($rt->binary('==', $wrapMode, $rt->i(1))) {
                @{$localUV} = map { $rt->f32($_) } @{($rt->component_wise('abs', $rt->binary('-', $rt->component_wise('mod', $rt->binary('+', $localUV, $rt->f(1), 2, 'float'), $rt->f(2)), $rt->f(1), 2, 'float')))};
            } else {
                @{$localUV} = map { $rt->f32($_) } @{($rt->component_wise('fract', $localUV))};
            }
        }
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->texture($_u_inputTex, $localUV))};
    };
    $main__void->();
    my $_c0 = $g->{fragColor};
    $out->[0] = $rt->f32($_c0->[0]); $out->[1] = $rt->f32($_c0->[1]); $out->[2] = $rt->f32($_c0->[2]); $out->[3] = $rt->f32($_c0->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0, output_names => ['fragColor'] };
