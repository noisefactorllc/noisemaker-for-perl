# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($main__void);
    my $_retc;
    my $_u_SHAPE = exists $U->{'SHAPE'} ? $U->{'SHAPE'} : 0;
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_resolution = exists $U->{'resolution'} ? $U->{'resolution'} : $rt->construct(2, 0.0);
    my $_u_mode = exists $U->{'mode'} ? $U->{'mode'} : 0;
    my $_u_radius = exists $U->{'radius'} ? $U->{'radius'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $main__void = sub {
        my ($_for0_first, $_for1_first, $_for2_first, $acc, $d, $hi, $i, $lo, $o, $r, $r2, $s, $sL, $sR, $texel, $uv, $x, $y);
        $uv = $rt->binary('/', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_resolution, 2, 'float');
        $texel = $rt->binary('/', $rt->f(1), $_u_resolution, 2, 'float');
        $acc = $rt->texture($_u_inputTex, $uv);
        $r = $rt->f(0.0);
        $r2 = $rt->f(0.0);
        if ($rt->binary('==', $_u_SHAPE, $rt->i(1))) {
            $r = $rt->component_wise('min', $_u_radius, $rt->f(12));
            $r2 = $rt->binary('*', $r, $r, 1, 'float');
            $y = $rt->unary('-', $rt->i(12));
            $_for0_first = 1;
            for my $_for0 (0 .. 1048575) {
                if (!$_for0_first) {
                    $y = $rt->binary('+', $y, $rt->i(1), 1, 'int');
                }
                $_for0_first = 0;
                if (!($rt->binary('<=', $y, $rt->i(12)))) {
                    last;
                }
                $x = $rt->unary('-', $rt->i(12));
                $_for1_first = 1;
                for my $_for1 (0 .. 1048575) {
                    if (!$_for1_first) {
                        $x = $rt->binary('+', $x, $rt->i(1), 1, 'int');
                    }
                    $_for1_first = 0;
                    if (!($rt->binary('<=', $x, $rt->i(12)))) {
                        last;
                    }
                    if ((($rt->binary('==', $x, $rt->i(0))) && ($rt->binary('==', $y, $rt->i(0))) ? 1 : 0)) {
                        next;
                    }
                    $d = $rt->construct(2, $rt->construct(1, $x), $rt->construct(1, $y));
                    if ($rt->binary('>', $rt->dot($d, $d), $r2)) {
                        next;
                    }
                    $s = $rt->texture($_u_inputTex, $rt->binary('+', $uv, $rt->binary('*', $d, $texel, 2, 'float'), 2, 'float'));
                    $hi = $rt->component_wise('max', $acc, $s);
                    $lo = $rt->component_wise('min', $acc, $s);
                    @{$acc} = map { $rt->f32($_) } @{($rt->component_wise('mix', $hi, $lo, $rt->construct(1, $_u_mode)))};
                }
            }
        } else {
            $r = $rt->component_wise('min', $_u_radius, $rt->f(32));
            $i = $rt->i(1);
            $_for2_first = 1;
            for my $_for2 (0 .. 1048575) {
                if (!$_for2_first) {
                    $i = $rt->binary('+', $i, $rt->i(1), 1, 'int');
                }
                $_for2_first = 0;
                if (!($rt->binary('<=', $i, $rt->i(32)))) {
                    last;
                }
                if ($rt->binary('>', $rt->construct(1, $i), $r)) {
                    last;
                }
                $o = $rt->binary('*', $rt->construct(2, $rt->construct(1, $i), $rt->f(0)), $texel, 2, 'float');
                $sL = $rt->texture($_u_inputTex, $rt->binary('-', $uv, $o, 2, 'float'));
                $sR = $rt->texture($_u_inputTex, $rt->binary('+', $uv, $o, 2, 'float'));
                $hi = $rt->component_wise('max', $acc, $rt->component_wise('max', $sL, $sR));
                $lo = $rt->component_wise('min', $acc, $rt->component_wise('min', $sL, $sR));
                @{$acc} = map { $rt->f32($_) } @{($rt->component_wise('mix', $hi, $lo, $rt->construct(1, $_u_mode)))};
            }
        }
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($acc)};
    };
    $main__void->();
    my $_c = $g->{fragColor};
    $out->[0] = $rt->f32($_c->[0]); $out->[1] = $rt->f32($_c->[1]); $out->[2] = $rt->f32($_c->[2]); $out->[3] = $rt->f32($_c->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0 };
