# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($srgbToLinear__vec3, $linearToSrgb__vec3, $applyWhiteBalance__vec3_float_float, $shadowWeight__float, $highlightWeight__float, $midtoneWeight__float, $whitesWeight__float, $blacksWeight__float, $applyTonalRanges__vec3_float_float_float_float, $applyContrast__vec3_float, $applyCurve__vec3_float_float_float, $applySaturation__vec3_float, $main__void);
    my $_retc;
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_temperature = exists $U->{'temperature'} ? $U->{'temperature'} : $rt->f(0.0);
    my $_u_tint = exists $U->{'tint'} ? $U->{'tint'} : $rt->f(0.0);
    my $_u_exposure = exists $U->{'exposure'} ? $U->{'exposure'} : $rt->f(0.0);
    my $_u_contrast = exists $U->{'contrast'} ? $U->{'contrast'} : $rt->f(0.0);
    my $_u_highlights = exists $U->{'highlights'} ? $U->{'highlights'} : $rt->f(0.0);
    my $_u_shadows = exists $U->{'shadows'} ? $U->{'shadows'} : $rt->f(0.0);
    my $_u_whites = exists $U->{'whites'} ? $U->{'whites'} : $rt->f(0.0);
    my $_u_blacks = exists $U->{'blacks'} ? $U->{'blacks'} : $rt->f(0.0);
    my $_u_saturation = exists $U->{'saturation'} ? $U->{'saturation'} : $rt->f(0.0);
    my $_u_curveShadows = exists $U->{'curveShadows'} ? $U->{'curveShadows'} : $rt->f(0.0);
    my $_u_curveMidtones = exists $U->{'curveMidtones'} ? $U->{'curveMidtones'} : $rt->f(0.0);
    my $_u_curveHighlights = exists $U->{'curveHighlights'} ? $U->{'curveHighlights'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $g->{LUMA_WEIGHTS} = $rt->construct(3, $rt->f(0.21260000000000001), $rt->f(0.71519999999999995), $rt->f(0.0722));
    $srgbToLinear__vec3 = sub {
        my ($srgb) = @_;
        $srgb = $rt->copy($srgb, 'float');
        my ($_for0_first, $i, $linear);
        $linear = $rt->construct(3, 0.0);
        $i = $rt->i(0);
        $_for0_first = 1;
        for my $_for0 (0 .. 1048575) {
            if (!$_for0_first) {
                $i = $rt->binary('+', $i, $rt->i(1), 1, 'int');
            }
            $_for0_first = 0;
            if (!($rt->binary('<', $i, $rt->i(3)))) {
                last;
            }
            if ($rt->binary('<=', $srgb->[int($i)], $rt->f(0.04045))) {
                $linear->[int($i)] = $rt->binary('/', $srgb->[int($i)], $rt->f(12.92), 1, 'float');
            } else {
                $linear->[int($i)] = $rt->component_wise('pow', $rt->binary('/', $rt->binary('+', $srgb->[int($i)], $rt->f(0.055), 1, 'float'), $rt->f(1.0549999999999999), 1, 'float'), $rt->f(2.3999999999999999));
            }
        }
        return $linear;
    };
    $linearToSrgb__vec3 = sub {
        my ($linear) = @_;
        $linear = $rt->copy($linear, 'float');
        my ($_for1_first, $i, $srgb);
        $srgb = $rt->construct(3, 0.0);
        $i = $rt->i(0);
        $_for1_first = 1;
        for my $_for1 (0 .. 1048575) {
            if (!$_for1_first) {
                $i = $rt->binary('+', $i, $rt->i(1), 1, 'int');
            }
            $_for1_first = 0;
            if (!($rt->binary('<', $i, $rt->i(3)))) {
                last;
            }
            if ($rt->binary('<=', $linear->[int($i)], $rt->f(0.0031308))) {
                $srgb->[int($i)] = $rt->binary('*', $linear->[int($i)], $rt->f(12.92), 1, 'float');
            } else {
                $srgb->[int($i)] = $rt->binary('-', $rt->binary('*', $rt->f(1.0549999999999999), $rt->component_wise('pow', $linear->[int($i)], $rt->binary('/', $rt->f(1), $rt->f(2.3999999999999999), 1, 'float')), 1, 'float'), $rt->f(0.055), 1, 'float');
            }
        }
        return $srgb;
    };
    $applyWhiteBalance__vec3_float_float = sub {
        my ($rgb, $temp, $tint) = @_;
        $rgb = $rt->copy($rgb, 'float');
        my ($shift);
        $shift = $rt->construct(3, $rt->binary('+', $rt->f(1), $rt->binary('*', $temp, $rt->f(0.5), 1, 'float'), 1, 'float'), $rt->binary('-', $rt->f(1), $rt->binary('*', $tint, $rt->f(0.5), 1, 'float'), 1, 'float'), $rt->binary('-', $rt->f(1), $rt->binary('*', $temp, $rt->f(0.5), 1, 'float'), 1, 'float'));
        return $rt->binary('*', $rgb, $shift, 3, 'float');
    };
    $shadowWeight__float = sub {
        my ($luma) = @_;
        return $rt->binary('-', $rt->f(1), $rt->component_wise('smoothstep', $rt->f(0), $rt->f(0.5), $luma), 1, 'float');
    };
    $highlightWeight__float = sub {
        my ($luma) = @_;
        return $rt->component_wise('smoothstep', $rt->f(0.5), $rt->f(1), $luma);
    };
    $midtoneWeight__float = sub {
        my ($luma) = @_;
        return $rt->binary('-', $rt->f(1), $rt->binary('*', $rt->component_wise('abs', $rt->binary('-', $luma, $rt->f(0.5), 1, 'float')), $rt->f(2), 1, 'float'), 1, 'float');
    };
    $whitesWeight__float = sub {
        my ($luma) = @_;
        return $rt->component_wise('smoothstep', $rt->f(0.69999999999999996), $rt->f(1), $luma);
    };
    $blacksWeight__float = sub {
        my ($luma) = @_;
        return $rt->binary('-', $rt->f(1), $rt->component_wise('smoothstep', $rt->f(0), $rt->f(0.29999999999999999), $luma), 1, 'float');
    };
    $applyTonalRanges__vec3_float_float_float_float = sub {
        my ($rgb, $highlights, $shadows, $whites, $blacks) = @_;
        $rgb = $rt->copy($rgb, 'float');
        my ($bWeight, $chroma, $hWeight, $luma, $lumaAdjust, $newLuma, $sWeight, $wWeight);
        $luma = $rt->dot($rgb, $g->{LUMA_WEIGHTS});
        $chroma = $rt->binary('-', $rgb, $luma, 3, 'float');
        $hWeight = $highlightWeight__float->($luma);
        $sWeight = $shadowWeight__float->($luma);
        $wWeight = $whitesWeight__float->($luma);
        $bWeight = $blacksWeight__float->($luma);
        $lumaAdjust = $rt->f(0);
        $lumaAdjust = $rt->binary('+', $lumaAdjust, $rt->binary('*', $rt->binary('*', $highlights, $hWeight, 1, 'float'), $rt->f(0.5), 1, 'float'), 1, 'float');
        $lumaAdjust = $rt->binary('+', $lumaAdjust, $rt->binary('*', $rt->binary('*', $shadows, $sWeight, 1, 'float'), $rt->f(0.5), 1, 'float'), 1, 'float');
        $lumaAdjust = $rt->binary('+', $lumaAdjust, $rt->binary('*', $rt->binary('*', $whites, $wWeight, 1, 'float'), $rt->f(0.29999999999999999), 1, 'float'), 1, 'float');
        $lumaAdjust = $rt->binary('+', $lumaAdjust, $rt->binary('*', $rt->binary('*', $blacks, $bWeight, 1, 'float'), $rt->f(0.29999999999999999), 1, 'float'), 1, 'float');
        $newLuma = $rt->binary('+', $luma, $lumaAdjust, 1, 'float');
        $newLuma = $rt->component_wise('max', $newLuma, $rt->f(0));
        return $rt->binary('+', $newLuma, $chroma, 3, 'float');
    };
    $applyContrast__vec3_float = sub {
        my ($rgb, $contrast) = @_;
        $rgb = $rt->copy($rgb, 'float');
        my ($chroma, $factor, $luma, $newLuma, $pivot);
        if ($rt->binary('<', $rt->component_wise('abs', $contrast), $rt->f(0.001))) {
            return $rgb;
        }
        $luma = $rt->dot($rgb, $g->{LUMA_WEIGHTS});
        $chroma = $rt->binary('-', $rgb, $luma, 3, 'float');
        $pivot = $rt->f(0.5);
        $factor = $rt->binary('+', $rt->f(1), $contrast, 1, 'float');
        $newLuma = $rt->binary('+', $rt->binary('*', $rt->binary('-', $luma, $pivot, 1, 'float'), $factor, 1, 'float'), $pivot, 1, 'float');
        $newLuma = $rt->component_wise('clamp', $newLuma, $rt->f(0), $rt->f(1.5));
        return $rt->binary('+', $newLuma, $chroma, 3, 'float');
    };
    $applyCurve__vec3_float_float_float = sub {
        my ($rgb, $shadowLift, $midGamma, $highGain) = @_;
        $rgb = $rt->copy($rgb, 'float');
        my ($chroma, $gain, $gamma, $hW, $lift, $luma, $mW, $newLuma, $sW);
        $luma = $rt->dot($rgb, $g->{LUMA_WEIGHTS});
        $chroma = $rt->binary('-', $rgb, $luma, 3, 'float');
        $sW = $shadowWeight__float->($luma);
        $mW = $midtoneWeight__float->($luma);
        $hW = $highlightWeight__float->($luma);
        $lift = $rt->binary('*', $rt->binary('*', $shadowLift, $sW, 1, 'float'), $rt->f(0.20000000000000001), 1, 'float');
        $gamma = $rt->binary('-', $rt->f(1), $rt->binary('*', $rt->binary('*', $midGamma, $mW, 1, 'float'), $rt->f(0.29999999999999999), 1, 'float'), 1, 'float');
        $gain = $rt->binary('+', $rt->f(1), $rt->binary('*', $rt->binary('*', $highGain, $hW, 1, 'float'), $rt->f(0.5), 1, 'float'), 1, 'float');
        $newLuma = $rt->binary('+', $luma, $lift, 1, 'float');
        $newLuma = $rt->component_wise('pow', $rt->component_wise('max', $newLuma, $rt->f(0.001)), $gamma);
        $newLuma = $rt->binary('*', $newLuma, $gain, 1, 'float');
        return $rt->component_wise('max', $rt->binary('+', $newLuma, $chroma, 3, 'float'), $rt->construct(3, $rt->f(0)));
    };
    $applySaturation__vec3_float = sub {
        my ($rgb, $satAmount) = @_;
        $rgb = $rt->copy($rgb, 'float');
        my ($chroma, $luma);
        $luma = $rt->dot($rgb, $g->{LUMA_WEIGHTS});
        $chroma = $rt->binary('-', $rgb, $luma, 3, 'float');
        return $rt->binary('+', $luma, $rt->binary('*', $chroma, $satAmount, 3, 'float'), 3, 'float');
    };
    $main__void = sub {
        my ($color, $coord, $globalCoord, $rgb);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $coord = $rt->construct(2, $rt->swizzle($ctx->{frag_coord}, 'xy'), 'int');
        $color = $rt->texel_fetch($_u_inputTex, $coord, $rt->i(0));
        $rgb = $srgbToLinear__vec3->($rt->swizzle($color, 'rgb'));
        @{$rgb} = map { $rt->f32($_) } @{($applyWhiteBalance__vec3_float_float->($rgb, $_u_temperature, $_u_tint))};
        @{$rgb} = map { $rt->f32($_) } @{($rt->binary('*', $rgb, $rt->component_wise('pow', $rt->f(2), $_u_exposure), 3, 'float'))};
        @{$rgb} = map { $rt->f32($_) } @{($applyContrast__vec3_float->($rgb, $_u_contrast))};
        @{$rgb} = map { $rt->f32($_) } @{($applyTonalRanges__vec3_float_float_float_float->($rgb, $_u_highlights, $_u_shadows, $_u_whites, $_u_blacks))};
        @{$rgb} = map { $rt->f32($_) } @{($applyCurve__vec3_float_float_float->($rgb, $_u_curveShadows, $_u_curveMidtones, $_u_curveHighlights))};
        @{$rgb} = map { $rt->f32($_) } @{($applySaturation__vec3_float->($rgb, $_u_saturation))};
        @{$rgb} = map { $rt->f32($_) } @{($linearToSrgb__vec3->($rt->component_wise('max', $rgb, $rt->construct(3, $rt->f(0)))))};
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $rgb, $rt->swizzle($color, 'a')))};
    };
    $main__void->();
    my $_c0 = $g->{fragColor};
    $out->[0] = $rt->f32($_c0->[0]); $out->[1] = $rt->f32($_c0->[1]); $out->[2] = $rt->f32($_c0->[2]); $out->[3] = $rt->f32($_c0->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0, output_names => ['fragColor'] };
