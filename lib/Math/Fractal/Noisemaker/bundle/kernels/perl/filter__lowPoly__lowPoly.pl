# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($pcg__uvec3, $hash2__vec2_float, $lowPolySite__ivec2_float_float_float, $main__void);
    my $_retc;
    my $_u_LP_BORDER = exists $U->{'LP_BORDER'} ? $U->{'LP_BORDER'} : 0;
    my $_u_LP_LIGHT = exists $U->{'LP_LIGHT'} ? $U->{'LP_LIGHT'} : 0;
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_scale = exists $U->{'scale'} ? $U->{'scale'} : $rt->f(0.0);
    my $_u_seed = exists $U->{'seed'} ? $U->{'seed'} : $rt->f(0.0);
    my $_u_mode = exists $U->{'mode'} ? $U->{'mode'} : 0;
    my $_u_edgeStrength = exists $U->{'edgeStrength'} ? $U->{'edgeStrength'} : $rt->f(0.0);
    my $_u_edgeColor = exists $U->{'edgeColor'} ? $U->{'edgeColor'} : $rt->construct(3, 0.0);
    my $_u_speed = exists $U->{'speed'} ? $U->{'speed'} : $rt->f(0.0);
    my $_u_time = exists $U->{'time'} ? $U->{'time'} : $rt->f(0.0);
    my $_u_alpha = exists $U->{'alpha'} ? $U->{'alpha'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $g->{TAU} = $rt->f(6.2831853071800001);
    $pcg__uvec3 = sub {
        my ($v) = @_;
        $v = $rt->copy($v, 'uint');
        @{$v} = @{($rt->binary('+', $rt->binary('*', $v, $rt->i(1664525), 3, 'uint'), $rt->i(1013904223), 3, 'uint'))};
        $v = $rt->assign_swizzle($v, 'x', $rt->binary('+', $rt->swizzle($v, 'x'), $rt->binary('*', $rt->swizzle($v, 'y'), $rt->swizzle($v, 'z'), 1, 'uint'), 1, 'uint'));
        $v = $rt->assign_swizzle($v, 'y', $rt->binary('+', $rt->swizzle($v, 'y'), $rt->binary('*', $rt->swizzle($v, 'z'), $rt->swizzle($v, 'x'), 1, 'uint'), 1, 'uint'));
        $v = $rt->assign_swizzle($v, 'z', $rt->binary('+', $rt->swizzle($v, 'z'), $rt->binary('*', $rt->swizzle($v, 'x'), $rt->swizzle($v, 'y'), 1, 'uint'), 1, 'uint'));
        @{$v} = @{($rt->binary('^', $v, $rt->binary('>>', $v, $rt->i(16), 3, 'uint'), 3, 'uint'))};
        $v = $rt->assign_swizzle($v, 'x', $rt->binary('+', $rt->swizzle($v, 'x'), $rt->binary('*', $rt->swizzle($v, 'y'), $rt->swizzle($v, 'z'), 1, 'uint'), 1, 'uint'));
        $v = $rt->assign_swizzle($v, 'y', $rt->binary('+', $rt->swizzle($v, 'y'), $rt->binary('*', $rt->swizzle($v, 'z'), $rt->swizzle($v, 'x'), 1, 'uint'), 1, 'uint'));
        $v = $rt->assign_swizzle($v, 'z', $rt->binary('+', $rt->swizzle($v, 'z'), $rt->binary('*', $rt->swizzle($v, 'x'), $rt->swizzle($v, 'y'), 1, 'uint'), 1, 'uint'));
        return $v;
    };
    $hash2__vec2_float = sub {
        my ($p, $s) = @_;
        $p = $rt->copy($p, 'float');
        my ($v);
        $v = $pcg__uvec3->($rt->construct(3, $rt->construct(1, (($rt->binary('>=', $rt->swizzle($p, 'x'), $rt->f(0))) ? ($rt->binary('*', $rt->swizzle($p, 'x'), $rt->f(2), 1, 'float')) : ($rt->binary('+', $rt->binary('*', $rt->unary('-', $rt->swizzle($p, 'x')), $rt->f(2), 1, 'float'), $rt->f(1), 1, 'float'))), 'uint'), $rt->construct(1, (($rt->binary('>=', $rt->swizzle($p, 'y'), $rt->f(0))) ? ($rt->binary('*', $rt->swizzle($p, 'y'), $rt->f(2), 1, 'float')) : ($rt->binary('+', $rt->binary('*', $rt->unary('-', $rt->swizzle($p, 'y')), $rt->f(2), 1, 'float'), $rt->f(1), 1, 'float'))), 'uint'), $rt->construct(1, (($rt->binary('>=', $s, $rt->f(0))) ? ($rt->binary('*', $s, $rt->f(2), 1, 'float')) : ($rt->binary('+', $rt->binary('*', $rt->unary('-', $s), $rt->f(2), 1, 'float'), $rt->f(1), 1, 'float'))), 'uint'), 'uint'));
        return $rt->binary('/', $rt->construct(2, $rt->swizzle($v, 'xy')), $rt->construct(1, $rt->i(4294967295)), 2, 'float');
    };
    $lowPolySite__ivec2_float_float_float = sub {
        my ($siteCell, $n, $s, $spd) = @_;
        $siteCell = $rt->copy($siteCell, 'int');
        my ($angle, $animRand, $offset, $radius, $siteCellF);
        $siteCellF = $rt->construct(2, $siteCell);
        $offset = $hash2__vec2_float->($siteCellF, $s);
        $angle = $rt->f(0.0);
        $animRand = $rt->construct(2, 0.0);
        $radius = $rt->f(0.0);
        if ($rt->binary('>', $spd, $rt->f(0))) {
            $animRand = $hash2__vec2_float->($siteCellF, $rt->binary('+', $s, $rt->f(100), 1, 'float'));
            $angle = $rt->binary('+', $rt->binary('*', $_u_time, $g->{TAU}, 1, 'float'), $rt->binary('*', $rt->swizzle($animRand, 'x'), $g->{TAU}, 1, 'float'), 1, 'float');
            $radius = $rt->binary('*', $rt->swizzle($animRand, 'y'), $spd, 1, 'float');
            @{$offset} = map { $rt->f32($_) } @{($rt->component_wise('clamp', $rt->binary('+', $offset, $rt->binary('*', $rt->construct(2, $rt->component_wise('cos', $angle), $rt->component_wise('sin', $angle)), $radius, 2, 'float'), 2, 'float'), $rt->f(0), $rt->f(1)))};
        }
        return $rt->binary('/', $rt->binary('+', $siteCellF, $offset, 2, 'float'), $n, 2, 'float');
    };
    $main__void = sub {
        my ($_for0_first, $_for1_first, $_for2_first, $_for3_first, $_for4_first, $_for5_first, $angle, $animRand, $aspect, $auv, $bisectorDistance, $borderFeather, $borderHalfWidth, $borderMask, $borderNearestCell, $borderNearestDist, $borderNearestPoint, $candidateCell, $candidateDist, $candidatePoint, $cell, $cellColor, $cellRadius, $d, $distField, $distToEdge, $dx, $dy, $edgeDist, $edgeFactor, $exposure, $globalCoord, $globalUV, $globalUV_sample, $intensity, $litMode, $litValue, $localUV_sample, $minDist, $modeResult, $n, $nearestCell, $nearestPoint, $neighbor, $neighborF, $offset, $original, $paneValue, $point, $radius, $raw, $resolution, $result, $s, $scaled, $secondDist, $selectedDist, $siteDistance, $siteVector, $spd, $texSize, $thirdDist, $tileDims, $uv);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $texSize = $rt->texture_size($_u_inputTex);
        $tileDims = $rt->construct(2, $texSize);
        $resolution = (($rt->binary('>', $rt->swizzle($_u_fullResolution, 'x'), $rt->f(0))) ? ($_u_fullResolution) : ($tileDims));
        $uv = $rt->binary('/', $rt->swizzle($ctx->{frag_coord}, 'xy'), $tileDims, 2, 'float');
        $globalUV = $rt->binary('/', $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float'), $resolution, 2, 'float');
        $n = $rt->component_wise('max', $rt->binary('-', $rt->f(102), $_u_scale, 1, 'float'), $rt->f(2));
        $s = $_u_seed;
        $spd = $rt->binary('*', $_u_speed, $rt->f(0.29999999999999999), 1, 'float');
        $aspect = $rt->binary('/', $rt->swizzle($_u_fullResolution, 'x'), $rt->swizzle($_u_fullResolution, 'y'), 1, 'float');
        $auv = $rt->construct(2, $rt->binary('*', $rt->swizzle($globalUV, 'x'), $aspect, 1, 'float'), $rt->swizzle($globalUV, 'y'));
        $scaled = $rt->binary('*', $auv, $n, 2, 'float');
        $cell = $rt->construct(2, $rt->component_wise('floor', $scaled), 'int');
        $minDist = $rt->f(10000000000);
        $secondDist = $rt->f(10000000000);
        $thirdDist = $rt->f(10000000000);
        $nearestPoint = $rt->construct(2, $rt->f(0));
        $nearestCell = $rt->construct(2, 0.0, 'int');
        if ($rt->binary('>', $_u_LP_BORDER, $rt->i(0))) {
            $nearestCell = $rt->construct(2, $rt->i(0), 'int');
        }
        $dy = $rt->unary('-', $rt->i(1));
        $_for0_first = 1;
        for my $_for0 (0 .. 1048575) {
            if (!$_for0_first) {
                $dy = $rt->binary('+', $dy, $rt->i(1), 1, 'int');
            }
            $_for0_first = 0;
            if (!($rt->binary('<=', $dy, $rt->i(1)))) {
                last;
            }
            $dx = $rt->unary('-', $rt->i(1));
            $_for1_first = 1;
            for my $_for1 (0 .. 1048575) {
                if (!$_for1_first) {
                    $dx = $rt->binary('+', $dx, $rt->i(1), 1, 'int');
                }
                $_for1_first = 0;
                if (!($rt->binary('<=', $dx, $rt->i(1)))) {
                    last;
                }
                $neighbor = $rt->binary('+', $cell, $rt->construct(2, $dx, $dy, 'int'), 2, 'int');
                $neighborF = $rt->construct(2, $neighbor);
                $offset = $hash2__vec2_float->($neighborF, $s);
                $angle = $rt->f(0.0);
                $animRand = $rt->construct(2, 0.0);
                $radius = $rt->f(0.0);
                if ($rt->binary('>', $spd, $rt->f(0))) {
                    $animRand = $hash2__vec2_float->($neighborF, $rt->binary('+', $s, $rt->f(100), 1, 'float'));
                    $angle = $rt->binary('+', $rt->binary('*', $_u_time, $g->{TAU}, 1, 'float'), $rt->binary('*', $rt->swizzle($animRand, 'x'), $g->{TAU}, 1, 'float'), 1, 'float');
                    $radius = $rt->binary('*', $rt->swizzle($animRand, 'y'), $spd, 1, 'float');
                    @{$offset} = map { $rt->f32($_) } @{($rt->component_wise('clamp', $rt->binary('+', $offset, $rt->binary('*', $rt->construct(2, $rt->component_wise('cos', $angle), $rt->component_wise('sin', $angle)), $radius, 2, 'float'), 2, 'float'), $rt->f(0), $rt->f(1)))};
                }
                $point = $rt->binary('/', $rt->binary('+', $neighborF, $offset, 2, 'float'), $n, 2, 'float');
                $d = $rt->distance($auv, $point);
                if ($rt->binary('<', $d, $minDist)) {
                    $thirdDist = $secondDist;
                    $secondDist = $minDist;
                    $minDist = $d;
                    @{$nearestPoint} = map { $rt->f32($_) } @{($point)};
                    if ($rt->binary('>', $_u_LP_BORDER, $rt->i(0))) {
                        @{$nearestCell} = @{($neighbor)};
                    }
                } else {
                    if ($rt->binary('<', $d, $secondDist)) {
                        $thirdDist = $secondDist;
                        $secondDist = $d;
                    } else {
                        if ($rt->binary('<', $d, $thirdDist)) {
                            $thirdDist = $d;
                        }
                    }
                }
            }
        }
        $globalUV_sample = $rt->construct(2, $rt->binary('/', $rt->swizzle($nearestPoint, 'x'), $aspect, 1, 'float'), $rt->swizzle($nearestPoint, 'y'));
        $localUV_sample = $rt->binary('/', $rt->binary('-', $rt->binary('*', $globalUV_sample, $resolution, 2, 'float'), $_u_tileOffset, 2, 'float'), $tileDims, 2, 'float');
        $cellColor = $rt->texture($_u_inputTex, $localUV_sample);
        $result = $rt->construct(3, 0.0);
        $distField = $rt->f(0.0);
        $edgeDist = $rt->f(0.0);
        $edgeFactor = $rt->f(0.0);
        $raw = $rt->f(0.0);
        $selectedDist = $rt->f(0.0);
        if ($rt->binary('==', $_u_mode, $rt->i(0))) {
            @{$result} = map { $rt->f32($_) } @{($rt->swizzle($cellColor, 'rgb'))};
        } else {
            if ($rt->binary('==', $_u_mode, $rt->i(1))) {
                $edgeDist = $rt->component_wise('clamp', $rt->binary('*', $rt->binary('*', $rt->binary('-', $secondDist, $minDist, 1, 'float'), $n, 1, 'float'), $rt->f(2), 1, 'float'), $rt->f(0), $rt->f(1));
                $edgeFactor = $rt->component_wise('mix', $_u_edgeStrength, $rt->f(0), $edgeDist);
                @{$result} = map { $rt->f32($_) } @{($rt->component_wise('mix', $rt->swizzle($cellColor, 'rgb'), $_u_edgeColor, $edgeFactor))};
            } else {
                $selectedDist = (($rt->binary('==', $_u_mode, $rt->i(2))) ? ($secondDist) : ($thirdDist));
                $raw = $rt->component_wise('clamp', $rt->binary('*', $selectedDist, $n, 1, 'float'), $rt->f(0), $rt->f(1));
                $distField = $rt->component_wise('pow', $raw, $rt->component_wise('mix', $rt->f(0.5), $rt->f(3), $_u_edgeStrength));
                @{$result} = map { $rt->f32($_) } @{($rt->binary('*', $rt->swizzle($cellColor, 'rgb'), $distField, 3, 'float'))};
            }
        }
        $borderMask = $rt->f(0.0);
        $modeResult = $rt->construct(3, 0.0);
        if ((($rt->binary('>', $_u_LP_BORDER, $rt->i(0))) || ($rt->binary('>', $_u_LP_LIGHT, $rt->i(0))) ? 1 : 0)) {
            $modeResult = $result;
            $borderMask = $rt->f(0);
        }
        if ($rt->binary('>', $_u_LP_BORDER, $rt->i(0))) {
            $borderNearestPoint = $nearestPoint;
            $borderNearestCell = $nearestCell;
            $borderNearestDist = $minDist;
            $dy = $rt->unary('-', $rt->i(2));
            $_for2_first = 1;
            for my $_for2 (0 .. 1048575) {
                if (!$_for2_first) {
                    $dy = $rt->binary('+', $dy, $rt->i(1), 1, 'int');
                }
                $_for2_first = 0;
                if (!($rt->binary('<=', $dy, $rt->i(2)))) {
                    last;
                }
                $dx = $rt->unary('-', $rt->i(2));
                $_for3_first = 1;
                for my $_for3 (0 .. 1048575) {
                    if (!$_for3_first) {
                        $dx = $rt->binary('+', $dx, $rt->i(1), 1, 'int');
                    }
                    $_for3_first = 0;
                    if (!($rt->binary('<=', $dx, $rt->i(2)))) {
                        last;
                    }
                    $candidateCell = $rt->binary('+', $cell, $rt->construct(2, $dx, $dy, 'int'), 2, 'int');
                    $candidatePoint = $lowPolySite__ivec2_float_float_float->($candidateCell, $n, $s, $spd);
                    $candidateDist = $rt->distance($auv, $candidatePoint);
                    if ($rt->binary('<', $candidateDist, $borderNearestDist)) {
                        $borderNearestDist = $candidateDist;
                        @{$borderNearestPoint} = map { $rt->f32($_) } @{($candidatePoint)};
                        @{$borderNearestCell} = @{($candidateCell)};
                    }
                }
            }
            $distToEdge = $rt->f(10000000000);
            $dy = $rt->unary('-', $rt->i(2));
            $_for4_first = 1;
            for my $_for4 (0 .. 1048575) {
                if (!$_for4_first) {
                    $dy = $rt->binary('+', $dy, $rt->i(1), 1, 'int');
                }
                $_for4_first = 0;
                if (!($rt->binary('<=', $dy, $rt->i(2)))) {
                    last;
                }
                $dx = $rt->unary('-', $rt->i(2));
                $_for5_first = 1;
                for my $_for5 (0 .. 1048575) {
                    if (!$_for5_first) {
                        $dx = $rt->binary('+', $dx, $rt->i(1), 1, 'int');
                    }
                    $_for5_first = 0;
                    if (!($rt->binary('<=', $dx, $rt->i(2)))) {
                        last;
                    }
                    $candidateCell = $rt->binary('+', $cell, $rt->construct(2, $dx, $dy, 'int'), 2, 'int');
                    $bisectorDistance = $rt->f(0.0);
                    $candidatePoint = $rt->construct(2, 0.0);
                    $siteDistance = $rt->f(0.0);
                    $siteVector = $rt->construct(2, 0.0);
                    if ($rt->component_wise('any', $rt->component_wise('notEqual', $candidateCell, $borderNearestCell))) {
                        $candidatePoint = $lowPolySite__ivec2_float_float_float->($candidateCell, $n, $s, $spd);
                        $siteVector = $rt->binary('-', $candidatePoint, $borderNearestPoint, 2, 'float');
                        $siteDistance = $rt->component_wise('max', $rt->length($siteVector), $rt->f(1e-08));
                        $bisectorDistance = $rt->dot($rt->binary('-', $rt->binary('*', $rt->binary('+', $borderNearestPoint, $candidatePoint, 2, 'float'), $rt->f(0.5), 2, 'float'), $auv, 2, 'float'), $rt->binary('/', $siteVector, $siteDistance, 2, 'float'));
                        $distToEdge = $rt->component_wise('min', $distToEdge, $bisectorDistance);
                    }
                }
            }
            $cellRadius = $rt->binary('/', $rt->f(0.5), $n, 1, 'float');
            $borderHalfWidth = $rt->binary('*', $rt->binary('/', $rt->construct(1, $_u_LP_BORDER), $rt->f(100), 1, 'float'), $cellRadius, 1, 'float');
            $borderFeather = $rt->component_wise('max', $rt->fwidth($distToEdge), $rt->f(9.9999999999999995e-07));
            $borderMask = $rt->binary('-', $rt->f(1), $rt->component_wise('smoothstep', $rt->binary('-', $borderHalfWidth, $borderFeather, 1, 'float'), $rt->binary('+', $borderHalfWidth, $borderFeather, 1, 'float'), $distToEdge), 1, 'float');
            @{$result} = map { $rt->f32($_) } @{($rt->component_wise('mix', $modeResult, $_u_edgeColor, $borderMask))};
        }
        if ($rt->binary('>', $_u_LP_LIGHT, $rt->i(0))) {
            $intensity = $rt->component_wise('clamp', $rt->binary('/', $rt->construct(1, $_u_LP_LIGHT), $rt->f(100), 1, 'float'), $rt->f(0), $rt->f(1));
            $paneValue = $rt->component_wise('max', $rt->component_wise('max', $rt->swizzle($modeResult, 'r'), $rt->swizzle($modeResult, 'g')), $rt->swizzle($modeResult, 'b'));
            $exposure = $rt->component_wise('mix', $rt->f(1), $rt->f(2.25), $intensity);
            $litValue = $rt->binary('-', $rt->f(1), $rt->component_wise('pow', $rt->component_wise('max', $rt->binary('-', $rt->f(1), $paneValue, 1, 'float'), $rt->f(0)), $exposure), 1, 'float');
            $litMode = (($rt->binary('>', $paneValue, $rt->f(9.9999999999999995e-07))) ? ($rt->binary('*', $modeResult, $rt->binary('/', $litValue, $paneValue, 1, 'float'), 3, 'float')) : ($modeResult));
            @{$result} = map { $rt->f32($_) } @{($rt->component_wise('mix', $litMode, $_u_edgeColor, $borderMask))};
        }
        $original = $rt->texture($_u_inputTex, $uv);
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $rt->component_wise('mix', $rt->swizzle($original, 'rgb'), $result, $_u_alpha), $rt->swizzle($original, 'a')))};
    };
    $main__void->();
    my $_c = $g->{fragColor};
    $out->[0] = $rt->f32($_c->[0]); $out->[1] = $rt->f32($_c->[1]); $out->[2] = $rt->f32($_c->[2]); $out->[3] = $rt->f32($_c->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 1 };
