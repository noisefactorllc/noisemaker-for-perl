# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($main__void);
    my $_retc;
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_channel = exists $U->{'channel'} ? $U->{'channel'} : 0;
    my $_u_scale = exists $U->{'scale'} ? $U->{'scale'} : $rt->f(0.0);
    my $_u_offset = exists $U->{'offset'} ? $U->{'offset'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $main__void = sub {
        my ($c, $globalCoord, $st, $v);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $st = $rt->binary('/', $rt->binary('-', $rt->swizzle($ctx->{frag_coord}, 'xy'), $rt->f(0.5), 2, 'float'), $rt->construct(2, $rt->texture_size($_u_inputTex)), 2, 'float');
        $c = $rt->texture($_u_inputTex, $st);
        $v = $rt->f(0.0);
        if ($rt->binary('==', $_u_channel, $rt->i(0))) {
            $v = $rt->swizzle($c, 'r');
        } else {
            if ($rt->binary('==', $_u_channel, $rt->i(1))) {
                $v = $rt->swizzle($c, 'g');
            } else {
                if ($rt->binary('==', $_u_channel, $rt->i(2))) {
                    $v = $rt->swizzle($c, 'b');
                } else {
                    $v = $rt->swizzle($c, 'a');
                }
            }
        }
        $v = $rt->component_wise('fract', $rt->binary('+', $rt->binary('*', $v, $_u_scale, 1, 'float'), $_u_offset, 1, 'float'));
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $rt->construct(3, $v), $rt->f(1)))};
    };
    $main__void->();
    my $_c0 = $g->{fragColor};
    $out->[0] = $rt->f32($_c0->[0]); $out->[1] = $rt->f32($_c0->[1]); $out->[2] = $rt->f32($_c0->[2]); $out->[3] = $rt->f32($_c0->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0, output_names => ['fragColor'] };
