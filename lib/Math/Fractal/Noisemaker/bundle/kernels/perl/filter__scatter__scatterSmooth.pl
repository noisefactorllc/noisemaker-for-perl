# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($main__void);
    my $_retc;
    my $_u_MODE = exists $U->{'MODE'} ? $U->{'MODE'} : 0;
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_resolution = exists $U->{'resolution'} ? $U->{'resolution'} : $rt->construct(2, 0.0);
    my $_u_smoothness = exists $U->{'smoothness'} ? $U->{'smoothness'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $main__void = sub {
        my ($_for0_first, $_for1_first, $blurred, $src, $sum, $texel, $uv, $w, $wsum, $x, $y);
        $uv = $rt->binary('/', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_resolution, 2, 'float');
        $texel = $rt->binary('/', $rt->f(1), $_u_resolution, 2, 'float');
        $src = $rt->texture($_u_inputTex, $uv);
        $sum = $rt->construct(4, $rt->f(0));
        $wsum = $rt->f(0);
        $y = $rt->unary('-', $rt->i(1));
        $_for0_first = 1;
        for my $_for0 (0 .. 1048575) {
            if (!$_for0_first) {
                $y = $rt->binary('+', $y, $rt->i(1), 1, 'int');
            }
            $_for0_first = 0;
            if (!($rt->binary('<=', $y, $rt->i(1)))) {
                last;
            }
            $x = $rt->unary('-', $rt->i(1));
            $_for1_first = 1;
            for my $_for1 (0 .. 1048575) {
                if (!$_for1_first) {
                    $x = $rt->binary('+', $x, $rt->i(1), 1, 'int');
                }
                $_for1_first = 0;
                if (!($rt->binary('<=', $x, $rt->i(1)))) {
                    last;
                }
                $w = $rt->binary('*', $rt->binary('-', $rt->f(2), $rt->component_wise('abs', $rt->construct(1, $x)), 1, 'float'), $rt->binary('-', $rt->f(2), $rt->component_wise('abs', $rt->construct(1, $y)), 1, 'float'), 1, 'float');
                @{$sum} = map { $rt->f32($_) } @{($rt->binary('+', $sum, $rt->binary('*', $rt->texture($_u_inputTex, $rt->binary('+', $uv, $rt->binary('*', $rt->construct(2, $rt->construct(1, $x), $rt->construct(1, $y)), $texel, 2, 'float'), 2, 'float')), $w, 4, 'float'), 4, 'float'))};
                $wsum = $rt->binary('+', $wsum, $w, 1, 'float');
            }
        }
        $blurred = $rt->binary('/', $sum, $wsum, 4, 'float');
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->component_wise('mix', $src, $blurred, $rt->component_wise('clamp', $rt->binary('/', $_u_smoothness, $rt->f(100), 1, 'float'), $rt->f(0), $rt->f(1))))};
    };
    $main__void->();
    my $_c = $g->{fragColor};
    $out->[0] = $rt->f32($_c->[0]); $out->[1] = $rt->f32($_c->[1]); $out->[2] = $rt->f32($_c->[2]); $out->[3] = $rt->f32($_c->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0 };
