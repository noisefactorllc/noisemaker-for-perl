# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($hash3__vec3, $atlasTexel__ivec3_int, $sampleState__ivec3_int, $sampleSeed__ivec3_int, $countMooreNeighbors__ivec3_int, $countVonNeumannNeighbors__ivec3_int, $shouldBeBorn__int_int, $shouldSurvive__int_int, $main__void);
    my $_retc;
    my $_u_time = exists $U->{'time'} ? $U->{'time'} : $rt->f(0.0);
    my $_u_seed = exists $U->{'seed'} ? $U->{'seed'} : 0;
    my $_u_volumeSize = exists $U->{'volumeSize'} ? $U->{'volumeSize'} : 0;
    my $_u_ruleIndex = exists $U->{'ruleIndex'} ? $U->{'ruleIndex'} : 0;
    my $_u_neighborMode = exists $U->{'neighborMode'} ? $U->{'neighborMode'} : 0;
    my $_u_speed = exists $U->{'speed'} ? $U->{'speed'} : $rt->f(0.0);
    my $_u_density = exists $U->{'density'} ? $U->{'density'} : $rt->f(0.0);
    my $_u_weight = exists $U->{'weight'} ? $U->{'weight'} : $rt->f(0.0);
    my $_u_resetState = exists $U->{'resetState'} ? $U->{'resetState'} : 0;
    my $_u_stateTex = $ctx->texture_binding('stateTex');
    my $_u_seedTex = $ctx->texture_binding('seedTex');
    $g->{fragColor} = $rt->construct(4, 0.0);
    $hash3__vec3 = sub {
        my ($p) = @_;
        $p = $rt->copy($p, 'float');
        @{$p} = map { $rt->f32($_) } @{($rt->binary('+', $p, $rt->binary('*', $rt->construct(1, $_u_seed), $rt->f(0.10000000000000001), 1, 'float'), 3, 'float'))};
        @{$p} = map { $rt->f32($_) } @{($rt->component_wise('fract', $rt->binary('*', $p, $rt->construct(3, $rt->f(0.1031), $rt->f(0.10299999999999999), $rt->f(0.097299999999999998)), 3, 'float')))};
        @{$p} = map { $rt->f32($_) } @{($rt->binary('+', $p, $rt->dot($p, $rt->binary('+', $rt->swizzle($p, 'yxz'), $rt->f(33.329999999999998), 3, 'float')), 3, 'float'))};
        return $rt->component_wise('fract', $rt->binary('*', $rt->binary('+', $rt->swizzle($p, 'x'), $rt->swizzle($p, 'y'), 1, 'float'), $rt->swizzle($p, 'z'), 1, 'float'));
    };
    $atlasTexel__ivec3_int = sub {
        my ($p, $volSize) = @_;
        $p = $rt->copy($p, 'int');
        my ($wrapped);
        $wrapped = $rt->construct(3, $rt->binary('%', $rt->binary('+', $rt->swizzle($p, 'x'), $volSize, 1, 'int'), $volSize, 1, 'int'), $rt->binary('%', $rt->binary('+', $rt->swizzle($p, 'y'), $volSize, 1, 'int'), $volSize, 1, 'int'), $rt->binary('%', $rt->binary('+', $rt->swizzle($p, 'z'), $volSize, 1, 'int'), $volSize, 1, 'int'), 'int');
        return $rt->construct(2, $rt->swizzle($wrapped, 'x'), $rt->binary('+', $rt->swizzle($wrapped, 'y'), $rt->binary('*', $rt->swizzle($wrapped, 'z'), $volSize, 1, 'int'), 1, 'int'), 'int');
    };
    $sampleState__ivec3_int = sub {
        my ($voxel, $volSize) = @_;
        $voxel = $rt->copy($voxel, 'int');
        return $rt->texel_fetch($_u_stateTex, $atlasTexel__ivec3_int->($voxel, $volSize), $rt->i(0));
    };
    $sampleSeed__ivec3_int = sub {
        my ($voxel, $volSize) = @_;
        $voxel = $rt->copy($voxel, 'int');
        return $rt->texel_fetch($_u_seedTex, $atlasTexel__ivec3_int->($voxel, $volSize), $rt->i(0));
    };
    $countMooreNeighbors__ivec3_int = sub {
        my ($voxel, $volSize) = @_;
        $voxel = $rt->copy($voxel, 'int');
        my ($_for0_first, $_for1_first, $_for2_first, $count, $dx, $dy, $dz, $neighbor);
        $count = $rt->i(0);
        $dz = $rt->unary('-', $rt->i(1));
        $_for0_first = 1;
        for my $_for0 (0 .. 1048575) {
            if (!$_for0_first) {
                $dz = $rt->binary('+', $dz, $rt->i(1), 1, 'int');
            }
            $_for0_first = 0;
            if (!($rt->binary('<=', $dz, $rt->i(1)))) {
                last;
            }
            $dy = $rt->unary('-', $rt->i(1));
            $_for1_first = 1;
            for my $_for1 (0 .. 1048575) {
                if (!$_for1_first) {
                    $dy = $rt->binary('+', $dy, $rt->i(1), 1, 'int');
                }
                $_for1_first = 0;
                if (!($rt->binary('<=', $dy, $rt->i(1)))) {
                    last;
                }
                $dx = $rt->unary('-', $rt->i(1));
                $_for2_first = 1;
                for my $_for2 (0 .. 1048575) {
                    if (!$_for2_first) {
                        $dx = $rt->binary('+', $dx, $rt->i(1), 1, 'int');
                    }
                    $_for2_first = 0;
                    if (!($rt->binary('<=', $dx, $rt->i(1)))) {
                        last;
                    }
                    if ((((($rt->binary('==', $dx, $rt->i(0))) && ($rt->binary('==', $dy, $rt->i(0))) ? 1 : 0)) && ($rt->binary('==', $dz, $rt->i(0))) ? 1 : 0)) {
                        next;
                    }
                    $neighbor = $sampleState__ivec3_int->($rt->binary('+', $voxel, $rt->construct(3, $dx, $dy, $dz, 'int'), 3, 'int'), $volSize);
                    if ($rt->binary('>', $rt->swizzle($neighbor, 'r'), $rt->f(0.5))) {
                        $count = $rt->binary('+', $count, $rt->i(1), 1, 'int');
                    }
                }
            }
        }
        return $count;
    };
    $countVonNeumannNeighbors__ivec3_int = sub {
        my ($voxel, $volSize) = @_;
        $voxel = $rt->copy($voxel, 'int');
        my ($count, $xn, $xp, $yn, $yp, $zn, $zp);
        $count = $rt->i(0);
        $xp = $sampleState__ivec3_int->($rt->binary('+', $voxel, $rt->construct(3, $rt->i(1), $rt->i(0), $rt->i(0), 'int'), 3, 'int'), $volSize);
        $xn = $sampleState__ivec3_int->($rt->binary('+', $voxel, $rt->construct(3, $rt->unary('-', $rt->i(1)), $rt->i(0), $rt->i(0), 'int'), 3, 'int'), $volSize);
        $yp = $sampleState__ivec3_int->($rt->binary('+', $voxel, $rt->construct(3, $rt->i(0), $rt->i(1), $rt->i(0), 'int'), 3, 'int'), $volSize);
        $yn = $sampleState__ivec3_int->($rt->binary('+', $voxel, $rt->construct(3, $rt->i(0), $rt->unary('-', $rt->i(1)), $rt->i(0), 'int'), 3, 'int'), $volSize);
        $zp = $sampleState__ivec3_int->($rt->binary('+', $voxel, $rt->construct(3, $rt->i(0), $rt->i(0), $rt->i(1), 'int'), 3, 'int'), $volSize);
        $zn = $sampleState__ivec3_int->($rt->binary('+', $voxel, $rt->construct(3, $rt->i(0), $rt->i(0), $rt->unary('-', $rt->i(1)), 'int'), 3, 'int'), $volSize);
        if ($rt->binary('>', $rt->swizzle($xp, 'r'), $rt->f(0.5))) {
            $count = $rt->binary('+', $count, $rt->i(1), 1, 'int');
        }
        if ($rt->binary('>', $rt->swizzle($xn, 'r'), $rt->f(0.5))) {
            $count = $rt->binary('+', $count, $rt->i(1), 1, 'int');
        }
        if ($rt->binary('>', $rt->swizzle($yp, 'r'), $rt->f(0.5))) {
            $count = $rt->binary('+', $count, $rt->i(1), 1, 'int');
        }
        if ($rt->binary('>', $rt->swizzle($yn, 'r'), $rt->f(0.5))) {
            $count = $rt->binary('+', $count, $rt->i(1), 1, 'int');
        }
        if ($rt->binary('>', $rt->swizzle($zp, 'r'), $rt->f(0.5))) {
            $count = $rt->binary('+', $count, $rt->i(1), 1, 'int');
        }
        if ($rt->binary('>', $rt->swizzle($zn, 'r'), $rt->f(0.5))) {
            $count = $rt->binary('+', $count, $rt->i(1), 1, 'int');
        }
        return $count;
    };
    $shouldBeBorn__int_int = sub {
        my ($n, $rule) = @_;
        if ($rt->binary('==', $rule, $rt->i(0))) {
            return $rt->binary('==', $n, $rt->i(4));
        }
        if ($rt->binary('==', $rule, $rt->i(1))) {
            return (($rt->binary('>=', $n, $rt->i(6))) && ($rt->binary('<=', $n, $rt->i(8))) ? 1 : 0);
        }
        if ($rt->binary('==', $rule, $rt->i(2))) {
            return $rt->binary('>=', $n, $rt->i(9));
        }
        if ($rt->binary('==', $rule, $rt->i(3))) {
            return (((((($rt->binary('==', $n, $rt->i(4))) || ($rt->binary('==', $n, $rt->i(6))) ? 1 : 0)) || ($rt->binary('==', $n, $rt->i(8))) ? 1 : 0)) || ($rt->binary('==', $n, $rt->i(9))) ? 1 : 0);
        }
        if ($rt->binary('==', $rule, $rt->i(4))) {
            return $rt->binary('==', $n, $rt->i(3));
        }
        if ($rt->binary('==', $rule, $rt->i(5))) {
            return $rt->binary('>=', $n, $rt->i(13));
        }
        if ($rt->binary('==', $rule, $rt->i(6))) {
            return (($rt->binary('==', $n, $rt->i(1))) || ($rt->binary('==', $n, $rt->i(3))) ? 1 : 0);
        }
        if ($rt->binary('==', $rule, $rt->i(7))) {
            return (((($rt->binary('>=', $n, $rt->i(5))) && ($rt->binary('<=', $n, $rt->i(7))) ? 1 : 0)) || ($rt->binary('==', $n, $rt->i(12))) ? 1 : 0);
        }
        if ($rt->binary('==', $rule, $rt->i(8))) {
            return (($rt->binary('>=', $n, $rt->i(4))) && ($rt->binary('<=', $n, $rt->i(7))) ? 1 : 0);
        }
        if ($rt->binary('==', $rule, $rt->i(9))) {
            return $rt->binary('==', $n, $rt->i(4));
        }
        if ($rt->binary('==', $rule, $rt->i(10))) {
            return (($rt->binary('>=', $n, $rt->i(5))) && ($rt->binary('<=', $n, $rt->i(8))) ? 1 : 0);
        }
        return 0;
    };
    $shouldSurvive__int_int = sub {
        my ($n, $rule) = @_;
        if ($rt->binary('==', $rule, $rt->i(0))) {
            return $rt->binary('==', $n, $rt->i(4));
        }
        if ($rt->binary('==', $rule, $rt->i(1))) {
            return (($rt->binary('>=', $n, $rt->i(6))) && ($rt->binary('<=', $n, $rt->i(8))) ? 1 : 0);
        }
        if ($rt->binary('==', $rule, $rt->i(2))) {
            return (((((((($rt->binary('>=', $n, $rt->i(5))) && ($rt->binary('<=', $n, $rt->i(7))) ? 1 : 0)) || ($rt->binary('==', $n, $rt->i(12))) ? 1 : 0)) || ($rt->binary('==', $n, $rt->i(13))) ? 1 : 0)) || ($rt->binary('==', $n, $rt->i(15))) ? 1 : 0);
        }
        if ($rt->binary('==', $rule, $rt->i(3))) {
            return (((($rt->binary('>=', $n, $rt->i(3))) && ($rt->binary('<=', $n, $rt->i(6))) ? 1 : 0)) || ($rt->binary('==', $n, $rt->i(9))) ? 1 : 0);
        }
        if ($rt->binary('==', $rule, $rt->i(4))) {
            return (($rt->binary('==', $n, $rt->i(2))) || ($rt->binary('==', $n, $rt->i(3))) ? 1 : 0);
        }
        if ($rt->binary('==', $rule, $rt->i(5))) {
            return $rt->binary('>=', $n, $rt->i(13));
        }
        if ($rt->binary('==', $rule, $rt->i(6))) {
            return (((($rt->binary('==', $n, $rt->i(1))) || ($rt->binary('==', $n, $rt->i(2))) ? 1 : 0)) || ($rt->binary('==', $n, $rt->i(4))) ? 1 : 0);
        }
        if ($rt->binary('==', $rule, $rt->i(7))) {
            return (($rt->binary('>=', $n, $rt->i(5))) && ($rt->binary('<=', $n, $rt->i(8))) ? 1 : 0);
        }
        if ($rt->binary('==', $rule, $rt->i(8))) {
            return (($rt->binary('>=', $n, $rt->i(6))) && ($rt->binary('<=', $n, $rt->i(8))) ? 1 : 0);
        }
        if ($rt->binary('==', $rule, $rt->i(9))) {
            return (($rt->binary('==', $n, $rt->i(3))) || ($rt->binary('==', $n, $rt->i(4))) ? 1 : 0);
        }
        if ($rt->binary('==', $rule, $rt->i(10))) {
            return (((($rt->binary('==', $n, $rt->i(5))) || ($rt->binary('==', $n, $rt->i(6))) ? 1 : 0)) || ($rt->binary('==', $n, $rt->i(9))) ? 1 : 0);
        }
        return 0;
    };
    $main__void = sub {
        my ($age, $alive, $animSpeed, $bufferIsEmpty, $center, $dist, $finalAge, $finalAlive, $h, $hasSeedInput, $lum, $neighbors, $newAge, $newAlive, $p, $pixelCoord, $radius, $seedLum, $seedVal, $state, $threshold, $volSize, $volSizeF, $voxel, $x, $y, $z);
        $volSize = $_u_volumeSize;
        $volSizeF = $rt->construct(1, $volSize);
        $pixelCoord = $rt->construct(2, $rt->swizzle($ctx->{frag_coord}, 'xy'), 'int');
        $x = $rt->swizzle($pixelCoord, 'x');
        $y = $rt->binary('%', $rt->swizzle($pixelCoord, 'y'), $volSize, 1, 'int');
        $z = $rt->binary('/', $rt->swizzle($pixelCoord, 'y'), $volSize, 1, 'int');
        $voxel = $rt->construct(3, $x, $y, $z, 'int');
        if ((((($rt->binary('>=', $x, $volSize)) || ($rt->binary('>=', $y, $volSize)) ? 1 : 0)) || ($rt->binary('>=', $z, $volSize)) ? 1 : 0)) {
            @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $rt->f(0)))};
            return;
        }
        $state = $sampleState__ivec3_int->($voxel, $volSize);
        $alive = $rt->swizzle($state, 'r');
        $age = $rt->swizzle($state, 'g');
        $bufferIsEmpty = (((((($rt->binary('==', $rt->swizzle($state, 'r'), $rt->f(0))) && ($rt->binary('==', $rt->swizzle($state, 'g'), $rt->f(0))) ? 1 : 0)) && ($rt->binary('==', $rt->swizzle($state, 'b'), $rt->f(0))) ? 1 : 0)) && ($rt->binary('==', $rt->swizzle($state, 'a'), $rt->f(0))) ? 1 : 0);
        $hasSeedInput = 0;
        $seedVal = $rt->construct(4, 0.0);
        if ((($bufferIsEmpty) || ($_u_resetState) ? 1 : 0)) {
            $seedVal = $sampleSeed__ivec3_int->($voxel, $volSize);
            $hasSeedInput = (((($rt->binary('>', $rt->swizzle($seedVal, 'r'), $rt->f(0))) || ($rt->binary('>', $rt->swizzle($seedVal, 'g'), $rt->f(0))) ? 1 : 0)) || ($rt->binary('>', $rt->swizzle($seedVal, 'b'), $rt->f(0))) ? 1 : 0);
            $center = $rt->construct(3, 0.0);
            $dist = $rt->f(0.0);
            $h = $rt->f(0.0);
            $lum = $rt->f(0.0);
            $p = $rt->construct(3, 0.0);
            $radius = $rt->f(0.0);
            $threshold = $rt->f(0.0);
            if ($hasSeedInput) {
                $lum = $rt->binary('+', $rt->binary('+', $rt->binary('*', $rt->f(0.29899999999999999), $rt->swizzle($seedVal, 'r'), 1, 'float'), $rt->binary('*', $rt->f(0.58699999999999997), $rt->swizzle($seedVal, 'g'), 1, 'float'), 1, 'float'), $rt->binary('*', $rt->f(0.114), $rt->swizzle($seedVal, 'b'), 1, 'float'), 1, 'float');
                $alive = (($rt->binary('>', $lum, $rt->f(0.5))) ? ($rt->f(1)) : ($rt->f(0)));
                $age = $rt->f(0);
            } else {
                $p = $rt->construct(3, $rt->construct(1, $x), $rt->construct(1, $y), $rt->construct(1, $z));
                $h = $hash3__vec3->($p);
                $threshold = $rt->binary('*', $_u_density, $rt->f(0.01), 1, 'float');
                $center = $rt->construct(3, $rt->binary('*', $volSizeF, $rt->f(0.5), 1, 'float'));
                $dist = $rt->length($rt->binary('-', $p, $center, 3, 'float'));
                $radius = $rt->binary('*', $volSizeF, $rt->f(0.14999999999999999), 1, 'float');
                if ((($rt->binary('<', $h, $threshold)) || ($rt->binary('<', $dist, $radius)) ? 1 : 0)) {
                    $alive = $rt->f(1);
                    $age = $rt->f(0);
                } else {
                    $alive = $rt->f(0);
                    $age = $rt->f(0);
                }
            }
            @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $alive, $alive, $alive, $rt->f(1)))};
            return;
        }
        $neighbors = 0;
        if ($rt->binary('==', $_u_neighborMode, $rt->i(0))) {
            $neighbors = $countMooreNeighbors__ivec3_int->($voxel, $volSize);
        } else {
            $neighbors = $countVonNeumannNeighbors__ivec3_int->($voxel, $volSize);
        }
        $newAlive = $rt->f(0);
        $newAge = $age;
        if ($rt->binary('>', $alive, $rt->f(0.5))) {
            if ($shouldSurvive__int_int->($neighbors, $_u_ruleIndex)) {
                $newAlive = $rt->f(1);
                $newAge = $rt->component_wise('min', $rt->binary('+', $age, $rt->f(0.01), 1, 'float'), $rt->f(1));
            } else {
                $newAlive = $rt->f(0);
                $newAge = $rt->f(0);
            }
        } else {
            if ($shouldBeBorn__int_int->($neighbors, $_u_ruleIndex)) {
                $newAlive = $rt->f(1);
                $newAge = $rt->f(0);
            } else {
                $newAlive = $rt->f(0);
                $newAge = $rt->f(0);
            }
        }
        $animSpeed = $rt->binary('*', $_u_speed, $rt->f(0.01), 1, 'float');
        $finalAlive = $rt->component_wise('mix', $alive, $newAlive, $animSpeed);
        $finalAge = $rt->component_wise('mix', $age, $newAge, $animSpeed);
        $seedLum = $rt->f(0.0);
        if ($rt->binary('>', $_u_weight, $rt->f(0))) {
            $seedVal = $sampleSeed__ivec3_int->($voxel, $volSize);
            $seedLum = $rt->binary('+', $rt->binary('+', $rt->binary('*', $rt->f(0.29899999999999999), $rt->swizzle($seedVal, 'r'), 1, 'float'), $rt->binary('*', $rt->f(0.58699999999999997), $rt->swizzle($seedVal, 'g'), 1, 'float'), 1, 'float'), $rt->binary('*', $rt->f(0.114), $rt->swizzle($seedVal, 'b'), 1, 'float'), 1, 'float');
            $finalAlive = $rt->component_wise('mix', $finalAlive, $seedLum, $rt->binary('*', $_u_weight, $rt->f(0.01), 1, 'float'));
        }
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $finalAlive, $finalAlive, $finalAlive, $rt->f(1)))};
    };
    $main__void->();
    my $_c0 = $g->{fragColor};
    $out->[0] = $rt->f32($_c0->[0]); $out->[1] = $rt->f32($_c0->[1]); $out->[2] = $rt->f32($_c0->[2]); $out->[3] = $rt->f32($_c0->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0, output_names => ['fragColor'] };
