# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($main__void);
    my $_retc;
    my $_u_resolution = exists $U->{'resolution'} ? $U->{'resolution'} : $rt->construct(2, 0.0);
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_aspect = exists $U->{'aspect'} ? $U->{'aspect'} : $rt->f(0.0);
    my $_u_scaleX = exists $U->{'scaleX'} ? $U->{'scaleX'} : $rt->f(0.0);
    my $_u_scaleY = exists $U->{'scaleY'} ? $U->{'scaleY'} : $rt->f(0.0);
    my $_u_centerX = exists $U->{'centerX'} ? $U->{'centerX'} : $rt->f(0.0);
    my $_u_centerY = exists $U->{'centerY'} ? $U->{'centerY'} : $rt->f(0.0);
    my $_u_wrap = exists $U->{'wrap'} ? $U->{'wrap'} : 0;
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    $g->{fragColor} = $rt->construct(4, 0.0);
    $main__void = sub {
        my ($c, $globalCoord, $localUV, $st);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $st = $rt->binary('/', $globalCoord, $_u_fullResolution, 2, 'float');
        $c = $rt->construct(2, $rt->unary('-', $_u_centerX), $_u_centerY);
        @{$st} = map { $rt->f32($_) } @{($rt->binary('-', $st, $c, 2, 'float'))};
        $st = $rt->assign_swizzle($st, 'x', $rt->binary('*', $rt->swizzle($st, 'x'), $_u_aspect, 1, 'float'));
        @{$st} = map { $rt->f32($_) } @{($rt->binary('/', $st, $rt->construct(2, $_u_scaleX, $_u_scaleY), 2, 'float'))};
        $st = $rt->assign_swizzle($st, 'x', $rt->binary('/', $rt->swizzle($st, 'x'), $_u_aspect, 1, 'float'));
        @{$st} = map { $rt->f32($_) } @{($rt->binary('+', $st, $c, 2, 'float'))};
        $localUV = $rt->binary('/', $rt->binary('-', $rt->binary('*', $st, $_u_fullResolution, 2, 'float'), $_u_tileOffset, 2, 'float'), $_u_resolution, 2, 'float');
        if ($rt->binary('==', $_u_wrap, $rt->i(0))) {
            @{$localUV} = map { $rt->f32($_) } @{($rt->component_wise('abs', $rt->binary('-', $rt->component_wise('mod', $rt->binary('+', $localUV, $rt->f(1), 2, 'float'), $rt->f(2)), $rt->f(1), 2, 'float')))};
        } else {
            if ($rt->binary('==', $_u_wrap, $rt->i(1))) {
                @{$localUV} = map { $rt->f32($_) } @{($rt->component_wise('fract', $localUV))};
            } else {
                @{$localUV} = map { $rt->f32($_) } @{($rt->component_wise('clamp', $localUV, $rt->f(0), $rt->f(1)))};
            }
        }
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $rt->swizzle($rt->texture($_u_inputTex, $localUV), 'rgb'), $rt->f(1)))};
    };
    $main__void->();
    my $_c = $g->{fragColor};
    $out->[0] = $rt->f32($_c->[0]); $out->[1] = $rt->f32($_c->[1]); $out->[2] = $rt->f32($_c->[2]); $out->[3] = $rt->f32($_c->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0 };
