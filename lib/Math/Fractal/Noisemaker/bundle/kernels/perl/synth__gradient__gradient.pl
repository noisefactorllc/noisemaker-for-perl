# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($rotate2D__vec2_float, $getColor__int, $blendColors__float, $pcg__uvec3, $prng__vec3, $hash2D__vec2, $valueNoise__vec2, $fbmNoise__vec2, $main__void);
    my $_retc;
    my $_u_resolution = exists $U->{'resolution'} ? $U->{'resolution'} : $rt->construct(2, 0.0);
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_gradientType = exists $U->{'gradientType'} ? $U->{'gradientType'} : 0;
    my $_u_rotation = exists $U->{'rotation'} ? $U->{'rotation'} : $rt->f(0.0);
    my $_u_repeat = exists $U->{'repeat'} ? $U->{'repeat'} : 0;
    my $_u_colorCount = exists $U->{'colorCount'} ? $U->{'colorCount'} : 0;
    my $_u_color1 = exists $U->{'color1'} ? $U->{'color1'} : $rt->construct(3, 0.0);
    my $_u_color2 = exists $U->{'color2'} ? $U->{'color2'} : $rt->construct(3, 0.0);
    my $_u_color3 = exists $U->{'color3'} ? $U->{'color3'} : $rt->construct(3, 0.0);
    my $_u_color4 = exists $U->{'color4'} ? $U->{'color4'} : $rt->construct(3, 0.0);
    my $_u_seed = exists $U->{'seed'} ? $U->{'seed'} : 0;
    my $_u_time = exists $U->{'time'} ? $U->{'time'} : $rt->f(0.0);
    my $_u_speed = exists $U->{'speed'} ? $U->{'speed'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $rotate2D__vec2_float = sub {
        my ($st, $angle) = @_;
        $st = $rt->copy($st, 'float');
        my ($aspectRatio, $c, $s);
        $aspectRatio = $rt->binary('/', $rt->swizzle($_u_fullResolution, 'x'), $rt->swizzle($_u_fullResolution, 'y'), 1, 'float');
        $st = $rt->assign_swizzle($st, 'x', $rt->binary('*', $rt->swizzle($st, 'x'), $aspectRatio, 1, 'float'));
        @{$st} = map { $rt->f32($_) } @{($rt->binary('-', $st, $rt->construct(2, $rt->binary('*', $aspectRatio, $rt->f(0.5), 1, 'float'), $rt->f(0.5)), 2, 'float'))};
        $c = $rt->component_wise('cos', $angle);
        $s = $rt->component_wise('sin', $angle);
        @{$st} = map { $rt->f32($_) } @{($rt->matrix_mult($rt->construct(4, $c, $rt->unary('-', $s), $s, $c), $st, 2))};
        @{$st} = map { $rt->f32($_) } @{($rt->binary('+', $st, $rt->construct(2, $rt->binary('*', $aspectRatio, $rt->f(0.5), 1, 'float'), $rt->f(0.5)), 2, 'float'))};
        $st = $rt->assign_swizzle($st, 'x', $rt->binary('/', $rt->swizzle($st, 'x'), $aspectRatio, 1, 'float'));
        return $st;
    };
    $getColor__int = sub {
        my ($idx) = @_;
        if ($rt->binary('==', $idx, $rt->i(0))) {
            return $_u_color1;
        }
        if ($rt->binary('==', $idx, $rt->i(1))) {
            return $_u_color2;
        }
        if ($rt->binary('==', $idx, $rt->i(2))) {
            return $_u_color3;
        }
        return $_u_color4;
    };
    $blendColors__float = sub {
        my ($t) = @_;
        my ($idx, $localT, $next, $segment);
        $t = $rt->component_wise('fract', $t);
        $segment = $rt->binary('*', $t, $rt->construct(1, $_u_colorCount), 1, 'float');
        $idx = $rt->construct(1, $rt->component_wise('floor', $segment), 'int');
        $localT = $rt->component_wise('fract', $segment);
        $next = $rt->binary('+', $idx, $rt->i(1), 1, 'int');
        if ($rt->binary('>=', $next, $_u_colorCount)) {
            $next = $rt->i(0);
        }
        return $rt->component_wise('mix', $getColor__int->($idx), $getColor__int->($next), $localT);
    };
    $pcg__uvec3 = sub {
        my ($v) = @_;
        $v = $rt->copy($v, 'uint');
        @{$v} = @{($rt->binary('+', $rt->binary('*', $v, $rt->construct(1, $rt->i(1664525), 'uint'), 3, 'uint'), $rt->construct(1, $rt->i(1013904223), 'uint'), 3, 'uint'))};
        $v = $rt->assign_swizzle($v, 'x', $rt->binary('+', $rt->swizzle($v, 'x'), $rt->binary('*', $rt->swizzle($v, 'y'), $rt->swizzle($v, 'z'), 1, 'uint'), 1, 'uint'));
        $v = $rt->assign_swizzle($v, 'y', $rt->binary('+', $rt->swizzle($v, 'y'), $rt->binary('*', $rt->swizzle($v, 'z'), $rt->swizzle($v, 'x'), 1, 'uint'), 1, 'uint'));
        $v = $rt->assign_swizzle($v, 'z', $rt->binary('+', $rt->swizzle($v, 'z'), $rt->binary('*', $rt->swizzle($v, 'x'), $rt->swizzle($v, 'y'), 1, 'uint'), 1, 'uint'));
        @{$v} = @{($rt->binary('^', $v, $rt->binary('>>', $v, $rt->construct(1, $rt->i(16), 'uint'), 3, 'uint'), 3, 'uint'))};
        $v = $rt->assign_swizzle($v, 'x', $rt->binary('+', $rt->swizzle($v, 'x'), $rt->binary('*', $rt->swizzle($v, 'y'), $rt->swizzle($v, 'z'), 1, 'uint'), 1, 'uint'));
        $v = $rt->assign_swizzle($v, 'y', $rt->binary('+', $rt->swizzle($v, 'y'), $rt->binary('*', $rt->swizzle($v, 'z'), $rt->swizzle($v, 'x'), 1, 'uint'), 1, 'uint'));
        $v = $rt->assign_swizzle($v, 'z', $rt->binary('+', $rt->swizzle($v, 'z'), $rt->binary('*', $rt->swizzle($v, 'x'), $rt->swizzle($v, 'y'), 1, 'uint'), 1, 'uint'));
        return $v;
    };
    $prng__vec3 = sub {
        my ($p) = @_;
        $p = $rt->copy($p, 'float');
        $p = $rt->assign_swizzle($p, 'x', (($rt->binary('>=', $rt->swizzle($p, 'x'), $rt->f(0))) ? ($rt->binary('*', $rt->swizzle($p, 'x'), $rt->f(2), 1, 'float')) : ($rt->binary('+', $rt->binary('*', $rt->unary('-', $rt->swizzle($p, 'x')), $rt->f(2), 1, 'float'), $rt->f(1), 1, 'float'))));
        $p = $rt->assign_swizzle($p, 'y', (($rt->binary('>=', $rt->swizzle($p, 'y'), $rt->f(0))) ? ($rt->binary('*', $rt->swizzle($p, 'y'), $rt->f(2), 1, 'float')) : ($rt->binary('+', $rt->binary('*', $rt->unary('-', $rt->swizzle($p, 'y')), $rt->f(2), 1, 'float'), $rt->f(1), 1, 'float'))));
        $p = $rt->assign_swizzle($p, 'z', (($rt->binary('>=', $rt->swizzle($p, 'z'), $rt->f(0))) ? ($rt->binary('*', $rt->swizzle($p, 'z'), $rt->f(2), 1, 'float')) : ($rt->binary('+', $rt->binary('*', $rt->unary('-', $rt->swizzle($p, 'z')), $rt->f(2), 1, 'float'), $rt->f(1), 1, 'float'))));
        return $rt->binary('/', $rt->construct(3, $pcg__uvec3->($rt->construct(3, $p, 'uint'))), $rt->construct(1, $rt->construct(1, $rt->i(4294967295), 'uint')), 3, 'float');
    };
    $hash2D__vec2 = sub {
        my ($p) = @_;
        $p = $rt->copy($p, 'float');
        return $rt->swizzle($prng__vec3->($rt->construct(3, $p, $rt->construct(1, $_u_seed))), 'x');
    };
    $valueNoise__vec2 = sub {
        my ($p) = @_;
        $p = $rt->copy($p, 'float');
        my ($a, $b, $c, $d, $f, $i, $u);
        $i = $rt->component_wise('floor', $p);
        $f = $rt->component_wise('fract', $p);
        $u = $rt->binary('*', $rt->binary('*', $f, $f, 2, 'float'), $rt->binary('-', $rt->f(3), $rt->binary('*', $rt->f(2), $f, 2, 'float'), 2, 'float'), 2, 'float');
        $a = $hash2D__vec2->($i);
        $b = $hash2D__vec2->($rt->binary('+', $i, $rt->construct(2, $rt->f(1), $rt->f(0)), 2, 'float'));
        $c = $hash2D__vec2->($rt->binary('+', $i, $rt->construct(2, $rt->f(0), $rt->f(1)), 2, 'float'));
        $d = $hash2D__vec2->($rt->binary('+', $i, $rt->construct(2, $rt->f(1), $rt->f(1)), 2, 'float'));
        return $rt->component_wise('mix', $rt->component_wise('mix', $a, $b, $rt->swizzle($u, 'x')), $rt->component_wise('mix', $c, $d, $rt->swizzle($u, 'x')), $rt->swizzle($u, 'y'));
    };
    $fbmNoise__vec2 = sub {
        my ($p) = @_;
        $p = $rt->copy($p, 'float');
        my ($_for0_first, $amp, $freq, $i, $maxVal, $sum);
        $sum = $rt->f(0);
        $amp = $rt->f(0.5);
        $freq = $rt->f(1);
        $maxVal = $rt->f(0);
        $i = $rt->i(0);
        $_for0_first = 1;
        for my $_for0 (0 .. 1048575) {
            if (!$_for0_first) {
                $i = $rt->binary('+', $i, $rt->i(1), 1, 'int');
            }
            $_for0_first = 0;
            if (!($rt->binary('<', $i, $rt->i(4)))) {
                last;
            }
            $sum = $rt->binary('+', $sum, $rt->binary('*', $valueNoise__vec2->($rt->binary('*', $p, $freq, 2, 'float')), $amp, 1, 'float'), 1, 'float');
            $maxVal = $rt->binary('+', $maxVal, $amp, 1, 'float');
            $freq = $rt->binary('*', $freq, $rt->f(2), 1, 'float');
            $amp = $rt->binary('*', $amp, $rt->f(0.5), 1, 'float');
        }
        return $rt->binary('/', $sum, $maxVal, 1, 'float');
    };
    $main__void = sub {
        my ($a, $angle, $aspectRatio, $bottom, $c, $cBL, $cBR, $cTL, $cTR, $centered, $color, $cornerSt, $dist, $globalCoord, $noiseSt, $rotatedCentered, $rotatedPoint, $rotatedSt, $s, $st, $t, $timeOffset, $top);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $st = $rt->binary('/', $globalCoord, $_u_fullResolution, 2, 'float');
        $aspectRatio = $rt->binary('/', $rt->swizzle($_u_fullResolution, 'x'), $rt->swizzle($_u_fullResolution, 'y'), 1, 'float');
        $angle = $rt->binary('/', $rt->binary('*', $rt->unary('-', $_u_rotation), $rt->f(3.1415926535900001), 1, 'float'), $rt->f(180), 1, 'float');
        $rotatedSt = $rotate2D__vec2_float->($st, $angle);
        $centered = $rt->binary('-', $st, $rt->f(0.5), 2, 'float');
        $centered = $rt->assign_swizzle($centered, 'x', $rt->binary('*', $rt->swizzle($centered, 'x'), $aspectRatio, 1, 'float'));
        $rotatedCentered = $centered;
        $c = $rt->component_wise('cos', $angle);
        $s = $rt->component_wise('sin', $angle);
        @{$rotatedCentered} = map { $rt->f32($_) } @{($rt->matrix_mult($rt->construct(4, $c, $rt->unary('-', $s), $s, $c), $centered, 2))};
        $color = $rt->construct(3, 0.0);
        $t = $rt->f(0.0);
        $timeOffset = $rt->binary('*', $_u_time, $_u_speed, 1, 'float');
        $a = $rt->f(0.0);
        $bottom = $rt->construct(3, 0.0);
        $cBL = $rt->construct(3, 0.0);
        $cBR = $rt->construct(3, 0.0);
        $cTL = $rt->construct(3, 0.0);
        $cTR = $rt->construct(3, 0.0);
        $cornerSt = $rt->construct(2, 0.0);
        $dist = $rt->f(0.0);
        $noiseSt = $rt->construct(2, 0.0);
        $rotatedPoint = $rt->construct(2, 0.0);
        $top = $rt->construct(3, 0.0);
        if ($rt->binary('==', $_u_gradientType, $rt->i(0))) {
            $a = $rt->component_wise('atan', $rt->swizzle($rotatedCentered, 'y'), $rt->swizzle($rotatedCentered, 'x'));
            $t = $rt->binary('/', $rt->binary('+', $a, $rt->f(3.1415926535900001), 1, 'float'), $rt->f(6.2831853071800001), 1, 'float');
            $t = $rt->component_wise('fract', $rt->binary('+', $rt->binary('*', $t, $rt->construct(1, $_u_repeat), 1, 'float'), $timeOffset, 1, 'float'));
            @{$color} = map { $rt->f32($_) } @{($blendColors__float->($t))};
        } else {
            if ($rt->binary('==', $_u_gradientType, $rt->i(1))) {
                $t = $rt->binary('+', $rt->component_wise('abs', $rt->swizzle($rotatedCentered, 'x')), $rt->component_wise('abs', $rt->swizzle($rotatedCentered, 'y')), 1, 'float');
                $t = $rt->component_wise('fract', $rt->binary('+', $rt->binary('*', $t, $rt->construct(1, $_u_repeat), 1, 'float'), $timeOffset, 1, 'float'));
                @{$color} = map { $rt->f32($_) } @{($blendColors__float->($t))};
            } else {
                if ($rt->binary('==', $_u_gradientType, $rt->i(2))) {
                    $cornerSt = $rotate2D__vec2_float->($st, $angle);
                    $cTL = $_u_color1;
                    $cTR = (($rt->binary('>=', $_u_colorCount, $rt->i(3))) ? ($_u_color2) : ($_u_color1));
                    $cBL = (($rt->binary('>=', $_u_colorCount, $rt->i(3))) ? ($_u_color3) : ($_u_color2));
                    $cBR = (($rt->binary('>=', $_u_colorCount, $rt->i(4))) ? ($_u_color4) : ($cBL));
                    $top = $rt->component_wise('mix', $cTL, $cTR, $rt->swizzle($cornerSt, 'x'));
                    $bottom = $rt->component_wise('mix', $cBL, $cBR, $rt->swizzle($cornerSt, 'x'));
                    @{$color} = map { $rt->f32($_) } @{($rt->component_wise('mix', $bottom, $top, $rt->swizzle($cornerSt, 'y')))};
                } else {
                    if ($rt->binary('==', $_u_gradientType, $rt->i(3))) {
                        $t = $rt->swizzle($rotatedSt, 'y');
                        $t = $rt->component_wise('fract', $rt->binary('+', $rt->binary('*', $t, $rt->construct(1, $_u_repeat), 1, 'float'), $timeOffset, 1, 'float'));
                        @{$color} = map { $rt->f32($_) } @{($blendColors__float->($t))};
                    } else {
                        if ($rt->binary('==', $_u_gradientType, $rt->i(4))) {
                            $noiseSt = $rt->binary('*', $rotatedCentered, $rt->f(4), 2, 'float');
                            $t = $fbmNoise__vec2->($noiseSt);
                            $t = $rt->component_wise('fract', $rt->binary('+', $rt->binary('*', $t, $rt->construct(1, $_u_repeat), 1, 'float'), $timeOffset, 1, 'float'));
                            @{$color} = map { $rt->f32($_) } @{($blendColors__float->($t))};
                        } else {
                            if ($rt->binary('==', $_u_gradientType, $rt->i(5))) {
                                $rotatedPoint = $rt->matrix_mult($rt->construct(4, $c, $rt->unary('-', $s), $s, $c), $centered, 2);
                                $dist = $rt->binary('*', $rt->length($rotatedPoint), $rt->f(2), 1, 'float');
                                $t = $dist;
                                $t = $rt->component_wise('fract', $rt->binary('+', $rt->binary('*', $t, $rt->construct(1, $_u_repeat), 1, 'float'), $timeOffset, 1, 'float'));
                                @{$color} = map { $rt->f32($_) } @{($blendColors__float->($t))};
                            } else {
                                if ($rt->binary('==', $_u_gradientType, $rt->i(6))) {
                                    $a = $rt->component_wise('atan', $rt->swizzle($rotatedCentered, 'y'), $rt->swizzle($rotatedCentered, 'x'));
                                    $dist = $rt->length($centered);
                                    $t = $rt->component_wise('fract', $rt->binary('+', $rt->binary('/', $a, $rt->f(6.2831853071800001), 1, 'float'), $rt->binary('*', $dist, $rt->f(2), 1, 'float'), 1, 'float'));
                                    $t = $rt->component_wise('fract', $rt->binary('+', $rt->binary('*', $t, $rt->construct(1, $_u_repeat), 1, 'float'), $timeOffset, 1, 'float'));
                                    @{$color} = map { $rt->f32($_) } @{($blendColors__float->($t))};
                                }
                            }
                        }
                    }
                }
            }
        }
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $color, $rt->f(1)))};
    };
    $main__void->();
    my $_c0 = $g->{fragColor};
    $out->[0] = $rt->f32($_c0->[0]); $out->[1] = $rt->f32($_c0->[1]); $out->[2] = $rt->f32($_c0->[2]); $out->[3] = $rt->f32($_c0->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0, output_names => ['fragColor'] };
