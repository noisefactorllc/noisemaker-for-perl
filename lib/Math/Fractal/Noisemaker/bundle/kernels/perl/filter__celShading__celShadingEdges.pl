# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($getLuminosity__vec3, $wrapCoord__int_int, $main__void);
    my $_retc;
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_colorTex = $ctx->texture_binding('colorTex');
    my $_u_edgeWidth = exists $U->{'edgeWidth'} ? $U->{'edgeWidth'} : $rt->f(0.0);
    my $_u_edgeThreshold = exists $U->{'edgeThreshold'} ? $U->{'edgeThreshold'} : $rt->f(0.0);
    my $_u_renderScale = exists $U->{'renderScale'} ? $U->{'renderScale'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $getLuminosity__vec3 = sub {
        my ($color) = @_;
        $color = $rt->copy($color, 'float');
        return $rt->dot($color, $rt->construct(3, $rt->f(0.29899999999999999), $rt->f(0.58699999999999997), $rt->f(0.114)));
    };
    $wrapCoord__int_int = sub {
        my ($value, $size) = @_;
        my ($wrapped);
        if ($rt->binary('<=', $size, $rt->i(0))) {
            return $rt->i(0);
        }
        $wrapped = $rt->binary('%', $value, $size, 1, 'int');
        if ($rt->binary('<', $wrapped, $rt->i(0))) {
            $wrapped = $rt->binary('+', $wrapped, $size, 1, 'int');
        }
        return $wrapped;
    };
    $main__void = sub {
        my ($_for0_first, $_for1_first, $coord, $edge, $globalCoord, $gx, $gy, $idx, $kx, $ky, $magnitude, $offset, $sampleX, $sampleY, $samples, $texSize, $texel);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $texSize = $rt->texture_size($_u_colorTex);
        if ((($rt->binary('==', $rt->swizzle($texSize, 'x'), $rt->i(0))) || ($rt->binary('==', $rt->swizzle($texSize, 'y'), $rt->i(0))) ? 1 : 0)) {
            @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $rt->f(0)))};
            return;
        }
        $coord = $rt->construct(2, $rt->swizzle($ctx->{frag_coord}, 'xy'), 'int');
        $offset = $rt->component_wise('max', $rt->i(1), $rt->construct(1, $rt->binary('*', $_u_edgeWidth, $_u_renderScale, 1, 'float'), 'int'));
        $samples = $rt->new_array($rt->i(9), 1);
        $idx = $rt->i(0);
        $ky = $rt->unary('-', $rt->i(1));
        $_for0_first = 1;
        for my $_for0 (0 .. 1048575) {
            if (!$_for0_first) {
                $ky = $rt->binary('+', $ky, $rt->i(1), 1, 'int');
            }
            $_for0_first = 0;
            if (!($rt->binary('<=', $ky, $rt->i(1)))) {
                last;
            }
            $kx = $rt->unary('-', $rt->i(1));
            $_for1_first = 1;
            for my $_for1 (0 .. 1048575) {
                if (!$_for1_first) {
                    $kx = $rt->binary('+', $kx, $rt->i(1), 1, 'int');
                }
                $_for1_first = 0;
                if (!($rt->binary('<=', $kx, $rt->i(1)))) {
                    last;
                }
                $sampleX = $wrapCoord__int_int->($rt->binary('+', $rt->swizzle($coord, 'x'), $rt->binary('*', $kx, $offset, 1, 'int'), 1, 'int'), $rt->swizzle($texSize, 'x'));
                $sampleY = $wrapCoord__int_int->($rt->binary('+', $rt->swizzle($coord, 'y'), $rt->binary('*', $ky, $offset, 1, 'int'), 1, 'int'), $rt->swizzle($texSize, 'y'));
                $texel = $rt->texel_fetch($_u_colorTex, $rt->construct(2, $sampleX, $sampleY, 'int'), $rt->i(0));
                $samples->[int($idx)] = $getLuminosity__vec3->($rt->swizzle($texel, 'rgb'));
                $idx = $rt->binary('+', $idx, $rt->i(1), 1, 'int');
            }
        }
        $gx = $rt->binary('+', $rt->binary('-', $rt->binary('+', $rt->binary('-', $rt->binary('+', $rt->unary('-', $samples->[int($rt->i(0))]), $samples->[int($rt->i(2))], 1, 'float'), $rt->binary('*', $rt->f(2), $samples->[int($rt->i(3))], 1, 'float'), 1, 'float'), $rt->binary('*', $rt->f(2), $samples->[int($rt->i(5))], 1, 'float'), 1, 'float'), $samples->[int($rt->i(6))], 1, 'float'), $samples->[int($rt->i(8))], 1, 'float');
        $gy = $rt->binary('+', $rt->binary('+', $rt->binary('+', $rt->binary('-', $rt->binary('-', $rt->unary('-', $samples->[int($rt->i(0))]), $rt->binary('*', $rt->f(2), $samples->[int($rt->i(1))], 1, 'float'), 1, 'float'), $samples->[int($rt->i(2))], 1, 'float'), $samples->[int($rt->i(6))], 1, 'float'), $rt->binary('*', $rt->f(2), $samples->[int($rt->i(7))], 1, 'float'), 1, 'float'), $samples->[int($rt->i(8))], 1, 'float');
        $magnitude = $rt->component_wise('sqrt', $rt->binary('+', $rt->binary('*', $gx, $gx, 1, 'float'), $rt->binary('*', $gy, $gy, 1, 'float'), 1, 'float'));
        $edge = $rt->component_wise('smoothstep', $rt->binary('*', $_u_edgeThreshold, $rt->f(0.5), 1, 'float'), $rt->binary('*', $_u_edgeThreshold, $rt->f(1.5), 1, 'float'), $magnitude);
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $edge, $edge, $edge, $rt->f(1)))};
    };
    $main__void->();
    my $_c = $g->{fragColor};
    $out->[0] = $rt->f32($_c->[0]); $out->[1] = $rt->f32($_c->[1]); $out->[2] = $rt->f32($_c->[2]); $out->[3] = $rt->f32($_c->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0 };
