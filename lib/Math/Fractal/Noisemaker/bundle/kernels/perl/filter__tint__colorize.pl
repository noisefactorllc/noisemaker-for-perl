# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($rgb_to_hsv__vec3, $hsv_to_rgb__vec3, $main__void);
    my $_retc;
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_color = exists $U->{'color'} ? $U->{'color'} : $rt->construct(3, 0.0);
    my $_u_alpha = exists $U->{'alpha'} ? $U->{'alpha'} : $rt->f(0.0);
    my $_u_mode = exists $U->{'mode'} ? $U->{'mode'} : $rt->f(0.0);
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    $g->{fragColor} = $rt->construct(4, 0.0);
    $rgb_to_hsv__vec3 = sub {
        my ($rgb) = @_;
        $rgb = $rt->copy($rgb, 'float');
        my ($_g, $b, $delta, $hue, $max_c, $min_c, $r, $raw, $sat);
        $r = $rt->swizzle($rgb, 'x');
        $_g = $rt->swizzle($rgb, 'y');
        $b = $rt->swizzle($rgb, 'z');
        $max_c = $rt->component_wise('max', $rt->component_wise('max', $r, $_g), $b);
        $min_c = $rt->component_wise('min', $rt->component_wise('min', $r, $_g), $b);
        $delta = $rt->binary('-', $max_c, $min_c, 1, 'float');
        $hue = $rt->f(0);
        if ($rt->binary('!=', $delta, $rt->f(0))) {
            $raw = $rt->f(0.0);
            if ($rt->binary('==', $max_c, $r)) {
                $raw = $rt->binary('/', $rt->binary('-', $_g, $b, 1, 'float'), $delta, 1, 'float');
                $raw = $rt->binary('-', $raw, $rt->binary('*', $rt->component_wise('floor', $rt->binary('/', $raw, $rt->f(6), 1, 'float')), $rt->f(6), 1, 'float'), 1, 'float');
                if ($rt->binary('<', $raw, $rt->f(0))) {
                    $raw = $rt->binary('+', $raw, $rt->f(6), 1, 'float');
                }
                $hue = $raw;
            } else {
                if ($rt->binary('==', $max_c, $_g)) {
                    $hue = $rt->binary('+', $rt->binary('/', $rt->binary('-', $b, $r, 1, 'float'), $delta, 1, 'float'), $rt->f(2), 1, 'float');
                } else {
                    $hue = $rt->binary('+', $rt->binary('/', $rt->binary('-', $r, $_g, 1, 'float'), $delta, 1, 'float'), $rt->f(4), 1, 'float');
                }
            }
        }
        $hue = $rt->binary('/', $hue, $rt->f(6), 1, 'float');
        if ($rt->binary('<', $hue, $rt->f(0))) {
            $hue = $rt->binary('+', $hue, $rt->f(1), 1, 'float');
        }
        $sat = (($rt->binary('!=', $max_c, $rt->f(0))) ? ($rt->binary('/', $delta, $max_c, 1, 'float')) : ($rt->f(0)));
        return $rt->construct(3, $hue, $sat, $max_c);
    };
    $hsv_to_rgb__vec3 = sub {
        my ($hsv) = @_;
        $hsv = $rt->copy($hsv, 'float');
        my ($db, $dg, $dh, $dr, $h, $oms, $s, $v);
        $h = $rt->swizzle($hsv, 'x');
        $s = $rt->swizzle($hsv, 'y');
        $v = $rt->swizzle($hsv, 'z');
        $dh = $rt->binary('*', $h, $rt->f(6), 1, 'float');
        $dr = $rt->component_wise('clamp', $rt->binary('-', $rt->component_wise('abs', $rt->binary('-', $dh, $rt->f(3), 1, 'float')), $rt->f(1), 1, 'float'), $rt->f(0), $rt->f(1));
        $dg = $rt->component_wise('clamp', $rt->binary('+', $rt->unary('-', $rt->component_wise('abs', $rt->binary('-', $dh, $rt->f(2), 1, 'float'))), $rt->f(2), 1, 'float'), $rt->f(0), $rt->f(1));
        $db = $rt->component_wise('clamp', $rt->binary('+', $rt->unary('-', $rt->component_wise('abs', $rt->binary('-', $dh, $rt->f(4), 1, 'float'))), $rt->f(2), 1, 'float'), $rt->f(0), $rt->f(1));
        $oms = $rt->binary('-', $rt->f(1), $s, 1, 'float');
        return $rt->construct(3, $rt->binary('*', $rt->binary('+', $oms, $rt->binary('*', $s, $dr, 1, 'float'), 1, 'float'), $v, 1, 'float'), $rt->binary('*', $rt->binary('+', $oms, $rt->binary('*', $s, $dg, 1, 'float'), 1, 'float'), $v, 1, 'float'), $rt->binary('*', $rt->binary('+', $oms, $rt->binary('*', $s, $db, 1, 'float'), 1, 'float'), $v, 1, 'float'));
    };
    $main__void = sub {
        my ($base, $base_hsv, $base_rgb, $globalCoord, $m, $rgb, $st, $tintHue, $tinted);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $st = $rt->binary('/', $rt->swizzle($ctx->{frag_coord}, 'xy'), $rt->construct(2, $rt->component_wise('max', $rt->texture_size($_u_inputTex), $rt->construct(2, $rt->i(1), 'int'))), 2, 'float');
        $base = $rt->texture($_u_inputTex, $st);
        $base_rgb = $rt->component_wise('clamp', $rt->swizzle($base, 'rgb'), $rt->f(0), $rt->f(1));
        $m = $rt->construct(1, $_u_mode, 'int');
        $tinted = $rt->construct(3, 0.0);
        $base_hsv = $rt->construct(3, 0.0);
        $tintHue = $rt->f(0.0);
        if ($rt->binary('==', $m, $rt->i(1))) {
            @{$tinted} = map { $rt->f32($_) } @{($rt->binary('*', $base_rgb, $_u_color, 3, 'float'))};
        } else {
            if ($rt->binary('==', $m, $rt->i(2))) {
                $tintHue = $rt->swizzle($rgb_to_hsv__vec3->($_u_color), 'x');
                $base_hsv = $rgb_to_hsv__vec3->($base_rgb);
                @{$tinted} = map { $rt->f32($_) } @{($rt->component_wise('clamp', $hsv_to_rgb__vec3->($rt->construct(3, $tintHue, $rt->component_wise('clamp', $rt->swizzle($base_rgb, 'y'), $rt->f(0), $rt->f(1)), $rt->component_wise('clamp', $rt->swizzle($base_hsv, 'z'), $rt->f(0), $rt->f(1)))), $rt->f(0), $rt->f(1)))};
            } else {
                @{$tinted} = map { $rt->f32($_) } @{($_u_color)};
            }
        }
        $rgb = $rt->component_wise('mix', $base_rgb, $tinted, $_u_alpha);
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $rgb, $rt->swizzle($base, 'a')))};
    };
    $main__void->();
    my $_c = $g->{fragColor};
    $out->[0] = $rt->f32($_c->[0]); $out->[1] = $rt->f32($_c->[1]); $out->[2] = $rt->f32($_c->[2]); $out->[3] = $rt->f32($_c->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0 };
