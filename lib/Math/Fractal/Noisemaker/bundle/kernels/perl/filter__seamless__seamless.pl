# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($edgeWeight__float_float, $main__void);
    my $_retc;
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_blend = exists $U->{'blend'} ? $U->{'blend'} : $rt->f(0.0);
    my $_u_repeat = exists $U->{'repeat'} ? $U->{'repeat'} : $rt->f(0.0);
    my $_u_curve = exists $U->{'curve'} ? $U->{'curve'} : 0;
    $g->{fragColor} = $rt->construct(4, 0.0);
    $edgeWeight__float_float = sub {
        my ($t, $width) = @_;
        my ($d, $w);
        if ($rt->binary('<=', $width, $rt->f(0))) {
            return $rt->f(0);
        }
        $d = $rt->component_wise('min', $t, $rt->binary('-', $rt->f(1), $t, 1, 'float'));
        $w = $rt->binary('-', $rt->f(1), $rt->component_wise('clamp', $rt->binary('/', $d, $width, 1, 'float'), $rt->f(0), $rt->f(1)), 1, 'float');
        if ($rt->binary('==', $_u_curve, $rt->i(0))) {
            return $w;
        } else {
            if ($rt->binary('==', $_u_curve, $rt->i(2))) {
                return $rt->binary('*', $w, $w, 1, 'float');
            }
        }
        return $rt->binary('*', $rt->binary('*', $w, $w, 1, 'float'), $rt->binary('-', $rt->f(3), $rt->binary('*', $rt->f(2), $w, 1, 'float'), 1, 'float'), 1, 'float');
    };
    $main__void = sub {
        my ($c00, $c01, $c10, $c11, $mx0, $mx1, $result, $st, $texSize, $uv, $wx, $wy);
        $texSize = $rt->texture_size($_u_inputTex);
        $uv = $rt->binary('/', $rt->swizzle($ctx->{frag_coord}, 'xy'), $rt->construct(2, $texSize), 2, 'float');
        $st = $rt->binary('*', $uv, $_u_repeat, 2, 'float');
        @{$st} = map { $rt->f32($_) } @{($rt->component_wise('fract', $st))};
        $wx = $edgeWeight__float_float->($rt->swizzle($st, 'x'), $_u_blend);
        $wy = $edgeWeight__float_float->($rt->swizzle($st, 'y'), $_u_blend);
        $c00 = $rt->texture($_u_inputTex, $st);
        $c10 = $rt->texture($_u_inputTex, $rt->component_wise('fract', $rt->binary('+', $st, $rt->construct(2, $rt->f(0.5), $rt->f(0)), 2, 'float')));
        $c01 = $rt->texture($_u_inputTex, $rt->component_wise('fract', $rt->binary('+', $st, $rt->construct(2, $rt->f(0), $rt->f(0.5)), 2, 'float')));
        $c11 = $rt->texture($_u_inputTex, $rt->component_wise('fract', $rt->binary('+', $st, $rt->construct(2, $rt->f(0.5), $rt->f(0.5)), 2, 'float')));
        $mx0 = $rt->component_wise('mix', $c00, $c10, $wx);
        $mx1 = $rt->component_wise('mix', $c01, $c11, $wx);
        $result = $rt->component_wise('mix', $mx0, $mx1, $wy);
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $rt->swizzle($result, 'rgb'), $rt->f(1)))};
    };
    $main__void->();
    my $_c0 = $g->{fragColor};
    $out->[0] = $rt->f32($_c0->[0]); $out->[1] = $rt->f32($_c0->[1]); $out->[2] = $rt->f32($_c0->[2]); $out->[3] = $rt->f32($_c0->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0, output_names => ['fragColor'] };
