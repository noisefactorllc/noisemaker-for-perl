# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($main__void);
    my $_retc;
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_accumTex = $ctx->texture_binding('accumTex');
    my $_u_resolution = exists $U->{'resolution'} ? $U->{'resolution'} : $rt->construct(2, 0.0);
    my $_u_alpha = exists $U->{'alpha'} ? $U->{'alpha'} : $rt->f(0.0);
    my $_u_intensity = exists $U->{'intensity'} ? $U->{'intensity'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $main__void = sub {
        my ($a, $accum, $blended, $i, $inputColor, $result, $st);
        $st = $rt->binary('/', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_resolution, 2, 'float');
        $inputColor = $rt->texture($_u_inputTex, $st);
        $accum = $rt->texture($_u_accumTex, $st);
        $a = $rt->binary('/', $_u_alpha, $rt->f(100), 1, 'float');
        $i = $rt->binary('/', $_u_intensity, $rt->f(100), 1, 'float');
        $blended = $rt->component_wise('max', $inputColor, $rt->binary('*', $accum, $i, 4, 'float'));
        $result = $rt->component_wise('mix', $inputColor, $blended, $a);
        $result = $rt->assign_swizzle($result, 'a', $rt->component_wise('max', $rt->swizzle($inputColor, 'a'), $rt->swizzle($accum, 'a')));
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($result)};
    };
    $main__void->();
    my $_c0 = $g->{fragColor};
    $out->[0] = $rt->f32($_c0->[0]); $out->[1] = $rt->f32($_c0->[1]); $out->[2] = $rt->f32($_c0->[2]); $out->[3] = $rt->f32($_c0->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0, output_names => ['fragColor'] };
