# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($hash_uint__uint, $hash__uint, $noise2D__vec2, $fbm__vec2, $main__void);
    my $_retc;
    my $_u_resolution = exists $U->{'resolution'} ? $U->{'resolution'} : $rt->construct(2, 0.0);
    my $_u_time = exists $U->{'time'} ? $U->{'time'} : $rt->f(0.0);
    my $_u_gravity = exists $U->{'gravity'} ? $U->{'gravity'} : $rt->f(0.0);
    my $_u_wind = exists $U->{'wind'} ? $U->{'wind'} : $rt->f(0.0);
    my $_u_energy = exists $U->{'energy'} ? $U->{'energy'} : $rt->f(0.0);
    my $_u_drag = exists $U->{'drag'} ? $U->{'drag'} : $rt->f(0.0);
    my $_u_deviation = exists $U->{'deviation'} ? $U->{'deviation'} : $rt->f(0.0);
    my $_u_wander = exists $U->{'wander'} ? $U->{'wander'} : $rt->f(0.0);
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_xyzTex = $ctx->texture_binding('xyzTex');
    my $_u_velTex = $ctx->texture_binding('velTex');
    my $_u_rgbaTex = $ctx->texture_binding('rgbaTex');
    $g->{outXYZ} = $rt->construct(4, 0.0);
    $g->{outVel} = $rt->construct(4, 0.0);
    $g->{outRGBA} = $rt->construct(4, 0.0);
    $hash_uint__uint = sub {
        my ($seed) = @_;
        my ($state, $word);
        $state = $rt->binary('+', $rt->binary('*', $seed, $rt->i(747796405), 1, 'uint'), $rt->i(2891336453), 1, 'uint');
        $word = $rt->binary('*', $rt->binary('^', $rt->binary('>>', $state, $rt->binary('+', $rt->binary('>>', $state, $rt->i(28), 1, 'uint'), $rt->i(4), 1, 'uint'), 1, 'uint'), $state, 1, 'uint'), $rt->i(277803737), 1, 'uint');
        return $rt->binary('^', $rt->binary('>>', $word, $rt->i(22), 1, 'uint'), $word, 1, 'uint');
    };
    $hash__uint = sub {
        my ($seed) = @_;
        return $rt->binary('/', $rt->construct(1, $rt->hash_uint($seed)), $rt->f(4294967295), 1, 'float');
    };
    $noise2D__vec2 = sub {
        my ($p) = @_;
        $p = $rt->copy($p, 'float');
        my ($a, $b, $c, $d, $f, $i, $n);
        $i = $rt->component_wise('floor', $p);
        $f = $rt->component_wise('fract', $p);
        @{$f} = map { $rt->f32($_) } @{($rt->binary('*', $rt->binary('*', $f, $f, 2, 'float'), $rt->binary('-', $rt->f(3), $rt->binary('*', $rt->f(2), $f, 2, 'float'), 2, 'float'), 2, 'float'))};
        $n = $rt->binary('+', $rt->construct(1, $rt->swizzle($i, 'x'), 'uint'), $rt->binary('*', $rt->construct(1, $rt->swizzle($i, 'y'), 'uint'), $rt->i(57), 1, 'uint'), 1, 'uint');
        $a = $hash__uint->($n);
        $b = $hash__uint->($rt->binary('+', $n, $rt->i(1), 1, 'uint'));
        $c = $hash__uint->($rt->binary('+', $n, $rt->i(57), 1, 'uint'));
        $d = $hash__uint->($rt->binary('+', $n, $rt->i(58), 1, 'uint'));
        return $rt->component_wise('mix', $rt->component_wise('mix', $a, $b, $rt->swizzle($f, 'x')), $rt->component_wise('mix', $c, $d, $rt->swizzle($f, 'x')), $rt->swizzle($f, 'y'));
    };
    $fbm__vec2 = sub {
        my ($p) = @_;
        $p = $rt->copy($p, 'float');
        my ($_for0_first, $a, $i, $v);
        $v = $rt->f(0);
        $a = $rt->f(0.5);
        $i = $rt->i(0);
        $_for0_first = 1;
        for my $_for0 (0 .. 1048575) {
            if (!$_for0_first) {
                $i = $rt->binary('+', $i, $rt->i(1), 1, 'int');
            }
            $_for0_first = 0;
            if (!($rt->binary('<', $i, $rt->i(3)))) {
                last;
            }
            $v = $rt->binary('+', $v, $rt->binary('*', $a, $noise2D__vec2->($p), 1, 'float'), 1, 'float');
            @{$p} = map { $rt->f32($_) } @{($rt->binary('*', $p, $rt->f(2), 2, 'float'))};
            $a = $rt->binary('*', $a, $rt->f(0.5), 1, 'float');
        }
        return $v;
    };
    $main__void = sub {
        my ($alive, $ax, $ay, $coord, $deviationMultiplier, $dragFactor, $needsRespawn, $noiseScale, $px, $py, $pz, $rgba, $seed_f, $stateSize, $vel, $vx, $vy, $vz, $wanderAngle, $wanderStrength, $wanderX, $wanderY, $xyz);
        $coord = $rt->construct(2, $rt->swizzle($ctx->{frag_coord}, 'xy'), 'int');
        $stateSize = $rt->texture_size($_u_xyzTex);
        $xyz = $rt->texel_fetch($_u_xyzTex, $coord, $rt->i(0));
        $vel = $rt->texel_fetch($_u_velTex, $coord, $rt->i(0));
        $rgba = $rt->texel_fetch($_u_rgbaTex, $coord, $rt->i(0));
        $px = $rt->swizzle($xyz, 'x');
        $py = $rt->swizzle($xyz, 'y');
        $pz = $rt->swizzle($xyz, 'z');
        $alive = $rt->swizzle($xyz, 'w');
        $vx = $rt->swizzle($vel, 'x');
        $vy = $rt->swizzle($vel, 'y');
        $vz = $rt->swizzle($vel, 'z');
        $seed_f = $rt->swizzle($vel, 'w');
        if ($rt->binary('<', $alive, $rt->f(0.5))) {
            @{$g->{outXYZ}} = map { $rt->f32($_) } @{($xyz)};
            @{$g->{outVel}} = map { $rt->f32($_) } @{($vel)};
            @{$g->{outRGBA}} = map { $rt->f32($_) } @{($rgba)};
            return;
        }
        $deviationMultiplier = $rt->binary('+', $rt->f(1), $rt->binary('*', $rt->binary('*', $rt->binary('-', $seed_f, $rt->f(0.5), 1, 'float'), $_u_deviation, 1, 'float'), $rt->f(2), 1, 'float'), 1, 'float');
        $noiseScale = $rt->f(2);
        $wanderAngle = $rt->binary('*', $rt->binary('*', $fbm__vec2->($rt->binary('+', $rt->binary('*', $rt->construct(2, $px, $py), $noiseScale, 2, 'float'), $rt->binary('*', $_u_time, $rt->f(0.5), 1, 'float'), 2, 'float')), $rt->f(6.2831849999999996), 1, 'float'), $rt->f(2), 1, 'float');
        $wanderStrength = $rt->binary('*', $_u_wander, $rt->f(0.002), 1, 'float');
        $wanderX = $rt->binary('*', $rt->component_wise('cos', $wanderAngle), $wanderStrength, 1, 'float');
        $wanderY = $rt->binary('*', $rt->component_wise('sin', $wanderAngle), $wanderStrength, 1, 'float');
        $ax = $rt->binary('*', $rt->binary('+', $rt->binary('*', $_u_wind, $rt->f(0.01), 1, 'float'), $wanderX, 1, 'float'), $_u_energy, 1, 'float');
        $ay = $rt->binary('*', $rt->binary('+', $rt->binary('*', $rt->unary('-', $_u_gravity), $rt->f(0.01), 1, 'float'), $wanderY, 1, 'float'), $_u_energy, 1, 'float');
        $vx = $rt->binary('+', $vx, $rt->binary('*', $ax, $deviationMultiplier, 1, 'float'), 1, 'float');
        $vy = $rt->binary('+', $vy, $rt->binary('*', $ay, $deviationMultiplier, 1, 'float'), 1, 'float');
        $dragFactor = $rt->binary('-', $rt->f(1), $_u_drag, 1, 'float');
        $vx = $rt->binary('*', $vx, $dragFactor, 1, 'float');
        $vy = $rt->binary('*', $vy, $dragFactor, 1, 'float');
        $px = $rt->binary('+', $px, $vx, 1, 'float');
        $py = $rt->binary('+', $py, $vy, 1, 'float');
        $needsRespawn = 0;
        if ((((((($rt->binary('<', $px, $rt->f(0))) || ($rt->binary('>', $px, $rt->f(1))) ? 1 : 0)) || ($rt->binary('<', $py, $rt->f(0))) ? 1 : 0)) || ($rt->binary('>', $py, $rt->f(1))) ? 1 : 0)) {
            $needsRespawn = 1;
        }
        if ($needsRespawn) {
            @{$g->{outXYZ}} = map { $rt->f32($_) } @{($rt->construct(4, $px, $py, $pz, $rt->f(0)))};
            @{$g->{outVel}} = map { $rt->f32($_) } @{($rt->construct(4, $vx, $vy, $vz, $seed_f))};
            @{$g->{outRGBA}} = map { $rt->f32($_) } @{($rgba)};
        } else {
            @{$g->{outXYZ}} = map { $rt->f32($_) } @{($rt->construct(4, $px, $py, $pz, $rt->f(1)))};
            @{$g->{outVel}} = map { $rt->f32($_) } @{($rt->construct(4, $vx, $vy, $vz, $seed_f))};
            @{$g->{outRGBA}} = map { $rt->f32($_) } @{($rgba)};
        }
    };
    $main__void->();
    my $_c0 = $g->{outXYZ};
    $out->[0] = $rt->f32($_c0->[0]); $out->[1] = $rt->f32($_c0->[1]); $out->[2] = $rt->f32($_c0->[2]); $out->[3] = $rt->f32($_c0->[3]);
    my $_c1 = $g->{outVel};
    $out->[4] = $rt->f32($_c1->[0]); $out->[5] = $rt->f32($_c1->[1]); $out->[6] = $rt->f32($_c1->[2]); $out->[7] = $rt->f32($_c1->[3]);
    my $_c2 = $g->{outRGBA};
    $out->[8] = $rt->f32($_c2->[0]); $out->[9] = $rt->f32($_c2->[1]); $out->[10] = $rt->f32($_c2->[2]); $out->[11] = $rt->f32($_c2->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0, output_names => ['outXYZ', 'outVel', 'outRGBA'] };
