# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($main__void);
    my $_retc;
    my $_u_MODE = exists $U->{'MODE'} ? $U->{'MODE'} : 0;
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_size = exists $U->{'size'} ? $U->{'size'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $main__void = sub {
        my ($_for0_first, $_for1_first, $bestC, $bestV, $c, $cc, $d, $dims, $fr, $frSq, $icenter, $m, $m0, $m1, $m2, $m3, $m4, $m5, $m6, $m7, $n0, $n1, $n2, $n3, $n4, $n5, $n6, $n7, $q0, $q1, $q2, $q3, $q4, $q5, $q6, $q7, $radius, $sampleLimit, $sc, $tv, $v, $x, $y);
        $icenter = $rt->construct(2, $rt->swizzle($ctx->{frag_coord}, 'xy'), 'int');
        $dims = $rt->texture_size($_u_inputTex);
        $radius = $rt->f(0.0);
        if ($rt->binary('==', $_u_MODE, $rt->i(0))) {
            $radius = $rt->component_wise('min', $_u_size, $rt->f(3));
        } else {
            $radius = $_u_size;
        }
        $fr = $rt->component_wise('clamp', $radius, $rt->f(1), $rt->f(12));
        $frSq = $rt->binary('*', $fr, $fr, 1, 'float');
        $sampleLimit = $rt->construct(1, $rt->component_wise('ceil', $fr), 'int');
        $m0 = $rt->construct(3, $rt->f(0));
        $m1 = $rt->construct(3, $rt->f(0));
        $m2 = $rt->construct(3, $rt->f(0));
        $m3 = $rt->construct(3, $rt->f(0));
        $m4 = $rt->construct(3, $rt->f(0));
        $m5 = $rt->construct(3, $rt->f(0));
        $m6 = $rt->construct(3, $rt->f(0));
        $m7 = $rt->construct(3, $rt->f(0));
        $q0 = $rt->construct(3, $rt->f(0));
        $q1 = $rt->construct(3, $rt->f(0));
        $q2 = $rt->construct(3, $rt->f(0));
        $q3 = $rt->construct(3, $rt->f(0));
        $q4 = $rt->construct(3, $rt->f(0));
        $q5 = $rt->construct(3, $rt->f(0));
        $q6 = $rt->construct(3, $rt->f(0));
        $q7 = $rt->construct(3, $rt->f(0));
        $n0 = $rt->f(0);
        $n1 = $rt->f(0);
        $n2 = $rt->f(0);
        $n3 = $rt->f(0);
        $n4 = $rt->f(0);
        $n5 = $rt->f(0);
        $n6 = $rt->f(0);
        $n7 = $rt->f(0);
        $y = $rt->unary('-', $sampleLimit);
        $_for0_first = 1;
        for my $_for0 (0 .. 1048575) {
            if (!$_for0_first) {
                $y = $rt->binary('+', $y, $rt->i(1), 1, 'int');
            }
            $_for0_first = 0;
            if (!($rt->binary('<=', $y, $sampleLimit))) {
                last;
            }
            $x = $rt->unary('-', $sampleLimit);
            $_for1_first = 1;
            for my $_for1 (0 .. 1048575) {
                if (!$_for1_first) {
                    $x = $rt->binary('+', $x, $rt->i(1), 1, 'int');
                }
                $_for1_first = 0;
                if (!($rt->binary('<=', $x, $sampleLimit))) {
                    last;
                }
                $d = $rt->construct(2, $rt->construct(1, $x), $rt->construct(1, $y));
                if ((((($rt->binary('>', $rt->component_wise('abs', $rt->swizzle($d, 'x')), $fr)) || ($rt->binary('>', $rt->component_wise('abs', $rt->swizzle($d, 'y')), $fr)) ? 1 : 0)) || ($rt->binary('>', $rt->dot($d, $d), $frSq)) ? 1 : 0)) {
                    next;
                }
                if ((((($rt->binary('>', $fr, $rt->f(8))) && ($rt->binary('>', $rt->dot($d, $d), $rt->f(64))) ? 1 : 0)) && ($rt->binary('!=', $rt->binary('%', $rt->binary('+', $rt->component_wise('abs', $x), $rt->component_wise('abs', $y), 1, 'int'), $rt->i(2), 1, 'int'), $rt->i(0))) ? 1 : 0)) {
                    next;
                }
                $sc = $rt->component_wise('clamp', $rt->binary('+', $icenter, $rt->construct(2, $x, $y, 'int'), 2, 'int'), $rt->construct(2, $rt->i(0), 'int'), $rt->binary('-', $dims, $rt->construct(2, $rt->i(1), 'int'), 2, 'int'));
                $c = $rt->swizzle($rt->texel_fetch($_u_inputTex, $sc, $rt->i(0)), 'rgb');
                $cc = $rt->binary('*', $c, $c, 3, 'float');
                if ((($rt->binary('==', $x, $rt->i(0))) && ($rt->binary('==', $y, $rt->i(0))) ? 1 : 0)) {
                    @{$m4} = map { $rt->f32($_) } @{($rt->binary('+', $m4, $c, 3, 'float'))};
                    @{$q4} = map { $rt->f32($_) } @{($rt->binary('+', $q4, $cc, 3, 'float'))};
                    $n4 = $rt->binary('+', $n4, $rt->f(1), 1, 'float');
                } else {
                    if ((($rt->binary('>', $rt->swizzle($d, 'x'), $rt->f(0))) && ($rt->binary('>=', $rt->swizzle($d, 'y'), $rt->f(0))) ? 1 : 0)) {
                        if ($rt->binary('<=', $rt->component_wise('abs', $rt->swizzle($d, 'x')), $rt->component_wise('abs', $rt->swizzle($d, 'y')))) {
                            @{$m5} = map { $rt->f32($_) } @{($rt->binary('+', $m5, $c, 3, 'float'))};
                            @{$q5} = map { $rt->f32($_) } @{($rt->binary('+', $q5, $cc, 3, 'float'))};
                            $n5 = $rt->binary('+', $n5, $rt->f(1), 1, 'float');
                        } else {
                            @{$m4} = map { $rt->f32($_) } @{($rt->binary('+', $m4, $c, 3, 'float'))};
                            @{$q4} = map { $rt->f32($_) } @{($rt->binary('+', $q4, $cc, 3, 'float'))};
                            $n4 = $rt->binary('+', $n4, $rt->f(1), 1, 'float');
                        }
                    } else {
                        if ((($rt->binary('<=', $rt->swizzle($d, 'x'), $rt->f(0))) && ($rt->binary('>', $rt->swizzle($d, 'y'), $rt->f(0))) ? 1 : 0)) {
                            if ($rt->binary('<', $rt->component_wise('abs', $rt->swizzle($d, 'x')), $rt->component_wise('abs', $rt->swizzle($d, 'y')))) {
                                @{$m6} = map { $rt->f32($_) } @{($rt->binary('+', $m6, $c, 3, 'float'))};
                                @{$q6} = map { $rt->f32($_) } @{($rt->binary('+', $q6, $cc, 3, 'float'))};
                                $n6 = $rt->binary('+', $n6, $rt->f(1), 1, 'float');
                            } else {
                                @{$m7} = map { $rt->f32($_) } @{($rt->binary('+', $m7, $c, 3, 'float'))};
                                @{$q7} = map { $rt->f32($_) } @{($rt->binary('+', $q7, $cc, 3, 'float'))};
                                $n7 = $rt->binary('+', $n7, $rt->f(1), 1, 'float');
                            }
                        } else {
                            if ((($rt->binary('<', $rt->swizzle($d, 'x'), $rt->f(0))) && ($rt->binary('<=', $rt->swizzle($d, 'y'), $rt->f(0))) ? 1 : 0)) {
                                if ($rt->binary('<=', $rt->component_wise('abs', $rt->swizzle($d, 'x')), $rt->component_wise('abs', $rt->swizzle($d, 'y')))) {
                                    @{$m1} = map { $rt->f32($_) } @{($rt->binary('+', $m1, $c, 3, 'float'))};
                                    @{$q1} = map { $rt->f32($_) } @{($rt->binary('+', $q1, $cc, 3, 'float'))};
                                    $n1 = $rt->binary('+', $n1, $rt->f(1), 1, 'float');
                                } else {
                                    @{$m0} = map { $rt->f32($_) } @{($rt->binary('+', $m0, $c, 3, 'float'))};
                                    @{$q0} = map { $rt->f32($_) } @{($rt->binary('+', $q0, $cc, 3, 'float'))};
                                    $n0 = $rt->binary('+', $n0, $rt->f(1), 1, 'float');
                                }
                            } else {
                                if ($rt->binary('<', $rt->component_wise('abs', $rt->swizzle($d, 'x')), $rt->component_wise('abs', $rt->swizzle($d, 'y')))) {
                                    @{$m2} = map { $rt->f32($_) } @{($rt->binary('+', $m2, $c, 3, 'float'))};
                                    @{$q2} = map { $rt->f32($_) } @{($rt->binary('+', $q2, $cc, 3, 'float'))};
                                    $n2 = $rt->binary('+', $n2, $rt->f(1), 1, 'float');
                                } else {
                                    @{$m3} = map { $rt->f32($_) } @{($rt->binary('+', $m3, $c, 3, 'float'))};
                                    @{$q3} = map { $rt->f32($_) } @{($rt->binary('+', $q3, $cc, 3, 'float'))};
                                    $n3 = $rt->binary('+', $n3, $rt->f(1), 1, 'float');
                                }
                            }
                        }
                    }
                }
            }
        }
        $bestC = $rt->construct(3, $rt->f(0));
        $bestV = $rt->f(1000000000);
        $m = $rt->construct(3, 0.0);
        $tv = $rt->f(0.0);
        $v = $rt->construct(3, 0.0);
        if ($rt->binary('>=', $n0, $rt->f(1))) {
            $m = $rt->binary('/', $m0, $n0, 3, 'float');
            $v = $rt->binary('-', $rt->binary('/', $q0, $n0, 3, 'float'), $rt->binary('*', $m, $m, 3, 'float'), 3, 'float');
            $tv = $rt->binary('+', $rt->binary('+', $rt->swizzle($v, 'r'), $rt->swizzle($v, 'g'), 1, 'float'), $rt->swizzle($v, 'b'), 1, 'float');
            if ($rt->binary('<', $tv, $bestV)) {
                $bestV = $tv;
                @{$bestC} = map { $rt->f32($_) } @{($m)};
            }
        }
        if ($rt->binary('>=', $n1, $rt->f(1))) {
            $m = $rt->binary('/', $m1, $n1, 3, 'float');
            $v = $rt->binary('-', $rt->binary('/', $q1, $n1, 3, 'float'), $rt->binary('*', $m, $m, 3, 'float'), 3, 'float');
            $tv = $rt->binary('+', $rt->binary('+', $rt->swizzle($v, 'r'), $rt->swizzle($v, 'g'), 1, 'float'), $rt->swizzle($v, 'b'), 1, 'float');
            if ($rt->binary('<', $tv, $bestV)) {
                $bestV = $tv;
                @{$bestC} = map { $rt->f32($_) } @{($m)};
            }
        }
        if ($rt->binary('>=', $n2, $rt->f(1))) {
            $m = $rt->binary('/', $m2, $n2, 3, 'float');
            $v = $rt->binary('-', $rt->binary('/', $q2, $n2, 3, 'float'), $rt->binary('*', $m, $m, 3, 'float'), 3, 'float');
            $tv = $rt->binary('+', $rt->binary('+', $rt->swizzle($v, 'r'), $rt->swizzle($v, 'g'), 1, 'float'), $rt->swizzle($v, 'b'), 1, 'float');
            if ($rt->binary('<', $tv, $bestV)) {
                $bestV = $tv;
                @{$bestC} = map { $rt->f32($_) } @{($m)};
            }
        }
        if ($rt->binary('>=', $n3, $rt->f(1))) {
            $m = $rt->binary('/', $m3, $n3, 3, 'float');
            $v = $rt->binary('-', $rt->binary('/', $q3, $n3, 3, 'float'), $rt->binary('*', $m, $m, 3, 'float'), 3, 'float');
            $tv = $rt->binary('+', $rt->binary('+', $rt->swizzle($v, 'r'), $rt->swizzle($v, 'g'), 1, 'float'), $rt->swizzle($v, 'b'), 1, 'float');
            if ($rt->binary('<', $tv, $bestV)) {
                $bestV = $tv;
                @{$bestC} = map { $rt->f32($_) } @{($m)};
            }
        }
        if ($rt->binary('>=', $n4, $rt->f(1))) {
            $m = $rt->binary('/', $m4, $n4, 3, 'float');
            $v = $rt->binary('-', $rt->binary('/', $q4, $n4, 3, 'float'), $rt->binary('*', $m, $m, 3, 'float'), 3, 'float');
            $tv = $rt->binary('+', $rt->binary('+', $rt->swizzle($v, 'r'), $rt->swizzle($v, 'g'), 1, 'float'), $rt->swizzle($v, 'b'), 1, 'float');
            if ($rt->binary('<', $tv, $bestV)) {
                $bestV = $tv;
                @{$bestC} = map { $rt->f32($_) } @{($m)};
            }
        }
        if ($rt->binary('>=', $n5, $rt->f(1))) {
            $m = $rt->binary('/', $m5, $n5, 3, 'float');
            $v = $rt->binary('-', $rt->binary('/', $q5, $n5, 3, 'float'), $rt->binary('*', $m, $m, 3, 'float'), 3, 'float');
            $tv = $rt->binary('+', $rt->binary('+', $rt->swizzle($v, 'r'), $rt->swizzle($v, 'g'), 1, 'float'), $rt->swizzle($v, 'b'), 1, 'float');
            if ($rt->binary('<', $tv, $bestV)) {
                $bestV = $tv;
                @{$bestC} = map { $rt->f32($_) } @{($m)};
            }
        }
        if ($rt->binary('>=', $n6, $rt->f(1))) {
            $m = $rt->binary('/', $m6, $n6, 3, 'float');
            $v = $rt->binary('-', $rt->binary('/', $q6, $n6, 3, 'float'), $rt->binary('*', $m, $m, 3, 'float'), 3, 'float');
            $tv = $rt->binary('+', $rt->binary('+', $rt->swizzle($v, 'r'), $rt->swizzle($v, 'g'), 1, 'float'), $rt->swizzle($v, 'b'), 1, 'float');
            if ($rt->binary('<', $tv, $bestV)) {
                $bestV = $tv;
                @{$bestC} = map { $rt->f32($_) } @{($m)};
            }
        }
        if ($rt->binary('>=', $n7, $rt->f(1))) {
            $m = $rt->binary('/', $m7, $n7, 3, 'float');
            $v = $rt->binary('-', $rt->binary('/', $q7, $n7, 3, 'float'), $rt->binary('*', $m, $m, 3, 'float'), 3, 'float');
            $tv = $rt->binary('+', $rt->binary('+', $rt->swizzle($v, 'r'), $rt->swizzle($v, 'g'), 1, 'float'), $rt->swizzle($v, 'b'), 1, 'float');
            if ($rt->binary('<', $tv, $bestV)) {
                $bestV = $tv;
                @{$bestC} = map { $rt->f32($_) } @{($m)};
            }
        }
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $bestC, $rt->f(1)))};
    };
    $main__void->();
    my $_c = $g->{fragColor};
    $out->[0] = $rt->f32($_c->[0]); $out->[1] = $rt->f32($_c->[1]); $out->[2] = $rt->f32($_c->[2]); $out->[3] = $rt->f32($_c->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0 };
