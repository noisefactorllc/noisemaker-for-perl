# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($main__void);
    my $_retc;
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_textTex = $ctx->texture_binding('textTex');
    my $_u_resolution = exists $U->{'resolution'} ? $U->{'resolution'} : $rt->construct(2, 0.0);
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_matteColor = exists $U->{'matteColor'} ? $U->{'matteColor'} : $rt->construct(3, 0.0);
    my $_u_matteOpacity = exists $U->{'matteOpacity'} ? $U->{'matteOpacity'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $main__void = sub {
        my ($alpha, $globalCoord, $inputColor, $matteAlpha, $rgb, $st, $text, $textPresence);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $st = $rt->binary('/', $globalCoord, $_u_fullResolution, 2, 'float');
        $inputColor = $rt->texture($_u_inputTex, $rt->binary('/', $rt->swizzle($ctx->{frag_coord}, 'xy'), $rt->construct(2, $rt->texture_size($_u_inputTex)), 2, 'float'));
        $text = $rt->texture($_u_textTex, $st);
        $textPresence = $rt->swizzle($text, 'a');
        $matteAlpha = $_u_matteOpacity;
        $rgb = $rt->binary('+', $rt->binary('+', $rt->binary('*', $rt->swizzle($text, 'rgb'), $textPresence, 3, 'float'), $rt->binary('*', $rt->binary('*', $rt->swizzle($inputColor, 'rgb'), $rt->binary('-', $rt->f(1), $textPresence, 1, 'float'), 3, 'float'), $rt->binary('-', $rt->f(1), $matteAlpha, 1, 'float'), 3, 'float'), 3, 'float'), $rt->binary('*', $rt->binary('*', $_u_matteColor, $matteAlpha, 3, 'float'), $rt->binary('-', $rt->f(1), $textPresence, 1, 'float'), 3, 'float'), 3, 'float');
        $alpha = $rt->component_wise('max', $textPresence, $rt->component_wise('mix', $rt->swizzle($inputColor, 'a'), $rt->f(1), $matteAlpha));
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $rgb, $alpha))};
    };
    $main__void->();
    my $_c0 = $g->{fragColor};
    $out->[0] = $rt->f32($_c0->[0]); $out->[1] = $rt->f32($_c0->[1]); $out->[2] = $rt->f32($_c0->[2]); $out->[3] = $rt->f32($_c0->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0, output_names => ['fragColor'] };
