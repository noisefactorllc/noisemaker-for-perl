# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($lum__vec3, $tonemap2__float_vec3_vec3, $rgbToCmyk__vec3, $rotate2D__vec2_float, $boxBlur3__vec2_vec2, $cellSampleFromRuv__vec2_float_vec2, $halftoneCoverage__float_float_float, $roundDotCoverage__vec2_float_float, $main__void);
    my $_retc;
    my $_u_MODE = exists $U->{'MODE'} ? $U->{'MODE'} : 0;
    my $_u_PATTERN = exists $U->{'PATTERN'} ? $U->{'PATTERN'} : 0;
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_resolution = exists $U->{'resolution'} ? $U->{'resolution'} : $rt->construct(2, 0.0);
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_frequency = exists $U->{'frequency'} ? $U->{'frequency'} : $rt->f(0.0);
    my $_u_cyanAngle = exists $U->{'cyanAngle'} ? $U->{'cyanAngle'} : $rt->f(0.0);
    my $_u_magentaAngle = exists $U->{'magentaAngle'} ? $U->{'magentaAngle'} : $rt->f(0.0);
    my $_u_yellowAngle = exists $U->{'yellowAngle'} ? $U->{'yellowAngle'} : $rt->f(0.0);
    my $_u_blackAngle = exists $U->{'blackAngle'} ? $U->{'blackAngle'} : $rt->f(0.0);
    my $_u_monoAngle = exists $U->{'monoAngle'} ? $U->{'monoAngle'} : $rt->f(0.0);
    my $_u_sharpness = exists $U->{'sharpness'} ? $U->{'sharpness'} : $rt->f(0.0);
    my $_u_inkColor = exists $U->{'inkColor'} ? $U->{'inkColor'} : $rt->construct(3, 0.0);
    my $_u_paperColor = exists $U->{'paperColor'} ? $U->{'paperColor'} : $rt->construct(3, 0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $g->{DOT_AREA_CAP} = $rt->f(0.5);
    $g->{PI} = $rt->f(3.1415926535897931);
    $g->{MID_DOT_RADIUS} = $rt->f(0.39894227999999998);
    $g->{MAX_DOT_RADIUS} = $rt->f(0.47999999999999998);
    $lum__vec3 = sub {
        my ($c) = @_;
        $c = $rt->copy($c, 'float');
        return $rt->dot($c, $rt->construct(3, $rt->f(0.21260000000000001), $rt->f(0.71519999999999995), $rt->f(0.0722)));
    };
    $tonemap2__float_vec3_vec3 = sub {
        my ($t, $ink, $paper) = @_;
        $ink = $rt->copy($ink, 'float');
        $paper = $rt->copy($paper, 'float');
        return $rt->component_wise('mix', $ink, $paper, $rt->component_wise('clamp', $t, $rt->f(0), $rt->f(1)));
    };
    $rgbToCmyk__vec3 = sub {
        my ($rgb) = @_;
        $rgb = $rt->copy($rgb, 'float');
        my ($cmy, $k, $scale);
        $k = $rt->binary('-', $rt->f(1), $rt->component_wise('max', $rt->component_wise('max', $rt->swizzle($rgb, 'r'), $rt->swizzle($rgb, 'g')), $rt->swizzle($rgb, 'b')), 1, 'float');
        $scale = $rt->component_wise('max', $rt->binary('-', $rt->f(1), $k, 1, 'float'), $rt->f(1.0000000000000001e-05));
        $cmy = $rt->component_wise('clamp', $rt->binary('/', $rt->binary('-', $rt->binary('-', $rt->f(1), $rgb, 3, 'float'), $rt->construct(3, $k), 3, 'float'), $scale, 3, 'float'), $rt->f(0), $rt->f(1));
        return $rt->construct(4, $cmy, $k);
    };
    $rotate2D__vec2_float = sub {
        my ($v, $angleDeg) = @_;
        $v = $rt->copy($v, 'float');
        my ($a, $co, $si);
        $a = $rt->component_wise('radians', $angleDeg);
        $co = $rt->component_wise('cos', $a);
        $si = $rt->component_wise('sin', $a);
        return $rt->matrix_mult($rt->construct(4, $co, $rt->unary('-', $si), $si, $co), $v, 2);
    };
    $boxBlur3__vec2_vec2 = sub {
        my ($uv, $texel) = @_;
        $uv = $rt->copy($uv, 'float');
        $texel = $rt->copy($texel, 'float');
        my ($_for0_first, $_for1_first, $o, $sum, $x, $y);
        $sum = $rt->construct(3, $rt->f(0));
        $y = $rt->unary('-', $rt->i(1));
        $_for0_first = 1;
        for my $_for0 (0 .. 1048575) {
            if (!$_for0_first) {
                $y = $rt->binary('+', $y, $rt->i(1), 1, 'int');
            }
            $_for0_first = 0;
            if (!($rt->binary('<=', $y, $rt->i(1)))) {
                last;
            }
            $x = $rt->unary('-', $rt->i(1));
            $_for1_first = 1;
            for my $_for1 (0 .. 1048575) {
                if (!$_for1_first) {
                    $x = $rt->binary('+', $x, $rt->i(1), 1, 'int');
                }
                $_for1_first = 0;
                if (!($rt->binary('<=', $x, $rt->i(1)))) {
                    last;
                }
                $o = $rt->binary('*', $rt->construct(2, $rt->construct(1, $x), $rt->construct(1, $y)), $texel, 2, 'float');
                @{$sum} = map { $rt->f32($_) } @{($rt->binary('+', $sum, $rt->swizzle($rt->texture($_u_inputTex, $rt->component_wise('clamp', $rt->binary('+', $uv, $o, 2, 'float'), $rt->f(0), $rt->f(1))), 'rgb'), 3, 'float'))};
            }
        }
        return $rt->binary('/', $sum, $rt->f(9), 3, 'float');
    };
    $cellSampleFromRuv__vec2_float_vec2 = sub {
        my ($ruv, $angleDeg, $texel) = @_;
        $ruv = $rt->copy($ruv, 'float');
        $texel = $rt->copy($texel, 'float');
        my ($cellCenterGc, $cellId, $cellUV);
        $cellId = $rt->binary('+', $rt->component_wise('floor', $ruv), $rt->f(0.5), 2, 'float');
        $cellCenterGc = $rotate2D__vec2_float->($rt->binary('*', $cellId, $_u_frequency, 2, 'float'), $rt->unary('-', $angleDeg));
        $cellUV = $rt->component_wise('clamp', $rt->binary('/', $rt->binary('-', $cellCenterGc, $_u_tileOffset, 2, 'float'), $_u_resolution, 2, 'float'), $rt->f(0), $rt->f(1));
        return $boxBlur3__vec2_vec2->($cellUV, $texel);
    };
    $halftoneCoverage__float_float_float = sub {
        my ($d, $value, $sharpnessPct) = @_;
        my ($aa, $softness, $spot);
        $spot = $rt->binary('*', $rt->component_wise('sqrt', $rt->component_wise('clamp', $value, $rt->f(0), $rt->f(1))), $rt->f(0.70709999999999995), 1, 'float');
        $softness = $rt->binary('-', $rt->f(1), $rt->component_wise('clamp', $rt->binary('/', $sharpnessPct, $rt->f(100), 1, 'float'), $rt->f(0), $rt->f(1)), 1, 'float');
        $aa = $rt->component_wise('max', $rt->component_wise('mix', $rt->binary('*', $rt->fwidth($d), $rt->f(1.5), 1, 'float'), $rt->f(0.34999999999999998), $softness), $rt->f(1.0000000000000001e-05));
        return $rt->binary('-', $rt->f(1), $rt->component_wise('smoothstep', $rt->binary('-', $spot, $aa, 1, 'float'), $rt->binary('+', $spot, $aa, 1, 'float'), $d), 1, 'float');
    };
    $roundDotCoverage__vec2_float_float = sub {
        my ($offset, $value, $sharpnessPct) = @_;
        $offset = $rt->copy($offset, 'float');
        my ($centerAA, $centerDistance, $inkAmount, $inkRadius, $resolvedInk, $softness);
        $inkAmount = $rt->component_wise('clamp', $value, $rt->f(0), $rt->f(1));
        $centerDistance = $rt->length($offset);
        $inkRadius = $rt->component_wise('sqrt', $rt->binary('/', $rt->component_wise('min', $inkAmount, $g->{DOT_AREA_CAP}), $g->{PI}, 1, 'float'));
        if ($rt->binary('>', $inkAmount, $g->{DOT_AREA_CAP})) {
            $inkRadius = $rt->component_wise('mix', $g->{MID_DOT_RADIUS}, $g->{MAX_DOT_RADIUS}, $rt->binary('/', $rt->binary('-', $inkAmount, $g->{DOT_AREA_CAP}, 1, 'float'), $rt->binary('-', $rt->f(1), $g->{DOT_AREA_CAP}, 1, 'float'), 1, 'float'));
        }
        $softness = $rt->binary('-', $rt->f(1), $rt->component_wise('clamp', $rt->binary('/', $sharpnessPct, $rt->f(100), 1, 'float'), $rt->f(0), $rt->f(1)), 1, 'float');
        $centerAA = $rt->component_wise('max', $rt->component_wise('mix', $rt->binary('*', $rt->fwidth($centerDistance), $rt->f(1.5), 1, 'float'), $rt->f(0.34999999999999998), $softness), $rt->f(1.0000000000000001e-05));
        $resolvedInk = $rt->component_wise('smoothstep', $rt->f(0), $rt->binary('/', $rt->f(1), $rt->f(255), 1, 'float'), $value);
        return $rt->binary('*', $rt->binary('-', $rt->f(1), $rt->component_wise('smoothstep', $rt->unary('-', $centerAA), $centerAA, $rt->binary('-', $centerDistance, $inkRadius, 1, 'float')), 1, 'float'), $resolvedInk, 1, 'float');
    };
    $main__void = sub {
        my ($alpha, $center, $d, $dotOffset, $globalCoord, $ink, $inkC, $inkK, $inkM, $inkY, $off, $rd, $ruv, $ruvC, $ruvK, $ruvM, $ruvY, $screened, $texel, $uv, $valC, $valK, $valM, $valY, $value);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $uv = $rt->binary('/', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_resolution, 2, 'float');
        $texel = $rt->binary('/', $rt->f(1), $_u_resolution, 2, 'float');
        $alpha = $rt->swizzle($rt->texture($_u_inputTex, $uv), 'a');
        $d = $rt->f(0.0);
        $dotOffset = $rt->construct(2, 0.0);
        $inkC = $rt->f(0.0);
        $inkK = $rt->f(0.0);
        $inkM = $rt->f(0.0);
        $inkY = $rt->f(0.0);
        $ruvC = $rt->construct(2, 0.0);
        $ruvK = $rt->construct(2, 0.0);
        $ruvM = $rt->construct(2, 0.0);
        $ruvY = $rt->construct(2, 0.0);
        $screened = $rt->construct(3, 0.0);
        $valC = $rt->f(0.0);
        $valK = $rt->f(0.0);
        $valM = $rt->f(0.0);
        $valY = $rt->f(0.0);
        $value = $rt->f(0.0);
        if ($rt->binary('==', $_u_MODE, $rt->i(0))) {
            $ruvC = $rt->binary('/', $rotate2D__vec2_float->($globalCoord, $_u_cyanAngle), $_u_frequency, 2, 'float');
            $ruvM = $rt->binary('/', $rotate2D__vec2_float->($globalCoord, $_u_magentaAngle), $_u_frequency, 2, 'float');
            $ruvY = $rt->binary('/', $rotate2D__vec2_float->($globalCoord, $_u_yellowAngle), $_u_frequency, 2, 'float');
            $ruvK = $rt->binary('/', $rotate2D__vec2_float->($globalCoord, $_u_blackAngle), $_u_frequency, 2, 'float');
            $valC = $rt->swizzle($rgbToCmyk__vec3->($cellSampleFromRuv__vec2_float_vec2->($ruvC, $_u_cyanAngle, $texel)), 'r');
            $valM = $rt->swizzle($rgbToCmyk__vec3->($cellSampleFromRuv__vec2_float_vec2->($ruvM, $_u_magentaAngle, $texel)), 'g');
            $valY = $rt->swizzle($rgbToCmyk__vec3->($cellSampleFromRuv__vec2_float_vec2->($ruvY, $_u_yellowAngle, $texel)), 'b');
            $valK = $rt->swizzle($rgbToCmyk__vec3->($cellSampleFromRuv__vec2_float_vec2->($ruvK, $_u_blackAngle, $texel)), 'a');
            $inkC = $roundDotCoverage__vec2_float_float->($rt->binary('-', $rt->component_wise('fract', $ruvC), $rt->f(0.5), 2, 'float'), $valC, $_u_sharpness);
            $inkM = $roundDotCoverage__vec2_float_float->($rt->binary('-', $rt->component_wise('fract', $ruvM), $rt->f(0.5), 2, 'float'), $valM, $_u_sharpness);
            $inkY = $roundDotCoverage__vec2_float_float->($rt->binary('-', $rt->component_wise('fract', $ruvY), $rt->f(0.5), 2, 'float'), $valY, $_u_sharpness);
            $inkK = $roundDotCoverage__vec2_float_float->($rt->binary('-', $rt->component_wise('fract', $ruvK), $rt->f(0.5), 2, 'float'), $valK, $_u_sharpness);
            $screened = $rt->binary('*', $rt->binary('-', $rt->construct(3, $rt->f(1)), $rt->construct(3, $inkC, $inkM, $inkY), 3, 'float'), $rt->binary('-', $rt->f(1), $inkK, 1, 'float'), 3, 'float');
            @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $screened, $alpha))};
            return;
        } else {
            $value = $rt->f(0.0);
            $d = $rt->f(0.0);
            $dotOffset = $rt->construct(2, $rt->f(0));
            $center = $rt->construct(2, 0.0);
            $off = $rt->construct(2, 0.0);
            $rd = $rt->f(0.0);
            $ruv = $rt->construct(2, 0.0);
            if ($rt->binary('==', $_u_PATTERN, $rt->i(2))) {
                $center = $rt->binary('*', $_u_fullResolution, $rt->f(0.5), 2, 'float');
                $value = $rt->binary('-', $rt->f(1), $lum__vec3->($boxBlur3__vec2_vec2->($uv, $texel)), 1, 'float');
                $rd = $rt->binary('/', $rt->length($rt->binary('-', $globalCoord, $center, 2, 'float')), $_u_frequency, 1, 'float');
                $d = $rt->component_wise('abs', $rt->binary('-', $rt->component_wise('fract', $rd), $rt->f(0.5), 1, 'float'));
            } else {
                $ruv = $rt->binary('/', $rotate2D__vec2_float->($globalCoord, $_u_monoAngle), $_u_frequency, 2, 'float');
                $value = $rt->binary('-', $rt->f(1), $lum__vec3->($cellSampleFromRuv__vec2_float_vec2->($ruv, $_u_monoAngle, $texel)), 1, 'float');
                $off = $rt->binary('-', $rt->component_wise('fract', $ruv), $rt->f(0.5), 2, 'float');
                @{$dotOffset} = map { $rt->f32($_) } @{($off)};
                if ($rt->binary('==', $_u_PATTERN, $rt->i(1))) {
                    $d = $rt->component_wise('abs', $rt->swizzle($off, 'y'));
                } else {
                    $d = $rt->length($off);
                }
            }
            $ink = $rt->f(0.0);
            if ($rt->binary('==', $_u_PATTERN, $rt->i(0))) {
                $ink = $roundDotCoverage__vec2_float_float->($dotOffset, $value, $_u_sharpness);
            } else {
                $ink = $halftoneCoverage__float_float_float->($d, $value, $_u_sharpness);
            }
            @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $tonemap2__float_vec3_vec3->($rt->binary('-', $rt->f(1), $ink, 1, 'float'), $_u_inkColor, $_u_paperColor), $alpha))};
        }
    };
    $main__void->();
    my $_c = $g->{fragColor};
    $out->[0] = $rt->f32($_c->[0]); $out->[1] = $rt->f32($_c->[1]); $out->[2] = $rt->f32($_c->[2]); $out->[3] = $rt->f32($_c->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 1 };
