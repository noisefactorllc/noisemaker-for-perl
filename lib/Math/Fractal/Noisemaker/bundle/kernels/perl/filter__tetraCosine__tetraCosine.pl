# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($hsv2rgb__vec3, $oklab2linear__vec3, $linear2srgb__vec3, $oklab2rgb__vec3, $oklch2rgb__vec3, $cosinePalette__float_vec3_vec3_vec3_vec3, $main__void);
    my $_retc;
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_colorMode = exists $U->{'colorMode'} ? $U->{'colorMode'} : 0;
    my $_u_offsetR = exists $U->{'offsetR'} ? $U->{'offsetR'} : $rt->f(0.0);
    my $_u_offsetG = exists $U->{'offsetG'} ? $U->{'offsetG'} : $rt->f(0.0);
    my $_u_offsetB = exists $U->{'offsetB'} ? $U->{'offsetB'} : $rt->f(0.0);
    my $_u_ampR = exists $U->{'ampR'} ? $U->{'ampR'} : $rt->f(0.0);
    my $_u_ampG = exists $U->{'ampG'} ? $U->{'ampG'} : $rt->f(0.0);
    my $_u_ampB = exists $U->{'ampB'} ? $U->{'ampB'} : $rt->f(0.0);
    my $_u_freqR = exists $U->{'freqR'} ? $U->{'freqR'} : $rt->f(0.0);
    my $_u_freqG = exists $U->{'freqG'} ? $U->{'freqG'} : $rt->f(0.0);
    my $_u_freqB = exists $U->{'freqB'} ? $U->{'freqB'} : $rt->f(0.0);
    my $_u_phaseR = exists $U->{'phaseR'} ? $U->{'phaseR'} : $rt->f(0.0);
    my $_u_phaseG = exists $U->{'phaseG'} ? $U->{'phaseG'} : $rt->f(0.0);
    my $_u_phaseB = exists $U->{'phaseB'} ? $U->{'phaseB'} : $rt->f(0.0);
    my $_u_repeat = exists $U->{'repeat'} ? $U->{'repeat'} : $rt->f(0.0);
    my $_u_offset = exists $U->{'offset'} ? $U->{'offset'} : $rt->f(0.0);
    my $_u_alpha = exists $U->{'alpha'} ? $U->{'alpha'} : $rt->f(0.0);
    my $_u_rotation = exists $U->{'rotation'} ? $U->{'rotation'} : 0;
    my $_u_time = exists $U->{'time'} ? $U->{'time'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $g->{TAU} = $rt->f(6.2831853071795862);
    $hsv2rgb__vec3 = sub {
        my ($hsv) = @_;
        $hsv = $rt->copy($hsv, 'float');
        my ($c, $h, $hp, $m, $rgb, $s, $v, $x);
        $h = $rt->swizzle($hsv, 'x');
        $s = $rt->swizzle($hsv, 'y');
        $v = $rt->swizzle($hsv, 'z');
        $c = $rt->binary('*', $v, $s, 1, 'float');
        $hp = $rt->binary('*', $h, $rt->f(6), 1, 'float');
        $x = $rt->binary('*', $c, $rt->binary('-', $rt->f(1), $rt->component_wise('abs', $rt->binary('-', $rt->component_wise('mod', $hp, $rt->f(2)), $rt->f(1), 1, 'float')), 1, 'float'), 1, 'float');
        $m = $rt->binary('-', $v, $c, 1, 'float');
        $rgb = $rt->construct(3, 0.0);
        if ($rt->binary('<', $hp, $rt->f(1))) {
            @{$rgb} = map { $rt->f32($_) } @{($rt->construct(3, $c, $x, $rt->f(0)))};
        } else {
            if ($rt->binary('<', $hp, $rt->f(2))) {
                @{$rgb} = map { $rt->f32($_) } @{($rt->construct(3, $x, $c, $rt->f(0)))};
            } else {
                if ($rt->binary('<', $hp, $rt->f(3))) {
                    @{$rgb} = map { $rt->f32($_) } @{($rt->construct(3, $rt->f(0), $c, $x))};
                } else {
                    if ($rt->binary('<', $hp, $rt->f(4))) {
                        @{$rgb} = map { $rt->f32($_) } @{($rt->construct(3, $rt->f(0), $x, $c))};
                    } else {
                        if ($rt->binary('<', $hp, $rt->f(5))) {
                            @{$rgb} = map { $rt->f32($_) } @{($rt->construct(3, $x, $rt->f(0), $c))};
                        } else {
                            @{$rgb} = map { $rt->f32($_) } @{($rt->construct(3, $c, $rt->f(0), $x))};
                        }
                    }
                }
            }
        }
        return $rt->binary('+', $rgb, $rt->construct(3, $m), 3, 'float');
    };
    $oklab2linear__vec3 = sub {
        my ($lab) = @_;
        $lab = $rt->copy($lab, 'float');
        my ($L, $a, $b, $l, $l_, $m, $m_, $s, $s_);
        $L = $rt->swizzle($lab, 'x');
        $a = $rt->swizzle($lab, 'y');
        $b = $rt->swizzle($lab, 'z');
        $l_ = $rt->binary('+', $rt->binary('+', $L, $rt->binary('*', $rt->f(0.39633777739999998), $a, 1, 'float'), 1, 'float'), $rt->binary('*', $rt->f(0.21580375730000001), $b, 1, 'float'), 1, 'float');
        $m_ = $rt->binary('-', $rt->binary('-', $L, $rt->binary('*', $rt->f(0.1055613458), $a, 1, 'float'), 1, 'float'), $rt->binary('*', $rt->f(0.063854172799999998), $b, 1, 'float'), 1, 'float');
        $s_ = $rt->binary('-', $rt->binary('-', $L, $rt->binary('*', $rt->f(0.089484177499999998), $a, 1, 'float'), 1, 'float'), $rt->binary('*', $rt->f(1.291485548), $b, 1, 'float'), 1, 'float');
        $l = $rt->binary('*', $rt->binary('*', $l_, $l_, 1, 'float'), $l_, 1, 'float');
        $m = $rt->binary('*', $rt->binary('*', $m_, $m_, 1, 'float'), $m_, 1, 'float');
        $s = $rt->binary('*', $rt->binary('*', $s_, $s_, 1, 'float'), $s_, 1, 'float');
        return $rt->construct(3, $rt->binary('+', $rt->binary('-', $rt->binary('*', $rt->f(4.0767416620999999), $l, 1, 'float'), $rt->binary('*', $rt->f(3.3077115912999999), $m, 1, 'float'), 1, 'float'), $rt->binary('*', $rt->f(0.23096992920000001), $s, 1, 'float'), 1, 'float'), $rt->binary('-', $rt->binary('+', $rt->binary('*', $rt->unary('-', $rt->f(1.2684380045999999)), $l, 1, 'float'), $rt->binary('*', $rt->f(2.6097574011), $m, 1, 'float'), 1, 'float'), $rt->binary('*', $rt->f(0.34131939649999998), $s, 1, 'float'), 1, 'float'), $rt->binary('+', $rt->binary('-', $rt->binary('*', $rt->unary('-', $rt->f(0.0041960862999999996)), $l, 1, 'float'), $rt->binary('*', $rt->f(0.7034186147), $m, 1, 'float'), 1, 'float'), $rt->binary('*', $rt->f(1.707614701), $s, 1, 'float'), 1, 'float'));
    };
    $linear2srgb__vec3 = sub {
        my ($linear) = @_;
        $linear = $rt->copy($linear, 'float');
        my ($high, $low);
        $low = $rt->binary('*', $linear, $rt->f(12.92), 3, 'float');
        $high = $rt->binary('-', $rt->binary('*', $rt->f(1.0549999999999999), $rt->component_wise('pow', $rt->component_wise('max', $linear, $rt->construct(3, $rt->f(0))), $rt->construct(3, $rt->binary('/', $rt->f(1), $rt->f(2.3999999999999999), 1, 'float'))), 3, 'float'), $rt->f(0.055), 3, 'float');
        return $rt->component_wise('mix', $high, $low, $rt->component_wise('step', $linear, $rt->construct(3, $rt->f(0.0031308))));
    };
    $oklab2rgb__vec3 = sub {
        my ($lab) = @_;
        $lab = $rt->copy($lab, 'float');
        my ($L, $a, $b, $linear_rgb);
        $L = $rt->swizzle($lab, 'x');
        $a = $rt->binary('*', $rt->binary('-', $rt->swizzle($lab, 'y'), $rt->f(0.5), 1, 'float'), $rt->f(0.80000000000000004), 1, 'float');
        $b = $rt->binary('*', $rt->binary('-', $rt->swizzle($lab, 'z'), $rt->f(0.5), 1, 'float'), $rt->f(0.80000000000000004), 1, 'float');
        $linear_rgb = $oklab2linear__vec3->($rt->construct(3, $L, $a, $b));
        return $rt->component_wise('clamp', $linear2srgb__vec3->($linear_rgb), $rt->f(0), $rt->f(1));
    };
    $oklch2rgb__vec3 = sub {
        my ($lch) = @_;
        $lch = $rt->copy($lch, 'float');
        my ($C, $H, $L, $a, $b, $linear_rgb);
        $L = $rt->swizzle($lch, 'x');
        $C = $rt->binary('*', $rt->swizzle($lch, 'y'), $rt->f(0.40000000000000002), 1, 'float');
        $H = $rt->binary('*', $rt->swizzle($lch, 'z'), $g->{TAU}, 1, 'float');
        $a = $rt->binary('*', $C, $rt->component_wise('cos', $H), 1, 'float');
        $b = $rt->binary('*', $C, $rt->component_wise('sin', $H), 1, 'float');
        $linear_rgb = $oklab2linear__vec3->($rt->construct(3, $L, $a, $b));
        return $rt->component_wise('clamp', $linear2srgb__vec3->($linear_rgb), $rt->f(0), $rt->f(1));
    };
    $cosinePalette__float_vec3_vec3_vec3_vec3 = sub {
        my ($t, $offset, $amp, $freq, $phase) = @_;
        $offset = $rt->copy($offset, 'float');
        $amp = $rt->copy($amp, 'float');
        $freq = $rt->copy($freq, 'float');
        $phase = $rt->copy($phase, 'float');
        return $rt->component_wise('clamp', $rt->binary('+', $offset, $rt->binary('*', $amp, $rt->component_wise('cos', $rt->binary('*', $g->{TAU}, $rt->binary('+', $rt->binary('*', $freq, $t, 3, 'float'), $phase, 3, 'float'), 3, 'float')), 3, 'float'), 3, 'float'), $rt->f(0), $rt->f(1));
    };
    $main__void = sub {
        my ($amp, $blendedColor, $finalColor, $freq, $globalCoord, $inputColor, $lum, $offset, $paletteColor, $phase, $t, $texSize, $uv);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $texSize = $rt->construct(2, $rt->texture_size($_u_inputTex));
        $uv = $rt->binary('/', $rt->swizzle($ctx->{frag_coord}, 'xy'), $texSize, 2, 'float');
        $inputColor = $rt->texture($_u_inputTex, $uv);
        $lum = $rt->dot($rt->swizzle($inputColor, 'rgb'), $rt->construct(3, $rt->f(0.29899999999999999), $rt->f(0.58699999999999997), $rt->f(0.114)));
        $t = $rt->binary('+', $rt->binary('*', $lum, $_u_repeat, 1, 'float'), $_u_offset, 1, 'float');
        if ($rt->binary('==', $_u_rotation, $rt->unary('-', $rt->i(1)))) {
            $t = $rt->binary('+', $t, $_u_time, 1, 'float');
        } else {
            if ($rt->binary('==', $_u_rotation, $rt->i(1))) {
                $t = $rt->binary('-', $t, $_u_time, 1, 'float');
            }
        }
        $t = $rt->component_wise('fract', $t);
        $offset = $rt->construct(3, $_u_offsetR, $_u_offsetG, $_u_offsetB);
        $amp = $rt->construct(3, $_u_ampR, $_u_ampG, $_u_ampB);
        $freq = $rt->construct(3, $_u_freqR, $_u_freqG, $_u_freqB);
        $phase = $rt->construct(3, $_u_phaseR, $_u_phaseG, $_u_phaseB);
        $paletteColor = $cosinePalette__float_vec3_vec3_vec3_vec3->($t, $offset, $amp, $freq, $phase);
        $finalColor = $rt->construct(3, 0.0);
        if ($rt->binary('==', $_u_colorMode, $rt->i(1))) {
            @{$finalColor} = map { $rt->f32($_) } @{($hsv2rgb__vec3->($paletteColor))};
        } else {
            if ($rt->binary('==', $_u_colorMode, $rt->i(2))) {
                @{$finalColor} = map { $rt->f32($_) } @{($oklab2rgb__vec3->($paletteColor))};
            } else {
                if ($rt->binary('==', $_u_colorMode, $rt->i(3))) {
                    @{$finalColor} = map { $rt->f32($_) } @{($oklch2rgb__vec3->($paletteColor))};
                } else {
                    @{$finalColor} = map { $rt->f32($_) } @{($paletteColor)};
                }
            }
        }
        $blendedColor = $rt->component_wise('mix', $rt->swizzle($inputColor, 'rgb'), $finalColor, $_u_alpha);
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $blendedColor, $rt->swizzle($inputColor, 'a')))};
    };
    $main__void->();
    my $_c = $g->{fragColor};
    $out->[0] = $rt->f32($_c->[0]); $out->[1] = $rt->f32($_c->[1]); $out->[2] = $rt->f32($_c->[2]); $out->[3] = $rt->f32($_c->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0 };
