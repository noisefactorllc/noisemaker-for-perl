# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($sampleGlyph__int_int_int, $renderNumber__int_vec2, $checkerboard__vec2, $colorBars__vec2, $gradient__vec2, $uvMap__vec2, $gridLines__vec2, $hue2rgb__float, $colorGrid__vec2, $dotGrid__vec2, $main__void);
    my $_retc;
    my $_u_resolution = exists $U->{'resolution'} ? $U->{'resolution'} : $rt->construct(2, 0.0);
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_gridSize = exists $U->{'gridSize'} ? $U->{'gridSize'} : 0;
    my $_u_pattern = exists $U->{'pattern'} ? $U->{'pattern'} : 0;
    $g->{fragColor} = $rt->construct(4, 0.0);
    $g->{GLYPH} = $rt->array([$rt->i(31599), $rt->i(9362), $rt->i(29671), $rt->i(29391), $rt->i(23497), $rt->i(31183), $rt->i(31215), $rt->i(29257), $rt->i(31727), $rt->i(31695)]);
    $sampleGlyph__int_int_int = sub {
        my ($digit, $x, $y) = @_;
        my ($bitIndex);
        if ((((((((((($rt->binary('<', $digit, $rt->i(0))) || ($rt->binary('>', $digit, $rt->i(9))) ? 1 : 0)) || ($rt->binary('<', $x, $rt->i(0))) ? 1 : 0)) || ($rt->binary('>', $x, $rt->i(2))) ? 1 : 0)) || ($rt->binary('<', $y, $rt->i(0))) ? 1 : 0)) || ($rt->binary('>', $y, $rt->i(4))) ? 1 : 0)) {
            return 0;
        }
        $bitIndex = $rt->binary('+', $rt->binary('*', $y, $rt->i(3), 1, 'int'), $rt->binary('-', $rt->i(2), $x, 1, 'int'), 1, 'int');
        return $rt->binary('==', $rt->binary('&', $rt->binary('>>', $g->{GLYPH}->[int($digit)], $bitIndex, 1, 'int'), $rt->i(1), 1, 'int'), $rt->i(1));
    };
    $renderNumber__int_vec2 = sub {
        my ($number, $cellUV) = @_;
        $cellUV = $rt->copy($cellUV, 'float');
        my ($_for0_first, $_for1_first, $d, $digit, $digitX, $digits, $glyphHeight, $glyphWidth, $gx, $gy, $i, $localX, $localY, $numDigits, $spacing, $startX, $startY, $temp, $totalWidth);
        $numDigits = $rt->i(1);
        if ($rt->binary('>=', $number, $rt->i(10))) {
            $numDigits = $rt->i(2);
        }
        if ($rt->binary('>=', $number, $rt->i(100))) {
            $numDigits = $rt->i(3);
        }
        $glyphWidth = $rt->f(0.14999999999999999);
        $glyphHeight = $rt->f(0.34999999999999998);
        $spacing = $rt->f(0.050000000000000003);
        $totalWidth = $rt->binary('+', $rt->binary('*', $rt->construct(1, $numDigits), $glyphWidth, 1, 'float'), $rt->binary('*', $rt->construct(1, $rt->binary('-', $numDigits, $rt->i(1), 1, 'int')), $spacing, 1, 'float'), 1, 'float');
        $startX = $rt->binary('-', $rt->f(0.5), $rt->binary('*', $totalWidth, $rt->f(0.5), 1, 'float'), 1, 'float');
        $startY = $rt->binary('-', $rt->f(0.5), $rt->binary('*', $glyphHeight, $rt->f(0.5), 1, 'float'), 1, 'float');
        if ((($rt->binary('<', $rt->swizzle($cellUV, 'y'), $startY)) || ($rt->binary('>=', $rt->swizzle($cellUV, 'y'), $rt->binary('+', $startY, $glyphHeight, 1, 'float'))) ? 1 : 0)) {
            return 0;
        }
        $digits = $rt->new_array($rt->i(3), 1);
        $temp = $number;
        $i = $rt->i(0);
        $_for0_first = 1;
        for my $_for0 (0 .. 1048575) {
            if (!$_for0_first) {
                $i = $rt->binary('+', $i, $rt->i(1), 1, 'int');
            }
            $_for0_first = 0;
            if (!($rt->binary('<', $i, $rt->i(3)))) {
                last;
            }
            $digits->[int($i)] = $rt->binary('%', $temp, $rt->i(10), 1, 'int');
            $temp = $rt->binary('/', $temp, $rt->i(10), 1, 'int');
        }
        $d = $rt->i(0);
        $_for1_first = 1;
        for my $_for1 (0 .. 1048575) {
            if (!$_for1_first) {
                $d = $rt->binary('+', $d, $rt->i(1), 1, 'int');
            }
            $_for1_first = 0;
            if (!($rt->binary('<', $d, $numDigits))) {
                last;
            }
            $digitX = $rt->binary('+', $startX, $rt->binary('*', $rt->construct(1, $d), $rt->binary('+', $glyphWidth, $spacing, 1, 'float'), 1, 'float'), 1, 'float');
            $digit = 0;
            $gx = 0;
            $gy = 0;
            $localX = $rt->f(0.0);
            $localY = $rt->f(0.0);
            if ((($rt->binary('>=', $rt->swizzle($cellUV, 'x'), $digitX)) && ($rt->binary('<', $rt->swizzle($cellUV, 'x'), $rt->binary('+', $digitX, $glyphWidth, 1, 'float'))) ? 1 : 0)) {
                $localX = $rt->binary('/', $rt->binary('-', $rt->swizzle($cellUV, 'x'), $digitX, 1, 'float'), $glyphWidth, 1, 'float');
                $localY = $rt->binary('/', $rt->binary('-', $rt->swizzle($cellUV, 'y'), $startY, 1, 'float'), $glyphHeight, 1, 'float');
                $gx = $rt->construct(1, $rt->binary('*', $localX, $rt->f(3), 1, 'float'), 'int');
                $gy = $rt->construct(1, $rt->binary('*', $localY, $rt->f(5), 1, 'float'), 'int');
                $digit = $digits->[int($rt->binary('-', $rt->binary('-', $numDigits, $rt->i(1), 1, 'int'), $d, 1, 'int'))];
                return $sampleGlyph__int_int_int->($digit, $gx, $gy);
            }
        }
        return 0;
    };
    $checkerboard__vec2 = sub {
        my ($uv) = @_;
        $uv = $rt->copy($uv, 'float');
        my ($cellColor, $cellNum, $cellUV, $cellX, $cellY, $finalColor, $glyphColor, $isGlyph, $isWhiteCell, $n);
        $n = $rt->component_wise('max', $_u_gridSize, $rt->i(1));
        $cellX = $rt->binary('%', $rt->construct(1, $rt->binary('*', $rt->swizzle($uv, 'x'), $rt->construct(1, $n), 1, 'float'), 'int'), $n, 1, 'int');
        $cellY = $rt->binary('%', $rt->construct(1, $rt->binary('*', $rt->swizzle($uv, 'y'), $rt->construct(1, $n), 1, 'float'), 'int'), $n, 1, 'int');
        $cellNum = $rt->binary('+', $rt->binary('*', $rt->binary('-', $rt->binary('-', $n, $rt->i(1), 1, 'int'), $cellY, 1, 'int'), $n, 1, 'int'), $cellX, 1, 'int');
        $isWhiteCell = $rt->binary('==', $rt->binary('%', $rt->binary('+', $cellX, $cellY, 1, 'int'), $rt->i(2), 1, 'int'), $rt->i(0));
        $cellUV = $rt->component_wise('fract', $rt->binary('*', $uv, $rt->construct(1, $n), 2, 'float'));
        $isGlyph = $renderNumber__int_vec2->($cellNum, $cellUV);
        $cellColor = (($isWhiteCell) ? ($rt->f(1)) : ($rt->f(0)));
        $glyphColor = (($isWhiteCell) ? ($rt->f(0)) : ($rt->f(1)));
        $finalColor = (($isGlyph) ? ($glyphColor) : ($cellColor));
        return $rt->construct(4, $rt->construct(3, $finalColor), $rt->f(1));
    };
    $colorBars__vec2 = sub {
        my ($uv) = @_;
        $uv = $rt->copy($uv, 'float');
        my ($bar, $colors);
        $bar = $rt->construct(1, $rt->binary('*', $rt->swizzle($uv, 'x'), $rt->f(8), 1, 'float'), 'int');
        $bar = $rt->component_wise('clamp', $bar, $rt->i(0), $rt->i(7));
        $colors = $rt->array([$rt->construct(3, $rt->f(1), $rt->f(1), $rt->f(1)), $rt->construct(3, $rt->f(1), $rt->f(1), $rt->f(0)), $rt->construct(3, $rt->f(0), $rt->f(1), $rt->f(1)), $rt->construct(3, $rt->f(0), $rt->f(1), $rt->f(0)), $rt->construct(3, $rt->f(1), $rt->f(0), $rt->f(1)), $rt->construct(3, $rt->f(1), $rt->f(0), $rt->f(0)), $rt->construct(3, $rt->f(0), $rt->f(0), $rt->f(1)), $rt->construct(3, $rt->f(0), $rt->f(0), $rt->f(0))]);
        return $rt->construct(4, $colors->[int($bar)], $rt->f(1));
    };
    $gradient__vec2 = sub {
        my ($uv) = @_;
        $uv = $rt->copy($uv, 'float');
        return $rt->construct(4, $rt->construct(3, $rt->swizzle($uv, 'x')), $rt->f(1));
    };
    $uvMap__vec2 = sub {
        my ($uv) = @_;
        $uv = $rt->copy($uv, 'float');
        return $rt->construct(4, $rt->swizzle($uv, 'x'), $rt->swizzle($uv, 'y'), $rt->f(0), $rt->f(1));
    };
    $gridLines__vec2 = sub {
        my ($uv) = @_;
        $uv = $rt->copy($uv, 'float');
        my ($cellUV, $edge, $fw, $line, $n);
        $n = $rt->component_wise('max', $_u_gridSize, $rt->i(1));
        $cellUV = $rt->component_wise('fract', $rt->binary('*', $uv, $rt->construct(1, $n), 2, 'float'));
        $edge = $rt->component_wise('min', $cellUV, $rt->binary('-', $rt->f(1), $cellUV, 2, 'float'));
        $fw = $rt->binary('*', $rt->binary('/', $rt->construct(2, $rt->f(1)), $_u_fullResolution, 2, 'float'), $rt->construct(1, $n), 2, 'float');
        $line = $rt->binary('-', $rt->f(1), $rt->binary('*', $rt->component_wise('smoothstep', $rt->f(0), $rt->binary('*', $rt->f(2), $rt->swizzle($fw, 'x'), 1, 'float'), $rt->swizzle($edge, 'x')), $rt->component_wise('smoothstep', $rt->f(0), $rt->binary('*', $rt->f(2), $rt->swizzle($fw, 'y'), 1, 'float'), $rt->swizzle($edge, 'y')), 1, 'float'), 1, 'float');
        return $rt->construct(4, $rt->construct(3, $line), $rt->f(1));
    };
    $hue2rgb__float = sub {
        my ($h) = @_;
        my ($_g, $b, $r);
        $r = $rt->binary('-', $rt->component_wise('abs', $rt->binary('-', $rt->binary('*', $h, $rt->f(6), 1, 'float'), $rt->f(3), 1, 'float')), $rt->f(1), 1, 'float');
        $_g = $rt->binary('-', $rt->f(2), $rt->component_wise('abs', $rt->binary('-', $rt->binary('*', $h, $rt->f(6), 1, 'float'), $rt->f(2), 1, 'float')), 1, 'float');
        $b = $rt->binary('-', $rt->f(2), $rt->component_wise('abs', $rt->binary('-', $rt->binary('*', $h, $rt->f(6), 1, 'float'), $rt->f(4), 1, 'float')), 1, 'float');
        return $rt->component_wise('clamp', $rt->construct(3, $r, $_g, $b), $rt->f(0), $rt->f(1));
    };
    $colorGrid__vec2 = sub {
        my ($uv) = @_;
        $uv = $rt->copy($uv, 'float');
        my ($cellIndex, $cellX, $cellY, $hue, $n);
        $n = $rt->component_wise('max', $_u_gridSize, $rt->i(1));
        $cellX = $rt->binary('%', $rt->construct(1, $rt->binary('*', $rt->swizzle($uv, 'x'), $rt->construct(1, $n), 1, 'float'), 'int'), $n, 1, 'int');
        $cellY = $rt->binary('%', $rt->construct(1, $rt->binary('*', $rt->swizzle($uv, 'y'), $rt->construct(1, $n), 1, 'float'), 'int'), $n, 1, 'int');
        $cellIndex = $rt->binary('+', $rt->binary('*', $cellY, $n, 1, 'int'), $cellX, 1, 'int');
        $hue = $rt->component_wise('fract', $rt->binary('*', $rt->construct(1, $cellIndex), $rt->f(0.61803398874989501), 1, 'float'));
        return $rt->construct(4, $hue2rgb__float->($hue), $rt->f(1));
    };
    $dotGrid__vec2 = sub {
        my ($uv) = @_;
        $uv = $rt->copy($uv, 'float');
        my ($dist, $dot, $n, $nearest, $scaled);
        $n = $rt->component_wise('max', $_u_gridSize, $rt->i(1));
        $scaled = $rt->binary('*', $uv, $rt->construct(1, $n), 2, 'float');
        $nearest = $rt->component_wise('round', $scaled);
        $dist = $rt->length($rt->binary('-', $scaled, $nearest, 2, 'float'));
        $dot = $rt->binary('-', $rt->f(1), $rt->component_wise('smoothstep', $rt->f(0.12), $rt->f(0.14999999999999999), $dist), 1, 'float');
        return $rt->construct(4, $rt->construct(3, $dot), $rt->f(1));
    };
    $main__void = sub {
        my ($globalCoord, $uv);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $uv = $rt->binary('/', $globalCoord, $_u_fullResolution, 2, 'float');
        if ($rt->binary('==', $_u_pattern, $rt->i(1))) {
            @{$g->{fragColor}} = map { $rt->f32($_) } @{($colorBars__vec2->($uv))};
        } else {
            if ($rt->binary('==', $_u_pattern, $rt->i(2))) {
                @{$g->{fragColor}} = map { $rt->f32($_) } @{($gradient__vec2->($uv))};
            } else {
                if ($rt->binary('==', $_u_pattern, $rt->i(3))) {
                    @{$g->{fragColor}} = map { $rt->f32($_) } @{($uvMap__vec2->($uv))};
                } else {
                    if ($rt->binary('==', $_u_pattern, $rt->i(4))) {
                        @{$g->{fragColor}} = map { $rt->f32($_) } @{($gridLines__vec2->($uv))};
                    } else {
                        if ($rt->binary('==', $_u_pattern, $rt->i(5))) {
                            @{$g->{fragColor}} = map { $rt->f32($_) } @{($colorGrid__vec2->($uv))};
                        } else {
                            if ($rt->binary('==', $_u_pattern, $rt->i(6))) {
                                @{$g->{fragColor}} = map { $rt->f32($_) } @{($dotGrid__vec2->($uv))};
                            } else {
                                @{$g->{fragColor}} = map { $rt->f32($_) } @{($checkerboard__vec2->($uv))};
                            }
                        }
                    }
                }
            }
        }
    };
    $main__void->();
    my $_c = $g->{fragColor};
    $out->[0] = $rt->f32($_c->[0]); $out->[1] = $rt->f32($_c->[1]); $out->[2] = $rt->f32($_c->[2]); $out->[3] = $rt->f32($_c->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0 };
