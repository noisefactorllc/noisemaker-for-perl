# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($_kernel__float_float_float, $main__void);
    my $_retc;
    my $_u_densityTex = $ctx->texture_binding('densityTex');
    my $_u_resolution = exists $U->{'resolution'} ? $U->{'resolution'} : $rt->construct(2, 0.0);
    my $_u_muK = exists $U->{'muK'} ? $U->{'muK'} : $rt->f(0.0);
    my $_u_sigmaK = exists $U->{'sigmaK'} ? $U->{'sigmaK'} : $rt->f(0.0);
    my $_u_searchRadius = exists $U->{'searchRadius'} ? $U->{'searchRadius'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $g->{EPSILON} = $rt->f(0.0001);
    $g->{PI} = $rt->f(3.1415926535900001);
    $_kernel__float_float_float = sub {
        my ($r, $mu, $sigma) = @_;
        my ($x);
        $x = $rt->binary('/', $rt->binary('-', $r, $mu, 1, 'float'), $sigma, 1, 'float');
        return $rt->component_wise('exp', $rt->binary('*', $rt->unary('-', $x), $x, 1, 'float'));
    };
    $main__void = sub {
        my ($_U, $_for0_first, $_for1_first, $_for2_first, $density, $densitySize, $dr, $dx, $dy, $i, $iRadius, $kVal, $numSamples, $r, $sampleUV, $texelSize, $uv, $wK);
        $densitySize = $rt->construct(2, $rt->texture_size($_u_densityTex));
        $uv = $rt->binary('/', $rt->swizzle($ctx->{frag_coord}, 'xy'), $densitySize, 2, 'float');
        $texelSize = $rt->binary('/', $rt->f(1), $densitySize, 2, 'float');
        $wK = $rt->f(0);
        $numSamples = $rt->i(64);
        $dr = $rt->binary('/', $_u_searchRadius, $rt->construct(1, $numSamples), 1, 'float');
        $i = $rt->i(0);
        $_for0_first = 1;
        for my $_for0 (0 .. 1048575) {
            if (!$_for0_first) {
                $i = $rt->binary('+', $i, $rt->i(1), 1, 'int');
            }
            $_for0_first = 0;
            if (!($rt->binary('<', $i, $numSamples))) {
                last;
            }
            $r = $rt->binary('*', $rt->binary('+', $rt->construct(1, $i), $rt->f(0.5), 1, 'float'), $dr, 1, 'float');
            $wK = $rt->binary('+', $wK, $rt->binary('*', $rt->binary('*', $_kernel__float_float_float->($r, $_u_muK, $_u_sigmaK), $r, 1, 'float'), $dr, 1, 'float'), 1, 'float');
        }
        $wK = $rt->binary('/', $rt->f(1), $rt->component_wise('max', $rt->binary('*', $rt->binary('*', $wK, $rt->f(2), 1, 'float'), $g->{PI}, 1, 'float'), $g->{EPSILON}), 1, 'float');
        $_U = $rt->f(0);
        $iRadius = $rt->construct(1, $rt->component_wise('ceil', $_u_searchRadius), 'int');
        $dy = $rt->unary('-', $iRadius);
        $_for1_first = 1;
        for my $_for1 (0 .. 1048575) {
            if (!$_for1_first) {
                $dy = $rt->binary('+', $dy, $rt->i(1), 1, 'int');
            }
            $_for1_first = 0;
            if (!($rt->binary('<=', $dy, $iRadius))) {
                last;
            }
            $dx = $rt->unary('-', $iRadius);
            $_for2_first = 1;
            for my $_for2 (0 .. 1048575) {
                if (!$_for2_first) {
                    $dx = $rt->binary('+', $dx, $rt->i(1), 1, 'int');
                }
                $_for2_first = 0;
                if (!($rt->binary('<=', $dx, $iRadius))) {
                    last;
                }
                $r = $rt->length($rt->construct(2, $rt->construct(1, $dx), $rt->construct(1, $dy)));
                if ($rt->binary('>', $r, $_u_searchRadius)) {
                    next;
                }
                $sampleUV = $rt->component_wise('fract', $rt->binary('+', $uv, $rt->binary('*', $rt->construct(2, $rt->construct(1, $dx), $rt->construct(1, $dy)), $texelSize, 2, 'float'), 2, 'float'));
                $density = $rt->swizzle($rt->texture($_u_densityTex, $sampleUV), 'r');
                $kVal = $rt->binary('*', $_kernel__float_float_float->($r, $_u_muK, $_u_sigmaK), $wK, 1, 'float');
                $_U = $rt->binary('+', $_U, $rt->binary('*', $density, $kVal, 1, 'float'), 1, 'float');
            }
        }
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $_U, $rt->f(0), $rt->f(0), $rt->f(1)))};
    };
    $main__void->();
    my $_c0 = $g->{fragColor};
    $out->[0] = $rt->f32($_c0->[0]); $out->[1] = $rt->f32($_c0->[1]); $out->[2] = $rt->f32($_c0->[2]); $out->[3] = $rt->f32($_c0->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0, output_names => ['fragColor'] };
