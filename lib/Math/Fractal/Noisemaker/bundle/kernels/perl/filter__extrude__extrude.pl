# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($hash12__vec2, $lum__vec3, $toSampleUV__vec2, $cellAvgColor3x3__vec2, $cellHeight__vec2_vec2, $baryWeights__vec2_vec2_vec2_vec2, $pyramidTriHit__vec2_vec2_vec2_vec2, $sideShade__vec2_vec2, $main__void);
    my $_retc;
    my $_u_DEPTH_SOURCE = exists $U->{'DEPTH_SOURCE'} ? $U->{'DEPTH_SOURCE'} : 0;
    my $_u_EXTRUDE_TYPE = exists $U->{'EXTRUDE_TYPE'} ? $U->{'EXTRUDE_TYPE'} : 0;
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_resolution = exists $U->{'resolution'} ? $U->{'resolution'} : $rt->construct(2, 0.0);
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_size = exists $U->{'size'} ? $U->{'size'} : $rt->f(0.0);
    my $_u_depth = exists $U->{'depth'} ? $U->{'depth'} : $rt->f(0.0);
    my $_u_solidFront = exists $U->{'solidFront'} ? $U->{'solidFront'} : 0;
    $g->{fragColor} = $rt->construct(4, 0.0);
    $g->{TOP_SIGN} = $rt->f(1);
    $g->{SHADE_TOP} = $rt->f(0.88749999999999996);
    $g->{SHADE_BOTTOM} = $rt->f(0.66249999999999998);
    $g->{SHADE_LEFT} = $rt->f(0.96985600000000005);
    $g->{SHADE_RIGHT} = $rt->f(0.58014399999999999);
    $g->{EPS} = $rt->f(0.0001);
    $hash12__vec2 = sub {
        my ($p) = @_;
        $p = $rt->copy($p, 'float');
        my ($p3);
        $p3 = $rt->component_wise('fract', $rt->binary('*', $rt->construct(3, $rt->swizzle($p, 'xyx')), $rt->f(0.1031), 3, 'float'));
        @{$p3} = map { $rt->f32($_) } @{($rt->binary('+', $p3, $rt->dot($p3, $rt->binary('+', $rt->swizzle($p3, 'yzx'), $rt->f(33.329999999999998), 3, 'float')), 3, 'float'))};
        return $rt->component_wise('fract', $rt->binary('*', $rt->binary('+', $rt->swizzle($p3, 'x'), $rt->swizzle($p3, 'y'), 1, 'float'), $rt->swizzle($p3, 'z'), 1, 'float'));
    };
    $lum__vec3 = sub {
        my ($c) = @_;
        $c = $rt->copy($c, 'float');
        return $rt->dot($c, $rt->construct(3, $rt->f(0.21260000000000001), $rt->f(0.71519999999999995), $rt->f(0.0722)));
    };
    $toSampleUV__vec2 = sub {
        my ($globalPixelPos) = @_;
        $globalPixelPos = $rt->copy($globalPixelPos, 'float');
        return $rt->component_wise('clamp', $rt->binary('/', $rt->binary('-', $globalPixelPos, $_u_tileOffset, 2, 'float'), $_u_resolution, 2, 'float'), $rt->f(0), $rt->f(1));
    };
    $cellAvgColor3x3__vec2 = sub {
        my ($centerPx) = @_;
        $centerPx = $rt->copy($centerPx, 'float');
        my ($_for0_first, $_for1_first, $i, $j, $p, $sp, $sum);
        $sp = $rt->binary('*', $_u_size, $rt->f(0.25), 1, 'float');
        $sum = $rt->construct(4, $rt->f(0));
        $j = $rt->unary('-', $rt->i(1));
        $_for0_first = 1;
        for my $_for0 (0 .. 1048575) {
            if (!$_for0_first) {
                $j = $rt->binary('+', $j, $rt->i(1), 1, 'int');
            }
            $_for0_first = 0;
            if (!($rt->binary('<=', $j, $rt->i(1)))) {
                last;
            }
            $i = $rt->unary('-', $rt->i(1));
            $_for1_first = 1;
            for my $_for1 (0 .. 1048575) {
                if (!$_for1_first) {
                    $i = $rt->binary('+', $i, $rt->i(1), 1, 'int');
                }
                $_for1_first = 0;
                if (!($rt->binary('<=', $i, $rt->i(1)))) {
                    last;
                }
                $p = $rt->binary('+', $centerPx, $rt->binary('*', $rt->construct(2, $rt->construct(1, $i), $rt->construct(1, $j)), $sp, 2, 'float'), 2, 'float');
                @{$sum} = map { $rt->f32($_) } @{($rt->binary('+', $sum, $rt->texture($_u_inputTex, $toSampleUV__vec2->($p)), 4, 'float'))};
            }
        }
        return $rt->binary('*', $sum, $rt->binary('/', $rt->f(1), $rt->f(9), 1, 'float'), 4, 'float');
    };
    $cellHeight__vec2_vec2 = sub {
        my ($cellC, $cellIdxF) = @_;
        $cellC = $rt->copy($cellC, 'float');
        $cellIdxF = $rt->copy($cellIdxF, 'float');
        if ($rt->binary('==', $_u_DEPTH_SOURCE, $rt->i(1))) {
            return $hash12__vec2->($cellIdxF);
        } else {
            return $lum__vec3->($rt->swizzle($cellAvgColor3x3__vec2->($cellC), 'rgb'));
        }
    };
    $baryWeights__vec2_vec2_vec2_vec2 = sub {
        my ($p, $a, $b, $c) = @_;
        $p = $rt->copy($p, 'float');
        $a = $rt->copy($a, 'float');
        $b = $rt->copy($b, 'float');
        $c = $rt->copy($c, 'float');
        my ($d00, $d01, $d11, $d20, $d21, $denom, $u, $v, $v0, $v1, $v2, $w);
        $v0 = $rt->binary('-', $b, $a, 2, 'float');
        $v1 = $rt->binary('-', $c, $a, 2, 'float');
        $v2 = $rt->binary('-', $p, $a, 2, 'float');
        $d00 = $rt->dot($v0, $v0);
        $d01 = $rt->dot($v0, $v1);
        $d11 = $rt->dot($v1, $v1);
        $d20 = $rt->dot($v2, $v0);
        $d21 = $rt->dot($v2, $v1);
        $denom = $rt->binary('-', $rt->binary('*', $d00, $d11, 1, 'float'), $rt->binary('*', $d01, $d01, 1, 'float'), 1, 'float');
        if ($rt->binary('<', $rt->component_wise('abs', $denom), $rt->f(1e-08))) {
            return $rt->construct(3, $rt->unary('-', $rt->f(2)));
        }
        $v = $rt->binary('/', $rt->binary('-', $rt->binary('*', $d11, $d20, 1, 'float'), $rt->binary('*', $d01, $d21, 1, 'float'), 1, 'float'), $denom, 1, 'float');
        $w = $rt->binary('/', $rt->binary('-', $rt->binary('*', $d00, $d21, 1, 'float'), $rt->binary('*', $d01, $d20, 1, 'float'), 1, 'float'), $denom, 1, 'float');
        $u = $rt->binary('-', $rt->binary('-', $rt->f(1), $v, 1, 'float'), $w, 1, 'float');
        return $rt->construct(3, $u, $v, $w);
    };
    $pyramidTriHit__vec2_vec2_vec2_vec2 = sub {
        my ($P, $cellC, $apex, $halfCell) = @_;
        $P = $rt->copy($P, 'float');
        $cellC = $rt->copy($cellC, 'float');
        $apex = $rt->copy($apex, 'float');
        $halfCell = $rt->copy($halfCell, 'float');
        my ($Cbl, $Cbr, $Ctl, $Ctr, $bc, $botC, $leftX, $rightX, $topC);
        $topC = $rt->binary('+', $cellC, $rt->binary('*', $g->{TOP_SIGN}, $rt->construct(2, $rt->f(0), $rt->swizzle($halfCell, 'y')), 2, 'float'), 2, 'float');
        $botC = $rt->binary('-', $cellC, $rt->binary('*', $g->{TOP_SIGN}, $rt->construct(2, $rt->f(0), $rt->swizzle($halfCell, 'y')), 2, 'float'), 2, 'float');
        $leftX = $rt->binary('-', $rt->swizzle($cellC, 'x'), $rt->swizzle($halfCell, 'x'), 1, 'float');
        $rightX = $rt->binary('+', $rt->swizzle($cellC, 'x'), $rt->swizzle($halfCell, 'x'), 1, 'float');
        $Cbl = $rt->construct(2, $leftX, $rt->swizzle($botC, 'y'));
        $Cbr = $rt->construct(2, $rightX, $rt->swizzle($botC, 'y'));
        $Ctr = $rt->construct(2, $rightX, $rt->swizzle($topC, 'y'));
        $Ctl = $rt->construct(2, $leftX, $rt->swizzle($topC, 'y'));
        $bc = $baryWeights__vec2_vec2_vec2_vec2->($P, $Cbl, $Cbr, $apex);
        if ((((($rt->binary('>=', $rt->swizzle($bc, 'x'), $rt->unary('-', $g->{EPS}))) && ($rt->binary('>=', $rt->swizzle($bc, 'y'), $rt->unary('-', $g->{EPS}))) ? 1 : 0)) && ($rt->binary('>=', $rt->swizzle($bc, 'z'), $rt->unary('-', $g->{EPS}))) ? 1 : 0)) {
            return $rt->i(0);
        }
        @{$bc} = map { $rt->f32($_) } @{($baryWeights__vec2_vec2_vec2_vec2->($P, $Cbr, $Ctr, $apex))};
        if ((((($rt->binary('>=', $rt->swizzle($bc, 'x'), $rt->unary('-', $g->{EPS}))) && ($rt->binary('>=', $rt->swizzle($bc, 'y'), $rt->unary('-', $g->{EPS}))) ? 1 : 0)) && ($rt->binary('>=', $rt->swizzle($bc, 'z'), $rt->unary('-', $g->{EPS}))) ? 1 : 0)) {
            return $rt->i(1);
        }
        @{$bc} = map { $rt->f32($_) } @{($baryWeights__vec2_vec2_vec2_vec2->($P, $Ctr, $Ctl, $apex))};
        if ((((($rt->binary('>=', $rt->swizzle($bc, 'x'), $rt->unary('-', $g->{EPS}))) && ($rt->binary('>=', $rt->swizzle($bc, 'y'), $rt->unary('-', $g->{EPS}))) ? 1 : 0)) && ($rt->binary('>=', $rt->swizzle($bc, 'z'), $rt->unary('-', $g->{EPS}))) ? 1 : 0)) {
            return $rt->i(2);
        }
        @{$bc} = map { $rt->f32($_) } @{($baryWeights__vec2_vec2_vec2_vec2->($P, $Ctl, $Cbl, $apex))};
        if ((((($rt->binary('>=', $rt->swizzle($bc, 'x'), $rt->unary('-', $g->{EPS}))) && ($rt->binary('>=', $rt->swizzle($bc, 'y'), $rt->unary('-', $g->{EPS}))) ? 1 : 0)) && ($rt->binary('>=', $rt->swizzle($bc, 'z'), $rt->unary('-', $g->{EPS}))) ? 1 : 0)) {
            return $rt->i(3);
        }
        return $rt->unary('-', $rt->i(1));
    };
    $sideShade__vec2_vec2 = sub {
        my ($P, $cellC) = @_;
        $P = $rt->copy($P, 'float');
        $cellC = $rt->copy($cellC, 'float');
        my ($d, $dyUp);
        $d = $rt->binary('-', $P, $cellC, 2, 'float');
        $dyUp = $rt->binary('*', $rt->swizzle($d, 'y'), $g->{TOP_SIGN}, 1, 'float');
        if ($rt->binary('>', $rt->component_wise('abs', $rt->swizzle($d, 'x')), $rt->component_wise('abs', $dyUp))) {
            return (($rt->binary('>', $rt->swizzle($d, 'x'), $rt->f(0))) ? ($g->{SHADE_RIGHT}) : ($g->{SHADE_LEFT}));
        }
        return (($rt->binary('>', $dyUp, $rt->f(0))) ? ($g->{SHADE_TOP}) : ($g->{SHADE_BOTTOM}));
    };
    $main__void = sub {
        my ($Cbl, $Cbr, $Ci, $Ci1, $Ctl, $Ctr, $P, $_for2_first, $apex, $apexW, $baseColor, $bc, $bestCenterPx, $bestIsTop, $bestPriority, $bestS, $bestTri, $botC, $cellC, $cellIdxF, $distToCenter, $faceCenter, $faceHalf, $found, $h, $halfCell, $i, $imgCenter, $leftX, $localPos, $meanColor, $outColor, $priority, $rightX, $s, $samplePos, $shade, $shadeConst, $sideHit, $stepDir, $t, $toCenter, $topC, $topHit, $tri);
        $P = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $imgCenter = $rt->binary('*', $_u_fullResolution, $rt->f(0.5), 2, 'float');
        $halfCell = $rt->construct(2, $rt->binary('*', $_u_size, $rt->f(0.5), 1, 'float'));
        $toCenter = $rt->binary('-', $imgCenter, $P, 2, 'float');
        $distToCenter = $rt->length($toCenter);
        $stepDir = (($rt->binary('>', $distToCenter, $rt->f(0))) ? ($rt->binary('/', $toCenter, $distToCenter, 2, 'float')) : ($rt->construct(2, $rt->f(0))));
        $bestPriority = $rt->unary('-', $rt->f(1000000000));
        $bestCenterPx = $rt->construct(2, $rt->f(0));
        $bestS = $rt->f(1);
        $bestIsTop = 0;
        $bestTri = $rt->unary('-', $rt->i(1));
        $found = 0;
        $i = $rt->i(0);
        $_for2_first = 1;
        for my $_for2 (0 .. 1048575) {
            if (!$_for2_first) {
                $i = $rt->binary('+', $i, $rt->i(1), 1, 'int');
            }
            $_for2_first = 0;
            if (!($rt->binary('<', $i, $rt->i(6)))) {
                last;
            }
            $t = $rt->component_wise('min', $rt->binary('*', $rt->construct(1, $i), $_u_size, 1, 'float'), $distToCenter);
            $samplePos = $rt->binary('+', $P, $rt->binary('*', $stepDir, $t, 2, 'float'), 2, 'float');
            $cellIdxF = $rt->component_wise('floor', $rt->binary('/', $rt->binary('-', $samplePos, $imgCenter, 2, 'float'), $_u_size, 2, 'float'));
            $cellC = $rt->binary('+', $imgCenter, $rt->binary('*', $rt->binary('+', $cellIdxF, $rt->f(0.5), 2, 'float'), $_u_size, 2, 'float'), 2, 'float');
            $h = $cellHeight__vec2_vec2->($cellC, $cellIdxF);
            $s = $rt->binary('+', $rt->f(1), $rt->binary('*', $rt->binary('*', $h, $rt->binary('/', $_u_depth, $rt->f(100), 1, 'float'), 1, 'float'), $rt->f(0.40000000000000002), 1, 'float'), 1, 'float');
            $apex = $rt->construct(2, 0.0);
            $faceCenter = $rt->construct(2, 0.0);
            $faceHalf = $rt->construct(2, 0.0);
            $sideHit = 0;
            $topHit = 0;
            $tri = 0;
            if ($rt->binary('==', $_u_EXTRUDE_TYPE, $rt->i(1))) {
                $apex = $rt->binary('+', $imgCenter, $rt->binary('*', $rt->binary('-', $cellC, $imgCenter, 2, 'float'), $s, 2, 'float'), 2, 'float');
                $tri = $pyramidTriHit__vec2_vec2_vec2_vec2->($P, $cellC, $apex, $halfCell);
                if ((($rt->binary('>=', $tri, $rt->i(0))) && ($rt->binary('>', $s, $bestPriority)) ? 1 : 0)) {
                    $bestPriority = $s;
                    @{$bestCenterPx} = map { $rt->f32($_) } @{($cellC)};
                    $bestS = $s;
                    $bestTri = $tri;
                    $found = 1;
                }
            } else {
                $faceCenter = $rt->binary('+', $imgCenter, $rt->binary('*', $rt->binary('-', $cellC, $imgCenter, 2, 'float'), $s, 2, 'float'), 2, 'float');
                $faceHalf = $rt->binary('*', $halfCell, $s, 2, 'float');
                $topHit = $rt->component_wise('all', $rt->component_wise('lessThanEqual', $rt->component_wise('abs', $rt->binary('-', $P, $faceCenter, 2, 'float')), $faceHalf));
                $sideHit = (((($topHit) ? 0 : 1)) && ($rt->component_wise('all', $rt->component_wise('lessThanEqual', $rt->component_wise('abs', $rt->binary('-', $P, $cellC, 2, 'float')), $halfCell))) ? 1 : 0);
                $priority = $rt->f(0.0);
                if ((($topHit) || ($sideHit) ? 1 : 0)) {
                    $priority = $rt->binary('+', $s, (($topHit) ? ($rt->f(1000)) : ($rt->f(0))), 1, 'float');
                    if ($rt->binary('>', $priority, $bestPriority)) {
                        $bestPriority = $priority;
                        @{$bestCenterPx} = map { $rt->f32($_) } @{($cellC)};
                        $bestS = $s;
                        $bestIsTop = $topHit;
                        $found = 1;
                    }
                }
            }
            if ($rt->binary('>=', $t, $distToCenter)) {
                last;
            }
        }
        $outColor = $rt->construct(4, 0.0);
        $cellC = $rt->construct(2, 0.0);
        if ((($found) ? 0 : 1)) {
            $cellC = $rt->binary('+', $imgCenter, $rt->binary('*', $rt->binary('+', $rt->component_wise('floor', $rt->binary('/', $rt->binary('-', $P, $imgCenter, 2, 'float'), $_u_size, 2, 'float')), $rt->f(0.5), 2, 'float'), $_u_size, 2, 'float'), 2, 'float');
            @{$outColor} = map { $rt->f32($_) } @{($cellAvgColor3x3__vec2->($cellC))};
        } else {
            $Cbl = $rt->construct(2, 0.0);
            $Cbr = $rt->construct(2, 0.0);
            $Ci = $rt->construct(2, 0.0);
            $Ci1 = $rt->construct(2, 0.0);
            $Ctl = $rt->construct(2, 0.0);
            $Ctr = $rt->construct(2, 0.0);
            $apex = $rt->construct(2, 0.0);
            $apexW = $rt->f(0.0);
            $baseColor = $rt->construct(4, 0.0);
            $bc = $rt->construct(3, 0.0);
            $botC = $rt->construct(2, 0.0);
            $leftX = $rt->f(0.0);
            $rightX = $rt->f(0.0);
            $shade = $rt->f(0.0);
            $shadeConst = $rt->f(0.0);
            $topC = $rt->construct(2, 0.0);
            if ($rt->binary('==', $_u_EXTRUDE_TYPE, $rt->i(1))) {
                $apex = $rt->binary('+', $imgCenter, $rt->binary('*', $rt->binary('-', $bestCenterPx, $imgCenter, 2, 'float'), $bestS, 2, 'float'), 2, 'float');
                $topC = $rt->binary('+', $bestCenterPx, $rt->binary('*', $g->{TOP_SIGN}, $rt->construct(2, $rt->f(0), $rt->swizzle($halfCell, 'y')), 2, 'float'), 2, 'float');
                $botC = $rt->binary('-', $bestCenterPx, $rt->binary('*', $g->{TOP_SIGN}, $rt->construct(2, $rt->f(0), $rt->swizzle($halfCell, 'y')), 2, 'float'), 2, 'float');
                $leftX = $rt->binary('-', $rt->swizzle($bestCenterPx, 'x'), $rt->swizzle($halfCell, 'x'), 1, 'float');
                $rightX = $rt->binary('+', $rt->swizzle($bestCenterPx, 'x'), $rt->swizzle($halfCell, 'x'), 1, 'float');
                $Cbl = $rt->construct(2, $leftX, $rt->swizzle($botC, 'y'));
                $Cbr = $rt->construct(2, $rightX, $rt->swizzle($botC, 'y'));
                $Ctr = $rt->construct(2, $rightX, $rt->swizzle($topC, 'y'));
                $Ctl = $rt->construct(2, $leftX, $rt->swizzle($topC, 'y'));
                $Ci = $rt->construct(2, 0.0);
                $Ci1 = $rt->construct(2, 0.0);
                $shadeConst = $rt->f(0.0);
                if ($rt->binary('==', $bestTri, $rt->i(0))) {
                    @{$Ci} = map { $rt->f32($_) } @{($Cbl)};
                    @{$Ci1} = map { $rt->f32($_) } @{($Cbr)};
                    $shadeConst = $g->{SHADE_BOTTOM};
                } else {
                    if ($rt->binary('==', $bestTri, $rt->i(1))) {
                        @{$Ci} = map { $rt->f32($_) } @{($Cbr)};
                        @{$Ci1} = map { $rt->f32($_) } @{($Ctr)};
                        $shadeConst = $g->{SHADE_RIGHT};
                    } else {
                        if ($rt->binary('==', $bestTri, $rt->i(2))) {
                            @{$Ci} = map { $rt->f32($_) } @{($Ctr)};
                            @{$Ci1} = map { $rt->f32($_) } @{($Ctl)};
                            $shadeConst = $g->{SHADE_TOP};
                        } else {
                            @{$Ci} = map { $rt->f32($_) } @{($Ctl)};
                            @{$Ci1} = map { $rt->f32($_) } @{($Cbl)};
                            $shadeConst = $g->{SHADE_LEFT};
                        }
                    }
                }
                $bc = $baryWeights__vec2_vec2_vec2_vec2->($P, $Ci, $Ci1, $apex);
                $apexW = $rt->component_wise('clamp', $rt->swizzle($bc, 'z'), $rt->f(0), $rt->f(1));
                $baseColor = $rt->construct(4, 0.0);
                $localPos = $rt->construct(2, 0.0);
                if ($_u_solidFront) {
                    @{$baseColor} = map { $rt->f32($_) } @{($cellAvgColor3x3__vec2->($bestCenterPx))};
                } else {
                    $localPos = $rt->binary('+', $rt->binary('+', $rt->binary('*', $rt->swizzle($bc, 'x'), $Ci, 2, 'float'), $rt->binary('*', $rt->swizzle($bc, 'y'), $Ci1, 2, 'float'), 2, 'float'), $rt->binary('*', $rt->swizzle($bc, 'z'), $bestCenterPx, 2, 'float'), 2, 'float');
                    @{$baseColor} = map { $rt->f32($_) } @{($rt->texture($_u_inputTex, $toSampleUV__vec2->($localPos)))};
                }
                $shade = $rt->component_wise('mix', $rt->f(1), $shadeConst, $apexW);
                @{$outColor} = map { $rt->f32($_) } @{($rt->construct(4, $rt->binary('*', $rt->swizzle($baseColor, 'rgb'), $shade, 3, 'float'), $rt->swizzle($baseColor, 'a')))};
            } else {
                $meanColor = $rt->construct(4, 0.0);
                if ($bestIsTop) {
                    $localPos = $rt->construct(2, 0.0);
                    if ($_u_solidFront) {
                        @{$outColor} = map { $rt->f32($_) } @{($cellAvgColor3x3__vec2->($bestCenterPx))};
                    } else {
                        $localPos = $rt->binary('+', $imgCenter, $rt->binary('/', $rt->binary('-', $P, $imgCenter, 2, 'float'), $bestS, 2, 'float'), 2, 'float');
                        @{$outColor} = map { $rt->f32($_) } @{($rt->texture($_u_inputTex, $toSampleUV__vec2->($localPos)))};
                    }
                } else {
                    $shade = $sideShade__vec2_vec2->($P, $bestCenterPx);
                    $meanColor = $cellAvgColor3x3__vec2->($bestCenterPx);
                    @{$outColor} = map { $rt->f32($_) } @{($rt->construct(4, $rt->binary('*', $rt->swizzle($meanColor, 'rgb'), $shade, 3, 'float'), $rt->swizzle($meanColor, 'a')))};
                }
            }
        }
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($outColor)};
    };
    $main__void->();
    my $_c0 = $g->{fragColor};
    $out->[0] = $rt->f32($_c0->[0]); $out->[1] = $rt->f32($_c0->[1]); $out->[2] = $rt->f32($_c0->[2]); $out->[3] = $rt->f32($_c0->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0, output_names => ['fragColor'] };
