# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($hsv2rgb__vec3, $bitOp__int_int_int_int, $main__void);
    my $_retc;
    my $_u_resolution = exists $U->{'resolution'} ? $U->{'resolution'} : $rt->construct(2, 0.0);
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_renderScale = exists $U->{'renderScale'} ? $U->{'renderScale'} : $rt->f(0.0);
    my $_u_time = exists $U->{'time'} ? $U->{'time'} : $rt->f(0.0);
    my $_u_operation = exists $U->{'operation'} ? $U->{'operation'} : 0;
    my $_u_scale = exists $U->{'scale'} ? $U->{'scale'} : $rt->f(0.0);
    my $_u_offsetX = exists $U->{'offsetX'} ? $U->{'offsetX'} : 0;
    my $_u_offsetY = exists $U->{'offsetY'} ? $U->{'offsetY'} : 0;
    my $_u_mask = exists $U->{'mask'} ? $U->{'mask'} : 0;
    my $_u_seed = exists $U->{'seed'} ? $U->{'seed'} : 0;
    my $_u_colorMode = exists $U->{'colorMode'} ? $U->{'colorMode'} : 0;
    my $_u_speed = exists $U->{'speed'} ? $U->{'speed'} : $rt->f(0.0);
    my $_u_rotation = exists $U->{'rotation'} ? $U->{'rotation'} : $rt->f(0.0);
    my $_u_colorOffset = exists $U->{'colorOffset'} ? $U->{'colorOffset'} : 0;
    $g->{fragColor} = $rt->construct(4, 0.0);
    $g->{PI} = $rt->f(3.14159265358979);
    $hsv2rgb__vec3 = sub {
        my ($c) = @_;
        $c = $rt->copy($c, 'float');
        my ($p);
        $p = $rt->component_wise('abs', $rt->binary('-', $rt->binary('*', $rt->component_wise('fract', $rt->binary('+', $rt->swizzle($c, 'xxx'), $rt->construct(3, $rt->f(1), $rt->binary('/', $rt->f(2), $rt->f(3), 1, 'float'), $rt->binary('/', $rt->f(1), $rt->f(3), 1, 'float')), 3, 'float')), $rt->f(6), 3, 'float'), $rt->f(3), 3, 'float'));
        return $rt->binary('*', $rt->swizzle($c, 'z'), $rt->component_wise('mix', $rt->construct(3, $rt->f(1)), $rt->component_wise('clamp', $rt->binary('-', $p, $rt->f(1), 3, 'float'), $rt->f(0), $rt->f(1)), $rt->swizzle($c, 'y')), 3, 'float');
    };
    $bitOp__int_int_int_int = sub {
        my ($a, $b, $op, $m) = @_;
        my ($r);
        $r = $rt->i(0);
        if ($rt->binary('==', $op, $rt->i(0))) {
            $r = $rt->binary('^', $a, $b, 1, 'int');
        } else {
            if ($rt->binary('==', $op, $rt->i(1))) {
                $r = $rt->binary('&', $a, $b, 1, 'int');
            } else {
                if ($rt->binary('==', $op, $rt->i(2))) {
                    $r = $rt->binary('|', $a, $b, 1, 'int');
                } else {
                    if ($rt->binary('==', $op, $rt->i(3))) {
                        $r = $rt->bit_not($rt->binary('&', $a, $b, 1, 'int'));
                    } else {
                        if ($rt->binary('==', $op, $rt->i(4))) {
                            $r = $rt->bit_not($rt->binary('^', $a, $b, 1, 'int'));
                        } else {
                            if ($rt->binary('==', $op, $rt->i(5))) {
                                $r = $rt->binary('*', $a, $b, 1, 'int');
                            } else {
                                if ($rt->binary('==', $op, $rt->i(6))) {
                                    $r = $rt->binary('+', $a, $b, 1, 'int');
                                } else {
                                    $r = $rt->binary('-', $a, $b, 1, 'int');
                                }
                            }
                        }
                    }
                }
            }
        }
        $r = $rt->binary('&', $r, $m, 1, 'int');
        return $rt->binary('/', $rt->construct(1, $r), $rt->construct(1, $m), 1, 'float');
    };
    $main__void = sub {
        my ($_g, $angle, $animOffset, $b, $c, $centered, $coord, $globalCoord, $hueScale, $pixelScale, $r, $rotated, $s, $v, $x, $y);
        $pixelScale = $rt->binary('*', $rt->binary('*', $_u_scale, $rt->f(0.10000000000000001), 1, 'float'), $_u_renderScale, 1, 'float');
        $angle = $rt->binary('/', $rt->binary('*', $_u_rotation, $g->{PI}, 1, 'float'), $rt->f(180), 1, 'float');
        $c = $rt->component_wise('cos', $angle);
        $s = $rt->component_wise('sin', $angle);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $centered = $rt->binary('-', $globalCoord, $rt->binary('*', $_u_fullResolution, $rt->f(0.5), 2, 'float'), 2, 'float');
        $rotated = $rt->construct(2, $rt->binary('-', $rt->binary('*', $rt->swizzle($centered, 'x'), $c, 1, 'float'), $rt->binary('*', $rt->swizzle($centered, 'y'), $s, 1, 'float'), 1, 'float'), $rt->binary('+', $rt->binary('*', $rt->swizzle($centered, 'x'), $s, 1, 'float'), $rt->binary('*', $rt->swizzle($centered, 'y'), $c, 1, 'float'), 1, 'float'));
        $coord = $rt->binary('+', $rotated, $rt->binary('*', $_u_fullResolution, $rt->f(0.5), 2, 'float'), 2, 'float');
        $animOffset = $rt->construct(1, $rt->component_wise('floor', $rt->binary('*', $rt->binary('*', $_u_time, $rt->construct(1, $rt->construct(1, $rt->unary('-', $_u_speed), 'int')), 1, 'float'), $rt->f(256), 1, 'float')), 'int');
        $x = $rt->binary('+', $rt->binary('+', $rt->construct(1, $rt->component_wise('floor', $rt->binary('/', $rt->swizzle($coord, 'x'), $pixelScale, 1, 'float')), 'int'), $_u_offsetX, 1, 'int'), $animOffset, 1, 'int');
        $y = $rt->binary('+', $rt->construct(1, $rt->component_wise('floor', $rt->binary('/', $rt->swizzle($coord, 'y'), $pixelScale, 1, 'float')), 'int'), $_u_offsetY, 1, 'int');
        $x = $rt->binary('^', $x, $_u_seed, 1, 'int');
        $y = $rt->binary('^', $y, $rt->binary('*', $_u_seed, $rt->i(3), 1, 'int'), 1, 'int');
        $v = $rt->f(0.0);
        $b = $rt->f(0.0);
        $_g = $rt->f(0.0);
        $hueScale = $rt->f(0.0);
        $r = $rt->f(0.0);
        if ($rt->binary('==', $_u_colorMode, $rt->i(0))) {
            $v = $bitOp__int_int_int_int->($x, $y, $_u_operation, $_u_mask);
            @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $v, $v, $v, $rt->f(1)))};
        } else {
            if ($rt->binary('==', $_u_colorMode, $rt->i(1))) {
                $r = $bitOp__int_int_int_int->($x, $y, $_u_operation, $_u_mask);
                $_g = $bitOp__int_int_int_int->($rt->binary('+', $x, $_u_colorOffset, 1, 'int'), $y, $_u_operation, $_u_mask);
                $b = $bitOp__int_int_int_int->($x, $rt->binary('+', $y, $_u_colorOffset, 1, 'int'), $_u_operation, $_u_mask);
                @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $r, $_g, $b, $rt->f(1)))};
            } else {
                $v = $bitOp__int_int_int_int->($x, $y, $_u_operation, $_u_mask);
                $hueScale = $rt->binary('/', $rt->construct(1, $_u_mask), $rt->construct(1, $rt->binary('+', $_u_mask, $rt->i(1), 1, 'int')), 1, 'float');
                @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $hsv2rgb__vec3->($rt->construct(3, $rt->binary('*', $v, $hueScale, 1, 'float'), $rt->f(1), $rt->f(1))), $rt->f(1)))};
            }
        }
    };
    $main__void->();
    my $_c = $g->{fragColor};
    $out->[0] = $rt->f32($_c->[0]); $out->[1] = $rt->f32($_c->[1]); $out->[2] = $rt->f32($_c->[2]); $out->[3] = $rt->f32($_c->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0 };
