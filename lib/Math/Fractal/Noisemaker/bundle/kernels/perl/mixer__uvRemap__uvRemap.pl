# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($mirrorWrap__float, $applyWrap__vec2_int, $main__void);
    my $_retc;
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_tex = $ctx->texture_binding('tex');
    my $_u_resolution = exists $U->{'resolution'} ? $U->{'resolution'} : $rt->construct(2, 0.0);
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_mapSource = exists $U->{'mapSource'} ? $U->{'mapSource'} : 0;
    my $_u_channel = exists $U->{'channel'} ? $U->{'channel'} : 0;
    my $_u_scale = exists $U->{'scale'} ? $U->{'scale'} : $rt->f(0.0);
    my $_u_offset = exists $U->{'offset'} ? $U->{'offset'} : $rt->f(0.0);
    my $_u_wrap = exists $U->{'wrap'} ? $U->{'wrap'} : 0;
    $g->{fragColor} = $rt->construct(4, 0.0);
    $mirrorWrap__float = sub {
        my ($t) = @_;
        my ($m);
        $m = $rt->component_wise('mod', $t, $rt->f(2));
        return (($rt->binary('>', $m, $rt->f(1))) ? ($rt->binary('-', $rt->f(2), $m, 1, 'float')) : ($m));
    };
    $applyWrap__vec2_int = sub {
        my ($uv, $wrapMode) = @_;
        $uv = $rt->copy($uv, 'float');
        if ($rt->binary('==', $wrapMode, $rt->i(0))) {
            return $rt->component_wise('clamp', $uv, $rt->f(0), $rt->f(1));
        } else {
            if ($rt->binary('==', $wrapMode, $rt->i(1))) {
                return $rt->construct(2, $mirrorWrap__float->($rt->swizzle($uv, 'x')), $mirrorWrap__float->($rt->swizzle($uv, 'y')));
            } else {
                return $rt->component_wise('fract', $uv);
            }
        }
    };
    $main__void = sub {
        my ($colorA, $colorB, $localUV, $mapColor, $rawUV, $remappedUV, $result, $s, $sampleFromB, $sampleUV);
        $localUV = $rt->binary('/', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_resolution, 2, 'float');
        $colorA = $rt->texture($_u_inputTex, $localUV);
        $colorB = $rt->texture($_u_tex, $localUV);
        $mapColor = (($rt->binary('==', $_u_mapSource, $rt->i(0))) ? ($colorA) : ($colorB));
        $sampleFromB = (($rt->binary('==', $_u_mapSource, $rt->i(0))) ? ($rt->i(1)) : ($rt->i(0)));
        $rawUV = $rt->construct(2, 0.0);
        if ($rt->binary('==', $_u_channel, $rt->i(0))) {
            @{$rawUV} = map { $rt->f32($_) } @{($rt->swizzle($mapColor, 'rg'))};
        } else {
            if ($rt->binary('==', $_u_channel, $rt->i(1))) {
                @{$rawUV} = map { $rt->f32($_) } @{($rt->construct(2, $rt->swizzle($mapColor, 'r'), $rt->swizzle($mapColor, 'b')))};
            } else {
                @{$rawUV} = map { $rt->f32($_) } @{($rt->construct(2, $rt->swizzle($mapColor, 'g'), $rt->swizzle($mapColor, 'b')))};
            }
        }
        $s = $rt->binary('/', $_u_scale, $rt->f(100), 1, 'float');
        $remappedUV = $rt->binary('+', $rt->binary('*', $rawUV, $s, 2, 'float'), $_u_offset, 2, 'float');
        @{$remappedUV} = map { $rt->f32($_) } @{($applyWrap__vec2_int->($remappedUV, $_u_wrap))};
        $sampleUV = $rt->binary('/', $rt->binary('-', $rt->binary('*', $remappedUV, $_u_fullResolution, 2, 'float'), $_u_tileOffset, 2, 'float'), $_u_resolution, 2, 'float');
        @{$sampleUV} = map { $rt->f32($_) } @{($rt->component_wise('fract', $sampleUV))};
        $result = $rt->construct(4, 0.0);
        if ($rt->binary('==', $sampleFromB, $rt->i(1))) {
            @{$result} = map { $rt->f32($_) } @{($rt->texture($_u_tex, $sampleUV))};
        } else {
            @{$result} = map { $rt->f32($_) } @{($rt->texture($_u_inputTex, $sampleUV))};
        }
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($result)};
    };
    $main__void->();
    my $_c0 = $g->{fragColor};
    $out->[0] = $rt->f32($_c0->[0]); $out->[1] = $rt->f32($_c0->[1]); $out->[2] = $rt->f32($_c0->[2]); $out->[3] = $rt->f32($_c0->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0, output_names => ['fragColor'] };
