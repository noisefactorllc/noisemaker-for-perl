# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($df64_quick_two_sum__float_float, $df64_two_sum__float_float, $df64_two_prod__float_float, $df64_add__vec2_vec2, $df64_sub__vec2_vec2, $df64_mul__vec2_vec2, $df64_mul_f__vec2_float, $df64_from__float, $df64_to_float__vec2, $df64_cmul__vec2_vec2_vec2_vec2_vec2_vec2, $transformCoords_df64__vec2_vec2_vec2_float_float_vec2_vec2, $getPOI__int, $main__void);
    my $_retc;
    my $_u_resolution = exists $U->{'resolution'} ? $U->{'resolution'} : $rt->construct(2, 0.0);
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_time = exists $U->{'time'} ? $U->{'time'} : $rt->f(0.0);
    my $_u_degree = exists $U->{'degree'} ? $U->{'degree'} : $rt->f(0.0);
    my $_u_relaxation = exists $U->{'relaxation'} ? $U->{'relaxation'} : $rt->f(0.0);
    my $_u_iterations = exists $U->{'iterations'} ? $U->{'iterations'} : $rt->f(0.0);
    my $_u_tolerance = exists $U->{'tolerance'} ? $U->{'tolerance'} : $rt->f(0.0);
    my $_u_poi = exists $U->{'poi'} ? $U->{'poi'} : $rt->f(0.0);
    my $_u_centerHiX = exists $U->{'centerHiX'} ? $U->{'centerHiX'} : $rt->f(0.0);
    my $_u_centerHiY = exists $U->{'centerHiY'} ? $U->{'centerHiY'} : $rt->f(0.0);
    my $_u_centerLoX = exists $U->{'centerLoX'} ? $U->{'centerLoX'} : $rt->f(0.0);
    my $_u_centerLoY = exists $U->{'centerLoY'} ? $U->{'centerLoY'} : $rt->f(0.0);
    my $_u_zoomSpeed = exists $U->{'zoomSpeed'} ? $U->{'zoomSpeed'} : $rt->f(0.0);
    my $_u_zoomDepth = exists $U->{'zoomDepth'} ? $U->{'zoomDepth'} : $rt->f(0.0);
    my $_u_degreeSpeed = exists $U->{'degreeSpeed'} ? $U->{'degreeSpeed'} : $rt->f(0.0);
    my $_u_degreeRange = exists $U->{'degreeRange'} ? $U->{'degreeRange'} : $rt->f(0.0);
    my $_u_relaxSpeed = exists $U->{'relaxSpeed'} ? $U->{'relaxSpeed'} : $rt->f(0.0);
    my $_u_relaxRange = exists $U->{'relaxRange'} ? $U->{'relaxRange'} : $rt->f(0.0);
    my $_u_rotation = exists $U->{'rotation'} ? $U->{'rotation'} : $rt->f(0.0);
    my $_u_outputMode = exists $U->{'outputMode'} ? $U->{'outputMode'} : $rt->f(0.0);
    my $_u_invert = exists $U->{'invert'} ? $U->{'invert'} : $rt->f(0.0);
    $g->{fragColor} = $rt->construct(4, 0.0);
    $g->{PI} = $rt->f(3.1415926535900001);
    $g->{TAU} = $rt->f(6.2831853071800001);
    $g->{PHI} = $rt->f(1.6180339886999999);
    $df64_quick_two_sum__float_float = sub {
        my ($a, $b) = @_;
        my ($e, $s);
        $s = $rt->binary('+', $a, $b, 1, 'float');
        $e = $rt->binary('-', $b, $rt->binary('-', $s, $a, 1, 'float'), 1, 'float');
        return $rt->construct(2, $s, $e);
    };
    $df64_two_sum__float_float = sub {
        my ($a, $b) = @_;
        my ($e, $s, $v);
        $s = $rt->binary('+', $a, $b, 1, 'float');
        $v = $rt->binary('-', $s, $a, 1, 'float');
        $e = $rt->binary('+', $rt->binary('-', $a, $rt->binary('-', $s, $v, 1, 'float'), 1, 'float'), $rt->binary('-', $b, $v, 1, 'float'), 1, 'float');
        return $rt->construct(2, $s, $e);
    };
    $df64_two_prod__float_float = sub {
        my ($a, $b) = @_;
        my ($ah, $al, $bh, $bl, $ca, $cb, $e, $p);
        $p = $rt->binary('*', $a, $b, 1, 'float');
        $ca = $rt->binary('*', $rt->f(4097), $a, 1, 'float');
        $ah = $rt->binary('-', $ca, $rt->binary('-', $ca, $a, 1, 'float'), 1, 'float');
        $al = $rt->binary('-', $a, $ah, 1, 'float');
        $cb = $rt->binary('*', $rt->f(4097), $b, 1, 'float');
        $bh = $rt->binary('-', $cb, $rt->binary('-', $cb, $b, 1, 'float'), 1, 'float');
        $bl = $rt->binary('-', $b, $bh, 1, 'float');
        $e = $rt->binary('+', $rt->binary('+', $rt->binary('+', $rt->binary('-', $rt->binary('*', $ah, $bh, 1, 'float'), $p, 1, 'float'), $rt->binary('*', $ah, $bl, 1, 'float'), 1, 'float'), $rt->binary('*', $al, $bh, 1, 'float'), 1, 'float'), $rt->binary('*', $al, $bl, 1, 'float'), 1, 'float');
        return $rt->construct(2, $p, $e);
    };
    $df64_add__vec2_vec2 = sub {
        my ($a, $b) = @_;
        $a = $rt->copy($a, 'float');
        $b = $rt->copy($b, 'float');
        my ($s);
        $s = $df64_two_sum__float_float->($rt->swizzle($a, 'x'), $rt->swizzle($b, 'x'));
        $s = $rt->assign_swizzle($s, 'y', $rt->binary('+', $rt->swizzle($s, 'y'), $rt->binary('+', $rt->swizzle($a, 'y'), $rt->swizzle($b, 'y'), 1, 'float'), 1, 'float'));
        return $df64_quick_two_sum__float_float->($rt->swizzle($s, 'x'), $rt->swizzle($s, 'y'));
    };
    $df64_sub__vec2_vec2 = sub {
        my ($a, $b) = @_;
        $a = $rt->copy($a, 'float');
        $b = $rt->copy($b, 'float');
        return $df64_add__vec2_vec2->($a, $rt->construct(2, $rt->unary('-', $rt->swizzle($b, 'x')), $rt->unary('-', $rt->swizzle($b, 'y'))));
    };
    $df64_mul__vec2_vec2 = sub {
        my ($a, $b) = @_;
        $a = $rt->copy($a, 'float');
        $b = $rt->copy($b, 'float');
        my ($p);
        $p = $df64_two_prod__float_float->($rt->swizzle($a, 'x'), $rt->swizzle($b, 'x'));
        $p = $rt->assign_swizzle($p, 'y', $rt->binary('+', $rt->swizzle($p, 'y'), $rt->binary('+', $rt->binary('*', $rt->swizzle($a, 'x'), $rt->swizzle($b, 'y'), 1, 'float'), $rt->binary('*', $rt->swizzle($a, 'y'), $rt->swizzle($b, 'x'), 1, 'float'), 1, 'float'), 1, 'float'));
        return $df64_quick_two_sum__float_float->($rt->swizzle($p, 'x'), $rt->swizzle($p, 'y'));
    };
    $df64_mul_f__vec2_float = sub {
        my ($a, $b) = @_;
        $a = $rt->copy($a, 'float');
        my ($p);
        $p = $df64_two_prod__float_float->($rt->swizzle($a, 'x'), $b);
        $p = $rt->assign_swizzle($p, 'y', $rt->binary('+', $rt->swizzle($p, 'y'), $rt->binary('*', $rt->swizzle($a, 'y'), $b, 1, 'float'), 1, 'float'));
        return $df64_quick_two_sum__float_float->($rt->swizzle($p, 'x'), $rt->swizzle($p, 'y'));
    };
    $df64_from__float = sub {
        my ($a) = @_;
        return $rt->construct(2, $a, $rt->f(0));
    };
    $df64_to_float__vec2 = sub {
        my ($a) = @_;
        $a = $rt->copy($a, 'float');
        return $rt->binary('+', $rt->swizzle($a, 'x'), $rt->swizzle($a, 'y'), 1, 'float');
    };
    $df64_cmul__vec2_vec2_vec2_vec2_vec2_vec2 = sub {
        my ($ar, $ai, $br, $bi, $rr, $ri) = @_;
        $ar = $rt->copy($ar, 'float');
        $ai = $rt->copy($ai, 'float');
        $br = $rt->copy($br, 'float');
        $bi = $rt->copy($bi, 'float');
        $rr = $rt->copy($rr, 'float');
        $ri = $rt->copy($ri, 'float');
        @{$rr} = map { $rt->f32($_) } @{($df64_sub__vec2_vec2->($df64_mul__vec2_vec2->($ar, $br), $df64_mul__vec2_vec2->($ai, $bi)))};
        @{$ri} = map { $rt->f32($_) } @{($df64_add__vec2_vec2->($df64_mul__vec2_vec2->($ar, $bi), $df64_mul__vec2_vec2->($ai, $br)))};
        return (undef, $rr, $ri);
    };
    $transformCoords_df64__vec2_vec2_vec2_float_float_vec2_vec2 = sub {
        my ($fragCoord, $cX_df, $cY_df, $z_zoom, $rot, $re_df, $im_df) = @_;
        $fragCoord = $rt->copy($fragCoord, 'float');
        $cX_df = $rt->copy($cX_df, 'float');
        $cY_df = $rt->copy($cY_df, 'float');
        $re_df = $rt->copy($re_df, 'float');
        $im_df = $rt->copy($im_df, 'float');
        my ($angle, $c, $s, $scale, $uv, $uv_im_df, $uv_re_df);
        $uv = $rt->binary('/', $rt->binary('-', $fragCoord, $rt->binary('*', $rt->f(0.5), $_u_fullResolution, 2, 'float'), 2, 'float'), $rt->component_wise('min', $rt->swizzle($_u_fullResolution, 'x'), $rt->swizzle($_u_fullResolution, 'y')), 2, 'float');
        $angle = $rt->binary('/', $rt->binary('*', $rt->unary('-', $rot), $g->{TAU}, 1, 'float'), $rt->f(360), 1, 'float');
        $c = $rt->component_wise('cos', $angle);
        $s = $rt->component_wise('sin', $angle);
        @{$uv} = map { $rt->f32($_) } @{($rt->matrix_mult($rt->construct(4, $c, $rt->unary('-', $s), $s, $c), $uv, 2))};
        $scale = $rt->binary('/', $rt->f(2.5), $z_zoom, 1, 'float');
        $uv_re_df = $df64_mul_f__vec2_float->($df64_from__float->($rt->swizzle($uv, 'x')), $scale);
        $uv_im_df = $df64_mul_f__vec2_float->($df64_from__float->($rt->swizzle($uv, 'y')), $scale);
        @{$re_df} = map { $rt->f32($_) } @{($df64_add__vec2_vec2->($uv_re_df, $cX_df))};
        @{$im_df} = map { $rt->f32($_) } @{($df64_add__vec2_vec2->($uv_im_df, $cY_df))};
        return (undef, $re_df, $im_df);
    };
    $getPOI__int = sub {
        my ($idx) = @_;
        if ($rt->binary('==', $idx, $rt->i(1))) {
            return [$rt->construct(4, $rt->f(0), $rt->f(0), $rt->f(0), $rt->f(0)), $rt->f(3), $rt->f(7)];
        }
        if ($rt->binary('==', $idx, $rt->i(2))) {
            return [$rt->construct(4, $rt->f(0.25), $rt->f(0.4330126941204071), $rt->f(0), $rt->f(7.7718000000000008e-09)), $rt->f(3), $rt->f(14)];
        }
        if ($rt->binary('==', $idx, $rt->i(3))) {
            return [$rt->construct(4, $rt->f(0), $rt->f(0), $rt->f(0), $rt->f(0)), $rt->f(5), $rt->f(7)];
        }
        if ($rt->binary('==', $idx, $rt->i(4))) {
            return [$rt->construct(4, $rt->f(0.65450847148895264), $rt->f(0.47552827000617981), $rt->f(2.5699000000000001e-08), $rt->unary('-', $rt->f(1.1859000000000001e-08))), $rt->f(5), $rt->f(14)];
        }
        if ($rt->binary('==', $idx, $rt->i(5))) {
            return [$rt->construct(4, $rt->f(0), $rt->f(0), $rt->f(0), $rt->f(0)), $rt->f(6), $rt->f(7)];
        }
        if ($rt->binary('==', $idx, $rt->i(6))) {
            return [$rt->construct(4, $rt->f(0), $rt->f(0), $rt->f(0), $rt->f(0)), $rt->f(8), $rt->f(7)];
        }
        return [$rt->construct(4, $rt->f(0)), $rt->f(3), $rt->f(7)];
    };
    $main__void = sub {
        my ($_for0_first, $_for1_first, $_for2_first, $_for3_first, $angle, $bailout, $cHi, $cLo, $convergeDist, $convergedRoot, $d, $denom, $di, $doInvert, $dr, $dx, $dy, $effDegree, $effRelax, $effZoomDepth, $fpzi, $fpzi_f, $fpzr, $fpzr_f, $fzi, $fzr, $globalCoord, $im_df, $intDeg, $inv_denom, $iter, $j, $k, $maxIter, $maxIterF, $n, $ni, $nr, $numRoots, $numRootsF, $outMode, $p, $poiIdx, $pwi, $pwr, $re_df, $roots, $smoothIter, $ti, $tr, $value, $zi_df, $zni, $znr, $zoom, $zoomPhase, $zr_df, $zx, $zy);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $maxIter = $rt->construct(1, $_u_iterations, 'int');
        $poiIdx = $rt->construct(1, $_u_poi, 'int');
        $outMode = $rt->construct(1, $_u_outputMode, 'int');
        $doInvert = $rt->binary('>', $_u_invert, $rt->f(0.5));
        $effDegree = $_u_degree;
        if ((($rt->binary('>', $_u_degreeSpeed, $rt->f(0))) && ($rt->binary('>', $_u_degreeRange, $rt->f(0))) ? 1 : 0)) {
            $effDegree = $rt->binary('+', $effDegree, $rt->binary('*', $_u_degreeRange, $rt->component_wise('sin', $rt->binary('*', $rt->binary('*', $_u_time, $_u_degreeSpeed, 1, 'float'), $g->{TAU}, 1, 'float')), 1, 'float'), 1, 'float');
            $effDegree = $rt->component_wise('clamp', $effDegree, $rt->f(3), $rt->f(8));
        }
        $effRelax = $_u_relaxation;
        if ((($rt->binary('>', $_u_relaxSpeed, $rt->f(0))) && ($rt->binary('>', $_u_relaxRange, $rt->f(0))) ? 1 : 0)) {
            $effRelax = $rt->binary('+', $effRelax, $rt->binary('*', $_u_relaxRange, $rt->component_wise('sin', $rt->binary('*', $rt->binary('*', $rt->binary('*', $_u_time, $_u_relaxSpeed, 1, 'float'), $g->{TAU}, 1, 'float'), $g->{PHI}, 1, 'float')), 1, 'float'), 1, 'float');
            $effRelax = $rt->component_wise('clamp', $effRelax, $rt->f(0.5), $rt->f(2));
        }
        $cHi = $rt->construct(2, 0.0);
        $cLo = $rt->construct(2, 0.0);
        $effZoomDepth = $_u_zoomDepth;
        $p = [$rt->construct(4, 0.0), $rt->f(0.0), $rt->f(0.0)];
        if ($rt->binary('>', $poiIdx, $rt->i(0))) {
            $p = $getPOI__int->($poiIdx);
            @{$cHi} = map { $rt->f32($_) } @{($rt->binary('+', $rt->swizzle($p->[0], 'xy'), $rt->construct(2, $_u_centerHiX, $_u_centerHiY), 2, 'float'))};
            @{$cLo} = map { $rt->f32($_) } @{($rt->binary('+', $rt->swizzle($p->[0], 'zw'), $rt->construct(2, $_u_centerLoX, $_u_centerLoY), 2, 'float'))};
            $effDegree = $p->[1];
            $effZoomDepth = $rt->component_wise('min', $_u_zoomDepth, $p->[2]);
        } else {
            @{$cHi} = map { $rt->f32($_) } @{($rt->construct(2, $_u_centerHiX, $_u_centerHiY))};
            @{$cLo} = map { $rt->f32($_) } @{($rt->construct(2, $_u_centerLoX, $_u_centerLoY))};
        }
        $zoom = $rt->f(0.0);
        $zoomPhase = $rt->f(0.0);
        if ($rt->binary('>', $_u_zoomSpeed, $rt->f(0))) {
            $zoomPhase = $rt->binary('*', $rt->f(0.5), $rt->binary('-', $rt->f(1), $rt->component_wise('cos', $rt->binary('*', $rt->binary('*', $_u_time, $_u_zoomSpeed, 1, 'float'), $g->{TAU}, 1, 'float')), 1, 'float'), 1, 'float');
            $zoom = $rt->component_wise('pow', $rt->f(10), $rt->binary('*', $effZoomDepth, $zoomPhase, 1, 'float'));
        } else {
            $zoom = $rt->component_wise('pow', $rt->f(10), $effZoomDepth);
        }
        $re_df = $rt->construct(2, 0.0);
        $im_df = $rt->construct(2, 0.0);
        ($_retc, $re_df, $im_df) = $transformCoords_df64__vec2_vec2_vec2_float_float_vec2_vec2->($globalCoord, $rt->construct(2, $rt->swizzle($cHi, 'x'), $rt->swizzle($cLo, 'x')), $rt->construct(2, $rt->swizzle($cHi, 'y'), $rt->swizzle($cLo, 'y')), $zoom, $_u_rotation, $re_df, $im_df);
        $intDeg = $rt->construct(1, $rt->component_wise('floor', $effDegree), 'int');
        $numRoots = $intDeg;
        $roots = $rt->new_array($rt->i(8), 2);
        $k = $rt->i(0);
        $_for0_first = 1;
        for my $_for0 (0 .. 1048575) {
            if (!$_for0_first) {
                $k = $rt->binary('+', $k, $rt->i(1), 1, 'int');
            }
            $_for0_first = 0;
            if (!($rt->binary('<', $k, $rt->i(8)))) {
                last;
            }
            if ($rt->binary('>=', $k, $numRoots)) {
                last;
            }
            $angle = $rt->binary('/', $rt->binary('*', $g->{TAU}, $rt->construct(1, $k), 1, 'float'), $rt->construct(1, $intDeg), 1, 'float');
            $roots->[int($k)] = $rt->construct(2, $rt->component_wise('cos', $angle), $rt->component_wise('sin', $angle));
        }
        $iter = $rt->f(0);
        $convergedRoot = $rt->unary('-', $rt->i(1));
        $convergeDist = $rt->f(1);
        $bailout = $rt->binary('*', $rt->f(10000000000), $effRelax, 1, 'float');
        $zr_df = $re_df;
        $zi_df = $im_df;
        $n = $rt->i(0);
        $_for1_first = 1;
        for my $_for1 (0 .. 1048575) {
            if (!$_for1_first) {
                $n = $rt->binary('+', $n, $rt->i(1), 1, 'int');
            }
            $_for1_first = 0;
            if (!($rt->binary('<', $n, $rt->i(500)))) {
                last;
            }
            if ($rt->binary('>=', $n, $maxIter)) {
                last;
            }
            $pwr = $df64_from__float->($rt->f(1));
            $pwi = $df64_from__float->($rt->f(0));
            $j = $rt->i(0);
            $_for2_first = 1;
            for my $_for2 (0 .. 1048575) {
                if (!$_for2_first) {
                    $j = $rt->binary('+', $j, $rt->i(1), 1, 'int');
                }
                $_for2_first = 0;
                if (!($rt->binary('<', $j, $rt->i(7)))) {
                    last;
                }
                if ($rt->binary('>=', $j, $rt->binary('-', $intDeg, $rt->i(1), 1, 'int'))) {
                    last;
                }
                $tr = $rt->construct(2, 0.0);
                $ti = $rt->construct(2, 0.0);
                ($_retc, $tr, $ti) = $df64_cmul__vec2_vec2_vec2_vec2_vec2_vec2->($pwr, $pwi, $zr_df, $zi_df, $tr, $ti);
                @{$pwr} = map { $rt->f32($_) } @{($tr)};
                @{$pwi} = map { $rt->f32($_) } @{($ti)};
            }
            $znr = $rt->construct(2, 0.0);
            $zni = $rt->construct(2, 0.0);
            ($_retc, $znr, $zni) = $df64_cmul__vec2_vec2_vec2_vec2_vec2_vec2->($pwr, $pwi, $zr_df, $zi_df, $znr, $zni);
            $fzr = $df64_sub__vec2_vec2->($znr, $df64_from__float->($rt->f(1)));
            $fzi = $zni;
            $fpzr = $df64_mul_f__vec2_float->($pwr, $rt->construct(1, $intDeg));
            $fpzi = $df64_mul_f__vec2_float->($pwi, $rt->construct(1, $intDeg));
            $fpzr_f = $df64_to_float__vec2->($fpzr);
            $fpzi_f = $df64_to_float__vec2->($fpzi);
            if ($rt->binary('<', $rt->binary('+', $rt->binary('*', $fpzr_f, $fpzr_f, 1, 'float'), $rt->binary('*', $fpzi_f, $fpzi_f, 1, 'float'), 1, 'float'), $rt->f(9.9999999999999995e-21))) {
                last;
            }
            $denom = $rt->binary('+', $rt->binary('*', $fpzr_f, $fpzr_f, 1, 'float'), $rt->binary('*', $fpzi_f, $fpzi_f, 1, 'float'), 1, 'float');
            $inv_denom = $rt->binary('/', $rt->f(1), $denom, 1, 'float');
            $nr = $df64_add__vec2_vec2->($df64_mul__vec2_vec2->($fzr, $fpzr), $df64_mul__vec2_vec2->($fzi, $fpzi));
            $ni = $df64_sub__vec2_vec2->($df64_mul__vec2_vec2->($fzi, $fpzr), $df64_mul__vec2_vec2->($fzr, $fpzi));
            $dr = $df64_mul_f__vec2_float->($nr, $inv_denom);
            $di = $df64_mul_f__vec2_float->($ni, $inv_denom);
            @{$zr_df} = map { $rt->f32($_) } @{($df64_sub__vec2_vec2->($zr_df, $df64_mul_f__vec2_float->($dr, $effRelax)))};
            @{$zi_df} = map { $rt->f32($_) } @{($df64_sub__vec2_vec2->($zi_df, $df64_mul_f__vec2_float->($di, $effRelax)))};
            $zx = $df64_to_float__vec2->($zr_df);
            $zy = $df64_to_float__vec2->($zi_df);
            if ($rt->binary('>', $rt->binary('+', $rt->binary('*', $zx, $zx, 1, 'float'), $rt->binary('*', $zy, $zy, 1, 'float'), 1, 'float'), $bailout)) {
                last;
            }
            $k = $rt->i(0);
            $_for3_first = 1;
            for my $_for3 (0 .. 1048575) {
                if (!$_for3_first) {
                    $k = $rt->binary('+', $k, $rt->i(1), 1, 'int');
                }
                $_for3_first = 0;
                if (!($rt->binary('<', $k, $rt->i(8)))) {
                    last;
                }
                if ($rt->binary('>=', $k, $numRoots)) {
                    last;
                }
                $dx = $rt->binary('-', $zx, $rt->swizzle($roots->[int($k)], 'x'), 1, 'float');
                $dy = $rt->binary('-', $zy, $rt->swizzle($roots->[int($k)], 'y'), 1, 'float');
                $d = $rt->component_wise('sqrt', $rt->binary('+', $rt->binary('*', $dx, $dx, 1, 'float'), $rt->binary('*', $dy, $dy, 1, 'float'), 1, 'float'));
                if ($rt->binary('<', $d, $_u_tolerance)) {
                    $convergedRoot = $k;
                    $convergeDist = $d;
                    last;
                }
            }
            if ($rt->binary('>=', $convergedRoot, $rt->i(0))) {
                last;
            }
            $iter = $rt->binary('+', $iter, $rt->f(1), 1, 'float');
        }
        $smoothIter = $iter;
        if ((((($rt->binary('>=', $convergedRoot, $rt->i(0))) && ($rt->binary('>', $convergeDist, $rt->f(0))) ? 1 : 0)) && ($rt->binary('<', $convergeDist, $_u_tolerance)) ? 1 : 0)) {
            $smoothIter = $rt->binary('-', $iter, $rt->component_wise('log2', $rt->binary('/', $rt->component_wise('log', $convergeDist), $rt->component_wise('log', $_u_tolerance), 1, 'float')), 1, 'float');
        }
        $value = $rt->f(0);
        $maxIterF = $rt->construct(1, $maxIter);
        $numRootsF = $rt->construct(1, $numRoots);
        if ($rt->binary('==', $outMode, $rt->i(0))) {
            $value = $rt->binary('/', $smoothIter, $maxIterF, 1, 'float');
        } else {
            if ($rt->binary('==', $outMode, $rt->i(1))) {
                if ($rt->binary('>=', $convergedRoot, $rt->i(0))) {
                    $value = $rt->binary('/', $rt->construct(1, $convergedRoot), $numRootsF, 1, 'float');
                }
            } else {
                if ($rt->binary('>=', $convergedRoot, $rt->i(0))) {
                    $value = $rt->binary('/', $rt->binary('+', $rt->construct(1, $convergedRoot), $rt->binary('/', $smoothIter, $maxIterF, 1, 'float'), 1, 'float'), $numRootsF, 1, 'float');
                }
            }
        }
        if ($doInvert) {
            $value = $rt->binary('-', $rt->f(1), $value, 1, 'float');
        }
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $rt->construct(3, $value), $rt->f(1)))};
    };
    $main__void->();
    my $_c = $g->{fragColor};
    $out->[0] = $rt->f32($_c->[0]); $out->[1] = $rt->f32($_c->[1]); $out->[2] = $rt->f32($_c->[2]); $out->[3] = $rt->f32($_c->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0 };
