# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($as_u32__float, $clamp_01__float, $normalized_sine__float, $periodic_value__float_float, $snow_fract_vec3__vec3, $snow_hash__vec3, $snow_noise__vec2_float_float_vec3, $main__void);
    my $_retc;
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_resolution = exists $U->{'resolution'} ? $U->{'resolution'} : $rt->construct(2, 0.0);
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_alpha = exists $U->{'alpha'} ? $U->{'alpha'} : $rt->f(0.0);
    my $_u_time = exists $U->{'time'} ? $U->{'time'} : $rt->f(0.0);
    my $_u_pause = exists $U->{'pause'} ? $U->{'pause'} : $rt->f(0.0);
    my $_u_density = exists $U->{'density'} ? $U->{'density'} : $rt->f(0.0);
    $g->{CHANNEL_COUNT} = $rt->i(4);
    $g->{TAU} = $rt->f(6.2831853071795862);
    $g->{TIME_SEED_OFFSETS} = $rt->construct(3, $rt->f(97), $rt->f(57), $rt->f(131));
    $g->{STATIC_SEED} = $rt->construct(3, $rt->f(37), $rt->f(17), $rt->f(53));
    $g->{LIMITER_SEED} = $rt->construct(3, $rt->f(113), $rt->f(71), $rt->f(193));
    $g->{fragColor} = $rt->construct(4, 0.0);
    $as_u32__float = sub {
        my ($value) = @_;
        return $rt->construct(1, $rt->component_wise('max', $rt->component_wise('round', $value), $rt->f(0)), 'uint');
    };
    $clamp_01__float = sub {
        my ($value) = @_;
        return $rt->component_wise('clamp', $value, $rt->f(0), $rt->f(1));
    };
    $normalized_sine__float = sub {
        my ($value) = @_;
        return $rt->binary('*', $rt->binary('+', $rt->component_wise('sin', $value), $rt->f(1), 1, 'float'), $rt->f(0.5), 1, 'float');
    };
    $periodic_value__float_float = sub {
        my ($time, $value) = @_;
        return $normalized_sine__float->($rt->binary('*', $rt->binary('-', $time, $value, 1, 'float'), $g->{TAU}, 1, 'float'));
    };
    $snow_fract_vec3__vec3 = sub {
        my ($value) = @_;
        $value = $rt->copy($value, 'float');
        return $rt->binary('-', $value, $rt->component_wise('floor', $value), 3, 'float');
    };
    $snow_hash__vec3 = sub {
        my ($input_sample) = @_;
        $input_sample = $rt->copy($input_sample, 'float');
        my ($combined, $dot_val, $fractional, $scaled, $shifted);
        $scaled = $snow_fract_vec3__vec3->($rt->binary('*', $input_sample, $rt->f(0.1031), 3, 'float'));
        $dot_val = $rt->dot($scaled, $rt->binary('+', $rt->swizzle($scaled, 'yzx'), $rt->construct(3, $rt->f(33.329999999999998)), 3, 'float'));
        $shifted = $rt->binary('+', $scaled, $dot_val, 3, 'float');
        $combined = $rt->binary('*', $rt->binary('+', $rt->swizzle($shifted, 'x'), $rt->swizzle($shifted, 'y'), 1, 'float'), $rt->swizzle($shifted, 'z'), 1, 'float');
        $fractional = $rt->binary('-', $combined, $rt->component_wise('floor', $combined), 1, 'float');
        return $rt->component_wise('clamp', $fractional, $rt->f(0), $rt->f(1));
    };
    $snow_noise__vec2_float_float_vec3 = sub {
        my ($coord, $time, $speed, $seed) = @_;
        $coord = $rt->copy($coord, 'float');
        $seed = $rt->copy($seed, 'float');
        my ($angle, $base_sample, $base_value, $periodic, $scaled_time, $time_sample, $time_seed, $time_value, $z_base);
        $angle = $rt->binary('*', $time, $g->{TAU}, 1, 'float');
        $z_base = $rt->binary('*', $rt->component_wise('cos', $angle), $speed, 1, 'float');
        $base_sample = $rt->construct(3, $rt->binary('+', $rt->swizzle($coord, 'x'), $rt->swizzle($seed, 'x'), 1, 'float'), $rt->binary('+', $rt->swizzle($coord, 'y'), $rt->swizzle($seed, 'y'), 1, 'float'), $rt->binary('+', $z_base, $rt->swizzle($seed, 'z'), 1, 'float'));
        $base_value = $snow_hash__vec3->($base_sample);
        if ((($rt->binary('==', $speed, $rt->f(0))) || ($rt->binary('==', $time, $rt->f(0))) ? 1 : 0)) {
            return $base_value;
        }
        $time_seed = $rt->binary('+', $seed, $g->{TIME_SEED_OFFSETS}, 3, 'float');
        $time_sample = $rt->construct(3, $rt->binary('+', $rt->swizzle($coord, 'x'), $rt->swizzle($time_seed, 'x'), 1, 'float'), $rt->binary('+', $rt->swizzle($coord, 'y'), $rt->swizzle($time_seed, 'y'), 1, 'float'), $rt->binary('+', $rt->f(1), $rt->swizzle($time_seed, 'z'), 1, 'float'));
        $time_value = $snow_hash__vec3->($time_sample);
        $scaled_time = $rt->binary('*', $periodic_value__float_float->($time, $time_value), $speed, 1, 'float');
        $periodic = $periodic_value__float_float->($scaled_time, $base_value);
        return $rt->component_wise('clamp', $periodic, $rt->f(0), $rt->f(1));
    };
    $main__void = sub {
        my ($alphaVal, $coords, $d, $exponent, $globalCoord, $limiter_mask, $limiter_value, $mixed_rgb, $pixelCoord, $speedVal, $static_color, $static_value, $texel, $timeVal);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $coords = $rt->construct(2, $rt->construct(1, $rt->swizzle($ctx->{frag_coord}, 'x'), 'int'), $rt->construct(1, $rt->swizzle($ctx->{frag_coord}, 'y'), 'int'), 'int');
        $texel = $rt->texel_fetch($_u_inputTex, $coords, $rt->i(0));
        $alphaVal = $rt->component_wise('clamp', $_u_alpha, $rt->f(0), $rt->f(1));
        if ($rt->binary('==', $alphaVal, $rt->f(0))) {
            @{$g->{fragColor}} = map { $rt->f32($_) } @{($texel)};
            return;
        }
        $pixelCoord = $rt->construct(2, $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'x'), $rt->swizzle($_u_tileOffset, 'x'), 1, 'float'), $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'y'), $rt->swizzle($_u_tileOffset, 'y'), 1, 'float'));
        $timeVal = (($rt->binary('>', $_u_pause, $rt->f(0.5))) ? ($rt->f(0)) : ($_u_time));
        $speedVal = $rt->f(100);
        $static_value = $snow_noise__vec2_float_float_vec3->($pixelCoord, $timeVal, $speedVal, $g->{STATIC_SEED});
        $limiter_value = $snow_noise__vec2_float_float_vec3->($pixelCoord, $timeVal, $speedVal, $g->{LIMITER_SEED});
        $d = $rt->component_wise('max', $rt->binary('*', $_u_density, $rt->f(0.01), 1, 'float'), $rt->f(0.0001));
        $exponent = $rt->binary('/', $rt->binary('-', $rt->f(1), $d, 1, 'float'), $d, 1, 'float');
        $limiter_mask = $rt->binary('*', $rt->component_wise('pow', $rt->component_wise('min', $limiter_value, $rt->f(0.98999999999999999)), $exponent), $alphaVal, 1, 'float');
        $static_color = $rt->construct(3, $static_value);
        $mixed_rgb = $rt->component_wise('mix', $rt->swizzle($texel, 'xyz'), $static_color, $rt->construct(3, $limiter_mask));
        @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->construct(4, $mixed_rgb, $rt->swizzle($texel, 'w')))};
    };
    $main__void->();
    my $_c0 = $g->{fragColor};
    $out->[0] = $rt->f32($_c0->[0]); $out->[1] = $rt->f32($_c0->[1]); $out->[2] = $rt->f32($_c0->[2]); $out->[3] = $rt->f32($_c0->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 0, output_names => ['fragColor'] };
