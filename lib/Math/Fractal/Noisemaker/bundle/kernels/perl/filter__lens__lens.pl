# Generated by Math::Fractal::Noisemaker::Transpiler - do not edit.
use strict;
use warnings;
no warnings qw(uninitialized);
my $run_pixel = sub {
    my ($ctx, $out) = @_;
    my $rt = $ctx->rt;
    my $U = $ctx->uniforms;
    my $g = {};
    my ($main__void);
    my $_retc;
    my $_u_inputTex = $ctx->texture_binding('inputTex');
    my $_u_tileOffset = exists $U->{'tileOffset'} ? $U->{'tileOffset'} : $rt->construct(2, 0.0);
    my $_u_fullResolution = exists $U->{'fullResolution'} ? $U->{'fullResolution'} : $rt->construct(2, 0.0);
    my $_u_lensDisplacement = exists $U->{'lensDisplacement'} ? $U->{'lensDisplacement'} : $rt->f(0.0);
    my $_u_aspectLens = exists $U->{'aspectLens'} ? $U->{'aspectLens'} : 0;
    my $_u_antialias = exists $U->{'antialias'} ? $U->{'antialias'} : 0;
    $g->{fragColor} = $rt->construct(4, 0.0);
    $g->{HALF_FRAME} = $rt->f(0.5);
    $main__void = sub {
        my ($aDist, $aspect, $centerWeight, $centerWeightSq, $col, $dims, $dispPixels, $displacement, $dist, $distFromCenter, $dx, $dy, $globalCoord, $isTileRendering, $maxDispPixels, $maxDist, $normalizedDist, $offset, $sampledUV, $texSize, $tileDims, $uv, $warpedGlobalUV, $zoom);
        $globalCoord = $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float');
        $texSize = $rt->texture_size($_u_inputTex);
        $tileDims = $rt->construct(2, $texSize);
        $dims = (($rt->binary('>', $rt->swizzle($_u_fullResolution, 'x'), $rt->f(0))) ? ($_u_fullResolution) : ($tileDims));
        $uv = $rt->binary('/', $rt->binary('+', $rt->swizzle($ctx->{frag_coord}, 'xy'), $_u_tileOffset, 2, 'float'), $dims, 2, 'float');
        $zoom = (($rt->binary('<', $_u_lensDisplacement, $rt->f(0))) ? ($rt->binary('*', $_u_lensDisplacement, $rt->unary('-', $rt->f(0.25)), 1, 'float')) : ($rt->f(0)));
        $aspect = $rt->binary('/', $rt->swizzle($dims, 'x'), $rt->swizzle($dims, 'y'), 1, 'float');
        $dist = $rt->binary('-', $uv, $g->{HALF_FRAME}, 2, 'float');
        $aDist = $dist;
        if ($_u_aspectLens) {
            $aDist = $rt->assign_swizzle($aDist, 'x', $rt->binary('*', $rt->swizzle($aDist, 'x'), $aspect, 1, 'float'));
        }
        $maxDist = $rt->length($rt->construct(2, (($_u_aspectLens) ? ($rt->binary('*', $aspect, $rt->f(0.5), 1, 'float')) : ($rt->f(0.5))), $rt->f(0.5)));
        $distFromCenter = $rt->length($aDist);
        $normalizedDist = $rt->component_wise('clamp', $rt->binary('/', $distFromCenter, $maxDist, 1, 'float'), $rt->f(0), $rt->f(1));
        $centerWeight = $rt->binary('-', $rt->f(1), $normalizedDist, 1, 'float');
        $centerWeightSq = $rt->binary('*', $centerWeight, $centerWeight, 1, 'float');
        $displacement = $rt->binary('+', $rt->binary('*', $aDist, $zoom, 2, 'float'), $rt->binary('*', $rt->binary('*', $aDist, $centerWeightSq, 2, 'float'), $_u_lensDisplacement, 2, 'float'), 2, 'float');
        if ($_u_aspectLens) {
            $displacement = $rt->assign_swizzle($displacement, 'x', $rt->binary('/', $rt->swizzle($displacement, 'x'), $aspect, 1, 'float'));
        }
        $isTileRendering = $rt->binary('>', $rt->length($_u_tileOffset), $rt->f(0));
        $dispPixels = $rt->f(0.0);
        $maxDispPixels = $rt->f(0.0);
        if ($isTileRendering) {
            $maxDispPixels = $rt->f(256);
            $dispPixels = $rt->length($rt->binary('*', $displacement, $dims, 2, 'float'));
            if ($rt->binary('>', $dispPixels, $maxDispPixels)) {
                @{$displacement} = map { $rt->f32($_) } @{($rt->binary('*', $displacement, $rt->binary('/', $maxDispPixels, $dispPixels, 1, 'float'), 2, 'float'))};
            }
        }
        $warpedGlobalUV = (($isTileRendering) ? ($rt->binary('-', $uv, $displacement, 2, 'float')) : ($rt->component_wise('fract', $rt->binary('-', $uv, $displacement, 2, 'float'))));
        $offset = $rt->binary('/', $rt->binary('-', $rt->binary('*', $warpedGlobalUV, $dims, 2, 'float'), $_u_tileOffset, 2, 'float'), $tileDims, 2, 'float');
        $sampledUV = $offset;
        $col = $rt->construct(4, 0.0);
        $dx = $rt->construct(2, 0.0);
        $dy = $rt->construct(2, 0.0);
        if ($_u_antialias) {
            $dx = $rt->dFdx($sampledUV);
            $dy = $rt->dFdy($sampledUV);
            $col = $rt->construct(4, $rt->f(0));
            @{$col} = map { $rt->f32($_) } @{($rt->binary('+', $col, $rt->texture($_u_inputTex, $rt->binary('+', $rt->binary('+', $sampledUV, $rt->binary('*', $dx, $rt->unary('-', $rt->f(0.375)), 2, 'float'), 2, 'float'), $rt->binary('*', $dy, $rt->unary('-', $rt->f(0.125)), 2, 'float'), 2, 'float')), 4, 'float'))};
            @{$col} = map { $rt->f32($_) } @{($rt->binary('+', $col, $rt->texture($_u_inputTex, $rt->binary('+', $rt->binary('+', $sampledUV, $rt->binary('*', $dx, $rt->f(0.125), 2, 'float'), 2, 'float'), $rt->binary('*', $dy, $rt->unary('-', $rt->f(0.375)), 2, 'float'), 2, 'float')), 4, 'float'))};
            @{$col} = map { $rt->f32($_) } @{($rt->binary('+', $col, $rt->texture($_u_inputTex, $rt->binary('+', $rt->binary('+', $sampledUV, $rt->binary('*', $dx, $rt->f(0.375), 2, 'float'), 2, 'float'), $rt->binary('*', $dy, $rt->f(0.125), 2, 'float'), 2, 'float')), 4, 'float'))};
            @{$col} = map { $rt->f32($_) } @{($rt->binary('+', $col, $rt->texture($_u_inputTex, $rt->binary('+', $rt->binary('+', $sampledUV, $rt->binary('*', $dx, $rt->unary('-', $rt->f(0.125)), 2, 'float'), 2, 'float'), $rt->binary('*', $dy, $rt->f(0.375), 2, 'float'), 2, 'float')), 4, 'float'))};
            @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->binary('*', $col, $rt->f(0.25), 4, 'float'))};
        } else {
            @{$g->{fragColor}} = map { $rt->f32($_) } @{($rt->texture($_u_inputTex, $sampledUV))};
        }
    };
    $main__void->();
    my $_c0 = $g->{fragColor};
    $out->[0] = $rt->f32($_c0->[0]); $out->[1] = $rt->f32($_c0->[1]); $out->[2] = $rt->f32($_c0->[2]); $out->[3] = $rt->f32($_c0->[3]);
};
{ kernel => $run_pixel, uses_derivatives => 1, output_names => ['fragColor'] };
