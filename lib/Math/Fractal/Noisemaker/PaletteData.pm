package Math::Fractal::Noisemaker::PaletteData;

# Vendored cosine-palette coefficient table (55 x 16), same provenance as the
# reference engine's paletteData. Populated by the parity grind; empty until
# the classicNoisedeck palette effects are ported.

use strict;
use warnings;

our @PALETTE_DATA = ();

1;
